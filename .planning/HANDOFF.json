{
  "version": "1.0",
  "timestamp": "2026-07-14T12:06:09.546Z",
  "phase": "11",
  "phase_name": "janela-de-hist-rico-retroativo",
  "phase_dir": ".planning/phases/11-janela-de-hist-rico-retroativo",
  "plan": null,
  "task": null,
  "total_tasks": 13,
  "status": "paused",
  "completed_tasks": [],
  "remaining_tasks": [
    {"id": "11-01", "name": "Migration 0024 (ad_accounts.backfill_days) + [BLOCKING] supabase db push + regen types + signState carries backfillDays", "status": "not_started", "wave": 1, "tasks": 3},
    {"id": "11-02", "name": "Google Ads: form input + connect route signs backfillDays + callback upserts backfill_days", "status": "not_started", "wave": 2, "depends_on": ["11-01"], "tasks": 3},
    {"id": "11-03", "name": "Meta Ads: form input + connect route validates/upserts backfill_days", "status": "not_started", "wave": 2, "depends_on": ["11-01"], "tasks": 2},
    {"id": "11-04", "name": "Post-connect edit (SET-05): updateBackfillWindow Server Action + inline optimistic-revert control", "status": "not_started", "wave": 2, "depends_on": ["11-01"], "tasks": 3},
    {"id": "11-05", "name": "N8N: both workflows select backfill_days and use it per-account on first sync", "status": "not_started", "wave": 2, "depends_on": ["11-01"], "tasks": 2}
  ],
  "blockers": [],
  "human_actions_pending": [
    {"action": "Confirm supabase CLI is authenticated (SUPABASE_ACCESS_TOKEN or `supabase login`) before executing Plan 11-01 Task 2", "context": "Plan 11-01 Task 2 is a [BLOCKING] task that runs `supabase db push` to push migration 0024 live. If non-TTY auth isn't configured, the task falls back to the Supabase MCP `apply_migration` tool, but that also needs the MCP server's Supabase account authorized. All 4 wave-2 plans depend on this task completing.", "blocking": true}
  ],
  "decisions": [
    {"decision": "Skipped research (RESEARCH.md) for Phase 11", "rationale": "CONTEXT.md's canonical_refs already pointed to a complete technical design in .planning/research/ARCHITECTURE.md §'Feature 3 — Configurable backfill window per tenant' (schema, migration, full data flow). User chose to skip when asked.", "phase": "11"},
    {"decision": "Skipped UI-SPEC.md generation for Phase 11", "rationale": "Phase has frontend-shaped keywords (settings, form) but CONTEXT.md explicitly pins the exact reused components/patterns (google-ads-form.tsx, meta-ads-form.tsx, ChannelStatusBadge) and states this phase must not invest in final visual polish — that's Phase 12's job (Settings redesign).", "phase": "11"},
    {"decision": "updateBackfillWindow Server Action does not use requireSuperAdmin()", "rationale": "SET-05 requires tenant_admin access too, so the gate replicates the connect-route pattern (super_admin any tenant; tenant_admin scoped via getClaims()), writing through the service client because ad_accounts RLS (migration 0006) blocks tenant_admin UPDATE.", "phase": "11"}
  ],
  "uncommitted_files": [
    ".planning/STATE.md",
    ".planning/research/ARCHITECTURE.md",
    "next-env.d.ts"
  ],
  "next_action": "Run /gsd-execute-phase 11 to execute all 5 plans (wave 1: 11-01 foundation/migration, wave 2: 11-02..11-05 in parallel).",
  "context_notes": "Phase 11 planning is fully done: CONTEXT.md (gathered earlier today), 5 PLAN.md files created by gsd-planner, verified by gsd-plan-checker with 0 blockers/0 warnings on first pass (no revision loop needed). Nothing has been executed yet — no SUMMARY.md files exist for this phase. STATE.md was updated via `gsd-tools state planned-phase` but the file is still uncommitted (routine leftover from the planning workflow, safe to commit alongside next work). The ARCHITECTURE.md and next-env.d.ts modifications, and the untracked .playwright-mcp/, d05-second-reset.png, prototipos/ files, predate this session (were already dirty at conversation start) — unrelated to Phase 11, left as-is."
}

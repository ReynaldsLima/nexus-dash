---
phase: 03-dashboard-ui
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/hooks/use-dashboard-data.ts
  - app/[tenant-slug]/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Dashboard shows real data from daily_rollups for the active tenant and selected date range"
    - "7 KPI cards render: ROAS, CPA, CTR, Spend, Impressions, Clicks, Conversions"
    - "Each KPI card shows period-over-period delta (absolute + %) color-coded by metric polarity"
    - "Delta shows em-dash when prior period has no data (no NaN/Infinity)"
    - "A trend area chart renders daily spend (per channel) for the selected period"
    - "A channel breakdown shows Google Ads vs Meta Ads absolute values + percentage contribution"
    - "Changing the global date range re-fetches and re-renders the dashboard automatically"
    - "Loading state shows skeletons; query error shows an inline message"
  artifacts:
    - path: "lib/hooks/use-dashboard-data.ts"
      provides: "useDashboardData hook — current + prior range queries over daily_rollups"
      contains: "useQuery"
    - path: "app/[tenant-slug]/dashboard/page.tsx"
      provides: "7 KPI cards + trend chart + channel split wired to real data"
      contains: "useDashboardData"
  key_links:
    - from: "lib/hooks/use-dashboard-data.ts"
      to: "daily_rollups"
      via: "supabase.from('daily_rollups').select"
      pattern: "from\\('daily_rollups'\\)"
    - from: "lib/hooks/use-dashboard-data.ts"
      to: "lib/stores/date-range.ts"
      via: "queryKey inclui from/to do store"
      pattern: "useDateRangeStore"
    - from: "app/[tenant-slug]/dashboard/page.tsx"
      to: "lib/dashboard-kpis.ts"
      via: "aggregateRollups + calcDelta + computeChannelSplit"
      pattern: "aggregateRollups"
---

<objective>
Substituir o mock data da dashboard por dados reais de `daily_rollups`, entregando DASH-01 (7 KPI cards com deltas period-over-period color-coded), DASH-02 (trend chart por período) e DASH-03 (channel breakdown Google vs Meta com valores absolutos + %). Criar o hook `useDashboardData` que busca o período atual E o período anterior (mesma duração) e usa as funções puras da Plan 01 para agregar e calcular deltas.

Purpose: A dashboard hoje mostra apenas 4 KPIs com mock. DASH-01 exige os 7 KPIs com delta period-over-period. O hook reage ao date range global (queryKey) e ao tenant ativo (RLS filtra por tenant_id automaticamente).

Output: `lib/hooks/use-dashboard-data.ts` + `app/[tenant-slug]/dashboard/page.tsx` reescrita consumindo dados reais. Sem novas deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-ui/03-CONTEXT.md
@.planning/phases/03-dashboard-ui/03-RESEARCH.md

<interfaces>
<!-- lib/dashboard-kpis.ts (Plan 01) -->
export type AggregatedKpis = { spend: number; roas: number; cpa: number; ctr: number; conversions: number; clicks: number; impressions: number }
export function aggregateRollups(rows: RollupRow[]): AggregatedKpis
export function calcDelta(current: number, prior: number): { absolute: number; pct: number | null }
export function computePriorRange(from: Date, to: Date): { priorFrom: Date; priorTo: Date }
export function computeChannelSplit(googleSpend: number, metaSpend: number): { google: { value: number; pct: number }; meta: { value: number; pct: number } }

<!-- lib/stores/date-range.ts (Plan 01) -->
export const useDateRangeStore: () => { from: Date; to: Date; ... }

<!-- lib/supabase/client.ts -->
export function createClient(): SupabaseClient<Database>   // singleton browser client, sessão do usuário logado → RLS aplica get_tenant_id()

<!-- lib/formatters.ts (Plan 01) -->
export const brl: (v: number) => string
export const num: (v: number) => string

<!-- daily_rollups: TEM linhas channel='all' (cross-channel) — confirmado em migration 0010 refresh_daily_rollups (UNION ALL gera 'all'). Usar channel='all' para KPI totais; channel IN ('google_ads','meta_ads') para o split. -->
<!-- daily_rollups colunas: total_spend, total_impressions, total_clicks, total_conversions, total_conv_value, channel, date, campaign_count -->
</interfaces>

<!-- A página atual (app/[tenant-slug]/dashboard/page.tsx) já tem KpiCard, AreaChart e PieChart funcionando com mock. REUTILIZAR esses componentes visuais (KpiCard, ChartContainer, configs de cor). REMOVER AccountBalanceCard + MOCK_ACCOUNT_BALANCES (não há fonte de dados real para saldo de conta em v1 — não é requisito da fase). REMOVER imports de lib/mock-data. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar useDashboardData hook (current + prior range sobre daily_rollups)</name>
  <read_first>
    - .planning/phases/03-dashboard-ui/03-RESEARCH.md (Pattern 2 — useQuery com queryKey reativo; Pattern 6 — period-over-period)
    - lib/supabase/client.ts (createClient singleton)
    - lib/stores/date-range.ts (Plan 01 — useDateRangeStore)
    - lib/dashboard-kpis.ts (Plan 01 — aggregateRollups, calcDelta, computePriorRange, computeChannelSplit)
    - supabase/migrations/0010_create_pipeline_functions.sql (confirma que refresh_daily_rollups cria linha channel='all')
  </read_first>
  <action>
Criar `lib/hooks/use-dashboard-data.ts` ('use client'). Exportar `export function useDashboardData(tenantSlug: string)`.
- Importar `useQuery` de `@tanstack/react-query`, `createClient` de `@/lib/supabase/client`, `useDateRangeStore` de `@/lib/stores/date-range`, e `computePriorRange` de `@/lib/dashboard-kpis`.
- Ler `{ from, to }` do store. Calcular `{ priorFrom, priorTo }` via `computePriorRange(from, to)`.
- Helper local `toDateStr = (d: Date) => d.toISOString().split('T')[0]`.
- `queryKey: ['dashboard', tenantSlug, from.toISOString(), to.toISOString()]`.
- `queryFn`: fazer DUAS queries em paralelo (`Promise.all`):
  1. período atual: `supabase.from('daily_rollups').select('channel,date,total_spend,total_impressions,total_clicks,total_conversions,total_conv_value').gte('date', toDateStr(from)).lte('date', toDateStr(to))`
  2. período anterior: mesma query com `gte/lte` em `priorFrom`/`priorTo`.
- Retornar do queryFn um objeto `{ current: rows[], prior: rows[] }` (lançar em `error`). RLS filtra por tenant automaticamente — NÃO adicionar `.eq('tenant_id', ...)`.
- `enabled: !!tenantSlug`.
- Retornar o resultado do `useQuery` diretamente (a página faz a agregação com as funções puras).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/use-dashboard-data.ts` contains `useQuery` and `from('daily_rollups')`
    - `lib/hooks/use-dashboard-data.ts` contains `useDateRangeStore` and `computePriorRange`
    - `lib/hooks/use-dashboard-data.ts` does NOT contain `.eq('tenant_id'` (RLS handles isolation)
    - `lib/hooks/use-dashboard-data.ts` contains `Promise.all`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hook busca período atual + anterior de daily_rollups, reativo ao store, isolado por RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever dashboard/page.tsx com 7 KPI cards + trend chart + channel split reais</name>
  <read_first>
    - app/[tenant-slug]/dashboard/page.tsx (página atual — REUTILIZAR KpiCard, AreaChart, PieChart, configs de cor; remover AccountBalanceCard e mocks)
    - lib/dashboard-kpis.ts (Plan 01 — aggregateRollups, calcDelta, computeChannelSplit)
    - lib/formatters.ts (Plan 01 — brl, num)
    - lib/hooks/use-dashboard-data.ts (Task 1 deste plano)
    - lib/stores/tenant-store.tsx (como obter o tenant slug ativo no client — OU usar useParams de next/navigation para ler [tenant-slug])
    - components/ui/skeleton.tsx (loading state)
    - components/ui/chart.tsx (ChartContainer, ChartTooltip, ChartTooltipContent)
  </read_first>
  <action>
Reescrever `app/[tenant-slug]/dashboard/page.tsx` (manter `'use client'`):
1. Obter o `tenantSlug` ativo via `useParams()` de `next/navigation` (`const params = useParams(); const tenantSlug = params['tenant-slug'] as string`).
2. Chamar `const { data, isLoading, isError } = useDashboardData(tenantSlug)`.
3. Loading: renderizar grid de `<Skeleton>` no lugar dos cards/charts. Error: mensagem inline "Erro ao carregar dados do dashboard."
4. Agregar com funções puras: `const cur = aggregateRollups(data.current.filter(r => r.channel === 'all'))` e `const prev = aggregateRollups(data.prior.filter(r => r.channel === 'all'))`. (channel='all' = totais cross-channel, conforme migration 0010.)
5. Renderizar **7 KPI cards** (DASH-01) usando o componente KpiCard existente, com delta de `calcDelta(cur.X, prev.X)`:
   - Gasto Total (brl, polaridade negativa — menor é melhor), ROAS (×, positiva), CPA (brl, negativa), CTR (%, positiva), Impressões (num, positiva), Cliques (num, positiva), Conversões (num, positiva).
   - Adaptar KpiCard para aceitar `pct: number | null` — quando `null`, exibir `—` no badge (Pitfall 7, sem NaN). Layout grid responsivo: `grid-cols-2 lg:grid-cols-4` (4+3) — discricionário.
6. **Trend chart (DASH-02):** transformar `data.current` (linhas channel='google_ads' e 'meta_ads') em série diária `{ date, google, meta }` por `date` (pivot no JS). Alimentar o AreaChart existente. Eixo X usa `date` formatado `dd/MM`.
7. **Channel split (DASH-03):** somar `total_spend` das linhas `channel==='google_ads'` e `channel==='meta_ads'` do período atual; usar `computeChannelSplit(googleSpend, metaSpend)` para obter valores + pcts; alimentar o PieChart + a legenda (mostrar `brl(value)` e `pct%`).
8. Remover `AccountBalanceCard`, `MOCK_*` imports e a seção de saldos. Atualizar o subtítulo do header para refletir o range ativo (ex: número de dias do período) — opcional/discricionário.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/[tenant-slug]/dashboard/page.tsx` contains `useDashboardData`
    - `app/[tenant-slug]/dashboard/page.tsx` contains `aggregateRollups` and `calcDelta` and `computeChannelSplit`
    - `app/[tenant-slug]/dashboard/page.tsx` does NOT contain `MOCK_` (mocks removidos)
    - grep count of `<KpiCard` (or `KpiCard ` invocations) in dashboard/page.tsx is >= 7
    - `app/[tenant-slug]/dashboard/page.tsx` contains `useParams`
    - `app/[tenant-slug]/dashboard/page.tsx` contains `Skeleton`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dashboard renderiza 7 KPIs com deltas, trend chart e channel split a partir de daily_rollups; reativo ao date range; sem mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (PostgREST) | Query de leitura em daily_rollups com a sessão JWT do usuário. RLS é a fronteira de isolamento. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering (cross-tenant leakage) | useDashboardData query | mitigate | RLS `(SELECT get_tenant_id())` em daily_rollups (Fase 1/2) filtra por tenant via JWT. Hook NÃO usa `.eq('tenant_id')` do client (não confiável) — depende exclusivamente de RLS. |
| T-03-03-02 | Information Disclosure | dashboard page client bundle | accept | Apenas dados de métricas agregadas do próprio tenant são renderizados; nenhum secret. Risco baixo. |
| T-03-03-03 | Denial of Service | full-table read sem limite | accept | daily_rollups é pré-agregado (1 linha/canal/dia). Período máximo (90 dias) × 3 canais = ~270 linhas. Volume trivial para v1 (1-3 tenants). |
</threat_model>

<verification>
- `npm test` verde (funções puras já cobertas na Plan 01)
- `npm run build` sucesso (Next.js compila a página client)
- 7 KPI cards presentes; deltas exibem `—` quando prior vazio
- Trend chart e channel split alimentados por daily_rollups reais
</verification>

<success_criteria>
- DASH-01: 7 KPI cards com delta period-over-period (absoluto + %, color-coded por polaridade), sem NaN
- DASH-02: trend chart diário por canal no período selecionado
- DASH-03: channel breakdown Google vs Meta em valores absolutos + % de contribuição
- Mudança de date range re-fetcha automaticamente (queryKey reativo)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-ui/03-03-SUMMARY.md`
</output>

---
phase: 08-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation/01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-06, DASH-01, DASH-02, DASH-03, DASH-04, DASH-03-ext, CAMP-01, CAMP-02, CAMP-03, CAMP-04, SET-02, LEADS-01, LEADS-02, LEADS-03, LEADS-04, LEADS-05]
user_setup: []

must_haves:
  truths:
    - "A formal 01-VERIFICATION.md exists with an explicit verdict for AUTH-01, AUTH-02, and AUTH-06"
    - "REQUIREMENTS.md shows no stale [ ] checkbox for any already-satisfied requirement (AUTH-01/02/06, DASH-01–04, CAMP-01–04, SET-02)"
    - "REQUIREMENTS.md traceability table contains rows for LEADS-01..05 and DASH-03-ext"
  artifacts:
    - path: ".planning/phases/01-foundation/01-VERIFICATION.md"
      provides: "Retroactive goal-backward verification of Phase 1 (AUTH-01/02/06)"
      contains: "AUTH-06"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected checkboxes and complete traceability table"
      contains: "LEADS-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md AUTH-01/02/06 checkboxes"
      to: ".planning/phases/01-foundation/01-VERIFICATION.md"
      via: "flip justified by the retroactive verification artifact"
      pattern: "- \\[x\\] \\*\\*AUTH-01"
---

<objective>
Close the documentation-lag tech debt from the v1.0 re-audit (`.planning/v1.0-MILESTONE-AUDIT.md`, status `tech_debt`): produce the missing formal Phase 1 verification artifact, then correct every stale checkbox and complete the traceability table in REQUIREMENTS.md.

Purpose: The re-audit confirmed 0 unsatisfied requirements but flagged that REQUIREMENTS.md still shows `[ ]`/Pending for requirements that are provably satisfied (AUTH-01/02/06, DASH-01–04, CAMP-01–04, SET-02), and that LEADS-01..05 / DASH-03-ext have no traceability rows at all. This is bookkeeping drift, not a functional gap.
Output: A new `01-VERIFICATION.md` and a fully-corrected `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-dashboard-ui/03-VERIFICATION.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author retroactive Phase 1 verification (01-VERIFICATION.md)</name>
  <files>.planning/phases/01-foundation/01-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-dashboard-ui/03-VERIFICATION.md (the format/structure to mirror: YAML frontmatter with phase/verified/status/score, then "Goal Achievement" → "Observable Truths" table with per-truth Status + Evidence)
    - .planning/ROADMAP.md (Phase 1 section, lines ~35-54 — the phase Goal and its 5 Success Criteria are the goal-backward source of truth)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (DB layer: tenants/tenant_users tables, 4 RLS policies, custom_access_token_hook, migrations 0002-0005 — evidence for AUTH-06)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (Supabase clients, middleware, auth Server Actions — evidence for AUTH-01/AUTH-02 login/logout/session)
    - .planning/phases/01-foundation/01-04-SUMMARY.md (Login UI + authenticated layout — evidence for AUTH-01)
    - .planning/phases/01-foundation/01-05-SUMMARY.md (tenant management + switcher — evidence for AUTH-03/04/05, already marked complete)
    - Live code, read directly (do NOT trust summaries alone): app/(auth) login route/action + lib/supabase/* clients + proxy.ts/middleware (AUTH-01/02 session persistence + invalidation), and supabase/migrations/0002_create_tenants.sql / 0004_create_rls_policies.sql / 0005_custom_access_token_hook.sql (AUTH-06 RLS + JWT claim injection)
  </read_first>
  <action>
    Create `.planning/phases/01-foundation/01-VERIFICATION.md` as a goal-backward verification report for Phase 1 (Foundation), mirroring the structure of `03-VERIFICATION.md`.

    Do NOT spawn a nested gsd-verifier agent. Read the Phase 1 PLAN/SUMMARY files AND the live code listed above, then author the report directly.

    Frontmatter (YAML): `phase: 01-foundation`, `verified: <ISO timestamp>`, `status: passed` (or `human_needed` if any truth genuinely cannot be confirmed from code — but AUTH-03/04/05 are already accepted, and AUTH-01/02/06 have strong live evidence), `score: N/6 requirements verified`, and a note that this is a retroactive verification closing the audit's documentation gap.

    Body must contain a "Goal Achievement" section with an "Observable Truths" table covering ALL SIX Phase 1 requirements, with an explicit per-row verdict (✓ VERIFIED / ⚠ / ✗) and concrete code-path evidence (file path + what it proves) for each:
      - AUTH-01: login with email+password, session persists across sessions (evidence: login Server Action + @supabase/ssr cookie-based session)
      - AUTH-02: logout from any page invalidates session (evidence: signOut Server Action + middleware)
      - AUTH-03: Super Admin tenant CRUD (evidence: lib/actions/tenants.ts + /tenants UI) — already accepted, cite briefly
      - AUTH-04: tenant switching (evidence: tenant-switcher.tsx) — already accepted, cite briefly
      - AUTH-05: three-role gates (evidence: role checks in layouts/proxy.ts) — already accepted, cite briefly
      - AUTH-06: RLS cross-tenant isolation + JWT claims (evidence: migrations 0004 RLS policies using `(SELECT get_tenant_id())`, 0005 custom_access_token_hook injecting tenant_id/role; note the live hook-wiring fix from Phase 5 debug session that confirmed claims populate correctly)

    Explicitly state the verdict for AUTH-01, AUTH-02, AUTH-06 (the three the audit lists as "partial — no formal artifact"). This artifact IS the paper trail that closes their documentation gap.

    Do NOT modify REQUIREMENTS.md in this task — that is Task 2 (which reads this file as its justification).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/01-VERIFICATION.md && grep -q "AUTH-01" .planning/phases/01-foundation/01-VERIFICATION.md && grep -q "AUTH-02" .planning/phases/01-foundation/01-VERIFICATION.md && grep -q "AUTH-06" .planning/phases/01-foundation/01-VERIFICATION.md && grep -qi "VERIFIED" .planning/phases/01-foundation/01-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/01-VERIFICATION.md` exists
    - File contains an explicit verdict token (e.g. "✓ VERIFIED") on rows for AUTH-01, AUTH-02, AUTH-06
    - File has YAML frontmatter with a `phase: 01-foundation` and a `status:` field
    - Each of the 6 AUTH requirements has a cited file-path evidence string (grep `grep -c "\.ts\|\.sql\|\.tsx" 01-VERIFICATION.md` returns >= 6)
  </acceptance_criteria>
  <done>A formal, code-backed Phase 1 verification report exists with an explicit passing verdict for AUTH-01, AUTH-02, and AUTH-06.</done>
</task>

<task type="auto">
  <name>Task 2: Correct all stale checkboxes and complete the traceability table in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being edited — read the current "## v1 Requirements" checkbox list AND the "## Traceability" table so you edit the exact existing lines)
    - .planning/phases/01-foundation/01-VERIFICATION.md (Task 1's output — the justification for flipping AUTH-01/02/06)
    - .planning/ROADMAP.md ("## Coverage Validation" table, lines ~230-273 — the authoritative REQ-ID → Phase → Plan → description mapping to copy LEADS-*/DASH-03-ext rows and plan numbers from)
  </read_first>
  <action>
    Make TWO sets of edits to `.planning/REQUIREMENTS.md`. Change nothing else.

    (A) Flip these checkboxes in the "## v1 Requirements" section from `- [ ]` to `- [x]` (these are all confirmed satisfied per `03-VERIFICATION.md`, this audit's integration checker, and Task 1's new `01-VERIFICATION.md`):
      - `AUTH-01` (line ~7), `AUTH-02` (line ~8), `AUTH-06` (line ~12) — justified by Task 1's 01-VERIFICATION.md
      - `DASH-01`, `DASH-02`, `DASH-03`, `DASH-04` (lines ~25-28)
      - `CAMP-01`, `CAMP-02`, `CAMP-03`, `CAMP-04` (lines ~32-35)
      - `SET-02` (line ~47)
      (Leave AUTH-03/04/05, SYNC-*, AI-*, AGENCY-*, and SET-01 as-is — they are already `[x]`.)

    (B) In the "## Traceability" table, change the Status column from `Pending` to `Complete` for: AUTH-01, AUTH-02, AUTH-06, DASH-01, DASH-02, DASH-03, DASH-04, CAMP-01, CAMP-02, CAMP-03, CAMP-04, SET-02. Fill their `Plan` column (currently `TBD`) using the ROADMAP coverage mapping:
      - AUTH-01 → `01-03, 01-04`; AUTH-02 → `01-03, 01-04`; AUTH-06 → `01-02` (verified by 01-VERIFICATION.md)
      - DASH-01/02/03 → `03-03`; DASH-04 → `03-02`; CAMP-01/02/03/04 → `03-04`; SET-02 → `03-05`

    (C) Append these SIX new rows to the bottom of the "## Traceability" table (mirroring the existing pipe format `| REQ-ID | Phase | Status | Plan |`), sourcing descriptions/phases from ROADMAP's coverage table:
      | LEADS-01 | 03.1 — Leads Management | Complete | 03.1-02 |
      | LEADS-02 | 03.1 — Leads Management | Complete | 03.1-01, 03.1-02 |
      | LEADS-03 | 03.1 — Leads Management | Complete | 03.1-03 |
      | LEADS-04 | 03.1 — Leads Management | Complete | 03.1-02, 03.1-03 |
      | LEADS-05 | 03.1 — Leads Management | Complete | 03.1-02 |
      | DASH-03-ext | 3 — Dashboard UI | Complete | 03-06 |

    (D) Also add a "### Leads Management (Phase 03.1)" subsection to the "## v1 Requirements" checkbox list (the audit's cross-cutting note flags that this section is entirely missing), with five `- [x]` rows using the ROADMAP coverage descriptions:
      - `- [x] **LEADS-01**: Lead status write-back to the source Google Sheet via a Service Account`
      - `- [x] **LEADS-02**: sheets_service_account credential storage (per-tenant JSONB column)`
      - `- [x] **LEADS-03**: Inline editable lead status dropdown in the leads table`
      - `- [x] **LEADS-04**: Optimistic status write with revert-on-failure (no auto-retry)`
      - `- [x] **LEADS-05**: Error mapping for Sheets failures (rate limit / permission)`

    Do not add or renumber any requirement IDs beyond the LEADS/DASH-03-ext rows that already exist in ROADMAP. This is a bookkeeping correction, not a scope change.
  </action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*AUTH-01" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*AUTH-06" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*DASH-01" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*CAMP-04" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*SET-02" .planning/REQUIREMENTS.md && grep -q "LEADS-01" .planning/REQUIREMENTS.md && grep -q "DASH-03-ext" .planning/REQUIREMENTS.md && ! grep -q "^| AUTH-01 .*Pending" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*\(AUTH-01\|AUTH-02\|AUTH-06\|DASH-0\|CAMP-0\|SET-02\)" .planning/REQUIREMENTS.md` returns 0 (no stale unchecked boxes for those IDs)
    - `grep "^| AUTH-01 " .planning/REQUIREMENTS.md` shows `Complete` (not `Pending`) and not `TBD` in the Plan column
    - `.planning/REQUIREMENTS.md` contains a `LEADS-01`, `LEADS-05`, and `DASH-03-ext` traceability row
    - `.planning/REQUIREMENTS.md` contains a "Leads Management" checkbox subsection with 5 `- [x] **LEADS-0` lines
    - No requirement ID that did not already exist in ROADMAP's coverage table was introduced
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects the milestone's true state: every satisfied requirement is `[x]`/Complete, and the traceability table is complete including LEADS-* and DASH-03-ext.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only edits inside `.planning/` — no runtime code, no external input, no live data. No trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering (accuracy) | REQUIREMENTS.md checkbox flips | mitigate | Each flip is gated on cited evidence (01-VERIFICATION.md for AUTH, 03-VERIFICATION.md for DASH/CAMP/SET) — no checkbox flipped without an evidence source |
| T-08-02 | Information disclosure | .planning docs | accept | Planning docs are internal repo artifacts, no secrets written |
</threat_model>

<verification>
- `01-VERIFICATION.md` exists and gives an explicit verdict for AUTH-01/02/06.
- REQUIREMENTS.md has zero stale `[ ]` for satisfied requirements and a complete traceability table.
- `git diff --stat` shows only `.planning/REQUIREMENTS.md` and the new `01-VERIFICATION.md`.
</verification>

<success_criteria>
Phase 1 has a formal verification artifact, and REQUIREMENTS.md is a truthful, complete record of the v1.0 milestone's satisfied requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-01-SUMMARY.md`.
</output>

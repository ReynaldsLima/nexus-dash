---
phase: 08-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0012_add_google_sheets_to_tenants.sql
  - .planning/OPS-FOLLOWUPS.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The dead migration file 0012_add_google_sheets_to_tenants.sql no longer exists in the working tree"
    - "The two remaining external/ops follow-ups (N8N daily-insights activation, Phase 0 VPS security check) are recorded in a durable tracking doc"
  artifacts:
    - path: ".planning/OPS-FOLLOWUPS.md"
      provides: "Durable checklist of manual/ops follow-ups outside this phase's code scope"
      contains: "N8N"
  key_links: []
---

<objective>
Two low-risk repo-hygiene items from the v1.0 re-audit: delete the stray, never-applied migration file, and record the remaining external/ops follow-ups in a durable doc so they don't get lost when v1.0 is archived.

Purpose: `supabase/migrations/0012_add_google_sheets_to_tenants.sql` is untracked scratch that was superseded by the shipped `sheets_service_account` approach (Phase 03.1) and was NEVER applied to any environment. Meanwhile two genuinely-manual follow-ups (N8N daily workflow activation, Phase 0 VPS security check) live only in scattered STATE.md prose and need a single home.
Output: The dead file removed; a new `.planning/OPS-FOLLOWUPS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead, never-applied migration file 0012</name>
  <files>supabase/migrations/0012_add_google_sheets_to_tenants.sql</files>
  <read_first>
    - supabase/migrations/0012_add_google_sheets_to_tenants.sql (confirm it only adds `sheet_id` / `sheets_api_key` columns — the superseded approach; the shipped path is `sheets_service_account` JSONB from Phase 03.1 migration 033b83b)
    - .planning/v1.0-MILESTONE-AUDIT.md (cross-cutting tech_debt item confirming this file is untracked, never-committed, never-applied, safe to delete)
  </read_first>
  <action>
    Delete the file `supabase/migrations/0012_add_google_sheets_to_tenants.sql` from the working tree.

    Since the file is UNTRACKED (confirmed via `git status --short` showing `??`, and `git ls-files` returns empty for it), a plain filesystem delete is correct — `git rm` is not applicable to an untracked file.

    CRITICAL — DO NOT run any schema-push / migration-apply command for this file. It was never applied to any Supabase environment (prod or staging), so there is nothing to push or roll back. Do NOT run `supabase db push`, `supabase migration up`, `apply_migration`, or any equivalent. Deleting the file is the entire task. The columns it defines (`sheet_id`, `sheets_api_key`) do not exist in the live schema and must NOT be created.
  </action>
  <verify>
    <automated>test ! -f supabase/migrations/0012_add_google_sheets_to_tenants.sql && ! git status --short | grep -q "0012_add_google_sheets_to_tenants.sql" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f supabase/migrations/0012_add_google_sheets_to_tenants.sql` is false (file gone)
    - `git status --short` no longer lists `supabase/migrations/0012_add_google_sheets_to_tenants.sql`
    - No schema-push/migration-apply command was executed (verify no new entry in the live schema for `sheet_id`/`sheets_api_key` — but do NOT query if it risks side effects; the file-absence check is sufficient)
  </acceptance_criteria>
  <done>The superseded, never-applied migration file is removed from the repo and the untracked entry is gone from git status.</done>
</task>

<task type="auto">
  <name>Task 2: Record remaining external/ops follow-ups in .planning/OPS-FOLLOWUPS.md</name>
  <files>.planning/OPS-FOLLOWUPS.md</files>
  <read_first>
    - .planning/STATE.md ("Deferred Items from Phase 00 Plan 01" section + "Next action" prose + "Blockers" — source of truth for the exact pending manual/ops items and their env-var names)
    - .planning/v1.0-MILESTONE-AUDIT.md (tech_debt items for phase 00-infrastructure and 04-ai-insights — the VPS security check and N8N activation details)
  </read_first>
  <action>
    Create `.planning/OPS-FOLLOWUPS.md` — a durable, plain checklist of the manual/ops actions that are OUTSIDE this phase's (and v1.0's) code scope. This is a TRACKING doc only. Do NOT attempt to perform, automate, or script any of these actions in this task.

    Include at minimum these two required entries as unchecked `- [ ]` items, each with: what it is, why it's manual, and the exact env vars / endpoints involved:

    1. **N8N daily-insights workflow activation** — Import + activate `n8n-workflows/daily-insights-and-anomaly-detection.json` on the live N8N instance (https://evo.wrdigitalgroup.com.br), wire credentials, replace the `VERCEL_APP_URL` placeholder, and set `active: true`. Requires `ANTHROPIC_API_KEY` and `N8N_INSIGHTS_SECRET` present in Vercel Production (and the N8N Header Auth credential). Blocks AI-02/AI-04 live (daily insight + anomaly producer). Why manual: requires logging into the live N8N editor and the Vercel dashboard — no CLI path wired.

    2. **Phase 0 VPS security check** — SSH into the Hostinger VPS hosting N8N: verify N8N version >= 1.88.0 (CVE-2025-68613, CVSS 10.0), confirm the N8N editor is not publicly accessible without auth, and confirm `N8N_ENCRYPTION_KEY` is persisted to disk (survives reboot). Closes `00-VERIFICATION.md`'s outstanding human_needed items. Why manual: VPS-side SSH-only checks, no application code involved.

    You MAY also list (clearly marked as lower-priority / already-tracked-elsewhere) the other known externally-blocked items from STATE.md so this doc is the single ops home: `ANTHROPIC_API_KEY` in Vercel (Prod/Preview/Dev), Google Ads Developer Token approval, Google Cloud OAuth Client creation + `GOOGLE_ADS_CLIENT_ID/SECRET/GOOGLE_OAUTH_STATE_SECRET` in Vercel, and per-tenant Meta System User tokens in Vault. Keep the two required entries above as the primary focus.

    Add a one-line header noting this doc tracks manual/ops follow-ups for the v1.0 milestone that are intentionally NOT code tasks.
  </action>
  <verify>
    <automated>test -f .planning/OPS-FOLLOWUPS.md && grep -qi "N8N" .planning/OPS-FOLLOWUPS.md && grep -q "N8N_INSIGHTS_SECRET" .planning/OPS-FOLLOWUPS.md && grep -qi "VPS" .planning/OPS-FOLLOWUPS.md && grep -q "\- \[ \]" .planning/OPS-FOLLOWUPS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/OPS-FOLLOWUPS.md` exists
    - Contains an unchecked `- [ ]` entry mentioning N8N daily-insights activation and both `ANTHROPIC_API_KEY` and `N8N_INSIGHTS_SECRET`
    - Contains an unchecked `- [ ]` entry mentioning the VPS security check (N8N version / editor auth)
    - Contains no command that was actually executed against N8N, the VPS, or Vercel (this is a tracking doc — grep the git diff to confirm only the markdown file changed)
  </acceptance_criteria>
  <done>A single durable doc tracks the two required ops follow-ups (plus optionally the other known external blockers), with no ops action actually performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

File deletion of an untracked scratch file + creation of a planning doc. No runtime code, no live data, no external input. The only integrity concern is accidentally applying (rather than deleting) the dead migration.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering (schema drift) | migration 0012 | mitigate | Explicit prohibition on any `supabase db push`/`apply_migration` for this file; task deletes only, never applies — verified by file-absence check, not by touching the DB |
| T-08-04 | Repudiation (lost ops items) | ops follow-ups | mitigate | Follow-ups captured in a durable committed doc rather than left in transient STATE.md prose |
</threat_model>

<verification>
- `supabase/migrations/0012_add_google_sheets_to_tenants.sql` is gone and no longer in git status.
- `.planning/OPS-FOLLOWUPS.md` exists with the two required ops entries.
- No migration-apply command was run against any Supabase environment.
</verification>

<success_criteria>
The repo no longer carries the dead migration scratch file, and the remaining manual/ops follow-ups have a durable home.
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-02-SUMMARY.md`.
</output>

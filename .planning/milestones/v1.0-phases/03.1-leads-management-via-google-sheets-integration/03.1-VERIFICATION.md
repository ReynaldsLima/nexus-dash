---
phase: 03.1-leads-management-via-google-sheets-integration
verified: 2026-07-05T06:27:36Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
---

# Phase 03.1: Leads Management via Google Sheets Integration Verification Report

**Phase Goal:** Super Admin/Tenant Admin can edit a lead status inline in the leads table, writing the change back to the source Google Sheet via a Service Account, with clear error handling on failure (revert + message, no auto-retry). Formalizes the already-shipped read path (dashboard, KPIs, funnel, AI chat).

**Verified:** 2026-07-05T06:27:36Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

No `success_criteria` array is defined in ROADMAP.md for this phase (confirmed via `roadmap get-phase 03.1 --raw` → `"success_criteria": []`), so must-haves were derived by merging the `must_haves.truths` frontmatter blocks of all three plans (01, 02, 03), per Step 2c/Option C. This is the full functional contract of the phase.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `tenants` has a `sheets_service_account` JSONB column | ✓ VERIFIED | `supabase/migrations/0015_add_sheets_service_account_to_tenants.sql:8-9` — `ALTER TABLE ... ADD COLUMN IF NOT EXISTS sheets_service_account JSONB` |
| 2 | Column exists on the LIVE database, not just the migration file | ✓ VERIFIED | `npx supabase migration list` shows `0015` and `0016` present in both Local and Remote columns |
| 3 | TS `Database` type recognizes `tenants.sheets_service_account` (Row/Insert/Update) | ✓ VERIFIED | `types/database.types.ts:291,301,311` — 3 occurrences (Row, Insert, Update) |
| 4 | `google-auth-library` installed | ✓ VERIFIED | `package.json` dependency `google-auth-library`; imported and used in `lib/sheets.ts:1` (`JWT` class) |
| 5 | Pure function maps lead id → sheet row (`id+2`) and A1 range `Leads!F{row}` | ✓ VERIFIED | `lib/sheets.ts:10-17` `rowForId`/`statusRange`; unit-tested in `tests/unit/sheets-row-mapping.test.ts` (11 tests, all passing) |
| 6 | PATCH route writes new status to column F via the Service Account | ✓ VERIFIED | `app/api/leads/[id]/status/route.ts:83` calls `updateLeadStatus(t.sheet_id, sa, rowIndex, status)`; `lib/sheets.ts:56-60` PUTs to Sheets API v4 `values/{range}` |
| 7 | Route rejects `viewer` (403) and unauthenticated (401) | ✓ VERIFIED | `route.ts:37-38` (401 no user), `:45-47` (403 role gate); `tests/unit/leads-status-route.test.ts` covers both cases (10/10 passing) |
| 8 | Route validates `id` as non-negative integer before composing the range | ✓ VERIFIED | `route.ts:30-33` `Number.isInteger(rowIndex) \|\| rowIndex < 0` → 400, checked before any DB/Sheets I/O |
| 9 | Sheets API errors mapped (429/403/401/other → clear messages) | ✓ VERIFIED | `lib/sheets.ts:26-40` `mapSheetsError`; 8 dedicated behavior cases in `tests/unit/sheets-row-mapping.test.ts` |
| 10 | Service Account `private_key` never returned in HTTP response nor logged | ✓ VERIFIED | `grep` confirms no `sheets_service_account`/`sa`/`private_key` in any `NextResponse.json` call; no `console.log` of the credential in `lib/sheets.ts` or the route |
| 11 | No audit table created (D-08) | ✓ VERIFIED | No new migration introduces an audit/history table; migrations 0015/0016 only touch `tenants` column privileges |
| 12 | Status column renders an inline editable dropdown (no modal) | ✓ VERIFIED | `app/[tenant-slug]/leads/page.tsx:49-73` `StatusDropdown` renders a native `<select>` directly in the table cell (`:302-308`) |
| 13 | Selecting a status sends `PATCH .../status` with `{tenant, status}` and updates UI optimistically | ✓ VERIFIED | `page.tsx:132-156` `changeStatus()` — `setLeads` optimistic update before `fetch(..., { method: 'PATCH', body: JSON.stringify({ tenant: slug, status: nextStatus }) })` |
| 14 | Failed write reverts the dropdown and shows an error message | ✓ VERIFIED | `page.tsx:149-152` catch block restores `prevStatus` via `setLeads` and sets `statusError`, rendered as a banner (`:207-211`) |
| 15 | No automatic retry in the UI on failure | ✓ VERIFIED | No re-fetch/backoff logic in `changeStatus`; error path only reverts state and displays message |
| 16 | Dropdown reuses `cat()`/`CATEGORY_LABELS`/`CATEGORY_BG` from `lib/leads.ts` (no duplicated categorization) | ✓ VERIFIED | `page.tsx:8` imports `cat, CATEGORY_LABELS, CATEGORY_BG` from `@/lib/leads`; no re-implementation found |

**Score:** 16/16 truths verified

### Post-Plan Hardening (Code Review Resolution) — Verified Against Current Code

The three plans' SUMMARY.md files predate a code-review pass (`03.1-REVIEW.md`) that found and fixed one critical and two warning-level issues after Plan 02/03 closed. The current state of the code was independently re-checked (not just trusted from REVIEW.md's narrative):

| Finding | Fix Claimed | Verified in Current Code |
|---------|-------------|---------------------------|
| CR-01 (critical): `sheets_service_account` readable by any authenticated tenant member via RLS, not just admins | Migration `0016` revokes table-level SELECT from `authenticated`, re-grants only on non-sensitive columns; route reads the credential via `createServiceClient()` | ✓ `supabase/migrations/0016_restrict_sheets_service_account_column.sql` present, committed (`cad2e4d`), and registered as applied on the Remote database (`supabase migration list` shows 0016 in Local+Remote). `app/api/leads/[id]/status/route.ts:64-65` uses `createServiceClient()` (service-role key, `lib/supabase/service.ts`) instead of the user-scoped client for the `tenants` select — confirmed by reading the current file, not the pre-fix SUMMARY version. |
| WR-01: free-text `status` allowed Sheets formula injection | `status` restricted to a Zod enum of the 4 canonical `CATEGORY_LABELS` values | ✓ `route.ts:14-18` — `VALID_STATUSES = Object.values(CATEGORY_LABELS)`, `status: z.enum(VALID_STATUSES)` — no free text reaches `updateLeadStatus`. Commit `55bee0c`. |
| WR-02: unsafe type assertion on JSONB credential | `ServiceAccountSchema` Zod shape validated via `safeParse` before use | ✓ `route.ts:22-25,77-81` — `ServiceAccountSchema.safeParse(t.sheets_service_account)`, returns 500 with a clear message on failure instead of an unchecked cast. Commit `55bee0c`. |

All three fixes are present in the code as it exists today, not merely claimed in a document.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/0015_add_sheets_service_account_to_tenants.sql` | Adds JSONB write-credential column | ✓ VERIFIED | Exists, applied live |
| `supabase/migrations/0016_restrict_sheets_service_account_column.sql` | Column-level REVOKE/GRANT closing CR-01 | ✓ VERIFIED | Exists, applied live, committed `cad2e4d` |
| `types/database.types.ts` | `sheets_service_account` in tenants Row/Insert/Update | ✓ VERIFIED | 3 occurrences |
| `lib/sheets.ts` | `rowForId`, `statusRange`, `mapSheetsError`, `updateLeadStatus` | ✓ VERIFIED | All 4 exported, unit-tested, wired into the route |
| `app/api/leads/[id]/status/route.ts` | PATCH handler, `runtime='nodejs'`, role gate, service-role read, enum validation | ✓ VERIFIED | All present; matches post-review hardened version |
| `app/[tenant-slug]/leads/page.tsx` | Editable dropdown, optimistic write, revert on failure | ✓ VERIFIED | `StatusDropdown` + `changeStatus` wired into the leads table |
| `lib/leads.ts` | `cat`, `CATEGORY_LABELS`, `CATEGORY_BG` (pre-existing, now committed) | ✓ VERIFIED | Present, committed (`572b5ed`), imported by both the route and the page |
| `app/api/leads/route.ts` | GET leads (pre-existing read path), now cache-tagged per tenant | ✓ VERIFIED | `next: { revalidate: 60, tags: [\`leads-${tenantSlug}\`] }`; untouched functionally otherwise |
| `tests/unit/sheets-row-mapping.test.ts` | Tests for row mapping + error mapping | ✓ VERIFIED | 11/11 passing |
| `tests/unit/leads-status-route.test.ts` | Tests for auth/role/validation/error | ✓ VERIFIED | 10/10 passing |
| `tests/integration/sheets-write.test.ts` | Self-skipping live-write integration test | ✓ VERIFIED | Present, self-skips without env vars, scaffold-sanity passes |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `app/[tenant-slug]/leads/page.tsx` | `PATCH /api/leads/{id}/status` | `fetch(..., { method: 'PATCH' })` in `changeStatus` | ✓ WIRED | `page.tsx:140-144` |
| `app/[tenant-slug]/leads/page.tsx` | `lib/leads.ts` | `import { cat, CATEGORY_LABELS, CATEGORY_BG }` | ✓ WIRED | `page.tsx:8` |
| `app/api/leads/[id]/status/route.ts` | `lib/sheets.ts` | `import { updateLeadStatus, mapSheetsError }` | ✓ WIRED | `route.ts:6`, called at `:83,87` |
| `app/api/leads/[id]/status/route.ts` | `supabase.rpc('get_user_role')` | role gate | ✓ WIRED | `route.ts:41-47` |
| `app/api/leads/[id]/status/route.ts` | `createServiceClient()` | service-role read of sensitive column | ✓ WIRED | `route.ts:64-70` — confirms CR-01 fix is live in current code, not just documented |
| `lib/sheets.ts` | Google Sheets API v4 | `google-auth-library` JWT + `client.fetch` | ✓ WIRED | `lib/sheets.ts:49-60` |
| `app/api/leads/[id]/status/route.ts` | `revalidateTag` | cache invalidation of `GET /api/leads` after successful write | ✓ WIRED | `route.ts:84` `revalidateTag(\`leads-${tenantSlug}\`, 'max')`, matching tag set in `app/api/leads/route.ts:26` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `page.tsx` leads table | `leads` (state) | `GET /api/leads?tenant=${slug}` → live Sheets API read (`app/api/leads/route.ts`) | Yes — real Sheets API call with `sheet_id`/`sheets_api_key` from DB, not a static/mocked array | ✓ FLOWING |
| `StatusDropdown` current value | `cat(lead.status)` | `lead.status` comes from the same live `leads` array | Yes | ✓ FLOWING |
| `PATCH` route write | `sa` (Service Account creds) | `createServiceClient().from('tenants').select('sheet_id, sheets_service_account')` — real DB row, not hardcoded | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unit test suite for row mapping + error classification | `npx vitest run tests/unit/sheets-row-mapping.test.ts` | 11/11 passed | ✓ PASS |
| Unit test suite for PATCH route auth/role/validation/error | `npx vitest run tests/unit/leads-status-route.test.ts` | 10/10 passed | ✓ PASS |
| Self-skipping integration test | `npx vitest run tests/integration/sheets-write.test.ts` | 1 skipped (no env), scaffold sanity passed | ✓ PASS |
| Full project test suite (regression check) | `npx vitest run` | 15 files, 118 passed / 1 skipped / 5 todo | ✓ PASS |
| Type check | `npx tsc --noEmit` | Only 4 pre-existing errors unrelated to this phase (`tests/integration/vault-rpc.test.ts`, `tests/tenants.test.ts`) — none in phase files | ✓ PASS |
| Production build | `npm run build` | Compiled successfully; `/api/leads`, `/api/leads/[id]/status`, `/[tenant-slug]/leads` all present in route manifest as dynamic (ƒ) routes | ✓ PASS |
| Migration application state | `npx supabase migration list` | `0015` and `0016` both present in Local AND Remote columns | ✓ PASS |

End-to-end manual production verification (dropdown write → Google Sheets persistence → immediate reload reflection) was reported as completed against `nexusdash-chi.vercel.app` (tenant `lukseg`) per the Plan 03 checkpoint and this session's context. This cannot be independently re-run by this verifier (no access to the live Sheets document or deployed environment), so it is not counted as an automated PASS, but neither is it flagged as an outstanding human-verification item since the phase's own `checkpoint:human-verify` gate (Plan 03, Task 2) already captured this and was reported approved.

### Requirements Coverage

**Important note (expected, already-logged gap):** `.planning/REQUIREMENTS.md` has no `### Leads Management` section and no `LEADS-*` rows in its Traceability table at all. This is not a new finding — it is documented in this phase's own `deferred-items.md` (items 1 and 2, logged during Plan 01 and Plan 02 execution) as a planner oversight: the ROADMAP.md phase section and all three PLAN.md files reference `LEADS-01..LEADS-05`, but the requirement text was never authored into REQUIREMENTS.md. `requirements mark-complete` calls for these IDs returned `not_found` throughout all three plans, consistent with this gap. This is being reported here for completeness, not as a new or blocking issue — it does not affect whether the phase's actual functional goal was achieved in code.

Cross-referencing the 5 requirement IDs declared in the ROADMAP/PLAN frontmatter against what the code demonstrably does:

| Requirement | Source Plan(s) | Inferred Intent (from ROADMAP goal + plan context) | Status | Evidence |
|-------------|----------------|------------------------------------------------------|--------|----------|
| LEADS-01 | Plan 02 | Core write-back capability (PATCH route + Sheets write) | ✓ SATISFIED | `lib/sheets.ts` + `app/api/leads/[id]/status/route.ts` implement and test the write path end-to-end |
| LEADS-02 | Plan 01 | Service Account credential storage per tenant | ✓ SATISFIED | `sheets_service_account` JSONB column live, now column-level restricted (0016) |
| LEADS-03 | Plan 03 | Inline editable UI (dropdown, no modal) | ✓ SATISFIED | `StatusDropdown` in `page.tsx` |
| LEADS-04 | Plans 02 & 03 | Optimistic write + clear error handling, no auto-retry | ✓ SATISFIED | `changeStatus()` revert-on-failure + `mapSheetsError` server-side messages |
| LEADS-05 | Plan 02 | Authorization/security (role gate, credential never leaked, no range injection) | ✓ SATISFIED | Role gate, service-role read (post-CR-01-fix), id validation, enum-restricted status (post-WR-01-fix) |

| Requirement | Traceability Table Row in REQUIREMENTS.md | Status |
|-------------|---------------------------------------------|--------|
| LEADS-01 through LEADS-05 | Missing entirely (no section, no rows) | ⚠️ ORPHANED (documentation gap, already logged in `deferred-items.md` items 1-2; does not block this verification since the underlying functional requirements are demonstrably met in code) |

### Anti-Patterns Found

None blocking. Scanned all phase-modified files (`lib/sheets.ts`, `lib/leads.ts`, `app/api/leads/route.ts`, `app/api/leads/[id]/status/route.ts`, `app/[tenant-slug]/leads/page.tsx`) for `TODO`/`FIXME`/`PLACEHOLDER`/`not implemented` — no matches. `IN-01` through `IN-04` from `03.1-REVIEW.md` (unused `CATEGORY_COLORS` export, missing `encodeURIComponent` on admin-provisioned `sheetId`, no `AbortController` on the leads fetch effect, `cat()` keyword-priority edge case) remain as documented, intentionally deferred info-level technical debt — none of these block goal achievement or represent a security/functional gap; they are recorded here for visibility, not as new findings.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `lib/leads.ts` | 31 | Unused export `CATEGORY_COLORS` | ℹ️ Info | Dead code, no functional impact (IN-01, deferred by design) |
| `lib/sheets.ts` | 55 | `sheetId` not `encodeURIComponent`-wrapped in Sheets API URL | ℹ️ Info | Low risk — `sheetId` is admin-provisioned, not end-user input (IN-02, deferred by design) |
| `app/[tenant-slug]/leads/page.tsx` | 92-102 | No `AbortController`/unmount guard on initial leads fetch | ℹ️ Info | Harmless React warning risk only (IN-03, deferred by design) |
| `lib/leads.ts` | 15-22 | `cat()` keyword-priority ordering can misclassify compound statuses (e.g. "Negociação encerrada") | ℹ️ Info | Could misclassify an edge-case status string in KPIs/funnel; no test coverage (IN-04, deferred by design) |

### Human Verification Required

None outstanding. The phase's one `checkpoint:human-verify` gate (Plan 03, Task 2 — dropdown grabs/persists/reverts/shows error, viewer rejected) was already executed and approved during this session, with results recorded in the Plan 03 SUMMARY.md ("Verified live in production... dropdown write persisted to the real Google Sheet, correct cell, correct row mapping") and independently corroborated by this verifier's read of the current, post-fix `route.ts` matching the description of what was tested. No new UI/visual/real-time behavior was introduced since that approval that would require fresh human verification.

### Gaps Summary

No functional or security gaps found. All 16 derived observable truths across the three plans are verified against the current state of the code (not the pre-hardening versions referenced in the original SUMMARY.md files). The one critical security finding (CR-01: privileged credential readable by any tenant member via RLS) and both warnings (WR-01 formula injection, WR-02 unsafe cast) from `03.1-REVIEW.md` were independently re-verified as fixed in the current code — `app/api/leads/[id]/status/route.ts` now uses `createServiceClient()`, a Zod enum for `status`, and `ServiceAccountSchema.safeParse` for the credential shape, and migration `0016` (column-level REVOKE/GRANT) is registered as applied on both Local and Remote per `supabase migration list`.

The only notable finding is a documentation gap, not a functional one: `.planning/REQUIREMENTS.md` has no `LEADS-*` section or Traceability rows, despite ROADMAP.md and all three PLAN.md files referencing `LEADS-01..LEADS-05`. This was discovered and logged by the executing plans themselves (`deferred-items.md` items 1-2) as an out-of-scope planner oversight, not hidden or newly discovered here. Recommended follow-up (not blocking phase closure): add a "Leads Management (Google Sheets Write-Back)" section to REQUIREMENTS.md with LEADS-01..LEADS-05 using the Phase 03.1 ROADMAP goal as source text, then run `requirements mark-complete` for all five IDs.

Full test suite (118 passed/1 skipped/5 todo), `npx tsc --noEmit` (no new errors), and `npm run build` (clean production build including all phase routes) all pass, corroborating that the phase is functionally complete and deployable as-is.

---

*Verified: 2026-07-05T06:27:36Z*
*Verifier: Claude (gsd-verifier)*

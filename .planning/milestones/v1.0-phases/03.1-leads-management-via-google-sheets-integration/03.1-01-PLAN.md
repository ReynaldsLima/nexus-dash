---
phase: 03.1-leads-management-via-google-sheets-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - supabase/migrations/0015_add_sheets_service_account_to_tenants.sql
  - types/database.types.ts
autonomous: true
requirements: [LEADS-02]
user_setup:
  - service: google-cloud
    why: "Escrita na Google Sheets API exige uma Service Account (OAuth2 server-to-server); a API key atual é somente-leitura (D-05)"
    dashboard_config:
      - task: "Criar uma Service Account no Google Cloud, habilitar a Google Sheets API, baixar o JSON de credenciais"
        location: "Google Cloud Console → IAM & Admin → Service Accounts"
      - task: "Compartilhar a planilha de leads com o client_email da Service Account como Editor (senão toda escrita retorna 403 PERMISSION_DENIED — ver RESEARCH Pitfall 2)"
        location: "Google Sheets → Compartilhar → colar o client_email (xxx@project.iam.gserviceaccount.com) com permissão Editor"
      - task: "Inserir o JSON da Service Account EXATAMENTE como baixado (byte a byte, sem reformatar as quebras \\n da private_key — ver RESEARCH Pitfall 4) na coluna tenants.sheets_service_account via Supabase Studio Table Editor"
        location: "Supabase Studio → Table Editor → tenants → coluna sheets_service_account (JSONB)"

must_haves:
  truths:
    - "A tabela tenants possui uma coluna sheets_service_account do tipo JSONB para armazenar a credencial de escrita por tenant"
    - "A coluna existe no banco de dados LIVE (não apenas no arquivo de migration)"
    - "O tipo TypeScript Database reconhece tenants.sheets_service_account, permitindo .select('sheet_id, sheets_service_account') sem erro de compilação"
    - "google-auth-library está instalado e disponível para os planos downstream"
  artifacts:
    - path: "supabase/migrations/0015_add_sheets_service_account_to_tenants.sql"
      provides: "ALTER TABLE tenants ADD COLUMN sheets_service_account JSONB"
      contains: "sheets_service_account JSONB"
    - path: "types/database.types.ts"
      provides: "sheets_service_account no tipo tenants (Row/Insert/Update)"
      contains: "sheets_service_account"
    - path: "package.json"
      provides: "dependência google-auth-library"
      contains: "google-auth-library"
  key_links:
    - from: "supabase/migrations/0015_add_sheets_service_account_to_tenants.sql"
      to: "banco de dados live (Supabase)"
      via: "supabase db push"
      pattern: "sheets_service_account"
---

<objective>
Preparar a camada de dados e a dependência necessárias para a escrita de status na planilha Google Sheets.

Esta fase formaliza um recurso já em produção (leitura de leads via Sheets API) e adiciona UMA capacidade nova: editar o status de um lead escrevendo de volta na planilha. Como a `sheets_api_key` atual é somente-leitura, a escrita exige uma credencial nova — uma Service Account (OAuth2 server-to-server) armazenada por tenant (D-05).

Este plano NÃO toca no path de leitura existente (`app/api/leads/route.ts`, `lib/leads.ts`, páginas) — apenas adiciona a coluna `sheets_service_account`, atualiza os tipos gerados e instala `google-auth-library`.

**Resolução de Open Questions (RESEARCH):**
- **OQ #1 (role gate):** resolvido no Plano 02 — a rota de escrita restringe a `super_admin`/`tenant_admin`, seguindo o padrão de `app/api/meta-ads/connect/route.ts` e AUTH-05.
- **OQ #2 (refatorar chat para @anthropic-ai/sdk):** DEFERIDO explicitamente. Não entra nesta fase. Motivo: o padrão real emergente do projeto (Fase 4) é o Vercel AI SDK, não `@anthropic-ai/sdk` (que não está instalado em lugar nenhum). Refatorar `app/api/leads/chat/route.ts` agora resolveria uma inconsistência do CLAUDE.md sem alinhar com a Fase 4 e expandiria escopo. Fica registrado como cleanup independente futuro.
- **OQ #3 (validação de linha pré-escrita):** NÃO implementar (respeita D-08 literalmente — "última escrita vence", sem auditoria).

Purpose: Sem a coluna no banco live e sem a lib de auth, a rota de escrita (Plano 02) não funciona.
Output: Coluna JSONB `sheets_service_account`, tipos atualizados, `google-auth-library` instalado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-CONTEXT.md
@.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-RESEARCH.md

<interfaces>
Bloco `tenants` atual em `types/database.types.ts` (linhas 283-312) — deve ganhar `sheets_service_account` em Row/Insert/Update:

```typescript
tenants: {
  Row: {
    active: boolean
    created_at: string
    id: string
    name: string
    sheet_id: string | null
    sheets_api_key: string | null
    slug: string
  }
  Insert: { ... sheet_id?: string | null; sheets_api_key?: string | null ... }
  Update: { ... sheet_id?: string | null; sheets_api_key?: string | null ... }
}
```

Padrão da migration 0012 (a seguir):
```sql
ALTER TABLE public.tenants
  ADD COLUMN IF NOT EXISTS sheet_id       TEXT,
  ADD COLUMN IF NOT EXISTS sheets_api_key TEXT;
COMMENT ON COLUMN public.tenants.sheet_id IS '...';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar google-auth-library, criar migration 0015 e atualizar tipos</name>
  <files>package.json, package-lock.json, supabase/migrations/0015_add_sheets_service_account_to_tenants.sql, types/database.types.ts</files>
  <read_first>
    - supabase/migrations/0012_add_google_sheets_to_tenants.sql (padrão exato de ALTER TABLE + COMMENT a replicar)
    - types/database.types.ts (linhas 283-312 — bloco tenants Row/Insert/Update a estender)
    - package.json (confirmar que google-auth-library ainda não está em dependencies)
    - .planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-RESEARCH.md (seção Standard Stack — por que google-auth-library e NÃO googleapis)
  </read_first>
  <action>
1. Instalar a dependência de autenticação (NÃO instalar o pacote `googleapis` completo — ~207MB unpacked vs ~600KB; ver RESEARCH Standard Stack):
   ```bash
   npm install google-auth-library
   ```
   Confirmar que `package.json` passa a listar `"google-auth-library"` em `dependencies` (versão `^10.x`).

2. Criar `supabase/migrations/0015_add_sheets_service_account_to_tenants.sql` com EXATAMENTE este conteúdo (mesmo estilo idempotente da 0012):
   ```sql
   -- Phase 03.1 — Leads status write-back
   -- Adiciona credencial de ESCRITA (Service Account) à tabela tenants.
   -- A sheets_api_key existente (0012) permanece para LEITURA; a service account
   -- é usada APENAS nas rotas de escrita (Google Sheets API v4 values.update).
   -- Ver 03.1-RESEARCH.md (D-05, Pitfall 1: API key não autentica escrita).

   ALTER TABLE public.tenants
     ADD COLUMN IF NOT EXISTS sheets_service_account JSONB;

   COMMENT ON COLUMN public.tenants.sheets_service_account IS
     'Google Service Account JSON (client_email + private_key) para escrita na Sheets API — server-side only, NUNCA exposto ao client, NUNCA retornado em resposta HTTP, NUNCA logado.';
   ```

3. Editar `types/database.types.ts`, bloco `tenants` (linhas 283-312), adicionando `sheets_service_account: Json | null` em Row e `sheets_service_account?: Json | null` em Insert e Update, imediatamente após a linha `sheets_api_key`:
   - Row: adicionar `sheets_service_account: Json | null` após `sheets_api_key: string | null`
   - Insert: adicionar `sheets_service_account?: Json | null` após `sheets_api_key?: string | null`
   - Update: adicionar `sheets_service_account?: Json | null` após `sheets_api_key?: string | null`

   (Edição manual porque a regeneração via CLI depende do push que ocorre na Task 2; este edit determinístico evita erro de compilação na rota do Plano 02.)
  </action>
  <verify>
    <automated>grep -q "sheets_service_account JSONB" supabase/migrations/0015_add_sheets_service_account_to_tenants.sql && grep -c "sheets_service_account" types/database.types.ts && node -e "require('./package.json').dependencies['google-auth-library'] ? process.exit(0) : process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0015_add_sheets_service_account_to_tenants.sql` existe e contém a string `sheets_service_account JSONB`
    - `types/database.types.ts` contém a string `sheets_service_account` em pelo menos 3 ocorrências (Row, Insert, Update)
    - `node -e "process.exit(require('./package.json').dependencies['google-auth-library'] ? 0 : 1)"` sai com código 0
    - `npx tsc --noEmit` não reporta erro novo em `types/database.types.ts`
  </acceptance_criteria>
  <done>Migration 0015 criada, tipos atualizados com sheets_service_account nos 3 blocos, google-auth-library em package.json.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar migration ao banco live (supabase db push)</name>
  <files>supabase/migrations/0015_add_sheets_service_account_to_tenants.sql</files>
  <read_first>
    - supabase/migrations/0015_add_sheets_service_account_to_tenants.sql (arquivo criado na Task 1)
    - .planning/STATE.md (Infrastructure Provisioned — projeto Supabase rvkkvjitfddtbdpkupok, sa-east-1)
  </read_first>
  <action>
BLOQUEANTE: a coluna precisa existir no banco LIVE antes de qualquer teste de integração ou verificação manual da escrita. Build e type-check passam SEM o push (os tipos vêm do arquivo gerado, não do schema live), o que criaria um falso-positivo de verificação — por isso este push é obrigatório.

Aplicar a migration:
```bash
supabase db push
```

Não-interativo: se o comando exigir autenticação, garantir que a variável de ambiente `SUPABASE_ACCESS_TOKEN` esteja definida antes de rodar. Se o projeto não estiver linkado, rodar `supabase link --project-ref rvkkvjitfddtbdpkupok` primeiro. Se surgir um prompt interativo de autenticação que não possa ser suprimido, pausar e sinalizar para intervenção manual (auth gate dinâmico).

Após o push, confirmar que a coluna existe no banco (via Supabase MCP `list_tables` na tabela `tenants`, ou `supabase db diff` retornando vazio para esta migration).
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep -q "0015"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completa sem erro
    - A coluna `sheets_service_account` aparece na tabela `tenants` no banco live (verificável via Supabase MCP `list_tables` filtrando schema `public`, tabela `tenants`, ou via `supabase migration list` mostrando 0015 como aplicada)
    - `supabase db diff` não reporta a coluna sheets_service_account como pendente
  </acceptance_criteria>
  <done>Coluna sheets_service_account existe no banco de dados live; migration 0015 registrada como aplicada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Super Admin (manual) → banco de dados | O JSON da Service Account (contém private_key RS256) é inserido manualmente na coluna JSONB via Supabase Studio |
| Banco de dados → código server-side | A coluna é lida apenas server-side (Plano 02); nunca cruza para o client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02 | Information Disclosure (V6) | coluna `tenants.sheets_service_account` (contém private_key) | mitigate | COMMENT explícito na coluna marcando "server-side only, nunca exposto ao client/HTTP/log". A leitura seletiva e o não-vazamento são enforçados na rota do Plano 02 (`.select('sheet_id, sheets_service_account')`, nunca `.select('*')`, nunca retornar `sa` na resposta). RLS já existente em `tenants` (migration 0004) isola a leitura por tenant. |
| T-03.1-06 | Tampering (integridade da credencial) | inserção manual do JSON via SQL/Studio | accept | RESEARCH Pitfall 4: colar o JSON reformatando as quebras `\n` da private_key corrompe a chave. Aceito como risco operacional documentado no `user_setup` (inserir byte a byte via Table Editor). Sem controle de código possível nesta fase (D-03: sem UI de onboarding). |
</threat_model>

<verification>
- `npm install google-auth-library` refletido em package.json + package-lock.json
- Migration 0015 criada seguindo o padrão idempotente da 0012
- `types/database.types.ts` estendido nos 3 blocos do tenants
- `supabase db push` aplicado — coluna existe no banco live (verificação obrigatória, não pular)
- `npx tsc --noEmit` sem erro novo
</verification>

<success_criteria>
- [ ] `sheets_service_account` (JSONB) existe na tabela tenants do banco LIVE
- [ ] `types/database.types.ts` reconhece a coluna (compilação da rota do Plano 02 não quebra)
- [ ] google-auth-library instalado
- [ ] Nenhum arquivo do path de leitura existente foi modificado
</success_criteria>

<output>
Após completar, criar `.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-01-SUMMARY.md`
</output>

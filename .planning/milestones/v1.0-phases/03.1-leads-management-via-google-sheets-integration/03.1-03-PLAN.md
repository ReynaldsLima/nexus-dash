---
phase: 03.1-leads-management-via-google-sheets-integration
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - app/[tenant-slug]/leads/page.tsx
autonomous: false
requirements: [LEADS-03, LEADS-04]

must_haves:
  truths:
    - "A coluna Status da tabela de leads renderiza um dropdown editável inline (sem modal/drawer)"
    - "Selecionar um novo status envia PATCH /api/leads/{id}/status com { tenant, status } e atualiza a UI otimisticamente"
    - "Se a escrita falhar, o dropdown reverte para o status anterior e uma mensagem de erro é exibida"
    - "Não há retry automático na UI em caso de falha"
    - "O dropdown reutiliza cat()/CATEGORY_LABELS de lib/leads.ts (sem recriar categorização)"
  artifacts:
    - path: "app/[tenant-slug]/leads/page.tsx"
      provides: "dropdown de status inline com escrita otimista + revert em erro"
      contains: "/api/leads/"
  key_links:
    - from: "app/[tenant-slug]/leads/page.tsx"
      to: "PATCH /api/leads/{id}/status"
      via: "fetch method PATCH no onChange do dropdown"
      pattern: "method: 'PATCH'"
    - from: "app/[tenant-slug]/leads/page.tsx"
      to: "lib/leads.ts"
      via: "import cat, CATEGORY_LABELS, LeadCategory"
      pattern: "CATEGORY_LABELS"
---

<objective>
Transformar a coluna "Status" (hoje um `<StatusBadge>` somente-leitura) em um dropdown editável inline (D-06), com escrita otimista para a rota PATCH do Plano 02 e reversão + mensagem de erro em caso de falha (D-07).

**Escopo cirúrgico:** modifica APENAS `app/[tenant-slug]/leads/page.tsx`. Não toca em KPIs, funil, filtros, busca ou ordenação — apenas a célula de status.

**Decisão de mensagem de erro (D-07):** o projeto NÃO tem `sonner`/toast instalado, e a página já usa um banner inline (`error` state) para erros. Para honrar D-07 sem expandir escopo (sem nova dependência), a falha de escrita exibe uma mensagem de erro transitória inline (banner no topo da tabela ou por-linha), consistente com o padrão de erro já existente na página. NÃO instalar `sonner`.

**Escrita otimista + revert (D-07):** ao selecionar, a UI atualiza o status do lead localmente na hora; em falha do PATCH, restaura o valor anterior e mostra a mensagem. Sem retry automático.

**Valores escritos:** o dropdown oferece as 4 categorias (`LeadCategory`); ao selecionar, envia `CATEGORY_LABELS[categoria]` como `status` (ex. "Negociando", "Quente", "Novo Lead", "Sem Resposta") — esses rótulos re-categorizam corretamente via `cat()` na próxima leitura (round-trip verificado: 'negociando'→negoc, 'quente'→quente, 'novo lead'→novo, 'sem resposta'→fim).

Purpose: entrega a capacidade visível ao usuário — a razão de ser da fase.
Output: dropdown de status funcional na página de leads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-CONTEXT.md

<interfaces>
De `lib/leads.ts` (reuso obrigatório):
```typescript
export type LeadCategory = 'negoc' | 'quente' | 'novo' | 'fim'
export function cat(s: string): LeadCategory
export const CATEGORY_LABELS: Record<LeadCategory, string>  // { negoc:'Negociando', quente:'Quente', novo:'Novo Lead', fim:'Sem Resposta' }
export const CATEGORY_BG: Record<LeadCategory, string>      // classes tailwind por categoria
```

Contrato da rota (Plano 02): `PATCH /api/leads/{id}/status`, body `{ tenant: slug, status: string }`, resposta `{ success: true }` (200) ou `{ error: string }` (4xx/5xx).

Estado atual da célula (page.tsx ~linha 245): `<td className="px-4 py-3"><StatusBadge status={lead.status} /></td>`.
A página já tem: `const [leads, setLeads] = useState<Lead[]>([])`, `const slug = params['tenant-slug']`, e um banner de erro (`error` state, ~linha 150).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir StatusBadge por dropdown de status editável com escrita otimista e revert</name>
  <files>app/[tenant-slug]/leads/page.tsx</files>
  <read_first>
    - app/[tenant-slug]/leads/page.tsx (arquivo a modificar — ver estado atual: StatusBadge na linha ~245, useState de leads, banner de erro)
    - lib/leads.ts (LeadCategory, cat, CATEGORY_LABELS, CATEGORY_BG a reutilizar)
    - .planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-CONTEXT.md (D-06 dropdown inline, D-07 revert + mensagem sem retry)
  </read_first>
  <action>
Modificar EXCLUSIVAMENTE `app/[tenant-slug]/leads/page.tsx`:

1. Adicionar um estado para mensagem de erro transitória de escrita e (opcional) o id em gravação:
   ```typescript
   const [statusError, setStatusError] = useState<string | null>(null)
   const [savingId, setSavingId] = useState<number | null>(null)
   ```

2. Adicionar a função de gravação (escrita otimista + revert, SEM retry — D-07):
   ```typescript
   async function changeStatus(lead: Lead, nextCat: LeadCategory) {
     const nextStatus = CATEGORY_LABELS[nextCat]
     const prevStatus = lead.status
     // otimista: atualiza local imediatamente
     setLeads(prev => prev.map(l => l.id === lead.id ? { ...l, status: nextStatus } : l))
     setSavingId(lead.id)
     setStatusError(null)
     try {
       const res = await fetch(`/api/leads/${lead.id}/status`, {
         method: 'PATCH',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ tenant: slug, status: nextStatus }),
       })
       if (!res.ok) {
         const d = await res.json().catch(() => ({}))
         throw new Error(d.error ?? 'Falha ao salvar status')
       }
     } catch (e) {
       // revert (D-07) — restaura status anterior
       setLeads(prev => prev.map(l => l.id === lead.id ? { ...l, status: prevStatus } : l))
       setStatusError(e instanceof Error ? e.message : 'Falha ao salvar status')
     } finally {
       setSavingId(null)
     }
   }
   ```

3. Criar um sub-componente `StatusDropdown` (dropdown nativo estilizado, consistente com os `<input>`/`<select>` nativos já usados na página — NÃO instalar shadcn select nem sonner):
   ```typescript
   function StatusDropdown({ lead, disabled, onChange }: {
     lead: Lead
     disabled: boolean
     onChange: (c: LeadCategory) => void
   }) {
     const current = cat(lead.status)
     const OPTIONS: LeadCategory[] = ['novo', 'quente', 'negoc', 'fim']
     return (
       <select
         value={current}
         disabled={disabled}
         onChange={e => onChange(e.target.value as LeadCategory)}
         className={cn(
           'inline-flex items-center px-2 py-0.5 rounded-md text-[11px] font-medium border bg-transparent cursor-pointer outline-none disabled:opacity-50',
           CATEGORY_BG[current],
         )}
       >
         {OPTIONS.map(c => (
           <option key={c} value={c} className="bg-card text-foreground">
             {CATEGORY_LABELS[c]}
           </option>
         ))}
       </select>
     )
   }
   ```

4. Substituir a célula de status na tabela (linha ~245) de:
   ```tsx
   <td className="px-4 py-3"><StatusBadge status={lead.status} /></td>
   ```
   para:
   ```tsx
   <td className="px-4 py-3">
     <StatusDropdown
       lead={lead}
       disabled={savingId === lead.id}
       onChange={c => changeStatus(lead, c)}
     />
   </td>
   ```

5. Exibir `statusError` como banner transitório. Reutilizar o mesmo estilo do banner de erro existente, adicionando abaixo dele (ou renderizando quando `statusError` for truthy):
   ```tsx
   {statusError && (
     <div className="bg-destructive/10 border border-destructive/25 rounded-xl p-3 text-sm text-destructive">
       {statusError}
     </div>
   )}
   ```
   Opcional: limpar `statusError` automaticamente após ~4s com um setTimeout dentro de um useEffect que observa `statusError` (NÃO é um retry — é apenas UX de dismiss).

`StatusBadge` pode permanecer no arquivo se ainda for usado em outro lugar; caso contrário, pode ser removido. Manter `import ... StatusBadge` só se ainda referenciado.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/[tenant-slug]/leads/page.tsx` contém `method: 'PATCH'` e a URL `/api/leads/${lead.id}/status`
    - contém um elemento `<select` na célula de status (dropdown inline — D-06)
    - contém a lógica de revert: após falha, `setLeads` restaura `prevStatus` (grep por `prevStatus`)
    - reutiliza `CATEGORY_LABELS` e `cat(` de `@/lib/leads` (import presente)
    - NÃO contém `import ... 'sonner'` nem adiciona `sonner` a package.json
    - NÃO contém lógica de retry automático do PATCH (nenhum re-fetch em caso de erro)
    - `npx tsc --noEmit` sem erro e `npm run build` completa com sucesso
  </acceptance_criteria>
  <done>Coluna Status vira dropdown editável; PATCH otimista com revert e mensagem de erro em falha; sem retry; sem nova dependência.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação manual do dropdown (revert + mensagem em falha)</name>
  <files>app/[tenant-slug]/leads/page.tsx</files>
  <action>Checkpoint de verificação humana (não-autônomo). O executor NÃO implementa nada aqui — pausa e apresenta ao usuário os passos de <how-to-verify> abaixo para validar manualmente o comportamento do dropdown (gravação, persistência na planilha, revert em falha, mensagem de erro, rejeição de viewer). Aguarda o resume-signal antes de fechar o plano.</action>
  <what-built>
    Dropdown de status inline na coluna Status da página de leads, com escrita de volta na planilha Google Sheets via PATCH, reversão do valor e mensagem de erro em caso de falha (D-06/D-07). Este comportamento é verificação manual-only por design (VALIDATION.md: interação de UI, baixo ROI para automação em fase de 1-3 tenants).
  </what-built>
  <how-to-verify>
    Pré-requisito: o tenant de teste precisa ter `sheets_service_account` preenchido no Supabase e a planilha compartilhada com o `client_email` da Service Account como Editor (ver user_setup do Plano 01).

    1. Abrir `/{tenant-slug}/leads` logado como super_admin ou tenant_admin.
    2. Na coluna Status, abrir o dropdown de um lead e selecionar uma categoria diferente.
       - Esperado: o badge muda de cor/rótulo imediatamente (otimista) e, ao recarregar a página, o novo status persiste (foi escrito na planilha coluna F da linha correta).
    3. Verificar na planilha Google Sheets real que a célula F da linha correspondente (linha = id do lead + 2) recebeu o novo valor.
    4. Simular falha de escrita: temporariamente revogar o acesso Editor da Service Account na planilha (ou apagar `sheet_id` do tenant), então tentar mudar um status.
       - Esperado: o dropdown REVERTE para o status anterior e uma mensagem de erro aparece (ex. "Sem permissão para editar a planilha..." para 403). Sem retry automático.
    5. (Opcional, se houver usuário viewer) Logado como viewer, confirmar que o PATCH é rejeitado com 403 (o dropdown deve reverter e mostrar erro de permissão).
  </how-to-verify>
  <resume-signal>Digite "approved" se o dropdown grava, reverte em falha e mostra a mensagem corretamente; ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (client) → rota PATCH | o client envia id + status; a autorização real é server-side (Plano 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Elevation of Privilege (V4) | dropdown visível a qualquer usuário | mitigate | A UI é apenas conveniência — a autorização é enforçada server-side no Plano 02 (viewer → 403). Esconder/desabilitar o dropdown para viewer é defense-in-depth opcional, NÃO a linha de defesa: mesmo que um viewer dispare o PATCH, a rota rejeita. |
| T-03.1-04 | Denial of Service (quota) | cliques repetidos / retry na UI | mitigate | `disabled={savingId === lead.id}` bloqueia gravações concorrentes na mesma linha; em falha NÃO há retry automático (D-07) — o usuário decide re-tentar manualmente. Nenhum polling adicionado. |
</threat_model>

<verification>
- `npx tsc --noEmit` sem erro
- `npm run build` completa
- `npm test` (suíte completa) permanece verde (nenhum teste existente quebrado)
- Verificação manual (Task 2): grava, persiste, reverte em falha, mostra mensagem
</verification>

<success_criteria>
- [ ] Coluna Status renderiza dropdown editável inline (D-06)
- [ ] Seleção grava via PATCH e atualiza a UI otimisticamente
- [ ] Falha reverte o dropdown e exibe mensagem de erro (D-07), sem retry
- [ ] Reutiliza cat()/CATEGORY_LABELS (sem duplicar categorização)
- [ ] Nenhuma nova dependência (sem sonner); apenas page.tsx modificado
- [ ] Checkpoint manual aprovado
</success_criteria>

<output>
Após completar, criar `.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-03-SUMMARY.md`
</output>

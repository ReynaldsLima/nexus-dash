---
phase: 03.1-leads-management-via-google-sheets-integration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/sheets.ts
  - app/api/leads/[id]/status/route.ts
  - tests/unit/sheets-row-mapping.test.ts
  - tests/unit/leads-status-route.test.ts
  - tests/integration/sheets-write.test.ts
autonomous: true
requirements: [LEADS-01, LEADS-02, LEADS-04, LEADS-05]

must_haves:
  truths:
    - "Existe uma função pura que mapeia o id de um lead para a linha real da planilha (row = id + 2) e monta o range A1 Leads!F{row}"
    - "Existe uma rota PATCH que escreve o novo status na coluna F da linha correta da planilha via Service Account"
    - "A rota rejeita requisições de usuários viewer (403) e não autenticados (401)"
    - "A rota valida o id como inteiro não-negativo antes de compor o range (evita range injection)"
    - "Erros da Sheets API são mapeados: 429 → mensagem de rate limit, 403 → mensagem de permissão, 401 → credencial inválida, outros → erro genérico"
    - "A private_key da Service Account nunca é retornada na resposta HTTP nem logada"
    - "Nenhuma tabela de auditoria é criada (D-08)"
  artifacts:
    - path: "lib/sheets.ts"
      provides: "rowForId, statusRange, mapSheetsError, updateLeadStatus"
      exports: ["rowForId", "statusRange", "mapSheetsError", "updateLeadStatus"]
      min_lines: 40
    - path: "app/api/leads/[id]/status/route.ts"
      provides: "PATCH handler para escrita de status"
      contains: "export const runtime = 'nodejs'"
    - path: "tests/unit/sheets-row-mapping.test.ts"
      provides: "testes da função pura de mapeamento + mapSheetsError"
    - path: "tests/unit/leads-status-route.test.ts"
      provides: "testes de auth/role/validação/erro da rota PATCH"
  key_links:
    - from: "app/api/leads/[id]/status/route.ts"
      to: "lib/sheets.ts"
      via: "import updateLeadStatus / mapSheetsError"
      pattern: "from '@/lib/sheets'"
    - from: "app/api/leads/[id]/status/route.ts"
      to: "supabase.rpc('get_user_role')"
      via: "role gate"
      pattern: "get_user_role"
    - from: "lib/sheets.ts"
      to: "google-auth-library JWT"
      via: "assinatura RS256 delegada à lib oficial"
      pattern: "google-auth-library"
---

<objective>
Implementar a lógica de escrita na planilha: a função de mapeamento de linha (pura/testável), a autenticação via Service Account e a rota PATCH que grava o novo status.

Entrega D-04 (escreve de volta na planilha), D-05 (usa a Service Account, não a API key de leitura), D-07 (mapeamento de erro para mensagens claras, sem retry automático) e respeita D-08 (sem auditoria).

**Padrão de referência:** a rota segue EXATAMENTE o fluxo de `app/api/meta-ads/connect/route.ts` — `auth.getUser()` → gate de role via `get_user_role()` → validação Zod → lógica → resposta apenas com campos seguros.

**Resolução OQ #1 (role gate):** a rota restringe a `super_admin`/`tenant_admin`, bloqueando `viewer` — consistente com AUTH-05 e com o padrão de `meta-ads/connect`.

**Isolamento de linha (Pattern 3 do RESEARCH):** `Lead.id` retornado pela rota GET é o índice 0-based do array `values` da planilha, e a leitura começa em `A2` — logo a linha real = `id + 2`. Determinístico enquanto a ordem das linhas não mudar (edição concorrente é aceita como "última escrita vence", D-08).

Purpose: É o núcleo funcional e de segurança da fase — sem esta rota, o dropdown do Plano 03 não tem para onde escrever.
Output: `lib/sheets.ts`, rota PATCH, e testes unitários (Wave 0 da VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-RESEARCH.md
@.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-VALIDATION.md

<interfaces>
De `app/api/leads/route.ts` (path de leitura existente — confirma o mapeamento de linha):
```typescript
// range = 'Leads!A2:H500'; leads = rows.map((r, i) => ({ id: i, ..., status: r[5] ?? '', ... }))
// → coluna F (índice 5) = status; id = índice do array → linha real = id + 2
```

De `app/api/meta-ads/connect/route.ts` (padrão de auth + role gate a replicar):
```typescript
import { z } from 'zod/v4'          // NOTA: import é 'zod/v4', não 'zod'
export const runtime = 'nodejs'
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
const { data: role, error: roleErr } = await supabase.rpc('get_user_role') // 'super_admin'|'tenant_admin'|'viewer'
if (roleErr || !role) return NextResponse.json({ error: '...' }, { status: 403 })
if (role !== 'super_admin' && role !== 'tenant_admin') return NextResponse.json({ error: '...' }, { status: 403 })
```

De `lib/leads.ts` (reuso — NÃO recriar categorização):
```typescript
export type LeadCategory = 'negoc' | 'quente' | 'novo' | 'fim'
export function cat(s: string): LeadCategory
export const CATEGORY_LABELS: Record<LeadCategory, string>
```

Padrão de teste de integração self-skip (de `tests/integration/vault-rpc.test.ts`):
```typescript
const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
const describeIfEnv = hasTestEnv ? describe : describe.skip
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar lib/sheets.ts (mapeamento de linha + auth Service Account + mapeamento de erro) com testes</name>
  <files>lib/sheets.ts, tests/unit/sheets-row-mapping.test.ts</files>
  <read_first>
    - app/api/leads/route.ts (confirma range 'Leads!A2:H500', id = índice, status = r[5]/coluna F)
    - .planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 3 — tabela de mapeamento de erro 429/403/401)
    - tests/unit/channel-split.test.ts (padrão de teste unitário: import de '@/lib/...', describe/it/expect de vitest)
  </read_first>
  <behavior>
    - rowForId(0) === 2; rowForId(1) === 3; rowForId(498) === 500
    - statusRange(0) === 'Leads!F2'; statusRange(5) === 'Leads!F7'
    - mapSheetsError com { response: { status: 429 } } → { status: 429, message contém "Limite de escrita" }
    - mapSheetsError com { response: { data: { error: { status: 'RESOURCE_EXHAUSTED' } } } } → status 429
    - mapSheetsError com { response: { status: 403 } } → { status: 403, message contém "permissão" }
    - mapSheetsError com { response: { data: { error: { status: 'PERMISSION_DENIED' } } } } → status 403
    - mapSheetsError com { response: { status: 401 } } → { status: 401, message contém "inválida" }
    - mapSheetsError com erro desconhecido (rede/5xx) → { status: 502, message genérica }
  </behavior>
  <action>
Criar `lib/sheets.ts` com EXATAMENTE estas exportações (extrair a lógica pura para funções testáveis — NÃO deixar inline na rota):

```typescript
import { JWT } from 'google-auth-library'

export interface ServiceAccountCreds {
  client_email: string
  private_key: string
}

// Lead.id (rota GET) é o índice 0-based do array `values`; a leitura começa em A2.
// Logo a linha real na planilha = id + 2. Ver 03.1-RESEARCH.md Pattern 3.
export function rowForId(id: number): number {
  return id + 2
}

// Coluna F = status (r[5] na rota GET). Range A1 de escrita.
export function statusRange(id: number): string {
  return `Leads!F${rowForId(id)}`
}

export interface SheetsWriteError {
  status: number   // status HTTP a retornar ao client
  message: string  // mensagem pt-BR (D-07)
}

// Mapeia GaxiosError → resposta HTTP. Ver 03.1-RESEARCH.md Pitfall 3.
// SEM retry automático (D-07) — apenas classifica e devolve mensagem.
export function mapSheetsError(e: unknown): SheetsWriteError {
  const err = e as { response?: { status?: number; data?: { error?: { status?: string } } } }
  const httpStatus = err?.response?.status
  const apiStatus = err?.response?.data?.error?.status
  if (httpStatus === 429 || apiStatus === 'RESOURCE_EXHAUSTED') {
    return { status: 429, message: 'Limite de escrita da planilha atingido. Tente novamente em 1 minuto.' }
  }
  if (httpStatus === 403 || apiStatus === 'PERMISSION_DENIED') {
    return { status: 403, message: 'Sem permissão para editar a planilha. Verifique o compartilhamento da Service Account como Editor.' }
  }
  if (httpStatus === 401) {
    return { status: 401, message: 'Credencial da planilha inválida ou expirada.' }
  }
  return { status: 502, message: 'Falha ao gravar na planilha.' }
}

// Escreve statusValue na célula de status do lead. NUNCA loga serviceAccount.
export async function updateLeadStatus(
  sheetId: string,
  serviceAccount: ServiceAccountCreds,
  id: number,
  statusValue: string,
): Promise<void> {
  const client = new JWT({
    email: serviceAccount.client_email,
    key: serviceAccount.private_key,
    scopes: ['https://www.googleapis.com/auth/spreadsheets'], // escopo mínimo — NUNCA 'drive' completo
  })
  const range = statusRange(id)
  const url = `https://sheets.googleapis.com/v4/spreadsheets/${sheetId}/values/${encodeURIComponent(range)}?valueInputOption=USER_ENTERED`
  await client.fetch(url, {
    method: 'PUT',
    data: { range, majorDimension: 'ROWS', values: [[statusValue]] },
  })
}
```

Criar `tests/unit/sheets-row-mapping.test.ts` cobrindo o bloco <behavior> acima (importar de `@/lib/sheets`, usar describe/it/expect do vitest). Rodar antes de considerar a task pronta.

NÃO implementar validação de linha pré-escrita (OQ #3 / D-08). NÃO implementar retry (D-07).
  </action>
  <verify>
    <automated>npm test -- tests/unit/sheets-row-mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/sheets.ts` contém `export function rowForId(`, `export function statusRange(`, `export function mapSheetsError(`, `export async function updateLeadStatus(`
    - `lib/sheets.ts` contém `from 'google-auth-library'` e `scopes: ['https://www.googleapis.com/auth/spreadsheets']`
    - `lib/sheets.ts` contém `valueInputOption=USER_ENTERED`
    - `npm test -- tests/unit/sheets-row-mapping.test.ts` sai com código 0 (todos os casos do <behavior> passam)
    - `lib/sheets.ts` NÃO contém nenhuma lógica de retry/loop (grep por `setTimeout`/`retry` retorna vazio)
  </acceptance_criteria>
  <done>lib/sheets.ts exporta rowForId/statusRange/mapSheetsError/updateLeadStatus; testes de mapeamento verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar rota PATCH /api/leads/[id]/status com testes de auth/role/validação/erro</name>
  <files>app/api/leads/[id]/status/route.ts, tests/unit/leads-status-route.test.ts</files>
  <read_first>
    - app/api/meta-ads/connect/route.ts (padrão exato: runtime nodejs, auth.getUser, get_user_role gate, zod/v4, resposta só com campos seguros)
    - app/api/leads/route.ts (padrão de select de tenant por slug isolado por RLS: .select(...).eq('slug', ...).eq('active', true).single())
    - lib/sheets.ts (criado na Task 1 — updateLeadStatus, mapSheetsError, ServiceAccountCreds)
    - .planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-RESEARCH.md (seção Code Examples — esqueleto completo da rota)
  </read_first>
  <behavior>
    - id inválido (não inteiro ou negativo, ex. 'abc' ou '-1') → resposta 400
    - sem usuário autenticado (getUser retorna user null) → 401
    - role 'viewer' → 403
    - role null / erro no RPC → 403
    - tenant não encontrado OU sem sheet_id OU sem sheets_service_account → 404
    - role 'super_admin' + updateLeadStatus resolve → 200 { success: true }
    - updateLeadStatus lança erro shape 429 → resposta 429 com mensagem de rate limit (via mapSheetsError)
    - updateLeadStatus lança erro shape 403 → resposta 403 com mensagem de permissão
  </behavior>
  <action>
Criar `app/api/leads/[id]/status/route.ts` com EXATAMENTE este handler (baseado no esqueleto do RESEARCH Code Examples, delegando a escrita e o mapeamento de erro a `lib/sheets.ts`):

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { z } from 'zod/v4'                               // import é 'zod/v4' (padrão do projeto)
import { createClient } from '@/lib/supabase/server'
import { updateLeadStatus, mapSheetsError, type ServiceAccountCreds } from '@/lib/sheets'

export const runtime = 'nodejs' // OBRIGATÓRIO: assinatura RS256 do JWT usa o módulo crypto do Node — não roda no Edge

const BodySchema = z.object({
  tenant: z.string().min(1),
  status: z.string().min(1).max(200), // status vem de texto livre na planilha; cat() em lib/leads.ts parseia por keyword
})

export async function PATCH(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  // 1. Validar id ANTES de tudo (V5 — evita range injection na URL da Sheets API, Threat T-03.1-03)
  const { id } = await params
  const rowIndex = Number(id)
  if (!Number.isInteger(rowIndex) || rowIndex < 0) {
    return NextResponse.json({ error: 'id inválido' }, { status: 400 })
  }

  // 2. Auth
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  // 3. Role gate — só super_admin/tenant_admin (OQ #1, AUTH-05, Threat T-03.1-01)
  const { data: role, error: roleErr } = await supabase.rpc('get_user_role')
  if (roleErr || !role) {
    return NextResponse.json({ error: 'Não foi possível verificar o papel do usuário' }, { status: 403 })
  }
  if (role !== 'super_admin' && role !== 'tenant_admin') {
    return NextResponse.json({ error: 'Apenas super_admin e tenant_admin podem editar status de leads' }, { status: 403 })
  }

  // 4. Validar body
  let body: unknown
  try { body = await req.json() } catch {
    return NextResponse.json({ error: 'Body inválido — JSON esperado' }, { status: 400 })
  }
  const parsed = BodySchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json({ error: parsed.error.issues[0]?.message ?? 'Dados inválidos' }, { status: 400 })
  }
  const { tenant: tenantSlug, status } = parsed.data

  // 5. Buscar credencial — SELECT EXPLÍCITO (nunca '*'); RLS em tenants isola por tenant (Threat T-03.1-02/05)
  const { data: t, error: tErr } = await supabase
    .from('tenants')
    .select('sheet_id, sheets_service_account')
    .eq('slug', tenantSlug)
    .eq('active', true)
    .single()

  if (tErr || !t?.sheet_id || !t?.sheets_service_account) {
    return NextResponse.json({ error: 'Planilha ou credencial de escrita não configurada' }, { status: 404 })
  }

  // 6. Escrever na planilha
  const sa = t.sheets_service_account as unknown as ServiceAccountCreds
  try {
    await updateLeadStatus(t.sheet_id, sa, rowIndex, status)
    return NextResponse.json({ success: true }) // NUNCA retornar `sa` (Threat T-03.1-02)
  } catch (e) {
    const mapped = mapSheetsError(e) // sem retry (D-07)
    return NextResponse.json({ error: mapped.message }, { status: mapped.status })
  }
}
```

Criar `tests/unit/leads-status-route.test.ts` cobrindo o bloco <behavior>:
- Usar `vi.mock('@/lib/supabase/server', ...)` para simular `createClient` retornando um objeto com `auth.getUser`, `rpc` e `from().select().eq().eq().single()` configuráveis por caso.
- Usar `vi.mock('@/lib/sheets', ...)` mantendo `mapSheetsError`/`statusRange`/`rowForId` reais (via `importActual`) e substituindo `updateLeadStatus` por um mock que resolve ou lança um objeto `{ response: { status: 429 } }` / `{ response: { status: 403 } }`.
- Importar `PATCH` da rota e chamá-la com um `Request`/`NextRequest` e `{ params: Promise.resolve({ id }) }`.
- Assertar o `status` e, quando aplicável, o corpo JSON.

NÃO adicionar retry, backoff ou revalidação de linha.
  </action>
  <verify>
    <automated>npm test -- tests/unit/leads-status-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/leads/[id]/status/route.ts` contém `export const runtime = 'nodejs'`
    - contém `from '@/lib/sheets'` e chama `updateLeadStatus(` e `mapSheetsError(`
    - contém `supabase.rpc('get_user_role')` e o gate `role !== 'super_admin' && role !== 'tenant_admin'`
    - contém `.select('sheet_id, sheets_service_account')` (NÃO `.select('*')`)
    - contém a validação `Number.isInteger(rowIndex)` e `rowIndex < 0`
    - a rota NÃO contém a string `sheets_service_account` em nenhum `NextResponse.json` (a credencial nunca é retornada)
    - `npm test -- tests/unit/leads-status-route.test.ts` sai com código 0 (todos os casos do <behavior>)
  </acceptance_criteria>
  <done>Rota PATCH implementada seguindo o padrão meta-ads/connect; testes de auth/role/validação/erro verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de integração opcional de escrita real (self-skip sem env)</name>
  <files>tests/integration/sheets-write.test.ts</files>
  <read_first>
    - tests/integration/vault-rpc.test.ts (padrão self-skip: hasTestEnv → describeIfEnv)
    - lib/sheets.ts (updateLeadStatus a exercitar)
  </read_first>
  <action>
Criar `tests/integration/sheets-write.test.ts` que exercita `updateLeadStatus` contra uma planilha de teste real, seguindo o padrão self-skip de `vault-rpc.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { updateLeadStatus, statusRange } from '@/lib/sheets'

const hasTestEnv =
  !!process.env.GOOGLE_SHEETS_TEST_SPREADSHEET_ID &&
  !!process.env.GOOGLE_SHEETS_TEST_SERVICE_ACCOUNT
const describeIfEnv = hasTestEnv ? describe : describe.skip

describeIfEnv('sheets write (integração — Pitfall 2)', () => {
  it('escreve um status em uma planilha de teste sem lançar', async () => {
    const sa = JSON.parse(process.env.GOOGLE_SHEETS_TEST_SERVICE_ACCOUNT!)
    await expect(
      updateLeadStatus(process.env.GOOGLE_SHEETS_TEST_SPREADSHEET_ID!, sa, 0, 'Novo Lead')
    ).resolves.not.toThrow()
  })
})

describe('sheets-write scaffold sanity', () => {
  it('detecção de env funciona', () => {
    expect(typeof hasTestEnv).toBe('boolean')
  })
})
```

Este teste NÃO deve bloquear a suíte local (auto-pula sem env). É opcional por design (VALIDATION.md).
  </action>
  <verify>
    <automated>npm test -- tests/integration/sheets-write.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/sheets-write.test.ts` existe e contém `describe.skip` no caminho sem env (`const describeIfEnv = hasTestEnv ? describe : describe.skip`)
    - `npm test -- tests/integration/sheets-write.test.ts` sai com código 0 sem as env vars (bloco de integração pulado, scaffold sanity passa)
    - Nenhuma env var de teste é hardcoded no arquivo
  </acceptance_criteria>
  <done>Teste de integração criado com self-skip; suíte local não bloqueia sem credenciais reais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → rota PATCH | body `{ tenant, status }` + `id` no path — entrada não confiável |
| rota PATCH → Google Sheets API | escrita autenticada via Service Account; sheetId vem do banco (não do input) |
| banco → rota | leitura da credencial de escrita (nunca repassada ao client) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Elevation of Privilege (V4) | rota PATCH — viewer escrevendo via chamada direta | mitigate | Role gate server-side via `get_user_role()`: rejeita explicitamente `viewer` com 403. Nunca confiar em esconder o dropdown na UI. Testado em `leads-status-route.test.ts` (caso role 'viewer' → 403). |
| T-03.1-02 | Information Disclosure (V6) | private_key da Service Account | mitigate | `.select('sheet_id, sheets_service_account')` explícito (nunca `*`); `sa` nunca aparece em `NextResponse.json`; nunca logado. `updateLeadStatus` não faz `console.log` da credencial. Assinatura RS256 delegada a `google-auth-library` (nunca hand-rolled). |
| T-03.1-03 | Tampering / range injection (V5) | `id` no path compondo `Leads!F{row}` | mitigate | `Number.isInteger(rowIndex) && rowIndex >= 0` validado ANTES de compor o range. A URL base (`sheets.googleapis.com/.../{sheetId}`) vem do banco, não do input. `encodeURIComponent(range)` na URL. Testado (id 'abc'/'-1' → 400). |
| T-03.1-04 | Denial of Service (quota) | loop de retry acidental esgotando quota da Sheets API | mitigate | D-07 proíbe retry automático — `mapSheetsError` apenas classifica e devolve; nenhum `setTimeout`/loop de retry no código. Acceptance criteria verifica ausência de retry. |
| T-03.1-05 | Spoofing/Tampering (cross-tenant) | tenant_admin escrevendo em planilha de outro tenant | mitigate | `.eq('slug', tenantSlug).eq('active', true)` sob RLS existente (`tenants_member_select`, migration 0004): id = get_tenant_id() → slug de outro tenant retorna 0 rows → 404. RLS é a camada de isolamento, o gate de role é a camada de autorização acima dela. |
</threat_model>

<verification>
- `npm test -- tests/unit/sheets-row-mapping.test.ts` verde
- `npm test -- tests/unit/leads-status-route.test.ts` verde
- `npm test -- tests/integration/sheets-write.test.ts` verde (bloco de integração auto-pulado sem env)
- `npm test` (suíte completa) verde
- `npx tsc --noEmit` sem erro
- grep confirma: role gate presente, select explícito, id validado, credencial nunca retornada, sem retry
</verification>

<success_criteria>
- [ ] lib/sheets.ts exporta as 4 funções, testes de mapeamento verdes
- [ ] Rota PATCH implementada com runtime nodejs, role gate, validação de id, mapeamento de erro
- [ ] viewer bloqueado (403), não autenticado (401), id inválido (400) — todos testados
- [ ] Credencial da Service Account nunca retornada em resposta HTTP
- [ ] Nenhuma tabela de auditoria criada (D-08); nenhum retry automático (D-07)
- [ ] Suíte completa verde
</success_criteria>

<output>
Após completar, criar `.planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-02-SUMMARY.md`
</output>

---
phase: 06-security-consistency-leads-endpoints
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified: []
autonomous: false
requirements: [AGENCY-08]
tags: [security, verification, manual]

must_haves:
  truths:
    - "The Agente IA chat renders the streamed reply as readable text after the SDK migration (no [object Object]/JSON error)"
    - "Exceeding the rate limit surfaces the pt-BR 429 message with a retry time, not a generic failure"
    - "The full test suite is green and no untracked files remain under the leads paths"
  artifacts:
    - path: ".planning/phases/06-security-consistency-leads-endpoints/06-04-SUMMARY.md"
      provides: "Manual verification sign-off record"
      contains: "06-04"
  key_links:
    - from: "browser chat UI"
      to: "POST /api/leads/chat (streamed)"
      via: "manual send + observe streamed render"
      pattern: "getReader"
---

<objective>
Manually verify the two behaviors that unit-mocked tests cannot exercise end-to-end: (1) the chat UI renders
a streamed text reply correctly after the raw-fetch→streamText migration (Pitfall 3 — the single highest-risk
regression in this phase), and (2) the per-user rate limit produces the intended pt-BR 429 UX. Also confirm
the automated phase gate is green.

Purpose: the SDK migration changes the chat response contract from JSON to a text stream; only a real browser
session against the running dev server can confirm the client's streamed-read change works without breaking
the chat. This is the phase's final gate before /gsd-verify-work.
Output: sign-off recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-security-consistency-leads-endpoints/06-CONTEXT.md
@.planning/phases/06-security-consistency-leads-endpoints/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated phase gate (full suite + git-clean check)</name>
  <read_first>
    - .planning/phases/06-security-consistency-leads-endpoints/06-VALIDATION.md (Sampling Rate — Phase gate)
  </read_first>
  <files></files>
  <action>
Run the automated portion of the phase gate before asking the user to verify manually:
1. `npm test` — full suite must be green. The only tolerated failure is the pre-existing, documented
   `anomaly_alerts` realtime websocket cold-start flake (STATE.md / Phase 4 Plan 02); if it appears, re-run
   that test in isolation to confirm it passes and note it as a known non-regression.
2. `npx tsc --noEmit` and `npm run build` — clean apart from the 2 pre-existing unrelated `vault-rpc.test.ts`
   errors.
3. `git status --porcelain -- app/api/leads "app/[tenant-slug]/leads"` — MUST return empty output (D-08).
If any of these fail (other than the two documented pre-existing items), stop and report before the checkpoint.
  </action>
  <verify>
    <automated>npm test && npx vitest run tests/unit/rate-limit.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts && git status --porcelain -- app/api/leads "app/[tenant-slug]/leads"</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (only the documented `anomaly_alerts` flake tolerated, confirmed via isolated re-run)
    - All three Phase 6 test files green (rate-limit, leads-get-route, leads-chat-route)
    - `git status --porcelain -- app/api/leads "app/[tenant-slug]/leads"` returns EMPTY (D-08)
  </acceptance_criteria>
  <done>Automated gate passes; ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — streamed chat reply + rate-limit UX</name>
  <action>
Pause for the user to manually verify the hardened chat end-to-end in a real browser. Do not proceed until
they respond. This confirms the two Manual-Only rows in 06-VALIDATION.md that unit mocks cannot exercise.
  </action>
  <what-built>
    Hardened `POST /api/leads/chat` (role gate + tenant/agency scope + per-user rate limit 20/5min + Claude
    call migrated from raw fetch to streamText/insightModel) and an updated Agente IA client that now sends
    `tenant: slug` and reads the streamed text reply via `res.body.getReader()`/`TextDecoder`. Also hardened
    `GET /api/leads` (role + getClaims() scope). All automated tests pass and both previously-untracked leads
    files are committed.
  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`) and log in as a `tenant_admin` (or `super_admin`).
    2. Open `/{tenant}/leads/agente` (the Agente IA chat). Confirm "N leads carregados" appears.
    3. Send a message (e.g. "Quais leads devo priorizar hoje?"). Confirm the reply renders as readable
       Portuguese text that streams in (token-by-token or appears complete) — NOT `[object Object]`, NOT raw
       JSON, NOT blank. Confirm no errors in the browser console.
    4. (Rate-limit UX) Send 21 messages within 5 minutes (rapid sends or a quick script hitting
       `POST /api/leads/chat`). Confirm the 21st shows the pt-BR rate-limit message
       ("Limite de mensagens atingido. Tente novamente em Ns.") — not a generic connection error.
    5. (Optional scope spot-check) As a `tenant_admin`, confirm you cannot load `/{otherTenant}/leads/agente`
       for a tenant you don't belong to (the underlying GET/chat calls return 403).
  </how-to-verify>
  <verify>Human confirms streamed reply renders correctly and the 429 UX appears.</verify>
  <done>User types "approved" (or issues are captured and fed back into a follow-up).</done>
  <resume-signal>Type "approved" if the streamed chat and 429 UX both work, or describe the issue observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → chat route (streamed) | This plan confirms the hardened boundary behaves correctly end-to-end in a real browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | POST /api/leads/chat | mitigate (verify) | Manually confirm the 429 rate-limit UX fires on the 21st request within the window |
| T-06-03 | Elevation of Privilege (IDOR/BOLA) | POST /api/leads/chat, GET /api/leads | mitigate (verify) | Manually spot-check that a tenant_admin cannot reach another tenant's leads/chat (403) |
| T-06-06 | Tampering (broken contract) | chat client streamed read | mitigate (verify) | Manually confirm the streamed reply renders correctly, catching the Pitfall 3 regression a unit mock cannot |
</threat_model>

<verification>
- Automated gate (Task 1) green before the checkpoint.
- Human confirms streamed chat reply renders and 429 UX appears — the two Manual-Only rows in 06-VALIDATION.md.
</verification>

<success_criteria>
- Automated phase gate passes (full suite + all 3 Phase 6 test files green + git clean under leads paths).
- Human verifies the Agente IA chat streams a readable reply and the rate limit produces the pt-BR 429 message.
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-consistency-leads-endpoints/06-04-SUMMARY.md`
recording the automated gate result and the human verification outcome.
</output>

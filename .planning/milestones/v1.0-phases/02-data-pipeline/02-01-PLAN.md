---
phase: 02-data-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/campaign-metrics-schema.test.ts
  - tests/unit/daily-rollups-schema.test.ts
  - tests/integration/sync-jobs-rls.test.ts
  - tests/integration/vault-rpc.test.ts
autonomous: true
requirements:
  - SYNC-04
  - SYNC-05
user_setup: []

must_haves:
  truths:
    - "Test scaffolds run via `npm test` and exit 0 even when SUPABASE_TEST_URL is unset (auto-skip)"
    - "Each scaffold contains it.todo() placeholders that describe the exact assertion each test will perform once migrations exist"
    - "Vitest grep filters (`campaign_metrics`, `daily_rollups`, `sync_jobs`, `vault`) each match at least one describe/it block"
  artifacts:
    - path: "tests/unit/campaign-metrics-schema.test.ts"
      provides: "Unit test scaffold for campaign_metrics constraints (NOT NULL, UNIQUE, CHECK channel, CHECK attribution_window)"
      min_lines: 25
    - path: "tests/unit/daily-rollups-schema.test.ts"
      provides: "Unit test scaffold for daily_rollups UNIQUE(tenant_id, channel, date)"
      min_lines: 20
    - path: "tests/integration/sync-jobs-rls.test.ts"
      provides: "Integration scaffold: tenant_admin sees only own sync_jobs; cross-tenant returns 0"
      min_lines: 25
    - path: "tests/integration/vault-rpc.test.ts"
      provides: "Integration scaffold: read_vault_secret rejects anon/authenticated, accepts service_role"
      min_lines: 25
  key_links:
    - from: "tests/unit/campaign-metrics-schema.test.ts"
      to: "supabase/migrations/0007_create_campaign_metrics.sql (Plan 02)"
      via: "describe('campaign_metrics schema (SYNC-05)') matches `--grep campaign_metrics` in 02-VALIDATION.md"
      pattern: "describe.*campaign_metrics"
    - from: "tests/integration/sync-jobs-rls.test.ts"
      to: "supabase/migrations/0008_create_sync_jobs.sql (Plan 02)"
      via: "describe('sync_jobs RLS (SYNC-04)') matches `--grep sync_jobs`"
      pattern: "describe.*sync_jobs"
    - from: "tests/integration/vault-rpc.test.ts"
      to: "supabase/migrations/0010_create_pipeline_functions.sql (Plan 02)"
      via: "describe('read_vault_secret RPC') matches `--grep vault`"
      pattern: "describe.*[Vv]ault"
---

<objective>
Criar os scaffolds de teste (Wave 0) que serão preenchidos contra o schema real assim que as migrations da Plan 02 forem aplicadas. Os scaffolds devem rodar via `npm test` e auto-skipar quando `SUPABASE_TEST_URL` não estiver definido — exatamente como `tests/rls.test.ts` faz hoje.

Purpose: Garantir cobertura Nyquist para SYNC-04 e SYNC-05 antes da implementação. Cada scaffold materializa as asserções de schema/RLS que a Plan 02 precisa satisfazer.
Output: 4 arquivos de teste em `tests/unit/` e `tests/integration/` com describe/it blocks que casam com os greps definidos em 02-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@.planning/phases/02-data-pipeline/02-VALIDATION.md
@tests/rls.test.ts
@tests/setup.ts

<interfaces>
<!-- Padrão estabelecido na Fase 1 (tests/rls.test.ts) que TODO scaffold de integration DEVE replicar -->

From tests/rls.test.ts:
```typescript
const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
const describeIfEnv = hasTestEnv ? describe : describe.skip

describeIfEnv('RLS tenant isolation (AUTH-06)', () => {
  it.todo('tenant A user sees only tenant A rows when SELECTing tenants table')
  // ...
})

describe('RLS scaffold sanity', () => {
  it('test environment detection works', () => {
    expect(typeof hasTestEnv).toBe('boolean')
  })
})
```

From tests/setup.ts:
```typescript
import { config } from 'dotenv'
import { resolve } from 'node:path'
config({ path: resolve(process.cwd(), '.env.test.local') })
```

Env vars usados por integration tests: `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_KEY`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold de teste unit para campaign_metrics (SYNC-05)</name>
  <files>tests/unit/campaign-metrics-schema.test.ts</files>
  <read_first>
    - tests/rls.test.ts (padrão de scaffold com auto-skip via hasTestEnv)
    - tests/setup.ts (loader de .env.test.local)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-03 (lista exata de colunas e constraints)
    - .planning/phases/02-data-pipeline/02-VALIDATION.md (verifica que `npm test -- --grep "campaign_metrics"` precisa casar com este arquivo)
  </read_first>
  <behavior>
    - Test 1 (todo): "attribution_window column NOT NULL with DEFAULT '7d_click'" — INSERT sem attribution_window deve gravar '7d_click' (SYNC-05)
    - Test 2 (todo): "channel CHECK constraint rejects values outside ('google_ads','meta_ads')"
    - Test 3 (todo): "UNIQUE(tenant_id, campaign_id, channel, date) — inserir duplicata retorna 23505"
    - Test 4 (todo): "spend, conversions, conversion_value default to 0 when omitted"
    - Test 5 (todo): "tenant_id FK ON DELETE CASCADE — apagar tenant remove métricas"
    - Sanity test (não-todo): valida que `hasTestEnv` é boolean (espelha tests/rls.test.ts)
  </behavior>
  <action>
    Criar `tests/unit/campaign-metrics-schema.test.ts` REPLICANDO o padrão de `tests/rls.test.ts`. Conteúdo obrigatório:

    ```typescript
    import { describe, it, expect } from 'vitest'

    /**
     * SYNC-05 — attribution_window NOT NULL com DEFAULT '7d_click' desde o dia 1.
     * SYNC-04 — schema base de campaign_metrics.
     *
     * Per CONTEXT.md §D-03, campaign_metrics tem:
     *   - UNIQUE(tenant_id, campaign_id, channel, date)
     *   - CHECK (channel IN ('google_ads','meta_ads'))
     *   - attribution_window NOT NULL DEFAULT '7d_click'
     *
     * These tests require a live Supabase project (staging) com a migration 0007 aplicada.
     * They self-skip if SUPABASE_TEST_URL is unset so the scaffold phase passes.
     *
     * Preenchidas pela verificação da Plan 02 (migrations).
     */
    const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
    const describeIfEnv = hasTestEnv ? describe : describe.skip

    describeIfEnv('campaign_metrics schema (SYNC-05, SYNC-04)', () => {
      it.todo("attribution_window NOT NULL com DEFAULT '7d_click' — INSERT sem coluna grava '7d_click'")
      it.todo("channel CHECK constraint rejeita valores fora de ('google_ads','meta_ads') com SQLSTATE 23514")
      it.todo('UNIQUE(tenant_id, campaign_id, channel, date) — inserir duplicata retorna SQLSTATE 23505')
      it.todo('spend, conversions, conversion_value default to 0 quando omitidos')
      it.todo('tenant_id FK ON DELETE CASCADE remove métricas órfãs')
    })

    describe('campaign_metrics scaffold sanity', () => {
      it('test environment detection works', () => {
        expect(typeof hasTestEnv).toBe('boolean')
      })
    })
    ```

    NÃO adicionar lógica de teste real ainda — apenas `it.todo()`. A Plan 02 (executor da migration) é quem preenche estes testes contra o Supabase staging.
  </action>
  <verify>
    <automated>npm test -- --grep "campaign_metrics"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/unit/campaign-metrics-schema.test.ts` existe
    - Arquivo contém literal `describeIfEnv('campaign_metrics schema (SYNC-05, SYNC-04)'`
    - Arquivo contém literal `it.todo("attribution_window NOT NULL com DEFAULT '7d_click'`
    - Arquivo contém literal `it.todo('UNIQUE(tenant_id, campaign_id, channel, date)`
    - Arquivo contém literal `it.todo("channel CHECK constraint rejeita`
    - `npm test -- --grep "campaign_metrics"` sai com código 0
    - Saída do test reporta pelo menos 1 sanity test passing + 5 todos
  </acceptance_criteria>
  <done>Scaffold criado, 5 it.todo + 1 sanity test, `npm test -- --grep "campaign_metrics"` passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold de teste unit para daily_rollups</name>
  <files>tests/unit/daily-rollups-schema.test.ts</files>
  <read_first>
    - tests/rls.test.ts (padrão hasTestEnv)
    - tests/unit/campaign-metrics-schema.test.ts (criado em Task 1 — mesmo formato)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-11 (schema daily_rollups)
  </read_first>
  <behavior>
    - Test 1 (todo): "UNIQUE(tenant_id, channel, date) — INSERT duplicado retorna SQLSTATE 23505"
    - Test 2 (todo): "channel pode aceitar 'google_ads', 'meta_ads' ou 'all' (cross-channel)"
    - Test 3 (todo): "Default values: total_spend=0, total_impressions=0, total_clicks=0, total_conversions=0, total_conv_value=0, campaign_count=0"
    - Test 4 (todo): "tenant_id FK ON DELETE CASCADE remove rollups órfãos"
    - Sanity test (não-todo): valida hasTestEnv como boolean
  </behavior>
  <action>
    Criar `tests/unit/daily-rollups-schema.test.ts` no MESMO formato de Task 1. Conteúdo obrigatório:

    ```typescript
    import { describe, it, expect } from 'vitest'

    /**
     * daily_rollups schema constraints (pré-requisito para queries de KPI da Fase 3).
     *
     * Per CONTEXT.md §D-11:
     *   - UNIQUE(tenant_id, channel, date)
     *   - channel aceita 'google_ads', 'meta_ads', 'all'
     *   - Todos os totals default to 0
     *
     * Self-skip if SUPABASE_TEST_URL unset.
     */
    const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
    const describeIfEnv = hasTestEnv ? describe : describe.skip

    describeIfEnv('daily_rollups schema', () => {
      it.todo('UNIQUE(tenant_id, channel, date) — inserir duplicata retorna SQLSTATE 23505')
      it.todo("channel aceita 'google_ads', 'meta_ads', e 'all' (cross-channel rollup)")
      it.todo('Defaults: total_spend=0, total_impressions=0, total_clicks=0, total_conversions=0, total_conv_value=0, campaign_count=0')
      it.todo('tenant_id FK ON DELETE CASCADE remove rollups órfãos')
    })

    describe('daily_rollups scaffold sanity', () => {
      it('test environment detection works', () => {
        expect(typeof hasTestEnv).toBe('boolean')
      })
    })
    ```

    Apenas it.todo() — preenchido pela Plan 02.
  </action>
  <verify>
    <automated>npm test -- --grep "daily_rollups"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/unit/daily-rollups-schema.test.ts` existe
    - Arquivo contém literal `describeIfEnv('daily_rollups schema'`
    - Arquivo contém literal `it.todo('UNIQUE(tenant_id, channel, date)`
    - Arquivo contém literal `"channel aceita 'google_ads', 'meta_ads', e 'all'`
    - `npm test -- --grep "daily_rollups"` sai com código 0
  </acceptance_criteria>
  <done>Scaffold criado, 4 it.todo + 1 sanity, grep daily_rollups passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold de teste integration para sync_jobs RLS (SYNC-04)</name>
  <files>tests/integration/sync-jobs-rls.test.ts</files>
  <read_first>
    - tests/rls.test.ts (padrão hasTestEnv + describeIfEnv)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-08 (schema sync_jobs)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 8: RLS para novas tabelas"
  </read_first>
  <behavior>
    - Test 1 (todo): "tenant_admin do tenant A não vê sync_jobs do tenant B (SELECT retorna 0 rows)"
    - Test 2 (todo): "super_admin vê todos os sync_jobs de todos os tenants"
    - Test 3 (todo): "anon role recebe 0 rows ou 401 ao tentar SELECT em sync_jobs (REVOKE ALL FROM anon)"
    - Test 4 (todo): "status CHECK constraint rejeita valores fora de ('running','success','failed')"
    - Sanity test (não-todo): hasTestEnv boolean
  </behavior>
  <action>
    Criar `tests/integration/sync-jobs-rls.test.ts` (criar pasta `tests/integration/` se não existir). Conteúdo obrigatório:

    ```typescript
    import { describe, it, expect } from 'vitest'

    /**
     * SYNC-04 — sync_jobs RLS isolation entre tenants.
     *
     * Per CONTEXT.md §D-08:
     *   - sync_jobs(tenant_id, channel, status, started_at, completed_at, records_synced, date_from, date_to, error_message)
     *   - status CHECK IN ('running','success','failed')
     *
     * Per RESEARCH.md §Padrão 8: RLS segue o mesmo wrapper (SELECT public.get_user_role()) e
     * (SELECT public.get_tenant_id()) da Fase 1. service_role bypassa RLS.
     *
     * Self-skip if SUPABASE_TEST_URL unset.
     */
    const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
    const describeIfEnv = hasTestEnv ? describe : describe.skip

    describeIfEnv('sync_jobs RLS (SYNC-04)', () => {
      it.todo('tenant_admin do tenant A vê 0 rows ao SELECT sync_jobs WHERE tenant_id = <tenant B>')
      it.todo('super_admin vê todos os sync_jobs de todos os tenants')
      it.todo('anon role recebe 0 rows (REVOKE ALL ON sync_jobs FROM anon)')
      it.todo("status CHECK rejeita valores fora de ('running','success','failed') com SQLSTATE 23514")
      it.todo('tenant_admin não consegue INSERT em sync_jobs (apenas service_role/super_admin)')
    })

    describe('sync_jobs RLS scaffold sanity', () => {
      it('test environment detection works', () => {
        expect(typeof hasTestEnv).toBe('boolean')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- --grep "sync_jobs"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/integration/sync-jobs-rls.test.ts` existe
    - Pasta `tests/integration/` existe (criada se necessário)
    - Arquivo contém literal `describeIfEnv('sync_jobs RLS (SYNC-04)'`
    - Arquivo contém literal `it.todo('tenant_admin do tenant A vê 0 rows`
    - Arquivo contém literal `it.todo('super_admin vê todos os sync_jobs`
    - `npm test -- --grep "sync_jobs"` sai com código 0
  </acceptance_criteria>
  <done>Scaffold criado, 5 it.todo + 1 sanity, grep sync_jobs passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Scaffold de teste integration para read_vault_secret RPC</name>
  <files>tests/integration/vault-rpc.test.ts</files>
  <read_first>
    - tests/rls.test.ts (padrão hasTestEnv)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 6: Supabase Vault" (SECURITY DEFINER + GRANT TO service_role only)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Security Domain" (T-2-03)
  </read_first>
  <behavior>
    - Test 1 (todo): "anon client recebe 401 ou 'function does not exist' ao chamar /rpc/read_vault_secret"
    - Test 2 (todo): "authenticated client (tenant_admin) recebe permission denied (REVOKE FROM authenticated)"
    - Test 3 (todo): "service_role client recebe o decrypted_secret correto quando passa um secret_name válido"
    - Test 4 (todo): "service_role com secret_name inexistente recebe NULL (não exception)"
    - Sanity test (não-todo): hasTestEnv boolean
  </behavior>
  <action>
    Criar `tests/integration/vault-rpc.test.ts`. Conteúdo obrigatório:

    ```typescript
    import { describe, it, expect } from 'vitest'

    /**
     * Vault RPC isolation — read_vault_secret deve ser acessível APENAS via service_role.
     *
     * Per RESEARCH.md §Padrão 6:
     *   CREATE FUNCTION public.read_vault_secret(p_secret_name TEXT)
     *     RETURNS TEXT SECURITY DEFINER ...
     *   GRANT EXECUTE TO service_role;
     *   REVOKE EXECUTE FROM authenticated, anon;
     *
     * Threat T-2-03: Elevation of Privilege se função estiver acessível via PostgREST sem
     * service_role. Este teste garante que GRANT/REVOKE estão corretos.
     *
     * Self-skip if SUPABASE_TEST_URL unset.
     */
    const hasTestEnv = !!process.env.SUPABASE_TEST_URL && !!process.env.SUPABASE_TEST_SERVICE_KEY
    const describeIfEnv = hasTestEnv ? describe : describe.skip

    describeIfEnv('read_vault_secret RPC (Threat T-2-03)', () => {
      it.todo('anon client recebe 401/permission denied ao POST /rest/v1/rpc/read_vault_secret')
      it.todo('authenticated client (tenant_admin) recebe permission denied (REVOKE EXECUTE FROM authenticated)')
      it.todo('service_role client recebe o decrypted_secret quando passa um secret_name válido')
      it.todo('service_role com secret_name inexistente recebe NULL (não exception)')
    })

    describe('vault rpc scaffold sanity', () => {
      it('test environment detection works', () => {
        expect(typeof hasTestEnv).toBe('boolean')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- --grep "vault"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/integration/vault-rpc.test.ts` existe
    - Arquivo contém literal `describeIfEnv('read_vault_secret RPC (Threat T-2-03)'`
    - Arquivo contém literal `it.todo('anon client recebe 401`
    - Arquivo contém literal `it.todo('service_role client recebe o decrypted_secret`
    - `npm test -- --grep "vault"` sai com código 0
    - `npm test` (full suite) sai com código 0 (não quebra suíte existente)
  </acceptance_criteria>
  <done>Scaffold criado, 4 it.todo + 1 sanity, full suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Supabase staging | Tests usam SUPABASE_TEST_SERVICE_KEY apenas via .env.test.local (não commitado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-WAVE0-01 | Information Disclosure | tests/setup.ts loader | mitigate | .env.test.local listado em .gitignore (já feito na Fase 1); scaffolds usam `process.env.SUPABASE_TEST_SERVICE_KEY` (nunca literal) |
| T-2-WAVE0-02 | Tampering | scaffolds vazios passam npm test | accept | it.todo() não passa por engano — a Plan 02 SUBSTITUI todos os it.todo por testes reais antes do verify final da fase |

</threat_model>

<verification>
- `npm test` sai com código 0 (todos os scaffolds + suite da Fase 1)
- `npm test -- --grep "campaign_metrics"` casa com Task 1
- `npm test -- --grep "daily_rollups"` casa com Task 2
- `npm test -- --grep "sync_jobs"` casa com Task 3
- `npm test -- --grep "vault"` casa com Task 4
- Cada arquivo tem o bloco `describe('... scaffold sanity', ...)` com 1 test que SEMPRE passa (não depende de SUPABASE_TEST_URL)
</verification>

<success_criteria>
- 4 arquivos de teste existem nos caminhos exatos especificados em files_modified
- Cada arquivo segue o padrão de `tests/rls.test.ts` (hasTestEnv + describeIfEnv + sanity describe)
- Todos os greps definidos em 02-VALIDATION.md "Per-Task Verification Map" casam com pelo menos um describe/it
- `npm test` continua passando sem variáveis de ambiente de Supabase staging (auto-skip)
- Nenhuma migration ainda criada — apenas os scaffolds
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-01-SUMMARY.md` documentando:
- Arquivos criados (paths absolutos)
- Confirmação de que `npm test` passa
- Lista dos 18+ it.todo() que a Plan 02 precisa preencher contra Supabase staging
- Qualquer desvio do padrão tests/rls.test.ts (não deve haver)
</output>

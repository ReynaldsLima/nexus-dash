---
phase: 02-data-pipeline
plan: 05
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - app/tenants/page.tsx
  - components/tenants/sync-status-section.tsx
  - lib/sync-status.ts
autonomous: false
requirements:
  - SYNC-03
  - SYNC-04
user_setup: []

must_haves:
  truths:
    - "Página /tenants exibe uma seção 'Status de Sync' abaixo da tabela de tenants"
    - "A seção mostra, para cada combinação (tenant, channel ∈ {google_ads, meta_ads}), o último sync_job: status (success/failed/running), completed_at formatado, error_message (se failed), records_synced"
    - "Se um tenant nunca teve um sync para um channel, exibe 'Nenhum sync ainda' nesse slot (não quebra o layout)"
    - "Query é Server Component (RSC) — sem TanStack Query — usando o client Supabase createClient() de lib/supabase/server.ts"
    - "A query usa DISTINCT ON (tenant_id, channel) ORDER BY completed_at DESC NULLS LAST para pegar o último por par"
    - "Apenas Super Admin vê a seção — viewers/tenant_admin não acessam /tenants (já protegido por middleware na Fase 1)"
  artifacts:
    - path: "lib/sync-status.ts"
      provides: "loadLastSyncByTenantChannel() Server-only data fetcher"
      exports: ["loadLastSyncByTenantChannel", "SyncStatusRow"]
    - path: "components/tenants/sync-status-section.tsx"
      provides: "Server Component renderizando tabela de último sync por tenant+channel"
      contains: "export function SyncStatusSection"
    - path: "app/tenants/page.tsx"
      provides: "Página atualizada incluindo <SyncStatusSection />"
      contains: "SyncStatusSection"
  key_links:
    - from: "components/tenants/sync-status-section.tsx"
      to: "lib/sync-status.ts"
      via: "import { loadLastSyncByTenantChannel } from '@/lib/sync-status'"
      pattern: "from '@/lib/sync-status'"
    - from: "lib/sync-status.ts"
      to: "public.sync_jobs (Plan 02 migration 0008)"
      via: "supabase.from('sync_jobs').select(...) com JOIN em tenants(name, slug)"
      pattern: "from\\('sync_jobs'\\)"
    - from: "app/tenants/page.tsx"
      to: "components/tenants/sync-status-section.tsx"
      via: "<SyncStatusSection /> renderizado abaixo de <TenantsTable />"
      pattern: "<SyncStatusSection"
---

<objective>
Adicionar a seção "Status de Sync" à página `/tenants` existente, exibindo o último sync_job por (tenant, channel) com status, timestamp e error_message (quando aplicável). Cobre SYNC-03 (UI de visibilidade do último sync) e completa a superfície de SYNC-04 (gravado pelos workflows, agora SURFACED ao Super Admin via UI).

Purpose: Em v1, o Super Admin precisa saber, com um olhar, se cada par tenant+channel está sincronizando corretamente. Sem essa visibilidade, falhas de sync ficam invisíveis até alguém abrir o Supabase SQL editor.
Output: Componente Server `SyncStatusSection` integrado à página `/tenants` + data fetcher `loadLastSyncByTenantChannel`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@app/tenants/page.tsx
@components/tenants/tenants-table.tsx
@lib/supabase/server.ts
@types/database.types.ts

<interfaces>
<!-- Padrões da Fase 1 que esta plan replica -->

From app/tenants/page.tsx (estrutura atual — extender, não substituir):
```typescript
async function loadTenants(): Promise<TenantRow[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('tenants')
    .select('id, name, slug, active')
    .order('created_at', { ascending: false })
  // ...
}

export default async function TenantsPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const tenants = await loadTenants()

  return (
    <section className="flex flex-col gap-8">
      <header>...</header>
      <Suspense fallback={...}>
        <TenantsTable tenants={tenants} />
      </Suspense>
      {/* SyncStatusSection vai aqui */}
    </section>
  )
}
```

From lib/supabase/server.ts (uso correto):
```typescript
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()  // Server Component context
```

From components/tenants/tenants-table.tsx (padrão de estrutura de table):
- Usa `@/components/ui/table` (Table, TableHeader, TableBody, TableRow, TableHead, TableCell)
- Empty state via `<div className="rounded-lg border border-border bg-card p-12 text-center">`

Schema sync_jobs (Plan 02 migration 0008):
```sql
sync_jobs(id, tenant_id, channel, status, started_at, completed_at, records_synced, date_from, date_to, error_message, created_at)
```

Channels a exibir: `google_ads`, `meta_ads` (fixos — D-02).

RLS: A página /tenants é Super Admin only (Fase 1 D-04, D-06). Super Admin policy permite SELECT em todos os sync_jobs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar lib/sync-status.ts (data fetcher Server-only)</name>
  <files>lib/sync-status.ts, lib/sync-status.test.ts</files>
  <read_first>
    - lib/supabase/server.ts (createClient + assinatura)
    - app/tenants/page.tsx (padrão de loadTenants)
    - types/database.types.ts (Row types — após Plan 02 Task 6, sync_jobs estará lá)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 11: SYNC-03"
  </read_first>
  <behavior>
    - Test 1: loadLastSyncByTenantChannel retorna array vazio quando não há sync_jobs (mock retorna data=[], error=null)
    - Test 2: loadLastSyncByTenantChannel retorna apenas o último por (tenant_id, channel) — quando mock devolve múltiplas linhas, apenas a mais recente por par é retornada
    - Test 3: loadLastSyncByTenantChannel retorna SyncStatusRow[] com forma { tenant_id, tenant_name, tenant_slug, channel, status, completed_at, started_at, records_synced, error_message }
    - Test 4: Em caso de error.code !== null, retorna [] e loga via console.error (não throw)
  </behavior>
  <action>
    Criar `lib/sync-status.ts`:

    ```typescript
    import 'server-only'
    import { createClient } from '@/lib/supabase/server'

    export interface SyncStatusRow {
      tenant_id: string
      tenant_name: string
      tenant_slug: string
      channel: 'google_ads' | 'meta_ads'
      status: 'running' | 'success' | 'failed' | null
      completed_at: string | null
      started_at: string | null
      records_synced: number | null
      error_message: string | null
    }

    /**
     * SYNC-03 / SYNC-04 UI surface.
     * Retorna o ÚLTIMO sync_job por (tenant_id, channel).
     *
     * Implementação: query simples ordenada DESC + dedupe in-memory. Em v1 com 1-3 tenants
     * × 2 channels, isso é ≤6 linhas após dedupe e ≤200 linhas brutas mesmo após 90 dias
     * de execuções a cada 3h/6h. Para escala maior, usar DISTINCT ON via .rpc().
     */
    export async function loadLastSyncByTenantChannel(): Promise<SyncStatusRow[]> {
      const supabase = await createClient()
      const { data, error } = await supabase
        .from('sync_jobs')
        .select(`
          tenant_id,
          channel,
          status,
          completed_at,
          started_at,
          records_synced,
          error_message,
          tenants:tenant_id (
            name,
            slug
          )
        `)
        .order('completed_at', { ascending: false, nullsFirst: false })
        .limit(500)

      if (error) {
        console.error('[lib/sync-status] load error:', error)
        return []
      }
      if (!data) return []

      // Dedupe in-memory: pegar o primeiro (mais recente) por (tenant_id, channel)
      const seen = new Set<string>()
      const result: SyncStatusRow[] = []
      for (const row of data) {
        const key = `${row.tenant_id}::${row.channel}`
        if (seen.has(key)) continue
        seen.add(key)
        // tenants pode ser objeto único ou array dependendo do shape do PostgREST embed
        const t = Array.isArray((row as any).tenants) ? (row as any).tenants[0] : (row as any).tenants
        result.push({
          tenant_id: row.tenant_id,
          tenant_name: t?.name ?? '(unknown)',
          tenant_slug: t?.slug ?? '',
          channel: row.channel as 'google_ads' | 'meta_ads',
          status: row.status as SyncStatusRow['status'],
          completed_at: row.completed_at,
          started_at: row.started_at,
          records_synced: row.records_synced,
          error_message: row.error_message,
        })
      }
      return result
    }
    ```

    Criar `lib/sync-status.test.ts` com 4 testes vitest mockando `@/lib/supabase/server`:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    const mockState = {
      response: { data: null as any, error: null as null | { code: string; message: string } },
    }

    vi.mock('@/lib/supabase/server', () => ({
      createClient: async () => ({
        from: () => ({
          select: () => ({
            order: () => ({
              limit: () => Promise.resolve(mockState.response),
            }),
          }),
        }),
      }),
    }))

    beforeEach(() => {
      mockState.response = { data: null, error: null }
    })

    describe('loadLastSyncByTenantChannel (SYNC-03)', () => {
      it('retorna [] quando não há sync_jobs', async () => {
        mockState.response = { data: [], error: null }
        const { loadLastSyncByTenantChannel } = await import('@/lib/sync-status')
        const result = await loadLastSyncByTenantChannel()
        expect(result).toEqual([])
      })

      it('dedupe: retorna apenas o último por (tenant_id, channel)', async () => {
        mockState.response = {
          data: [
            { tenant_id: 'T1', channel: 'google_ads', status: 'success', completed_at: '2026-05-16T10:00:00Z', started_at: '2026-05-16T09:55:00Z', records_synced: 100, error_message: null, tenants: { name: 'Acme', slug: 'acme' } },
            { tenant_id: 'T1', channel: 'google_ads', status: 'failed',  completed_at: '2026-05-16T07:00:00Z', started_at: '2026-05-16T06:55:00Z', records_synced: 0,   error_message: 'timeout', tenants: { name: 'Acme', slug: 'acme' } },
            { tenant_id: 'T1', channel: 'meta_ads',  status: 'success', completed_at: '2026-05-16T08:00:00Z', started_at: '2026-05-16T07:55:00Z', records_synced: 50,  error_message: null, tenants: { name: 'Acme', slug: 'acme' } },
          ],
          error: null,
        }
        const { loadLastSyncByTenantChannel } = await import('@/lib/sync-status')
        const result = await loadLastSyncByTenantChannel()
        expect(result).toHaveLength(2)
        const g = result.find(r => r.channel === 'google_ads')!
        expect(g.status).toBe('success')
        expect(g.completed_at).toBe('2026-05-16T10:00:00Z')
      })

      it('shape do SyncStatusRow inclui tenant_name e tenant_slug do embed', async () => {
        mockState.response = {
          data: [
            { tenant_id: 'T1', channel: 'google_ads', status: 'success', completed_at: '2026-05-16T10:00:00Z', started_at: null, records_synced: 100, error_message: null, tenants: { name: 'Acme Corp', slug: 'acme' } },
          ],
          error: null,
        }
        const { loadLastSyncByTenantChannel } = await import('@/lib/sync-status')
        const [row] = await loadLastSyncByTenantChannel()
        expect(row.tenant_name).toBe('Acme Corp')
        expect(row.tenant_slug).toBe('acme')
        expect(row.channel).toBe('google_ads')
      })

      it('retorna [] em caso de error sem throw', async () => {
        mockState.response = { data: null, error: { code: '42P01', message: 'relation does not exist' } }
        const errSpy = vi.spyOn(console, 'error').mockImplementation(() => {})
        const { loadLastSyncByTenantChannel } = await import('@/lib/sync-status')
        const result = await loadLastSyncByTenantChannel()
        expect(result).toEqual([])
        expect(errSpy).toHaveBeenCalled()
        errSpy.mockRestore()
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- --grep "loadLastSyncByTenantChannel"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `lib/sync-status.ts` existe
    - Arquivo contém literal `import 'server-only'`
    - Arquivo exporta `interface SyncStatusRow` com pelo menos os campos: `tenant_id, tenant_name, tenant_slug, channel, status, completed_at, error_message`
    - Arquivo exporta `async function loadLastSyncByTenantChannel(): Promise<SyncStatusRow[]>`
    - Função chama `supabase.from('sync_jobs').select(...)`
    - Função faz JOIN com tenants via `tenants:tenant_id` ou similar embed
    - Função faz dedupe in-memory por `tenant_id::channel`
    - Arquivo `lib/sync-status.test.ts` existe com 4 testes
    - `npm test -- --grep "loadLastSyncByTenantChannel"` exit code 0
    - Pelo menos 1 teste verifica dedupe (3 rows brutas → 2 rows após dedupe)
    - Pelo menos 1 teste verifica fallback em error (retorna [], não throw)
  </acceptance_criteria>
  <done>Data fetcher + 4 testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Criar components/tenants/sync-status-section.tsx (Server Component)</name>
  <files>components/tenants/sync-status-section.tsx</files>
  <read_first>
    - lib/sync-status.ts (Task 1)
    - components/tenants/tenants-table.tsx (padrão de Table + empty state)
    - components/ui/table.tsx (componentes shadcn disponíveis)
    - components/ui/badge.tsx (TenantStatusBadge pattern)
    - components/tenants/tenant-status-badge.tsx (padrão de badge)
    - types/database.types.ts (sync_jobs Row type)
  </read_first>
  <action>
    Criar `components/tenants/sync-status-section.tsx`:

    ```tsx
    import { loadLastSyncByTenantChannel, type SyncStatusRow } from '@/lib/sync-status'
    import {
      Table,
      TableBody,
      TableCell,
      TableHead,
      TableHeader,
      TableRow,
    } from '@/components/ui/table'
    import { Badge } from '@/components/ui/badge'

    const CHANNELS: Array<'google_ads' | 'meta_ads'> = ['google_ads', 'meta_ads']

    const CHANNEL_LABELS: Record<'google_ads' | 'meta_ads', string> = {
      google_ads: 'Google Ads',
      meta_ads: 'Meta Ads',
    }

    function StatusBadge({ status }: { status: SyncStatusRow['status'] }) {
      if (status === 'success') return <Badge variant="default" className="bg-green-600 hover:bg-green-700">Sucesso</Badge>
      if (status === 'failed')  return <Badge variant="destructive">Falha</Badge>
      if (status === 'running') return <Badge variant="secondary">Em execução</Badge>
      return <Badge variant="outline">Sem sync</Badge>
    }

    function formatTimestamp(iso: string | null): string {
      if (!iso) return '—'
      const d = new Date(iso)
      return d.toLocaleString('pt-BR', { dateStyle: 'short', timeStyle: 'short' })
    }

    /**
     * SYNC-03 / SYNC-04 UI section embedded in /tenants page.
     * Mostra o último sync_job por (tenant, channel). Server Component — RSC fetches
     * direto via createClient(). Sem TanStack Query (dados estáticos por load).
     */
    export async function SyncStatusSection() {
      const rows = await loadLastSyncByTenantChannel()

      // Agrupar por tenant: { tenant_id: { tenant_name, slug, google_ads?, meta_ads? } }
      const byTenant = new Map<string, { name: string; slug: string; entries: Partial<Record<'google_ads' | 'meta_ads', SyncStatusRow>> }>()
      for (const row of rows) {
        if (!byTenant.has(row.tenant_id)) {
          byTenant.set(row.tenant_id, { name: row.tenant_name, slug: row.tenant_slug, entries: {} })
        }
        byTenant.get(row.tenant_id)!.entries[row.channel] = row
      }

      const tenantList = Array.from(byTenant.entries())

      return (
        <section className="flex flex-col gap-4">
          <header>
            <h2 className="text-lg font-semibold leading-tight">Status de Sync</h2>
            <p className="text-sm text-muted-foreground mt-1">
              Último sync de campanhas por tenant e canal — SYNC-03
            </p>
          </header>

          {tenantList.length === 0 ? (
            <div className="rounded-lg border border-border bg-card p-8 text-center">
              <p className="text-sm text-muted-foreground">
                Nenhum sync registrado ainda. Os workflows N8N (Google Ads e Meta Ads) gravarão aqui após a primeira execução.
              </p>
            </div>
          ) : (
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>Tenant</TableHead>
                  <TableHead>Canal</TableHead>
                  <TableHead>Status</TableHead>
                  <TableHead>Último sync</TableHead>
                  <TableHead className="text-right">Registros</TableHead>
                  <TableHead>Erro</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {tenantList.flatMap(([tenantId, info]) =>
                  CHANNELS.map((ch) => {
                    const row = info.entries[ch]
                    return (
                      <TableRow key={`${tenantId}::${ch}`}>
                        <TableCell className="font-semibold">{info.name}</TableCell>
                        <TableCell>{CHANNEL_LABELS[ch]}</TableCell>
                        <TableCell><StatusBadge status={row?.status ?? null} /></TableCell>
                        <TableCell className="font-mono text-xs">
                          {row ? formatTimestamp(row.completed_at ?? row.started_at) : '—'}
                        </TableCell>
                        <TableCell className="text-right font-mono text-xs">
                          {row?.records_synced ?? '—'}
                        </TableCell>
                        <TableCell className="text-xs text-destructive">
                          {row?.status === 'failed' && row.error_message
                            ? row.error_message.length > 80
                              ? row.error_message.slice(0, 80) + '…'
                              : row.error_message
                            : ''}
                        </TableCell>
                      </TableRow>
                    )
                  })
                )}
              </TableBody>
            </Table>
          )}
        </section>
      )
    }
    ```
  </action>
  <verify>
    <automated>test -f components/tenants/sync-status-section.tsx && grep -q "export async function SyncStatusSection" components/tenants/sync-status-section.tsx && grep -q "loadLastSyncByTenantChannel" components/tenants/sync-status-section.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/tsc-output.txt | grep -E "components/tenants/sync-status-section.tsx" | grep "error" || true</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `components/tenants/sync-status-section.tsx` existe
    - Contém literal `export async function SyncStatusSection()`
    - Contém literal `import { loadLastSyncByTenantChannel, type SyncStatusRow } from '@/lib/sync-status'`
    - Contém literal `const CHANNELS: Array<'google_ads' | 'meta_ads'> = ['google_ads', 'meta_ads']`
    - Para cada tenant exibe AMBOS os channels (google_ads e meta_ads), mesmo que um não tenha sync ainda (slot exibe `<StatusBadge status={null} />` = "Sem sync")
    - Exibe empty state quando `rows.length === 0`
    - Exibe `error_message` truncado para 80 chars quando status='failed'
    - TypeScript compile (`npx tsc --noEmit`) não emite erros no arquivo recém-criado
  </acceptance_criteria>
  <done>Server Component criado, exibe todos os tenants × 2 channels, empty state, error truncation, type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Integrar SyncStatusSection em app/tenants/page.tsx</name>
  <files>app/tenants/page.tsx</files>
  <read_first>
    - app/tenants/page.tsx (estrutura atual — adicionar, não substituir)
    - components/tenants/sync-status-section.tsx (Task 2)
  </read_first>
  <action>
    Modificar `app/tenants/page.tsx` para incluir `<SyncStatusSection />` abaixo do `<TenantsTable />`. Conteúdo final do arquivo:

    ```typescript
    import { Suspense } from 'react'
    import { redirect } from 'next/navigation'

    import { CreateTenantForm } from '@/components/tenants/create-tenant-form'
    import { TenantsTable, type TenantRow } from '@/components/tenants/tenants-table'
    import { SyncStatusSection } from '@/components/tenants/sync-status-section'
    import { Skeleton } from '@/components/ui/skeleton'
    import { createClient } from '@/lib/supabase/server'

    async function loadTenants(): Promise<TenantRow[]> {
      const supabase = await createClient()
      const { data, error } = await supabase
        .from('tenants')
        .select('id, name, slug, active')
        .order('created_at', { ascending: false })

      if (error) {
        console.error('[/tenants] load error:', error)
        return []
      }
      return data ?? []
    }

    export default async function TenantsPage() {
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login')

      const tenants = await loadTenants()

      return (
        <section className="flex flex-col gap-8">
          <header className="flex items-start justify-between gap-4">
            <div>
              <h1 className="text-xl font-semibold leading-tight">Tenants</h1>
              <p className="text-sm text-muted-foreground mt-1">Gerencie os tenants da plataforma</p>
            </div>
            <CreateTenantForm />
          </header>
          <Suspense fallback={
            <div className="space-y-2">
              <Skeleton className="h-12 w-full" />
              <Skeleton className="h-12 w-full" />
              <Skeleton className="h-12 w-full" />
            </div>
          }>
            <TenantsTable tenants={tenants} />
          </Suspense>

          <Suspense fallback={
            <div className="space-y-2">
              <Skeleton className="h-8 w-48" />
              <Skeleton className="h-32 w-full" />
            </div>
          }>
            <SyncStatusSection />
          </Suspense>
        </section>
      )
    }
    ```
  </action>
  <verify>
    <automated>grep -q "import { SyncStatusSection }" app/tenants/page.tsx && grep -q "<SyncStatusSection />" app/tenants/page.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -E "app/tenants/page.tsx" | grep "error" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `app/tenants/page.tsx` contém literal `import { SyncStatusSection } from '@/components/tenants/sync-status-section'`
    - `app/tenants/page.tsx` contém literal `<SyncStatusSection />` renderizado dentro do JSX da page
    - `<SyncStatusSection />` está envolto em `<Suspense fallback={...}>`
    - Estrutura existente preservada (CreateTenantForm, TenantsTable, header com h1 "Tenants")
    - `npx tsc --noEmit` não emite erros no arquivo modificado
    - `npm run build` (Next.js build) completa com exit code 0 (verificar localmente antes do checkpoint)
  </acceptance_criteria>
  <done>page.tsx atualizado, Next.js build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — testar /tenants com dados reais de sync</name>
  <files>app/tenants/page.tsx, components/tenants/sync-status-section.tsx</files>
  <action>
    Inserir sync_jobs de teste no Supabase (ver SQL em <how-to-verify>), navegar para /tenants logado como Super Admin, validar visualmente que a seção "Status de Sync" renderiza corretamente, depois fazer cleanup do SQL.
  </action>
  <verify>
    <automated>test -f app/tenants/page.tsx &amp;&amp; grep -q "SyncStatusSection" app/tenants/page.tsx</automated>
  </verify>
  <done>Usuário digitou "aprovado" confirmando que a página /tenants exibe a seção "Status de Sync" com dedupe correto (apenas último sync por tenant+channel), badges de status com cores corretas, error_message truncado, e empty state cobrindo tenants sem sync.</done>
  <what-built>
    Página `/tenants` agora exibe seção "Status de Sync" embaixo da tabela de tenants.
    Mostra o último sync_job por (tenant, channel ∈ {google_ads, meta_ads}).
  </what-built>
  <how-to-verify>
    1. Inserir alguns sync_jobs de teste no Supabase SQL Editor (https://supabase.com/dashboard/project/rvkkvjitfddtbdpkupok/sql):
       ```sql
       -- Pré-requisito: pelo menos 1 tenant existe (deve existir após Phase 1)
       INSERT INTO public.sync_jobs (tenant_id, channel, status, started_at, completed_at, records_synced, date_from, date_to)
       SELECT id, 'google_ads', 'success', NOW() - INTERVAL '2 hours', NOW() - INTERVAL '1 hour 50 minutes', 142, CURRENT_DATE - 2, CURRENT_DATE
         FROM public.tenants LIMIT 1;

       INSERT INTO public.sync_jobs (tenant_id, channel, status, started_at, completed_at, records_synced, date_from, date_to, error_message)
       SELECT id, 'meta_ads', 'failed', NOW() - INTERVAL '6 hours', NOW() - INTERVAL '5 hours 55 minutes', 0, CURRENT_DATE - 2, CURRENT_DATE, 'Invalid OAuth access token: System User token has been invalidated'
         FROM public.tenants LIMIT 1;

       -- Linha mais recente para google_ads (testa dedupe)
       INSERT INTO public.sync_jobs (tenant_id, channel, status, started_at, completed_at, records_synced, date_from, date_to)
       SELECT id, 'google_ads', 'success', NOW() - INTERVAL '10 minutes', NOW() - INTERVAL '5 minutes', 155, CURRENT_DATE - 2, CURRENT_DATE
         FROM public.tenants LIMIT 1;
       ```
    2. Login como Super Admin na app: https://nexusdash-chi.vercel.app/login (ou localhost se rodando dev)
    3. Navegar para `/tenants`
    4. **Verificar:**
       a. A tabela "Tenants" original aparece (não foi quebrada)
       b. Abaixo dela aparece a seção "Status de Sync" com header "Status de Sync" + subtitle "SYNC-03"
       c. Para o tenant inserido: linha google_ads mostra "Sucesso" (badge verde) + timestamp ~5 min atrás + records_synced=155 (o MAIS RECENTE — confirma dedupe)
       d. Linha meta_ads mostra "Falha" (badge vermelho) + error_message truncado
       e. Para outros tenants (se existirem) sem sync algum, ambas as linhas mostram "Sem sync" (badge outline) + "—" em timestamp/records
    5. **Cleanup:** Deletar os sync_jobs de teste:
       ```sql
       DELETE FROM public.sync_jobs WHERE date_from = CURRENT_DATE - 2;
       ```

    **Aprovar se:**
    - Página renderiza sem erros
    - Dedupe funciona (apenas o sync mais recente por par aparece)
    - Status badges com cores corretas (sucesso=verde, falha=vermelho, sem sync=outline)
    - Timestamp em pt-BR
    - Error message truncada e em vermelho
    - Empty state aparece se NENHUM sync existir
    - Tabela de tenants original NÃO foi quebrada

    **Rejeitar se:**
    - Página crash (500 ou white screen)
    - Aparece linha duplicada para o mesmo (tenant, channel)
    - Tenant sem sync não aparece (deveria aparecer com "Sem sync")
    - Layout quebrado (overflow, badge muito grande, etc.)
  </how-to-verify>
  <resume-signal>Digite "aprovado" se a UI renderiza corretamente e o dedupe funciona, OU descreva o problema (ex: "tenant sem sync não aparece — deveria mostrar 'Sem sync' para ambos os channels").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Super Admin) → /tenants Server Component | RSC executado server-side; usuário autenticado via createServerClient + cookies |
| Server Component → Supabase PostgREST | Usa publishable key + JWT do usuário (cookies); RLS policies aplicam — Super Admin policy permite leitura cross-tenant |

## STRIDE Threat Register (ASVS L1: V4 Access Control)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05-01 | Information Disclosure | Tenant Admin acessa /tenants e vê sync_jobs de outros tenants | mitigate | Middleware da Fase 1 (proxy.ts) já bloqueia tenant_admin/viewer de /tenants (Super Admin only). Defense-in-depth: RLS sync_jobs_tenant_select restringe a tenant_id próprio mesmo se middleware falhasse. |
| T-2-05-02 | Information Disclosure | error_message do sync_job vaza dados sensíveis (ex: trecho de access_token) | mitigate | Workflows N8N (Plans 03/04) já sanitizam error_message com regex antes de gravar. UI exibe truncado para 80 chars no campo Erro. |
| T-2-05-03 | XSS | error_message contém HTML/JS é renderizado sem escape | mitigate | React JSX faz escape de strings em text content por default. Não usar dangerouslySetInnerHTML. |
| T-2-05-04 | DoS | Query carrega 90 dias × 8 syncs/dia × 3 tenants = 2160 rows | accept | Limit 500 + dedupe in-memory mantém custo baixo (~6 final rows). Para escala maior, mover para .rpc com DISTINCT ON. |

</threat_model>

<verification>
- `npm test` passa (testes da Task 1 verdes; testes da Fase 1 não quebram)
- `npx tsc --noEmit` sem erros nos arquivos modificados
- `npm run build` (Next.js) completa exit code 0
- Human verification (Task 4) aprovada
- Página `/tenants` renderiza ambas as seções (Tenants table + Sync Status section)
- Dedupe in-memory funciona (último por par tenant+channel)
</verification>

<success_criteria>
- 3 arquivos criados/modificados: `lib/sync-status.ts`, `components/tenants/sync-status-section.tsx`, `app/tenants/page.tsx`
- Para cada tenant na tabela `tenants`, AMBOS os channels (google_ads, meta_ads) aparecem na seção — mesmo sem sync (linha mostra "Sem sync")
- Apenas o sync MAIS RECENTE por par (tenant, channel) aparece (dedupe verificado pelo teste e pela checkpoint manual)
- error_message truncado para 80 chars
- Empty state cobre o caso "nenhum sync registrado ainda"
- Apenas Super Admin acessa (proteção do middleware Fase 1 + RLS defense-in-depth)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-05-SUMMARY.md` documentando:
- Paths dos 3 arquivos
- Screenshot da página /tenants mostrando ambas as seções
- Confirmação de que `npm test` e `npm run build` passam
- Confirmação de que UAT manual (Task 4) foi aprovado
- Phase 2 complete: SYNC-01 a SYNC-06 todos cobertos (com BLOCKER do Developer Token do Google Ads pendente para ativação real do workflow)
</output>

---
phase: 02-data-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - n8n-workflows/google-ads-sync.json
autonomous: false
requirements:
  - SYNC-01
  - SYNC-04
  - SYNC-05
  - SYNC-06
user_setup:
  - service: google-ads
    why: "Sync workflow precisa do Developer Token (Basic Access) + OAuth Client ID/Secret + refresh_token por tenant"
    env_vars:
      - name: GOOGLE_ADS_DEVELOPER_TOKEN
        source: "Google Ads → API Center (https://ads.google.com/aw/apicenter) — BLOCKER: aguardando aprovação Basic Access"
      - name: GOOGLE_OAUTH_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (já em Production per STATE.md)"
      - name: GOOGLE_OAUTH_CLIENT_SECRET
        source: "Google Cloud Console → mesmo OAuth Client"
      - name: GOOGLE_ADS_MANAGER_CUSTOMER_ID
        source: "Google Ads MCC account ID (se aplicável — usado em header login-customer-id)"
    dashboard_config:
      - task: "No N8N, criar Credentials do tipo 'Header Auth' (para Supabase service_role) e 'Generic Credential Type' (para Google Ads). NUNCA armazenar tokens literais no workflow JSON."
        location: "https://evo.wrdigitalgroup.com.br → Credentials"
      - task: "Importar n8n-workflows/google-ads-sync.json via N8N UI → Workflows → Import from File → atribuir credentials → Activate"
        location: "https://evo.wrdigitalgroup.com.br → Workflows"
      - task: "Inserir refresh_token de cada tenant no Supabase Vault: `SELECT vault.create_secret('<refresh_token>', 'tenant_<slug>_google_refresh_token', 'Google Ads refresh token');` — copiar UUID retornado para ad_accounts.vault_secret_id"
        location: "Supabase Dashboard → SQL Editor"

must_haves:
  truths:
    - "Workflow JSON 'Google Ads Sync' existe em n8n-workflows/google-ads-sync.json e pode ser importado via N8N UI"
    - "Workflow tem Schedule Trigger configurado com cron '0 */3 * * *' (a cada 3 horas)"
    - "Workflow tem uma constante GOOGLE_ADS_API_VERSION = 'v21' definida em UM ÚNICO Set node — TODA chamada à Google Ads API referencia essa variável (SYNC-06)"
    - "Workflow não contém credenciais em texto plano — todos os tokens vêm de N8N Credentials ou Supabase Vault"
    - "Workflow itera sobre ad_accounts WHERE channel='google_ads' AND active=true (loop multi-tenant — D-12)"
    - "Para cada tenant: ler refresh_token via /rpc/read_vault_secret → trocar por access_token → chamar googleAds:searchStream → upsert em campaign_metrics → chamar /rpc/refresh_daily_rollups → gravar em sync_jobs"
    - "Backfill: workflow detecta primeira execução verificando ausência de sync_jobs com status='success' e usa date_from = today - 90d; incremental usa today - 2d (D-16, D-17)"
    - "cost_micros é dividido por 1.000.000 antes do upsert (Pitfall 5)"
    - "attribution_window é '7d_click' literal em todo upsert (SYNC-05)"
    - "Falha de um tenant grava sync_jobs.status='failed' + error_message e continua para o próximo (D-13 item 4)"
  artifacts:
    - path: "n8n-workflows/google-ads-sync.json"
      provides: "Workflow N8N exportável (JSON) — Google Ads multi-tenant sync"
      contains: "\"GOOGLE_ADS_API_VERSION\""
      min_lines: 200
  key_links:
    - from: "n8n-workflows/google-ads-sync.json"
      to: "https://oauth2.googleapis.com/token"
      via: "HTTP Request node POST com grant_type=refresh_token"
      pattern: "oauth2\\.googleapis\\.com/token"
    - from: "n8n-workflows/google-ads-sync.json"
      to: "googleads.googleapis.com/{{API_VERSION}}/customers/{customer_id}/googleAds:searchStream"
      via: "HTTP Request node POST com GAQL query"
      pattern: "googleAds:searchStream"
    - from: "n8n-workflows/google-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/campaign_metrics?on_conflict=tenant_id,campaign_id,channel,date"
      via: "HTTP Request POST com Prefer: resolution=merge-duplicates"
      pattern: "on_conflict=tenant_id,campaign_id,channel,date"
    - from: "n8n-workflows/google-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/rpc/refresh_daily_rollups"
      via: "HTTP Request POST com body { p_tenant_id, p_date_from, p_date_to }"
      pattern: "rpc/refresh_daily_rollups"
    - from: "n8n-workflows/google-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/sync_jobs"
      via: "HTTP Request POST (status='running' no início, UPDATE para 'success'/'failed' no fim)"
      pattern: "sync_jobs"
---

<objective>
Criar o arquivo JSON do workflow N8N "Google Ads Sync" — multi-tenant, agendado a cada 3 horas, com OAuth2 refresh flow, chamadas à Google Ads API v21 via SearchStream, upserts em campaign_metrics via PostgREST, e logging em sync_jobs. Cobre SYNC-01, SYNC-04 (writes), SYNC-05 (attribution_window), SYNC-06 (API version como constante).

Purpose: Esta é a pipeline real que popula campaign_metrics com dados do Google Ads. A Fase 3 (Dashboard UI) DEPENDE dos dados produzidos por este workflow.
Output: Um arquivo JSON em `n8n-workflows/google-ads-sync.json` que pode ser importado via N8N UI.

**BLOCKER CRÍTICO:** Google Ads Developer Token (Basic Access) MISSING — workflow pode ser construído e validado estruturalmente, mas a execução real contra a API só é possível após aprovação do token (2-10+ dias úteis — ver STATE.md §Blockers). Esta plan NÃO bloqueia se o token não existir; o workflow é entregue como JSON inativo, ativação fica para depois.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@supabase/migrations/0007_create_campaign_metrics.sql
@supabase/migrations/0010_create_pipeline_functions.sql

<interfaces>
<!-- Endpoints e contratos exatos que o workflow chama -->

Supabase project: `rvkkvjitfddtbdpkupok` (sa-east-1)
Supabase URL pattern: `https://rvkkvjitfddtbdpkupok.supabase.co`
Service role auth header (criar como N8N Credential, NÃO literal):
```
apikey: {{SUPABASE_SERVICE_ROLE_KEY}}
Authorization: Bearer {{SUPABASE_SERVICE_ROLE_KEY}}
```

Tabela ad_accounts schema (read-only para este workflow):
```sql
ad_accounts(id, tenant_id, channel, account_id, vault_secret_id, refresh_token, token_expires_at, active, created_at)
WHERE channel='google_ads' AND active=true
```
Endpoint: `GET /rest/v1/ad_accounts?channel=eq.google_ads&active=eq.true&select=id,tenant_id,account_id,vault_secret_id`

read_vault_secret RPC (criado em Plan 02 Task 4):
```
POST /rest/v1/rpc/read_vault_secret
Body: { "p_secret_name": "tenant_<slug>_google_refresh_token" }
Returns: text (decrypted_secret) OR null
```
Alternativa pelo UUID: NÃO usada aqui — usar nome para legibilidade humana no Vault.

Google OAuth2 token endpoint:
```
POST https://oauth2.googleapis.com/token
Content-Type: application/x-www-form-urlencoded
Body: grant_type=refresh_token
      &client_id={{GOOGLE_OAUTH_CLIENT_ID}}
      &client_secret={{GOOGLE_OAUTH_CLIENT_SECRET}}
      &refresh_token={{REFRESH_TOKEN}}
Response: { "access_token": "...", "expires_in": 3600, "token_type": "Bearer" }
```

Google Ads SearchStream endpoint:
```
POST https://googleads.googleapis.com/{{GOOGLE_ADS_API_VERSION}}/customers/{{customer_id}}/googleAds:searchStream
Headers:
  Authorization: Bearer {{access_token}}
  developer-token: {{GOOGLE_ADS_DEVELOPER_TOKEN}}
  login-customer-id: {{GOOGLE_ADS_MANAGER_CUSTOMER_ID}}  (apenas se MCC)
  Content-Type: application/json
Body: { "query": "<GAQL>" }
Response: [{ "results": [...] }, ...]  ← array de chunks (Pitfall 4)
```

GAQL para sync (verbatim de RESEARCH.md §Padrão 2):
```
SELECT campaign.id, campaign.name, campaign.status,
       metrics.impressions, metrics.clicks, metrics.cost_micros,
       metrics.conversions, metrics.conversions_value,
       segments.date, ad_group.id
FROM campaign
WHERE segments.date BETWEEN '{date_from}' AND '{date_to}'
  AND campaign.status != 'REMOVED'
ORDER BY segments.date ASC
```

campaign_metrics upsert:
```
POST /rest/v1/campaign_metrics?on_conflict=tenant_id,campaign_id,channel,date
Headers:
  apikey + Authorization (service_role)
  Content-Type: application/json
  Prefer: resolution=merge-duplicates
Body: [ {tenant_id, campaign_id, campaign_name, channel:'google_ads', date, impressions, clicks, spend, conversions, conversion_value, status, ad_group_id, attribution_window:'7d_click', synced_at}, ... ]
```

refresh_daily_rollups RPC:
```
POST /rest/v1/rpc/refresh_daily_rollups
Body: { "p_tenant_id": "...", "p_date_from": "YYYY-MM-DD", "p_date_to": "YYYY-MM-DD" }
```

sync_jobs lifecycle:
1. POST /rest/v1/sync_jobs com {tenant_id, channel:'google_ads', status:'running', date_from, date_to}, body Prefer: return=representation → guardar id
2. No fim: PATCH /rest/v1/sync_jobs?id=eq.{id} com {status:'success'|'failed', completed_at:now(), records_synced, error_message}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar n8n-workflows/google-ads-sync.json (estrutura completa do workflow)</name>
  <files>n8n-workflows/google-ads-sync.json</files>
  <read_first>
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 1" (OAuth2 refresh flow)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 2" (GAQL SearchStream)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 4" (PostgREST upsert headers)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 9" (cron '0 */3 * * *')
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 10" (detecção first sync)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Code Examples" (parser SearchStream)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md Pitfalls 4, 5, 6 (camelCase/snake_case, cost_micros, array response)
  </read_first>
  <action>
    Criar a pasta `n8n-workflows/` (se ainda não existir) e o arquivo `n8n-workflows/google-ads-sync.json` contendo um workflow N8N exportável.

    **Estrutura mínima obrigatória de nodes (em ordem):**

    1. **Schedule Trigger** node — cron `0 */3 * * *` (a cada 3 horas)
    2. **Set (Constants)** node — `name="Set Constants"` — define:
       - `GOOGLE_ADS_API_VERSION` = `'v21'` (string literal — SYNC-06)
       - `BACKFILL_DAYS` = 90
       - `INCREMENTAL_DAYS` = 2
       - `SUPABASE_URL` = `https://rvkkvjitfddtbdpkupok.supabase.co`
    3. **HTTP Request** node — `name="List active Google Ads accounts"`:
       - Method: GET
       - URL: `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/ad_accounts?channel=eq.google_ads&active=eq.true&select=id,tenant_id,account_id,vault_secret_id,tenants(slug)`
       - Authentication: N8N Credential `Supabase Service Role` (Header Auth com apikey + Authorization Bearer)
    4. **Split In Batches** node — `name="Loop tenants"` — batchSize: 1
    5. **HTTP Request** node — `name="Check first sync"`:
       - GET `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs?tenant_id=eq.{{$json.tenant_id}}&channel=eq.google_ads&status=eq.success&select=id&limit=1`
    6. **Code** node — `name="Compute date range"`:
       ```javascript
       const today = new Date();
       const prevJobs = $('Check first sync').item.json;
       const isFirstSync = !Array.isArray(prevJobs) || prevJobs.length === 0;
       const days = isFirstSync ? $('Set Constants').first().json.BACKFILL_DAYS : $('Set Constants').first().json.INCREMENTAL_DAYS;
       const from = new Date(today); from.setDate(today.getDate() - days);
       return [{ json: {
         tenant_id: $('Loop tenants').item.json.tenant_id,
         account_id: $('Loop tenants').item.json.account_id,
         vault_secret_id: $('Loop tenants').item.json.vault_secret_id,
         tenant_slug: $('Loop tenants').item.json.tenants?.slug,
         date_from: from.toISOString().split('T')[0],
         date_to: today.toISOString().split('T')[0],
         is_first_sync: isFirstSync
       }}];
       ```
    7. **HTTP Request** node — `name="Open sync_job"`:
       - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs`
       - Header `Prefer: return=representation`
       - Body: `{ "tenant_id": "{{$json.tenant_id}}", "channel": "google_ads", "status": "running", "date_from": "{{$json.date_from}}", "date_to": "{{$json.date_to}}" }`
    8. **HTTP Request** node — `name="Read vault secret"`:
       - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/rpc/read_vault_secret`
       - Body: `{ "p_secret_name": "tenant_{{$json.tenant_slug}}_google_refresh_token" }`
       - Response: string (refresh_token)
    9. **HTTP Request** node — `name="Exchange refresh for access token"`:
       - POST `https://oauth2.googleapis.com/token`
       - Content-Type: `application/x-www-form-urlencoded`
       - Body (form data): `grant_type=refresh_token&client_id={{$credentials.googleOauth.clientId}}&client_secret={{$credentials.googleOauth.clientSecret}}&refresh_token={{$('Read vault secret').item.json}}`
    10. **HTTP Request** node — `name="Google Ads SearchStream"`:
        - POST `https://googleads.googleapis.com/{{$('Set Constants').item.json.GOOGLE_ADS_API_VERSION}}/customers/{{$('Compute date range').item.json.account_id}}/googleAds:searchStream`
        - Headers:
          - `Authorization: Bearer {{$('Exchange refresh for access token').item.json.access_token}}`
          - `developer-token: {{$credentials.googleAdsDevToken.token}}`
          - `login-customer-id: {{$credentials.googleAdsDevToken.managerId}}` (condicional)
        - Body:
          ```json
          { "query": "SELECT campaign.id, campaign.name, campaign.status, metrics.impressions, metrics.clicks, metrics.cost_micros, metrics.conversions, metrics.conversions_value, segments.date, ad_group.id FROM campaign WHERE segments.date BETWEEN '{{$('Compute date range').item.json.date_from}}' AND '{{$('Compute date range').item.json.date_to}}' AND campaign.status != 'REMOVED' ORDER BY segments.date ASC" }
          ```
    11. **Code** node — `name="Map Google Ads results"` (verbatim de RESEARCH.md §Code Examples, parsing SearchStream):
        ```javascript
        const tenantId = $('Compute date range').item.json.tenant_id;
        const rows = [];
        for (const chunk of $input.all()) {
          const results = chunk.json.results || [];
          for (const result of results) {
            const campaign = result.campaign || {};
            const metrics = result.metrics || {};
            const segments = result.segments || {};
            rows.push({
              tenant_id: tenantId,
              campaign_id: campaign.id,
              campaign_name: campaign.name,
              channel: 'google_ads',
              date: segments.date,
              impressions: parseInt(metrics.impressions || '0'),
              clicks: parseInt(metrics.clicks || '0'),
              spend: parseInt(metrics.costMicros || '0') / 1_000_000,
              conversions: parseFloat(metrics.conversions || '0'),
              conversion_value: parseFloat(metrics.conversionsValue || '0'),
              status: campaign.status,
              ad_group_id: result.adGroup?.id || null,
              attribution_window: '7d_click',
              synced_at: new Date().toISOString()
            });
          }
        }
        return rows.map(row => ({ json: row }));
        ```
    12. **HTTP Request** node — `name="Upsert campaign_metrics"`:
        - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/campaign_metrics?on_conflict=tenant_id,campaign_id,channel,date`
        - Headers: service_role + `Prefer: resolution=merge-duplicates`
        - Body: `={{ $('Map Google Ads results').all().map(i => i.json) }}`
    13. **HTTP Request** node — `name="Refresh daily_rollups"`:
        - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/rpc/refresh_daily_rollups`
        - Body: `{ "p_tenant_id": "{{$('Compute date range').item.json.tenant_id}}", "p_date_from": "{{$('Compute date range').item.json.date_from}}", "p_date_to": "{{$('Compute date range').item.json.date_to}}" }`
    14. **HTTP Request** node — `name="Close sync_job (success)"`:
        - PATCH `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs?id=eq.{{$('Open sync_job').item.json[0].id}}`
        - Body: `{ "status": "success", "completed_at": "{{$now.toISO()}}", "records_synced": {{$('Map Google Ads results').all().length}} }`
    15. **Error Trigger / IF** branch — em caso de erro em qualquer node entre 8-13:
        - PATCH sync_jobs com `{ "status": "failed", "completed_at": "{{$now.toISO()}}", "error_message": "{{$json.error.message}}" }`
        - Continue → próximo tenant (não abort)

    **Conventions obrigatórias:**
    - TODA chamada à Google Ads API DEVE usar `={{$('Set Constants').item.json.GOOGLE_ADS_API_VERSION}}` na URL — NUNCA `v21` hardcoded em outro lugar (SYNC-06)
    - Credentials (`Supabase Service Role`, `Google OAuth Credentials`, `Google Ads Dev Token`) são REFERENCIADOS por nome via `$credentials` — NUNCA valores literais
    - Topo do arquivo JSON deve ter `"name": "Google Ads Sync"` e `"meta": { "templateCredsSetupCompleted": false }`
    - Schedule Trigger DEVE estar com `"active": false` no JSON exportado — operador ativa manualmente após importar e atribuir credentials

    **Como gerar o JSON:** Criar manualmente seguindo o schema N8N (https://docs.n8n.io/workflows/export-import/). Como referência de estrutura, consultar `n8n_flows_importaveis.json` na raiz (se existir um workflow N8N exemplo já no repo). Estrutura mínima de arquivo:
    ```json
    {
      "name": "Google Ads Sync",
      "meta": { "templateCredsSetupCompleted": false },
      "active": false,
      "nodes": [ /* 15 nodes acima */ ],
      "connections": { /* edges em ordem topológica */ },
      "settings": { "executionOrder": "v1" },
      "versionId": "1"
    }
    ```
  </action>
  <verify>
    <automated>test -f n8n-workflows/google-ads-sync.json && node -e "const w=require('./n8n-workflows/google-ads-sync.json'); if(!w.nodes||w.nodes.length<14)throw new Error('expected >=14 nodes, got '+(w.nodes?.length||0)); const json=JSON.stringify(w); if(!json.includes('GOOGLE_ADS_API_VERSION'))throw new Error('missing API version constant'); if(json.match(/['\"]v21['\"]/g)?.length!==1)throw new Error('v21 must appear exactly once (in Set Constants), not hardcoded elsewhere — SYNC-06'); if(!json.includes('on_conflict=tenant_id,campaign_id,channel,date'))throw new Error('missing PostgREST upsert URL'); if(!json.includes('googleAds:searchStream'))throw new Error('missing SearchStream endpoint'); if(!json.includes('refresh_daily_rollups'))throw new Error('missing rpc call'); if(!json.includes('7d_click'))throw new Error('missing attribution_window default'); console.log('OK: '+w.nodes.length+' nodes');"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `n8n-workflows/google-ads-sync.json` existe e é JSON válido
    - JSON tem chave `"name": "Google Ads Sync"`
    - JSON tem chave `"active": false` (operador ativa após import)
    - `nodes` array tem ≥ 14 entradas
    - Existe exatamente UMA ocorrência da string literal `"v21"` no arquivo inteiro — dentro do node "Set Constants" — TODAS as outras chamadas referenciam via expressão `{{$('Set Constants')...GOOGLE_ADS_API_VERSION}}` (SYNC-06)
    - `grep -c 'cron'` ou inspeção retorna pelo menos 1 `"0 */3 * * *"` no Schedule Trigger
    - Existe node Set chamado "Set Constants" com `GOOGLE_ADS_API_VERSION`, `BACKFILL_DAYS=90`, `INCREMENTAL_DAYS=2`
    - Existe node HTTP com URL contendo `on_conflict=tenant_id,campaign_id,channel,date`
    - Existe node HTTP com URL contendo `googleAds:searchStream`
    - Existe node HTTP com URL contendo `rpc/refresh_daily_rollups`
    - Existe node HTTP com URL contendo `rpc/read_vault_secret`
    - Existe node HTTP com URL contendo `oauth2.googleapis.com/token`
    - Existe string literal `'7d_click'` no Code node "Map Google Ads results"
    - Existe expressão `parseInt(metrics.costMicros || '0') / 1_000_000` (Pitfall 5)
    - NENHUMA ocorrência de literal `developer-token: ` seguida de valor não-expressão (todos via $credentials)
    - NENHUMA ocorrência de literal Bearer seguido de uuid/token — apenas `Bearer {{...}}` expressões
    - JSON inclui node Split In Batches OU loop similar (loop tenants)
    - JSON inclui node Code "Map Google Ads results" com o parser exato fornecido
    - JSON inclui PATCH sync_jobs com `status: success` E `status: failed` (sucesso e erro paths)
  </acceptance_criteria>
  <done>Workflow JSON completo (≥14 nodes), API version como constante única, todos os endpoints corretos, credentials referenciados por expressão.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — importar workflow no N8N e validar estrutura</name>
  <files>n8n-workflows/google-ads-sync.json</files>
  <action>
    Importar manualmente o workflow no N8N (https://evo.wrdigitalgroup.com.br) seguindo os passos em <how-to-verify> abaixo. Esta é uma verificação HUMANA — Claude não tem acesso à UI do N8N nem credenciais de cada tenant.
  </action>
  <verify>
    <automated>test -f n8n-workflows/google-ads-sync.json</automated>
  </verify>
  <done>Usuário digitou "aprovado" confirmando que o JSON importou no N8N sem erro, schedule trigger mostra "0 */3 * * *", constante GOOGLE_ADS_API_VERSION está em Set Constants, e credentials foram atribuídas (com Google Ads Dev Token possivelmente vazio se ainda não aprovado).</done>
  <what-built>
    Workflow JSON `n8n-workflows/google-ads-sync.json` criado na Task 1.
  </what-built>
  <how-to-verify>
    1. Abrir N8N: https://evo.wrdigitalgroup.com.br
    2. Workflows → Import from File → selecionar `n8n-workflows/google-ads-sync.json`
    3. Verificar que TODOS os 14+ nodes foram importados sem erro
    4. Verificar que o Schedule Trigger mostra `0 */3 * * *` (a cada 3 horas)
    5. Verificar que o node "Set Constants" contém `GOOGLE_ADS_API_VERSION = v21`
    6. NÃO ativar o workflow ainda — manter `active: false`
    7. Criar as credentials necessárias em N8N → Credentials:
       - `Supabase Service Role` (Header Auth: apikey + Authorization Bearer com SERVICE_ROLE_KEY)
       - `Google OAuth Credentials` (client_id + client_secret)
       - `Google Ads Dev Token` (developer-token header value) — **PODE estar vazio até token ser aprovado**
    8. Atribuir as credentials aos respectivos nodes do workflow
    9. **Teste estrutural (mock):** Clicar em "Execute Workflow" com Schedule Trigger desativado. Esperado:
       - "List active Google Ads accounts" retorna lista (vazia se nenhum ad_account inserido ainda — ok)
       - Se lista vazia, loop não executa — ok para Task 2
       - Se lista tem entradas mas credenciais Google Ads ausentes → erro esperado em "Exchange refresh for access token" ou "Google Ads SearchStream" → confirma que workflow está estruturado, falta apenas o token

    **Aprovar se:**
    - JSON importou sem corromper
    - Schedule trigger correto
    - Constante API version visível e referenciada por expressões
    - Erros de execução são em pontos esperados (credentials missing — não em erros de sintaxe)

    **Rejeitar se:**
    - Erros de parsing de JSON na importação
    - Algum node tem `v21` hardcoded fora de Set Constants
    - Credentials estão como literal no workflow JSON
  </how-to-verify>
  <resume-signal>Digite "aprovado" se a estrutura está correta, OU descreva o problema (ex: "v21 aparece hardcoded em Map Google Ads results node — corrigir").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N8N (service_role) → Supabase PostgREST | service_role bypassa RLS — VPS N8N deve manter chave em N8N Credential criptografada |
| N8N → Google Ads API | OAuth2 Bearer token via refresh_token (curto-prazo, 3600s) |
| N8N → Google OAuth token endpoint | client_secret transmitido em form body — TLS obrigatório (default em N8N HTTP Request) |
| Supabase Vault → N8N (via rpc) | refresh_token decriptado retornado via TLS; N8N não persiste — usa apenas durante execução |

## STRIDE Threat Register (ASVS L1: V2 Auth, V4 Access, V6 Crypto)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-03-01 | Information Disclosure | service_role key em workflow JSON exportado | mitigate | Workflow referencia $credentials por nome, nunca literais. Validado pelo grep no acceptance_criteria. Workflow JSON pode ser commitado no repo sem expor secrets. |
| T-2-03-02 | Information Disclosure | refresh_token em logs N8N | mitigate | N8N redige automaticamente valores marcados como secret via Credential. Code node "Map Google Ads results" não loga o token. Para reforçar: definir N8N_LOG_LEVEL=info (não debug) na VPS. |
| T-2-03-03 | Tampering | Cross-tenant write (workflow grava em tenant errado) | mitigate | Cada upsert inclui `tenant_id` lido de `ad_accounts.tenant_id` — não há lookup por user_id. service_role bypassa RLS mas tenant_id é explícito por loop iteration. |
| T-2-03-04 | Elevation of Privilege | Atacante com acesso ao N8N reedita workflow para vazar tokens | accept | N8N protegido por basic auth (Phase 00). Mitigação adicional fica fora de scope desta fase. |
| T-2-03-05 | Denial of Service | Workflow consome rate limit Google Ads (15k ops/dia) | mitigate | Schedule a cada 3h × 1-3 tenants × 1 SearchStream call = 8-24 ops/dia. Bem abaixo do limite. |
| T-2-03-06 | Information Disclosure | error_message gravado em sync_jobs vaza token na string | mitigate | Code node de error handler sanitiza error.message com regex `replace(/Bearer [A-Za-z0-9._-]+/g, 'Bearer ***')` antes de PATCH sync_jobs |

</threat_model>

<verification>
- Arquivo `n8n-workflows/google-ads-sync.json` é JSON válido
- Comando `node -e "const w=require('./n8n-workflows/google-ads-sync.json'); console.log(w.nodes.length+' nodes, active='+w.active);"` retorna pelo menos 14 nodes e `active=false`
- `grep -c '"v21"' n8n-workflows/google-ads-sync.json` retorna `1` (apenas em Set Constants)
- `grep "0 \*/3 \* \* \*" n8n-workflows/google-ads-sync.json` retorna ≥1 match (Schedule Trigger)
- Human verification (Task 2) aprovada
- BLOCKER documentado: Workflow não pode ser ativado contra Google Ads real até Developer Token aprovado (per STATE.md)
</verification>

<success_criteria>
- Workflow JSON importável via N8N UI
- Schedule Trigger cron `0 */3 * * *` configurado
- GOOGLE_ADS_API_VERSION = 'v21' em Set Constants — referenciado por expressão em TODAS as chamadas à Google Ads API
- Workflow itera sobre ad_accounts WHERE channel='google_ads' AND active=true
- Para cada tenant: read_vault_secret → OAuth refresh → SearchStream → upsert campaign_metrics → refresh_daily_rollups → PATCH sync_jobs
- Erro de um tenant: sync_jobs.status='failed' + continua para próximo
- Backfill 90d na primeira execução, incremental 2d nas subsequentes
- cost_micros divide por 1.000.000 (Pitfall 5)
- attribution_window = '7d_click' literal em todo upsert (SYNC-05)
- Nenhuma credencial em texto plano no JSON exportado
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-03-SUMMARY.md` documentando:
- Path do arquivo JSON criado
- Contagem de nodes
- Confirmação visual via screenshot da N8N UI mostrando o workflow importado
- Lista de N8N Credentials criadas (nomes — NÃO valores)
- Status do BLOCKER Google Ads Developer Token (ainda pendente / aprovado)
- Próximo passo: ativar workflow após Developer Token aprovado + inserir refresh_tokens no Vault por tenant
</output>

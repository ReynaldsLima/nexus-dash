---
phase: 04-ai-insights
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - lib/hooks/use-ai-insights.ts
  - components/insights/streaming-insight-card.tsx
  - components/dashboard/ai-shortcut-card.tsx
  - app/[tenant-slug]/insights/page.tsx
  - app/[tenant-slug]/dashboard/page.tsx
autonomous: true
requirements: [AI-01, AI-03]

must_haves:
  truths:
    - "The insights history list renders real ai_insights rows for the active tenant (D-10) — MOCK_INSIGHTS import removed"
    - "Clicking 'Analisar agora' streams Claude's text token-by-token into a card at the top of the page (D-12), then the completed insight appears in the history list without a page refresh (D-03)"
    - "The history list shows an empty state for a tenant with zero insights and an error state when the query fails"
    - "The dashboard shows an 'Análise sob demanda' shortcut card that navigates to /insights?trigger=1 and auto-triggers the same generation (D-02)"
  artifacts:
    - path: "lib/hooks/use-ai-insights.ts"
      provides: "TanStack Query hook fetching ai_insights for the active tenant, mapped to AiInsight"
      contains: "useAiInsights"
    - path: "components/insights/streaming-insight-card.tsx"
      provides: "Streaming/completing/error display card (UI-SPEC Element 1)"
      contains: "StreamingInsightCard"
    - path: "components/dashboard/ai-shortcut-card.tsx"
      provides: "Dashboard on-demand shortcut card (UI-SPEC Element 4)"
      contains: "AiShortcutCard"
    - path: "app/[tenant-slug]/insights/page.tsx"
      provides: "Real-data insights page with streaming + empty/error states"
      contains: "useAiInsights"
  key_links:
    - from: "app/[tenant-slug]/insights/page.tsx"
      to: "app/api/insights/generate/route.ts"
      via: "fetch POST with streamed response body reader"
      pattern: "api/insights/generate"
    - from: "lib/hooks/use-ai-insights.ts"
      to: "ai_insights table"
      via: "supabase.from('ai_insights') filtered by tenant slug"
      pattern: "from\\('ai_insights'\\)"
    - from: "components/dashboard/ai-shortcut-card.tsx"
      to: "app/[tenant-slug]/insights/page.tsx"
      via: "router.push to /insights?trigger=1"
      pattern: "trigger=1"
---

<objective>
Wire the on-demand insights experience end-to-end (AI-01 UI + AI-03 history). Replace the mock data in the existing insights page with a real TanStack Query hook (D-10), swap the fake generating placeholder for a real streaming card that reads the Plan 03 route's token stream (D-12), auto-refresh the history on completion (D-03), add empty/error states, and add the dashboard shortcut card (D-02). Preserve the existing `InsightCard`/`TYPE_CONFIG`/`IMPACT_CONFIG` verbatim per 04-UI-SPEC.md.

Purpose: AI-01 needs the streaming trigger UI; AI-03 needs the tenant-filtered history list. This plan is the user-facing half of on-demand analysis.
Output: `use-ai-insights` hook, `StreamingInsightCard`, `AiShortcutCard`, rewritten insights page, modified dashboard page.

**Locked decisions:** D-02 (two trigger locations), D-03 (auto-save + auto-appear), D-10 (keep UI, real data), D-11 (filtered by active tenant), D-12 (inline streaming). **UI contract:** 04-UI-SPEC.md Elements 1 & 4, Empty/Error States, Copywriting Contract — follow verbatim (CTA label is exactly "Analisar agora"; do not invent copy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-insights/04-CONTEXT.md
@.planning/phases/04-ai-insights/04-UI-SPEC.md
@.planning/STATE.md

<interfaces>
Existing TanStack Query + browser-client hook pattern to replicate (from `lib/hooks/use-dashboard-data.ts`):
```typescript
'use client'
import { useQuery } from '@tanstack/react-query'
import { createClient } from '@/lib/supabase/client'
// useQuery({ queryKey: [...], queryFn: async () => { const supabase = createClient(); ... }, enabled: !!tenantSlug })
```

AiInsight target shape (from `lib/mock-data.ts`) the hook maps DB rows to:
`{ id, createdAt, type, title, summary, metrics, recommendations, impact }` — DB row `created_at` maps to `createdAt`; `metrics`/`recommendations` are JSONB (already arrays).

ai_insights RLS is super_admin-only (Plan 02). The active tenant is filtered in the query via a join on `tenants.slug` (super_admin sees all tenants' rows, so filtering is by slug, not RLS): `.select('*, tenants!inner(slug)').eq('tenants.slug', tenantSlug).order('created_at', { ascending: false })`.

Generate route (Plan 03): `POST /api/insights/generate` with JSON body `{ tenantSlug }`, returns a text stream (`toTextStreamResponse()`), super_admin-only. Read via `res.body.getReader()` + `TextDecoder`.

StreamingInsightCard props (defined in Task 1): `{ state: 'streaming' | 'completing' | 'error'; text: string; onRetry: () => void }`. Task 2 binds these to the page's local `streamState`, `streamedText`, and `handleGenerate` respectively (see Task 2 step 6).

Existing insights page baseline (`app/[tenant-slug]/insights/page.tsx`): `InsightCard` (lines 55-144), `TYPE_CONFIG` (11-36), `IMPACT_CONFIG` (38-42), `formatDate` (44-52), CTA button (165-187), generating placeholder (190-206) — REUSE the first three verbatim, REPLACE the placeholder + CTA label + data source.

Dashboard insert point (`app/[tenant-slug]/dashboard/page.tsx`): new full-width card between the KPI grid (ends line 308) and the Charts Row (`<div className="grid grid-cols-1 lg:grid-cols-5 gap-4">`, line 311). `tenantSlug` already in scope at line 158.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/hooks/use-ai-insights.ts + components/insights/streaming-insight-card.tsx</name>
  <files>lib/hooks/use-ai-insights.ts, components/insights/streaming-insight-card.tsx</files>
  <read_first>
    - lib/hooks/use-dashboard-data.ts (exact useQuery + createClient pattern to replicate)
    - lib/mock-data.ts (AiInsight shape the hook returns)
    - .planning/phases/04-ai-insights/04-UI-SPEC.md "New Element 1: Streaming Insight Card" (all states, exact colors, min-height, badge shape, cursor)
    - app/[tenant-slug]/insights/page.tsx (TYPE_CONFIG.opportunity's exact borderColor/bgColor/badge values the streaming card reuses)
  </read_first>
  <action>
Create `lib/hooks/use-ai-insights.ts` — a `'use client'` TanStack Query hook mirroring `use-dashboard-data.ts`:
- `useAiInsights(tenantSlug: string)` runs `createClient().from('ai_insights').select('id, tenant_id, source, type, title, summary, metrics, recommendations, impact, created_at, tenants!inner(slug)').eq('tenants.slug', tenantSlug).order('created_at', { ascending: false })`.
- Map each row to the `AiInsight` shape: `createdAt: row.created_at`, pass `metrics`/`recommendations` through (JSONB arrays), keep `type`/`title`/`summary`/`impact`.
- `queryKey: ['ai-insights', tenantSlug]`, `enabled: !!tenantSlug`. Return the `useQuery` result (callers use `data`, `isLoading`, `isError`, `refetch`).

Create `components/insights/streaming-insight-card.tsx` — a `'use client'` component per 04-UI-SPEC.md Element 1. Props: `{ state: 'streaming' | 'completing' | 'error'; text: string; onRetry: () => void }`. Shell: `rounded-xl border overflow-hidden p-5`, `borderLeftWidth: '3px'`, `borderColor: 'var(--chart-1)'`, `background: 'oklch(0.60 0.22 258 / 0.08)'`, `min-h-[140px]`, `aria-live="polite"` on the text container. Badge: `inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold`, bg `oklch(0.60 0.22 258 / 0.15)`, color `oklch(0.72 0.16 258)`, containing a pulsing dot (`size-1.5 rounded-full bg-current animate-pulse`) + icon + label. State deltas (exact per UI-SPEC):
  - streaming: `Sparkles` icon, label "Analisando campanhas…", blinking cursor (`inline-block w-1.5 h-3.5 ml-0.5 animate-pulse`, `background: var(--chart-1)`) after the text.
  - completing: `CheckCircle2` icon, badge tint `oklch(0.75 0.18 155 / 0.15)`/`oklch(0.75 0.18 155)`, label "Análise concluída", no cursor.
  - error: `AlertTriangle` icon, tint `oklch(0.65 0.20 15 / 0.15)`/`oklch(0.65 0.20 15)`, label "Falha na análise", body text replaced with "Não foi possível concluir a análise. Tente novamente.", plus a right-aligned `Button variant="outline" size="sm"` labelled "Tentar novamente" calling `onRetry`.
  Body text: `text-sm text-muted-foreground leading-relaxed whitespace-pre-wrap`. Icons from `lucide-react`, Button from `@/components/ui/button`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/use-ai-insights.ts` exports `useAiInsights`, contains `from('ai_insights')`, `eq('tenants.slug'`, and maps `created_at` -> `createdAt`
    - `components/insights/streaming-insight-card.tsx` exports `StreamingInsightCard`, contains the three state labels "Analisando campanhas…", "Análise concluída", "Falha na análise" and `aria-live="polite"`
    - The error state renders a "Tentar novamente" button wired to the `onRetry` prop
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Hook + streaming card created; compile clean; colors/labels match UI-SPEC verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/[tenant-slug]/insights/page.tsx (real data + streaming + trigger param + empty/error)</name>
  <files>app/[tenant-slug]/insights/page.tsx</files>
  <read_first>
    - app/[tenant-slug]/insights/page.tsx (current full file — preserve InsightCard/TYPE_CONFIG/IMPACT_CONFIG/formatDate verbatim; replace data source, CTA label, placeholder)
    - lib/hooks/use-ai-insights.ts, components/insights/streaming-insight-card.tsx (Task 1 — the imports to wire; note StreamingInsightCard's props: `state`, `text`, `onRetry`)
    - .planning/phases/04-ai-insights/04-UI-SPEC.md "Copywriting Contract" and "Empty / Error States" (exact copy strings)
    - app/[tenant-slug]/dashboard/page.tsx lines 165-177 (the exact error-state Card wrapper to mirror)
  </read_first>
  <action>
Rewrite `app/[tenant-slug]/insights/page.tsx`, keeping `'use client'`, `InsightCard`, `TYPE_CONFIG`, `IMPACT_CONFIG`, `formatDate` unchanged. Changes:

1. Remove `import { MOCK_INSIGHTS, type AiInsight }` — import `type AiInsight` from `@/lib/mock-data`, add `import { useAiInsights } from '@/lib/hooks/use-ai-insights'`, `import { StreamingInsightCard } from '@/components/insights/streaming-insight-card'`, `useParams`/`useSearchParams`/`useRouter` from `next/navigation`, `useEffect`/`useRef`/`useState`.

2. Get `tenantSlug` from `useParams()`. Call `const { data: insights, isLoading, isError, refetch } = useAiInsights(tenantSlug)`.

3. Header count uses `insights?.length ?? 0` instead of `MOCK_INSIGHTS.length`.

4. CTA button: change label from "Gerar Novo Insight" to "Analisar agora" (loading label "Analisando…" unchanged). Keep the exact class string and `style` (var(--sidebar-primary)). `disabled` while a streaming state is active.

5. Declare two pieces of local state that back the streaming card: `const [streamState, setStreamState] = useState<'idle' | 'streaming' | 'completing' | 'error'>('idle')` and `const [streamedText, setStreamedText] = useState('')`. `handleGenerate()`: guard against concurrent runs (return early if `streamState !== 'idle'`), set `setStreamState('streaming')` and `setStreamedText('')`, POST to `/api/insights/generate` with `{ tenantSlug }`, read the response via `res.body.getReader()` + `TextDecoder`, appending each decoded chunk to `streamedText` via `setStreamedText((t) => t + chunk)` so `StreamingInsightCard` renders progressively. Strip any `<insight_data>` tail from the displayed text (split on `<insight_data>`). On stream end: `setStreamState('completing')` for ~600ms (setTimeout), then `await refetch()`, `setStreamState('idle')`, and `setStreamedText('')` to clear the streaming card. On fetch reject / non-2xx: `setStreamState('error')`.

6. Render the streaming card at the top (above the history list) only while `streamState !== 'idle'`, binding all three props explicitly: `<StreamingInsightCard state={streamState} text={streamedText} onRetry={handleGenerate} />`. `state` is fed by `streamState`, `text` by the accumulated `streamedText`, and `onRetry` by `handleGenerate` (so the error-state "Tentar novamente" button re-invokes the same generation flow). The "Merging" step is just unmount (`streamState === 'idle'`) + refetch. Also gate the CTA button's `disabled` on `streamState !== 'idle'`.

7. History list: replace `MOCK_INSIGHTS.map` with `insights?.map`. Add:
   - Loading (isLoading): minimal `text-sm text-muted-foreground` "Carregando…" or a `Skeleton` list — keep light.
   - Error (isError): the exact Card wrapper + copy "Erro ao carregar insights. Tente recarregar a página." (mirrors dashboard/page.tsx line 172-173).
   - Empty (insights.length === 0, not loading/error): centered `flex flex-col items-center justify-center py-16 gap-2 text-center`, `Sparkles` `size-8 text-muted-foreground/40`, heading "Nenhuma análise ainda", body 'Clique em "Analisar agora" para gerar a primeira recomendação de IA para este tenant.'

8. Trigger param (D-02): on mount, if `useSearchParams().get('trigger') === '1'`, call `handleGenerate()` once (use a ref to prevent re-fire) then `router.replace(\`/${tenantSlug}/insights\`)` to strip the param so a refresh doesn't re-trigger.

Keep the footer note verbatim.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/[tenant-slug]/insights/page.tsx` no longer imports `MOCK_INSIGHTS` (grep: `MOCK_INSIGHTS` returns nothing)
    - Contains `useAiInsights`, `StreamingInsightCard`, a `fetch(` to `/api/insights/generate`, and `res.body` reader usage
    - Renders `<StreamingInsightCard state={streamState} text={streamedText} onRetry={handleGenerate} />` with all three props bound (grep: `onRetry={handleGenerate}` and `state={streamState}` and `text={streamedText}`)
    - CTA label is exactly `Analisar agora` (grep) and the empty-state heading "Nenhuma análise ainda" and error copy "Erro ao carregar insights. Tente recarregar a página." are present
    - Reads `searchParams.get('trigger')` and calls `router.replace` to strip it
    - `InsightCard`, `TYPE_CONFIG`, `IMPACT_CONFIG` still present unchanged (grep each)
    - `npx tsc --noEmit` and `npm run build` pass
  </acceptance_criteria>
  <done>Insights page renders real data, streams on trigger, handles empty/error, auto-triggers from the dashboard shortcut param.</done>
</task>

<task type="auto">
  <name>Task 3: components/dashboard/ai-shortcut-card.tsx + insert into dashboard page</name>
  <files>components/dashboard/ai-shortcut-card.tsx, app/[tenant-slug]/dashboard/page.tsx</files>
  <read_first>
    - .planning/phases/04-ai-insights/04-UI-SPEC.md "New Element 4: Dashboard Shortcut Card" (structure, icon tint, CTA class reuse, click behavior)
    - app/[tenant-slug]/dashboard/page.tsx (current file — insert point between KPI grid and Charts Row, ~line 309)
    - app/[tenant-slug]/insights/page.tsx (the exact CTA button class string to reuse verbatim)
  </read_first>
  <action>
Create `components/dashboard/ai-shortcut-card.tsx` — `'use client'`, props `{ tenantSlug: string }`, per 04-UI-SPEC.md Element 4:
- A full-width `Card`/`CardContent` (`@/components/ui/card`) in a flex row, `p-5`.
- Left: icon container `p-1.5 rounded-md`, `background: 'oklch(0.6 0.22 258 / 0.12)'`, `Sparkles` icon `color: var(--chart-1)`; then a text column — title "Análise sob demanda" (`text-sm font-semibold`) and body "A IA revisa as campanhas dos últimos 30 dias e sugere otimizações." (`text-sm text-muted-foreground`).
- Right: a button using the EXACT class string from the insights page CTA (`inline-flex items-center gap-2 rounded-md px-4 py-2 text-sm font-semibold ...`, `style` with `var(--sidebar-primary)`), label "Analisar agora".
- `useRouter` from `next/navigation`; onClick: `router.push(\`/${tenantSlug}/insights?trigger=1\`)`. No loading state on the card itself.

Modify `app/[tenant-slug]/dashboard/page.tsx`: import `AiShortcutCard`, and insert `<AiShortcutCard tenantSlug={tenantSlug} />` as a full-width row between the KPI grid closing `</div>` (line ~308) and the Charts Row `<div className="grid grid-cols-1 lg:grid-cols-5 gap-4">` (line ~311). `tenantSlug` is already in scope (line 158).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components/dashboard/ai-shortcut-card.tsx` exports `AiShortcutCard`, contains "Análise sob demanda", the body copy "A IA revisa as campanhas dos últimos 30 dias e sugere otimizações.", label "Analisar agora", and `router.push` to a URL containing `trigger=1`
    - `app/[tenant-slug]/dashboard/page.tsx` imports and renders `<AiShortcutCard tenantSlug={tenantSlug} />` between the KPI grid and the Charts Row
    - `npx tsc --noEmit` and `npm run build` pass
  </acceptance_criteria>
  <done>Dashboard shortcut card created and inserted; clicking it deep-links into the insights page with auto-trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → ai_insights (RLS) | The history query runs as the logged-in session; super_admin-only RLS is the access boundary, slug filter is scoping only |
| Insights page → /api/insights/generate | The route re-verifies super_admin server-side; the page cannot bypass it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | A non-super_admin reaching /insights and calling generate | mitigate | ai_insights RLS returns zero rows for non-super_admin; the generate route returns 403 (Plan 03) — the UI is defense-in-depth, the server is authoritative |
| T-04-09 | Tampering | Rendering unsanitized model text as HTML | accept | The existing `InsightCard` already uses `dangerouslySetInnerHTML` only for the `metrics[].value` field (pre-existing baseline, `&nbsp;` entities); the streamed prose renders as plain text (`whitespace-pre-wrap`), not HTML — no new injection surface introduced by this plan |
| T-04-10 | Denial of Service | Repeated rapid clicks spawning concurrent Claude generations | mitigate | CTA `disabled` for the full streaming/completing/merging duration (existing `disabled` pattern preserved); concurrent-run guard in `handleGenerate` |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass
- `npm test` exits 0 (no test regressions)
- Manual (per 04-VALIDATION.md): log in as super_admin, click "Analisar agora", confirm progressive streaming + auto-appear in history; verify empty state on a fresh tenant; verify the dashboard shortcut deep-links and auto-triggers
</verification>

<success_criteria>
- [ ] History list renders real ai_insights rows (MOCK_INSIGHTS removed)
- [ ] Streaming card shows token-by-token text, then merges into history without refresh
- [ ] Empty + error states present with the exact UI-SPEC copy
- [ ] Dashboard shortcut card navigates to /insights?trigger=1 and auto-triggers
- [ ] Existing InsightCard/TYPE_CONFIG/IMPACT_CONFIG preserved verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-insights/04-04-SUMMARY.md`
</output>

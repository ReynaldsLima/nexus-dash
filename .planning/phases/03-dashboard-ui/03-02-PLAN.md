---
phase: 03-dashboard-ui
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - package.json
  - app/providers.tsx
  - app/layout.tsx
  - app/[tenant-slug]/layout.tsx
  - components/layout/header-actions.tsx
  - components/dashboard/date-range-picker.tsx
  - components/ui/popover.tsx
  - components/ui/calendar.tsx
  - components/ui/sheet.tsx
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "App is wrapped in QueryClientProvider so any useQuery hook works app-wide"
    - "A date range picker button appears in the global header on every authenticated page"
    - "Clicking the picker opens a popover with 5 preset buttons + a dual-month range calendar"
    - "Selecting a preset or custom range updates useDateRangeStore"
    - "The picker button label shows the active range compactly (dd/MM – dd/MM)"
    - "Default range on load is Last 30 days and survives client-side navigation between pages"
  artifacts:
    - path: "app/providers.tsx"
      provides: "QueryClientProvider singleton wrapper ('use client')"
      contains: "QueryClientProvider"
    - path: "components/layout/header-actions.tsx"
      provides: "Client wrapper hosting DateRangePicker + TenantSwitcher + LogoutButton"
      contains: "DateRangePicker"
    - path: "components/dashboard/date-range-picker.tsx"
      provides: "Popover + presets + Calendar range picker wired to useDateRangeStore"
      contains: "useDateRangeStore"
    - path: "components/ui/sheet.tsx"
      provides: "shadcn Sheet (used by Plan 04)"
      contains: "Sheet"
  key_links:
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "Providers wraps children inside body"
      pattern: "Providers"
    - from: "app/[tenant-slug]/layout.tsx"
      to: "components/layout/header-actions.tsx"
      via: "HeaderActions replaces inline TenantSwitcher/LogoutButton"
      pattern: "HeaderActions"
    - from: "components/dashboard/date-range-picker.tsx"
      to: "lib/stores/date-range.ts"
      via: "import useDateRangeStore"
      pattern: "from '@/lib/stores/date-range'"
---

<objective>
Estabelecer a fundação de data-fetching e o date range picker global (DASH-04). Instalar `@tanstack/react-query`, adicionar os 3 componentes shadcn faltantes (popover, calendar, sheet), criar o `QueryClientProvider` singleton, e construir o `DateRangePicker` no header global — movendo os elementos interativos do header para um wrapper `'use client'` (`HeaderActions`) porque `app/[tenant-slug]/layout.tsx` é um Server Component.

Purpose: Sem o QueryClientProvider, nenhum `useQuery` (Plans 03/04) funciona. Sem o DateRangePicker no header e o store já criado na Plan 01, nenhuma página pode reagir ao range global. Esta plan é o gargalo do Wave 1 — Plans 03/04/05 dependem dela.

Output: Deps instaladas, 3 componentes shadcn, providers wired, header refatorado com DateRangePicker funcional persistindo o range na sessão (Zustand in-memory, sem `persist`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-ui/03-CONTEXT.md
@.planning/phases/03-dashboard-ui/03-RESEARCH.md

<interfaces>
<!-- lib/stores/date-range.ts (criado na Plan 01) -->
export type PresetKey = 'last7' | 'last14' | 'last30' | 'thisMonth' | 'lastMonth' | 'custom'
export type DateRange = { from: Date; to: Date }
export function getPresetRange(preset: Exclude<PresetKey,'custom'>): DateRange
export const useDateRangeStore: () => { from: Date; to: Date; setRange: (r: DateRange) => void; applyPreset: (p: PresetKey) => void }

<!-- TenantSwitcher (components/tenants/tenant-switcher.tsx) — JÁ é Client Component -->
export function TenantSwitcher({ role, tenants, activeSlug }: { role: ...; tenants: TenantOption[]; activeSlug: string }): JSX
export interface TenantOption { id: string; name: string; slug: string; active: boolean }

<!-- LogoutButton (components/auth/logout-button.tsx) — JÁ é Client Component, sem props -->
export function LogoutButton(): JSX

<!-- app/layout.tsx atual: <body> envolve {children} com <TenantStoreProvider> -->
<!-- app/[tenant-slug]/layout.tsx atual (linhas 64-67): header tem <div className="flex items-center gap-2"> com <TenantSwitcher .../> e <LogoutButton /> -->
</interfaces>

<!-- ATENÇÃO: shadcn neste projeto usa @base-ui/react (NÃO Radix). Ver components/ui/dialog.tsx (importa de '@base-ui/react/dialog'). Os componentes popover/calendar/sheet adicionados via `npx shadcn add` virão na variante configurada no projeto. APÓS instalar, ler o sheet.tsx gerado para descobrir a API real de "prevenir fechar ao clicar fora" (Plan 04 depende disso). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar TanStack Query + shadcn popover/calendar/sheet + criar Providers e wirar no root layout</name>
  <read_first>
    - package.json (confirmar que @tanstack/react-query NÃO está nas dependencies — está ausente)
    - app/layout.tsx (linhas 35-44 — onde <TenantStoreProvider> envolve children dentro de <body>)
    - components/ui/dialog.tsx (linhas 1-10 — confirma base @base-ui/react usada pelos componentes shadcn deste projeto)
    - .planning/phases/03-dashboard-ui/03-RESEARCH.md (Pattern 1 — código completo de providers.tsx)
  </read_first>
  <action>
1. Instalar TanStack Query: `npm install @tanstack/react-query@^5.101.0`
2. Adicionar os componentes shadcn faltantes: `npx shadcn add popover calendar sheet` (instala `react-day-picker@^9` como peer dep do calendar automaticamente). Se o CLI pedir confirmação, aceitar. Confirmar que criou `components/ui/popover.tsx`, `components/ui/calendar.tsx`, `components/ui/sheet.tsx`.
3. Criar `app/providers.tsx` ('use client') com QueryClient singleton de módulo (fora do componente) conforme RESEARCH.md Pattern 1:
```tsx
'use client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
const queryClient = new QueryClient({
  defaultOptions: { queries: { staleTime: 5 * 60 * 1000, retry: 1 } },
})
export function Providers({ children }: { children: React.ReactNode }) {
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
}
```
4. Em `app/layout.tsx`: importar `{ Providers }` de `'./providers'` e envolver os children. O `<TenantStoreProvider>` existente deve permanecer; aninhar como `<Providers><TenantStoreProvider>{children}</TenantStoreProvider></Providers>` dentro de `<body>`.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"@tanstack/react-query"`
    - `package.json` contains `"react-day-picker"` (peer dep instalada pelo calendar)
    - `components/ui/popover.tsx` exists
    - `components/ui/calendar.tsx` exists
    - `components/ui/sheet.tsx` exists
    - `app/providers.tsx` contains `QueryClientProvider` and `'use client'`
    - `app/layout.tsx` contains `Providers` wrapping `{children}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Deps + 3 componentes shadcn instalados; Providers criado e ativo no root layout; build de tipos limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar HeaderActions wrapper + DateRangePicker e wirar no tenant layout</name>
  <read_first>
    - app/[tenant-slug]/layout.tsx (linhas 58-68 — header atual com TenantSwitcher + LogoutButton inline)
    - components/tenants/tenant-switcher.tsx (props exatas: role, tenants, activeSlug)
    - components/auth/logout-button.tsx (sem props)
    - lib/stores/date-range.ts (criado na Plan 01 — useDateRangeStore, getPresetRange, PresetKey)
    - components/ui/calendar.tsx (RECÉM criado na Task 1 — ler para a API real de mode="range", prop selected/onSelect, e se aceita locale)
    - components/ui/popover.tsx (RECÉM criado — Popover, PopoverTrigger, PopoverContent)
    - .planning/phases/03-dashboard-ui/03-RESEARCH.md (Pattern 4 — código do DateRangePicker; Pattern 8 — HeaderActions)
  </read_first>
  <action>
1. Criar `components/dashboard/date-range-picker.tsx` ('use client') conforme RESEARCH.md Pattern 4. Importar `useDateRangeStore`, `getPresetRange`, `PresetKey` de `'@/lib/stores/date-range'`; `Popover/PopoverTrigger/PopoverContent` de `'@/components/ui/popover'`; `Calendar` de `'@/components/ui/calendar'`; `Button` de `'@/components/ui/button'`; `CalendarIcon` de `'lucide-react'`. Usar `Intl.DateTimeFormat('pt-BR', { day:'2-digit', month:'2-digit' })` para o label do botão (formato `dd/MM – dd/MM`) — NÃO instalar date-fns (não está no projeto). Painel esquerdo: 5 botões de preset (last7/last14/last30/thisMonth/lastMonth) chamando `applyPreset(key)` + fechar popover. Painel direito: `<Calendar mode="range" .../>` ajustado à API REAL lida do calendar.tsx (a prop pode ser `selected`/`onSelect` ou variante base-ui — usar o que o componente gerado expõe). Ao selecionar range completo (from && to): `setRange({ from, to })` e fechar. Estado `open` via `useState`.
2. Criar `components/layout/header-actions.tsx` ('use client') conforme RESEARCH.md Pattern 8:
```tsx
'use client'
import { TenantSwitcher, type TenantOption } from '@/components/tenants/tenant-switcher'
import { DateRangePicker } from '@/components/dashboard/date-range-picker'
import { LogoutButton } from '@/components/auth/logout-button'
type HeaderActionsProps = { role: string | null; tenants: TenantOption[]; activeSlug: string }
export function HeaderActions({ role, tenants, activeSlug }: HeaderActionsProps) {
  return (
    <div className="flex items-center gap-2">
      <DateRangePicker />
      <TenantSwitcher role={role} tenants={tenants} activeSlug={activeSlug} />
      <LogoutButton />
    </div>
  )
}
```
3. Em `app/[tenant-slug]/layout.tsx`: remover os imports inline de `TenantSwitcher` e `LogoutButton`; importar `{ HeaderActions }` de `'@/components/layout/header-actions'` (manter o import do type `TenantOption` se ainda usado por `loadTenantsForSwitcher`). Substituir o bloco `<div className="flex items-center gap-2">...</div>` (linhas ~64-67) por `<HeaderActions role={role} tenants={tenants} activeSlug={urlSlug} />`.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `components/dashboard/date-range-picker.tsx` contains `useDateRangeStore` and `mode="range"`
    - `components/dashboard/date-range-picker.tsx` does NOT contain `date-fns` (não instalar dep extra)
    - `components/layout/header-actions.tsx` contains `DateRangePicker` and `TenantSwitcher` and `LogoutButton`
    - `app/[tenant-slug]/layout.tsx` contains `HeaderActions` and does NOT contain `<TenantSwitcher` (movido para HeaderActions)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>DateRangePicker no header global de todas as páginas autenticadas; presets + calendário custom escrevem no store; build limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← env vars | `NEXT_PUBLIC_SUPABASE_*` já expostas (publishable key, por design). Nenhum secret novo introduzido nesta plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | app/providers.tsx (bundle do client) | mitigate | QueryClient não armazena secrets; somente cache de respostas já autorizadas por RLS. Nenhuma chave sensível importada. |
| T-03-02-02 | Elevation of Privilege | DateRangePicker / HeaderActions | accept | Componentes puramente de UI client-side. Date range não é input de segurança — apenas filtra queries que já passam por RLS no DB. |
</threat_model>

<verification>
- `npm test` continua verde (nenhum teste quebrado)
- `npx tsc --noEmit` limpo
- App carrega com QueryClientProvider ativo (pré-requisito para Plans 03/04)
- Header de todas as páginas `/[tenant-slug]/*` mostra o DateRangePicker
</verification>

<success_criteria>
- DASH-04 parcial: picker global presente, presets + custom, default Last 30 days, persiste na sessão via Zustand (re-fetch reativo é completado nas Plans 03/04 via queryKey)
- QueryClientProvider singleton ativo app-wide
- Sheet, Popover, Calendar disponíveis em components/ui para Plans 03/04
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-ui/03-02-SUMMARY.md`
</output>

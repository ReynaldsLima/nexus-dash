---
phase: 03-dashboard-ui
plan: 06
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - components/dashboard/channel-sheet.tsx
  - app/[tenant-slug]/dashboard/page.tsx
autonomous: true
gap_closure: true
gap_ids: [GAP-03-01]
requirements: [DASH-03-ext]

must_haves:
  truths:
    - "Clicar em um slice do PieChart (Google Ads ou Meta Ads) abre um Sheet lateral direito com 520px de largura"
    - "O Sheet exibe o gasto diário do canal em AreaChart para o período selecionado (seção 1)"
    - "O Sheet exibe 6 métricas agregadas do canal — Impressões, Cliques, CTR, Gasto, Conversões, ROAS (seção 2)"
    - "O Sheet exibe até 5 campanhas do canal ordenadas por spend desc (seção 3)"
    - "O Sheet não fecha ao clicar fora — apenas botão X ou tecla Esc"
    - "Cursor pointer nos slices do PieChart sinaliza interatividade"
    - "Empty state adequado quando canal não tem dados no período"
  artifacts:
    - path: "components/dashboard/channel-sheet.tsx"
      provides: "ChannelSheet component com 3 seções (spend chart, métricas, top campanhas)"
      exports: ["ChannelSheet"]
    - path: "app/[tenant-slug]/dashboard/page.tsx"
      provides: "DashboardPage com onClick no PieChart e ChannelSheet integrado"
      contains: "selectedChannel"
  key_links:
    - from: "app/[tenant-slug]/dashboard/page.tsx"
      to: "components/dashboard/channel-sheet.tsx"
      via: "<ChannelSheet channel={selectedChannel} channelRows={...} tenantSlug={tenantSlug} onClose={...}>"
      pattern: "ChannelSheet"
    - from: "components/dashboard/channel-sheet.tsx"
      to: "lib/hooks/use-campaigns-data.ts"
      via: "useCampaignsData(tenantSlug)"
      pattern: "useCampaignsData"
    - from: "app/[tenant-slug]/dashboard/page.tsx"
      to: "selectedChannel state"
      via: "Pie onClick={(data) => setSelectedChannel(data.name)}"
      pattern: "setSelectedChannel"
---

<objective>
Fechar GAP-03-01: adicionar channel click drill-down no PieChart do dashboard.

Quando o usuário clica em um slice do PieChart (Google Ads ou Meta Ads), um Sheet lateral
abre mostrando: (1) gasto diário do canal em AreaChart, (2) 6 métricas agregadas calculadas
de `data.current` já disponível, (3) top 5 campanhas do canal via `useCampaignsData`.

Purpose: Completar ROADMAP SC3 — "clicking a channel opens a drill-down view with detailed
time-series metrics" — usando apenas dados já carregados no dashboard, sem novas queries
(exceto para a seção de campanhas que reutiliza hook existente).

Output: `components/dashboard/channel-sheet.tsx` (novo) + `app/[tenant-slug]/dashboard/page.tsx` (modificado com onClick + ChannelSheet)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-ui/03-UI-SPEC.md
@.planning/phases/03-dashboard-ui/03-VERIFICATION.md

<interfaces>
<!-- Contratos de código que o executor precisa — extraídos do codebase. Não explorar novamente. -->

De lib/hooks/use-dashboard-data.ts:
```typescript
export type DailyRollupRow = {
  channel: string          // 'google_ads' | 'meta_ads' | 'all'
  date: string             // 'YYYY-MM-DD'
  total_spend: number
  total_impressions: number
  total_clicks: number
  total_conversions: number
  total_conv_value: number
}

export type DashboardData = {
  current: DailyRollupRow[]
  prior: DailyRollupRow[]
}

export function useDashboardData(tenantSlug: string): UseQueryResult<DashboardData>
```

De lib/hooks/use-campaigns-data.ts:
```typescript
export function useCampaignsData(tenantSlug: string): UseQueryResult<CampaignMetricRow[]>
// CampaignMetricRow tem: campaign_id, campaign_name, channel, status, date, impressions, clicks, spend, conversions, conversion_value
```

De lib/campaign-aggregation.ts:
```typescript
export function groupCampaignMetrics(rows: CampaignMetricRow[]): AggregatedCampaign[]
// AggregatedCampaign tem: id, name, channel, status, impressions, clicks, spend, conversions, convValue, cpa, roas, ctr
```

De lib/formatters.ts:
```typescript
export function brl(value: number): string   // ex: "R$ 1.234,56"
export function num(value: number): string   // ex: "1.234.567"
```

De components/ui/sheet.tsx (Base UI via @base-ui/react/dialog):
```typescript
// Props disponíveis em Sheet (padrão estabelecido no CampaignSheet):
// open, onOpenChange, disablePointerDismissal
// SheetContent: side="right", className (w-[520px] sm:max-w-[520px])
// SheetHeader, SheetTitle

// PADRÃO EXISTENTE (copiar exatamente de campaign-sheet.tsx linha 113-123):
<Sheet
  open={!!campaign}
  onOpenChange={(open, eventDetails) => {
    if (!open && eventDetails?.reason === 'outside-press') return
    if (!open) onClose()
  }}
  disablePointerDismissal
>
```

De components/campanhas/campaign-sheet.tsx (padrões a reusar):
```typescript
// ChannelBadge — inline (não extraído):
function ChannelBadge({ channel }: { channel: 'google_ads' | 'meta_ads' }) {
  // background: 'oklch(0.60 0.22 258 / 0.15)', color: 'oklch(0.72 0.16 258)' para google_ads
  // background: 'oklch(0.68 0.20 305 / 0.15)', color: 'oklch(0.75 0.16 305)' para meta_ads
  // px-2 py-1 (não py-0.5 — especificado no UI-SPEC)
}

// StatusDot:
function StatusDot({ status }: { status: 'active' | 'paused' }) {
  // size-1.5 rounded-full
  // active: 'oklch(0.75 0.18 155)', paused: 'oklch(0.556 0 0)'
}

// TotalsRow:
function TotalsRow({ label, value }: { label: string; value: string }) {
  // "flex items-center justify-between py-2 border-b border-border/50 last:border-0 text-sm"
}
```

Da app/[tenant-slug]/dashboard/page.tsx (estado e PieChart a modificar):
```typescript
// Estado a adicionar (linha após as variáveis de delta existentes):
const [selectedChannel, setSelectedChannel] = useState<'Google Ads' | 'Meta Ads' | null>(null)

// Mapeamento de canais (objeto de lookup — não switch):
const CHANNEL_KEY_MAP: Record<'Google Ads' | 'Meta Ads', string> = {
  'Google Ads': 'google_ads',
  'Meta Ads': 'meta_ads',
}

// Pie (já existe) — adicionar onClick e cursor nos Cells:
<Pie
  data={channelSplitData}
  dataKey="value"
  nameKey="name"
  cx="50%" cy="50%"
  innerRadius={48} outerRadius={70}
  stroke="none" paddingAngle={3}
  onClick={(data) => setSelectedChannel(data.name as 'Google Ads' | 'Meta Ads')}
>
  <Cell fill="var(--chart-1)" style={{ cursor: 'pointer' }} />
  <Cell fill="var(--chart-2)" style={{ cursor: 'pointer' }} />
</Pie>

// channelRows para passar ao ChannelSheet:
const channelKey = selectedChannel ? CHANNEL_KEY_MAP[selectedChannel] : null
const channelRows = data.current.filter((r) => r.channel === channelKey)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar components/dashboard/channel-sheet.tsx</name>
  <files>components/dashboard/channel-sheet.tsx</files>

  <read_first>
    - components/campanhas/campaign-sheet.tsx — padrão exato de Sheet, ChannelBadge, StatusDot, TotalsRow, AreaChart a replicar
    - lib/hooks/use-campaigns-data.ts — assinatura de useCampaignsData
    - lib/campaign-aggregation.ts — tipo AggregatedCampaign e função groupCampaignMetrics
    - lib/hooks/use-dashboard-data.ts — tipo DailyRollupRow
    - lib/formatters.ts — brl(), num()
    - .planning/phases/03-dashboard-ui/03-UI-SPEC.md — especificação visual completa
  </read_first>

  <action>
Criar `components/dashboard/channel-sheet.tsx` com as seguintes especificações exatas:

**Props:**
```typescript
type ChannelSheetProps = {
  channel: 'Google Ads' | 'Meta Ads' | null  // null = Sheet fechado
  channelRows: DailyRollupRow[]              // já filtrados por canal (período atual)
  tenantSlug: string
  onClose: () => void
}
```

**Estrutura do componente:**

1. Imports necessários:
   - `'use client'` no topo
   - `Area, AreaChart, CartesianGrid, XAxis, YAxis` de 'recharts'
   - `Sheet, SheetContent, SheetHeader, SheetTitle` de '@/components/ui/sheet'
   - `ChartContainer, ChartTooltip, ChartTooltipContent` de '@/components/ui/chart'
   - `Skeleton` de '@/components/ui/skeleton'
   - `useCampaignsData` de '@/lib/hooks/use-campaigns-data'
   - `groupCampaignMetrics` e `type AggregatedCampaign` de '@/lib/campaign-aggregation'
   - `brl, num` de '@/lib/formatters'
   - `type DailyRollupRow` de '@/lib/hooks/use-dashboard-data'

2. Sub-componentes inline (copiar/adaptar de campaign-sheet.tsx):
   - `ChannelBadge({ channel }: { channel: 'google_ads' | 'meta_ads' })` — usar `px-2 py-1` (não `py-0.5`)
   - `StatusDot({ status }: { status: 'active' | 'paused' })`
   - `TotalsRow({ label, value }: { label: string; value: string })`

3. Mapeamento de nomes de canal (objeto lookup, não switch):
   ```typescript
   const CHANNEL_DB_KEY: Record<'Google Ads' | 'Meta Ads', 'google_ads' | 'meta_ads'> = {
     'Google Ads': 'google_ads',
     'Meta Ads': 'meta_ads',
   }
   const CHART_COLOR: Record<'Google Ads' | 'Meta Ads', string> = {
     'Google Ads': 'var(--chart-1)',
     'Meta Ads': 'var(--chart-2)',
   }
   ```

4. Config do chart de spend:
   ```typescript
   // Config dinâmica baseada no canal — criada dentro do componente
   const spendConfig = channel
     ? { spend: { label: 'Gasto', color: CHART_COLOR[channel] } }
     : {}
   ```

5. Lógica de dados (dentro do componente, após hooks):
   - `useCampaignsData(tenantSlug)` para buscar todas campanhas
   - `groupCampaignMetrics(rawRows ?? [])` para agregar
   - Filtrar por `campaign.channel === CHANNEL_DB_KEY[channel]`
   - Ordenar por `spend` desc
   - Fatiar `.slice(0, 5)` para top 5
   
   Para o AreaChart de spend (seção 1):
   - Mapear `channelRows` para `{ date: formatDateLabel(row.date), spend: Number(row.total_spend) }`
   - `formatDateLabel` = `(dateStr: string) => { const [,month,day] = dateStr.split('-'); return day+'/'+month }`
   
   Para métricas agregadas (seção 2, calculadas de `channelRows`):
   ```typescript
   const totalImpressions = channelRows.reduce((s, r) => s + Number(r.total_impressions), 0)
   const totalClicks      = channelRows.reduce((s, r) => s + Number(r.total_clicks), 0)
   const totalSpend       = channelRows.reduce((s, r) => s + Number(r.total_spend), 0)
   const totalConversions = channelRows.reduce((s, r) => s + Number(r.total_conversions), 0)
   const totalConvValue   = channelRows.reduce((s, r) => s + Number(r.total_conv_value), 0)
   const ctr  = totalImpressions > 0 ? (totalClicks / totalImpressions) * 100 : 0
   const roas = totalSpend > 0 ? totalConvValue / totalSpend : 0
   ```

6. JSX — estrutura exata conforme UI-SPEC:

   **Sheet container** (mesmo padrão de campaign-sheet.tsx):
   ```tsx
   <Sheet
     open={!!channel}
     onOpenChange={(open, eventDetails) => {
       if (!open && eventDetails?.reason === 'outside-press') return
       if (!open) onClose()
     }}
     disablePointerDismissal
   >
     <SheetContent side="right" className="w-[520px] sm:max-w-[520px] overflow-y-auto">
       {channel && ( /* conteúdo */ )}
     </SheetContent>
   </Sheet>
   ```

   **SheetHeader** (pb-4 border-b border-border):
   - `SheetTitle` (text-base font-medium leading-tight pr-8): nome do canal
   - Badge de canal + subtexto `"{brl(totalSpend)} no período"`

   **Seção 1 — Gasto no Período** (px-4 pt-4):
   - Label uppercase: `"GASTO NO PERÍODO"` — `text-xs font-medium text-muted-foreground mb-3 uppercase tracking-wide`
   - Empty state se `channelRows.length === 0`: `"Sem dados para {channel} no período selecionado."` + `"Selecione outro período..."` — `text-center py-12 text-sm text-muted-foreground`
   - AreaChart h-44, margins {top:4, right:4, bottom:0, left:0}
   - YAxis hide, XAxis com formatDateLabel
   - linearGradient id="gChannelSpend", stopColor=CHART_COLOR[channel]
   - Tooltip: `formatter={(value) => brl(Number(value))}`

   **Seção 2 — Métricas do Canal** (px-4 pt-6):
   - Label uppercase: `"MÉTRICAS DO CANAL"`
   - Card: `rounded-lg border border-border overflow-hidden`
   - Dentro: `px-3 py-1`
   - 6 TotalsRow: Impressões (`num(totalImpressions)`), Cliques (`num(totalClicks)`), CTR (`${ctr.toFixed(2)}%`), Gasto (`brl(totalSpend)`), Conversões (`num(totalConversions)`), ROAS (`roas > 0 ? ${roas.toFixed(2)}× : '—'`)

   **Seção 3 — Top Campanhas** (px-4 pt-6):
   - Label uppercase: `"TOP CAMPANHAS"`
   - Loading: container `aria-busy="true" aria-label="Carregando campanhas"` com 3x `<Skeleton className="h-12 w-full rounded-md mb-2" />`
   - Error: `"Erro ao carregar campanhas. Tente recarregar a página."` — `text-xs text-muted-foreground text-center py-4`
   - Empty: `"Nenhuma campanha encontrada para este canal no período."` — `text-xs text-muted-foreground text-center py-4`
   - Lista: cada item:
     ```
     py-2 border-b border-border/50 last:border-0
     flex items-start justify-between
     [StatusDot] + Nome truncado (text-sm font-medium truncate max-w-[60%])
     Sub-info (text-xs text-muted-foreground): ChannelBadge + "ROAS: {X.XX}×"
     Spend (text-sm tabular-nums font-medium text-right)
     ```
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('components/dashboard/channel-sheet.tsx','utf8'); const checks=['ChannelSheet','disablePointerDismissal','useCampaignsData','groupCampaignMetrics','pt-6','py-1','font-medium','GASTO NO PERÍODO','MÉTRICAS DO CANAL','TOP CAMPANHAS','brl','num','DailyRollupRow']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length) { console.error('MISSING:',missing); process.exit(1); } console.log('OK - all checks pass');"</automated>
  </verify>

  <acceptance_criteria>
    - `components/dashboard/channel-sheet.tsx` existe e exporta `ChannelSheet`
    - Contém `disablePointerDismissal` (Sheet não fecha em clique externo)
    - Contém `'GASTO NO PERÍODO'`, `'MÉTRICAS DO CANAL'`, `'TOP CAMPANHAS'` (3 seções)
    - Usa `py-1` no ChannelBadge (não `py-0.5`)
    - Usa `pt-6` nos separadores de seção (não `pt-4` ou `pt-5`)
    - Não usa `font-semibold` — apenas `font-medium` e peso normal
    - Seção 2 calcula CTR como `totalClicks/totalImpressions*100` e ROAS como `totalConvValue/totalSpend`
    - Seção 3 usa `useCampaignsData` + `groupCampaignMetrics` + filter por canal + sort spend desc + slice(0,5)
    - Empty state para canal sem dados: texto `"Sem dados para"` presente
    - Skeleton com `aria-busy="true"` e `aria-label="Carregando campanhas"` para seção 3
  </acceptance_criteria>

  <done>ChannelSheet renderiza as 3 seções conforme UI-SPEC, reutiliza padrões do CampaignSheet, e o arquivo passa na verificação automatizada.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar onClick no PieChart e integrar ChannelSheet ao DashboardPage</name>
  <files>app/[tenant-slug]/dashboard/page.tsx</files>

  <read_first>
    - app/[tenant-slug]/dashboard/page.tsx — leitura obrigatória antes de qualquer edição (PieChart existente, imports existentes, estrutura do return)
    - components/dashboard/channel-sheet.tsx — props a passar (criado na Task 1)
    - .planning/phases/03-dashboard-ui/03-UI-SPEC.md — seção "Interaction Contract"
  </read_first>

  <action>
Modificar `app/[tenant-slug]/dashboard/page.tsx` para adicionar o drill-down de canal:

**1. Adicionar import de useState (se não existir):**
```typescript
import { useState } from 'react'
```

**2. Adicionar import do ChannelSheet:**
```typescript
import { ChannelSheet } from '@/components/dashboard/channel-sheet'
```

**3. Adicionar estado local em DashboardPage** (após a linha `const { data, isLoading, isError } = useDashboardData(tenantSlug)`):
```typescript
const [selectedChannel, setSelectedChannel] = useState<'Google Ads' | 'Meta Ads' | null>(null)
```

**4. Adicionar mapeamento de canal** (após `channelSplitData`):
```typescript
const CHANNEL_KEY_MAP: Record<'Google Ads' | 'Meta Ads', string> = {
  'Google Ads': 'google_ads',
  'Meta Ads': 'meta_ads',
}
const channelKey = selectedChannel ? CHANNEL_KEY_MAP[selectedChannel] : null
const channelRows = data.current.filter((r) => r.channel === channelKey)
```

**5. Modificar o `<Pie>` existente** — adicionar `onClick` e `cursor-pointer` nos `<Cell>`:

Localizar o `<Pie>` (dentro do Card "Split por Canal") e adicionar:
```tsx
<Pie
  data={channelSplitData}
  dataKey="value"
  nameKey="name"
  cx="50%"
  cy="50%"
  innerRadius={48}
  outerRadius={70}
  stroke="none"
  paddingAngle={3}
  onClick={(data) => setSelectedChannel(data.name as 'Google Ads' | 'Meta Ads')}
>
  <Cell fill="var(--chart-1)" style={{ cursor: 'pointer' }} />
  <Cell fill="var(--chart-2)" style={{ cursor: 'pointer' }} />
</Pie>
```

**6. Adicionar `<ChannelSheet>` no return** — antes do fechamento de `</section>` (última tag antes de fechar a section):
```tsx
<ChannelSheet
  channel={selectedChannel}
  channelRows={channelRows}
  tenantSlug={tenantSlug}
  onClose={() => setSelectedChannel(null)}
/>
```

**Não remover nada existente** — apenas adicionar onClick, style nos Cells, estado, lookup, import e componente.

**Não alterar** KPI cards, trend chart, estrutura de grid, DashboardSkeleton, formatDateLabel.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/[tenant-slug]/dashboard/page.tsx','utf8'); const checks=['selectedChannel','setSelectedChannel','ChannelSheet','channelRows','CHANNEL_KEY_MAP','cursor: \'pointer\'','onClick','useState']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length) { console.error('MISSING:',missing); process.exit(1); } console.log('OK - all checks pass');"</automated>
  </verify>

  <acceptance_criteria>
    - `import { useState } from 'react'` presente no topo do arquivo
    - `import { ChannelSheet } from '@/components/dashboard/channel-sheet'` presente
    - `useState<'Google Ads' | 'Meta Ads' | null>(null)` no corpo de DashboardPage
    - `onClick={(data) => setSelectedChannel(data.name as 'Google Ads' | 'Meta Ads')}` no `<Pie>`
    - `style={{ cursor: 'pointer' }}` em ambos os `<Cell>` do PieChart
    - `CHANNEL_KEY_MAP` declarado e `channelRows` calculado antes do return
    - `<ChannelSheet channel={selectedChannel} channelRows={channelRows} tenantSlug={tenantSlug} onClose={() => setSelectedChannel(null)} />` no return
    - Nenhum dos 7 KPI cards, trend chart, ou DashboardSkeleton foi alterado (verificar via grep que KpiCard ainda tem 7 invocações)
    - TypeScript não apresenta erros em `tsc --noEmit` (ou `next build` não falha por erros de tipo)
  </acceptance_criteria>

  <done>DashboardPage tem onClick no PieChart, estado selectedChannel gerenciado, e ChannelSheet integrado. Clicar em qualquer slice abre o Sheet lateral com dados do canal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase RLS | Dados de campanhas lidos via useCampaignsData — RLS isola tenant via JWT get_tenant_id() |
| UI → display only | channelRows vêm de data.current já carregado; nenhum input de usuário persiste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | ChannelSheet — exibe dados de campanhas | accept | RLS já mitiga no nível de DB; dados exibidos são os mesmos do dashboard principal, sem informação adicional sensível |
| T-03-06-02 | Denial of Service | useCampaignsData acionado ao abrir Sheet | accept | Hook usa TanStack Query com cache — não dispara nova query se já em cache para o mesmo tenantSlug+dateRange |
| T-03-06-03 | Tampering | `data.name` do PieChart passado como estado | mitigate | Cast tipado `as 'Google Ads' \| 'Meta Ads'` mais CHANNEL_KEY_MAP lookup — valor inválido retorna undefined no filtro (channelRows = []) e exibe empty state |
</threat_model>

<verification>
Verificação geral do gap closure GAP-03-01:

```bash
# 1. Arquivo novo existe
ls components/dashboard/channel-sheet.tsx

# 2. Arquivo modificado tem onChange correto
grep -n "setSelectedChannel\|ChannelSheet\|CHANNEL_KEY_MAP" app/\[tenant-slug\]/dashboard/page.tsx

# 3. Testes unitários existentes continuam passando
npm test -- date-range-store dashboard-kpis channel-split campaign-aggregation

# 4. Build sem erros de TypeScript
npx tsc --noEmit

# 5. Verificação de padrões críticos do UI-SPEC
grep -n "py-1\|pt-6\|font-medium\|disablePointerDismissal" components/dashboard/channel-sheet.tsx

# 6. Confirmar que font-semibold NÃO está no ChannelSheet
grep "font-semibold" components/dashboard/channel-sheet.tsx && echo "VIOLATION: font-semibold found" || echo "OK: no font-semibold"

# 7. Confirmar cursor-pointer no PieChart
grep "cursor" app/\[tenant-slug\]/dashboard/page.tsx
```
</verification>

<success_criteria>
- `components/dashboard/channel-sheet.tsx` existe com `ChannelSheet` exportado
- PieChart em dashboard/page.tsx tem `onClick` e `cursor: 'pointer'` nos Cells
- `<ChannelSheet>` integrado no return de DashboardPage
- Sheet abre ao clicar em Google Ads ou Meta Ads no PieChart
- Sheet não fecha com clique externo (`disablePointerDismissal` + guard `outside-press`)
- Seção 1 (AreaChart de spend diário) renderiza com dados de `channelRows`
- Seção 2 (6 métricas) calcula Impressões, Cliques, CTR, Gasto, Conversões, ROAS de `channelRows`
- Seção 3 (top campanhas) mostra até 5 campanhas do canal via `useCampaignsData` + `groupCampaignMetrics`
- Nenhum `font-semibold` no ChannelSheet, apenas `font-medium` e peso normal
- `py-1` no badge de canal (não `py-0.5`)
- `pt-6` nos separadores de seção (não `pt-5`)
- `npm test` passa nos 4 arquivos de unit test da Fase 3 (51 testes)
- `npx tsc --noEmit` sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-dashboard-ui/03-06-SUMMARY.md` com:
- Gaps fechados: GAP-03-01
- Arquivos criados: components/dashboard/channel-sheet.tsx
- Arquivos modificados: app/[tenant-slug]/dashboard/page.tsx
- Padrões seguidos: Sheet pattern de campaign-sheet.tsx, disablePointerDismissal, CHANNEL_KEY_MAP lookup
- Verificações passadas: tsc --noEmit, npm test (51 tests), grep checks
</output>

---
phase: 03-dashboard-ui
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/hooks/use-campaigns-data.ts
  - components/campanhas/campaign-sheet.tsx
  - app/[tenant-slug]/campanhas/page.tsx
autonomous: true
requirements: [CAMP-01, CAMP-02, CAMP-03, CAMP-04]
must_haves:
  truths:
    - "Campaigns page lists real campaigns aggregated from campaign_metrics for the active tenant"
    - "Each row shows Name, Channel, Status, Spend, ROAS, CPA, CTR, Clicks, Conversions"
    - "Campaigns can be filtered by channel (Google Ads / Meta / All)"
    - "The list respects the global date range picker (re-fetches when range changes)"
    - "Clicking a campaign row opens a right-side Sheet drill-down with trend lines + aggregated totals"
    - "The drill-down Sheet does NOT close on outside click — only via X or Esc"
    - "Loading shows skeletons; empty range shows an empty-state message"
  artifacts:
    - path: "lib/hooks/use-campaigns-data.ts"
      provides: "useCampaignsData (list) + useCampaignTimeseries (drill-down) hooks over campaign_metrics"
      contains: "useQuery"
    - path: "components/campanhas/campaign-sheet.tsx"
      provides: "Sheet drill-down: header + trend area chart + totals table, no outside-close"
      contains: "Sheet"
    - path: "app/[tenant-slug]/campanhas/page.tsx"
      provides: "Real campaign table + channel filter + date-range awareness + row click opens Sheet"
      contains: "useCampaignsData"
  key_links:
    - from: "lib/hooks/use-campaigns-data.ts"
      to: "campaign_metrics"
      via: "supabase.from('campaign_metrics').select"
      pattern: "from\\('campaign_metrics'\\)"
    - from: "app/[tenant-slug]/campanhas/page.tsx"
      to: "lib/campaign-aggregation.ts"
      via: "groupCampaignMetrics"
      pattern: "groupCampaignMetrics"
    - from: "app/[tenant-slug]/campanhas/page.tsx"
      to: "components/campanhas/campaign-sheet.tsx"
      via: "row onClick sets selectedCampaignId → renders CampaignSheet"
      pattern: "CampaignSheet"
---

<objective>
Substituir o mock data da página de Campanhas por dados reais de `campaign_metrics` agregados por campanha (CAMP-01), preservar o filtro por canal existente (CAMP-02), tornar a lista reativa ao date range global (CAMP-03), e adicionar o drill-down via `<Sheet>` lateral com trend lines + totais agregados (CAMP-04). O Sheet não fecha ao clicar fora (D-11) — só X ou Esc.

Purpose: A página de Campanhas já tem filtro, busca e ordenação funcionando com mock. Esta plan troca a fonte de dados e adiciona o drill-down. A agregação por `campaign_id` usa a função pura `groupCampaignMetrics` (Plan 01) porque PostgREST não suporta GROUP BY — agrega no JS (aceitável para v1, 1-3 tenants).

Output: `lib/hooks/use-campaigns-data.ts` (lista + timeseries do drill-down) + `components/campanhas/campaign-sheet.tsx` + `app/[tenant-slug]/campanhas/page.tsx` reescrita. Sem novas deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-ui/03-CONTEXT.md
@.planning/phases/03-dashboard-ui/03-RESEARCH.md

<interfaces>
<!-- lib/campaign-aggregation.ts (Plan 01) -->
export type AggregatedCampaign = { id: string; name: string; channel: 'google_ads'|'meta_ads'; status: 'active'|'paused'; impressions: number; clicks: number; ctr: number; spend: number; conversions: number; convValue: number; cpa: number; roas: number }
export function groupCampaignMetrics(rows: CampaignMetricRow[]): AggregatedCampaign[]

<!-- lib/stores/date-range.ts (Plan 01): useDateRangeStore → { from, to } -->
<!-- lib/supabase/client.ts: createClient() singleton (RLS por tenant via JWT) -->
<!-- lib/formatters.ts (Plan 01): brl, num -->
<!-- components/ui/sheet.tsx (Plan 02): API real depende do componente gerado (@base-ui/react ou Radix). LER o arquivo gerado para confirmar nomes (Sheet, SheetContent, SheetHeader, SheetTitle) e a prop para prevenir fechar ao clicar fora. -->
<!-- components/ui/chart.tsx: ChartContainer, ChartTooltip, ChartTooltipContent -->

<!-- campaign_metrics colunas relevantes: campaign_id, campaign_name, channel ('google_ads'|'meta_ads'), status, date, impressions, clicks, spend, conversions, conversion_value -->
<!-- A página atual já tem ChannelBadge, StatusDot, RoasValue, filtro de canal (tabs), busca e ordenação — REUTILIZAR. type Channel vem de lib/mock-data; pode-se manter o import só do type ou redefinir localmente. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar use-campaigns-data.ts (lista agregada + timeseries do drill-down)</name>
  <read_first>
    - .planning/phases/03-dashboard-ui/03-RESEARCH.md (Code Examples — query campaign_metrics + nota "agregar no JS"; Pattern 2 — queryKey reativo)
    - lib/supabase/client.ts (createClient)
    - lib/stores/date-range.ts (Plan 01)
    - lib/campaign-aggregation.ts (Plan 01 — groupCampaignMetrics, CampaignMetricRow)
    - types/database.types.ts (linhas 86-150 — colunas de campaign_metrics)
  </read_first>
  <action>
Criar `lib/hooks/use-campaigns-data.ts` ('use client'). Exportar duas funções:

1. `export function useCampaignsData(tenantSlug: string)` — lista da tabela (CAMP-01/CAMP-03):
   - `useDateRangeStore` → `{ from, to }`. `toDateStr = (d) => d.toISOString().split('T')[0]`.
   - `queryKey: ['campaigns', tenantSlug, from.toISOString(), to.toISOString()]`.
   - queryFn: `supabase.from('campaign_metrics').select('campaign_id,campaign_name,channel,status,date,impressions,clicks,spend,conversions,conversion_value').gte('date', toDateStr(from)).lte('date', toDateStr(to))`. NÃO usar `.eq('tenant_id')` (RLS). Lançar em error. Retornar os rows crus (a página agrega com `groupCampaignMetrics`). `enabled: !!tenantSlug`.

2. `export function useCampaignTimeseries(tenantSlug: string, campaignId: string | null)` — para o Sheet (CAMP-04):
   - mesmo `{ from, to }` do store.
   - `queryKey: ['campaign-ts', tenantSlug, campaignId, from.toISOString(), to.toISOString()]`.
   - queryFn: mesma seleção filtrando `.eq('campaign_id', campaignId).gte('date',...).lte('date',...)` ordenado por `date` ascendente. Retornar rows.
   - `enabled: !!tenantSlug && !!campaignId` (não busca enquanto nenhuma campanha selecionada).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `lib/hooks/use-campaigns-data.ts` contains `from('campaign_metrics')`
    - `lib/hooks/use-campaigns-data.ts` contains `useCampaignsData` and `useCampaignTimeseries`
    - `lib/hooks/use-campaigns-data.ts` does NOT contain `.eq('tenant_id'`
    - `lib/hooks/use-campaigns-data.ts` contains `enabled` with `campaignId` guard (drill-down não busca sem seleção)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hooks de lista e de timeseries de campanha criados; reativos ao date range; isolados por RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Criar CampaignSheet (drill-down) e reescrever campanhas/page.tsx com dados reais</name>
  <read_first>
    - app/[tenant-slug]/campanhas/page.tsx (página atual — REUTILIZAR ChannelBadge, StatusDot, RoasValue, tabs de filtro, busca, ordenação; remover MOCK_CAMPAIGNS)
    - components/ui/sheet.tsx (Plan 02 — LER a API real gerada: nomes dos subcomponentes e a prop/handler para prevenir fechar ao clicar fora)
    - lib/campaign-aggregation.ts (Plan 01 — groupCampaignMetrics, AggregatedCampaign)
    - lib/hooks/use-campaigns-data.ts (Task 1)
    - lib/formatters.ts (Plan 01 — brl, num)
    - components/ui/chart.tsx (ChartContainer, ChartTooltip, ChartTooltipContent)
    - app/[tenant-slug]/dashboard/page.tsx (referência do padrão AreaChart com gradientes e configs de cor de canal)
  </read_first>
  <action>
**A) `components/campanhas/campaign-sheet.tsx`** ('use client'):
- Props: `{ tenantSlug: string; campaign: AggregatedCampaign | null; onClose: () => void }`.
- Importar Sheet/SheetContent/SheetHeader/SheetTitle de `'@/components/ui/sheet'` (usar os nomes REAIS do componente gerado). Importar `useCampaignTimeseries` de `'@/lib/hooks/use-campaigns-data'`.
- `<Sheet open={!!campaign} onOpenChange={(open) => { if (!open) onClose() }}>`.
- `<SheetContent side="right" className="w-[520px] sm:max-w-[520px]" ...>` com os handlers para **prevenir fechar ao clicar fora** (D-11): aplicar `e.preventDefault()` no handler de interação externa que o componente gerado expõe (ex.: `onPointerDownOutside`/`onInteractOutside` se Radix, ou a prop equivalente do base-ui Dialog — confirmar no sheet.tsx lido). Esc e botão X DEVEM continuar fechando.
- Conteúdo (D-10): header com `campaign.name` + ChannelBadge + status; seção de trend lines = AreaChart com 3 séries do timeseries (Spend, ROAS diário, CTR diário — derivar ROAS=conv_value/spend e CTR=clicks/impressions por linha/dia, guard 0); seção de totais = tabela com Impressões, Cliques, Gasto, Conversões, Conv. Value, CPA, ROAS agregados (usar os campos de `campaign` já agregados). Loading do timeseries: skeleton dentro do Sheet.

**B) Reescrever `app/[tenant-slug]/campanhas/page.tsx`** (manter `'use client'`):
- `useParams()` → `tenantSlug`. `const { data, isLoading, isError } = useCampaignsData(tenantSlug)`.
- `const campaigns = data ? groupCampaignMetrics(data) : []`.
- Manter o filtro de canal (tabs all/google_ads/meta_ads — CAMP-02), busca e ordenação existentes, operando sobre `campaigns` (não mais MOCK).
- Loading: skeletons na tabela. Error: mensagem inline. Empty: "Nenhuma campanha no período selecionado."
- Estado `const [selected, setSelected] = useState<AggregatedCampaign | null>(null)`. Cada `<tr>` recebe `onClick={() => setSelected(c)}` e `className` com `cursor-pointer` (substituir o ícone ExternalLink decorativo). Renderizar `<CampaignSheet tenantSlug={tenantSlug} campaign={selected} onClose={() => setSelected(null)} />` no fim da section.
- Remover imports de `MOCK_CAMPAIGNS`. Manter `type Channel`/`Campaign` se necessário (ou usar `AggregatedCampaign`). Atualizar o subtítulo para refletir contagem real + período.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components/campanhas/campaign-sheet.tsx` contains `Sheet` and `useCampaignTimeseries`
    - `components/campanhas/campaign-sheet.tsx` contains `preventDefault` (no-close-on-outside wiring — D-11)
    - `app/[tenant-slug]/campanhas/page.tsx` contains `useCampaignsData` and `groupCampaignMetrics` and `CampaignSheet`
    - `app/[tenant-slug]/campanhas/page.tsx` does NOT contain `MOCK_CAMPAIGNS`
    - `app/[tenant-slug]/campanhas/page.tsx` contains `cursor-pointer` and `onClick`
    - `app/[tenant-slug]/campanhas/page.tsx` contains the channel filter tabs (grep `google_ads` and `meta_ads`)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Tabela de campanhas com dados reais agregados, filtro por canal, reativa ao date range, e drill-down via Sheet que não fecha ao clicar fora.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (PostgREST) | Leitura de campaign_metrics com a sessão JWT do usuário. RLS é a fronteira de isolamento por tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering (cross-tenant leakage) | useCampaignsData / useCampaignTimeseries | mitigate | RLS `(SELECT get_tenant_id())` em campaign_metrics filtra por tenant via JWT. Hooks não enviam `.eq('tenant_id')` do client. |
| T-03-04-02 | Denial of Service | full-period read de campaign_metrics + agregação JS | accept | v1 com 1-3 tenants × ~90 dias × poucas campanhas ≈ centenas de linhas. Aceitável (RESEARCH A4). Se >5.000 linhas/tenant, migrar para Postgres View. |
| T-03-04-03 | Information Disclosure | campaign_id no queryKey/Sheet | accept | campaign_id é ID externo da plataforma, não secret. Exposto apenas dentro do tenant do usuário. |
</threat_model>

<verification>
- `npm test` verde; `npm run build` sucesso
- Tabela lista campanhas reais agregadas por campaign_id
- Filtro de canal e date-range awareness funcionam
- Sheet abre ao clicar na linha e NÃO fecha ao clicar fora (verificação visual no UAT — manual-only por VALIDATION.md)
</verification>

<success_criteria>
- CAMP-01: tabela com Name, Channel, Status, Spend, ROAS, CPA, CTR, Clicks, Conversions reais
- CAMP-02: filtro por canal (Google/Meta/All)
- CAMP-03: respeita o date range global (queryKey reativo)
- CAMP-04: drill-down em Sheet com trend lines + totais; não fecha ao clicar fora (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-ui/03-04-SUMMARY.md`
</output>

---
phase: 05-agencia-multi-cliente
plan: 09
type: execute
wave: 5
depends_on: ["05-03", "05-04", "05-06", "05-07", "05-08"]
files_modified: []
autonomous: false
requirements: [AGENCY-01, AGENCY-02, AGENCY-03, AGENCY-04, AGENCY-05, AGENCY-06, AGENCY-07, AGENCY-08]

must_haves:
  truths:
    - "The full test suite passes, tsc --noEmit is clean, and npm run build succeeds with every Phase 5 plan's changes merged"
    - "A human has confirmed, end-to-end, that an agency user can log in, land on /agencia, enter a granted tenant, see a correctly-scoped sidebar, and edit a lead's status successfully"
    - "The Custom Access Token Hook is confirmed still selected in Supabase Dashboard → Authentication → Hooks after the 0019/0020 migrations"
  artifacts: []
  key_links: []
---

<objective>
Phase 5 closure: run the full automated verification suite across every plan's changes, then perform one consolidated manual UAT pass covering everything that Vitest structurally cannot verify (post-login routing, sidebar filtering, Supabase Dashboard hook state) — per this project's established pattern of a single verification checkpoint at the end rather than one per plan (see 05-VALIDATION.md's "Manual-Only Verifications" table).

Purpose: this is the phase-gate before `/gsd-verify-work` — confirms all 8 plans compose correctly (no cross-plan regressions) and that the module actually works for a real agency user, not just in mocked tests.
Output: A green full-suite run and a signed-off manual UAT confirming the Agência module works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agencia-multi-cliente/05-CONTEXT.md
@.planning/phases/05-agencia-multi-cliente/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite automated verification</name>
  <files></files>
  <read_first>
    - .planning/phases/05-agencia-multi-cliente/05-VALIDATION.md §"Manual-Only Verifications" (the three items Task 2's checkpoint must cover)
  </read_first>
  <action>
Run, in order, and confirm each is clean before proceeding to Task 2:

```bash
npm test
npx tsc --noEmit
npm run build
```

Grep every Phase 5 test file to confirm zero remaining `it.todo()` markers (Wave 0 fully closed out):
```bash
grep -rn "it.todo(" tests/agency-rls.test.ts tests/integration/tenant-role-migration.test.ts tests/agencies.test.ts tests/unit/leads-status-route.test.ts
```
This command SHOULD find nothing (exit code 1 from grep = success here). If any `it.todo()` remains, identify which plan was supposed to fill it and flag before proceeding — do not mark this task done with unfilled Wave 0 scaffolds.

If any of the three commands fail, do NOT proceed to Task 2 — fix the regression first (this is a cross-plan integration check; a failure here means two plans' changes conflict in a way no single plan's own verification caught).
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite, all pre-existing + Phase 5 tests)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `grep -rln "it.todo(" tests/agency-rls.test.ts tests/integration/tenant-role-migration.test.ts tests/agencies.test.ts tests/unit/leads-status-route.test.ts` returns no matches
  </acceptance_criteria>
  <done>All automated checks green across the merged output of every Phase 5 plan.</done>
</task>

<task id="05-09-T2" type="checkpoint:human-verify" autonomous="false" gate="blocking">
  <name>[BLOCKING] Manual UAT — full Agência module end-to-end</name>
  <read_first>
    - .planning/phases/05-agencia-multi-cliente/05-CONTEXT.md (decisions D-01..D-04)
    - .planning/phases/05-agencia-multi-cliente/05-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
Run the following 7 verification scripts manually. Requires: existing super_admin account, at least one existing Cliente tenant (`lukseg` or `beta-test`).

**Script 1 — Super Admin agency management (AGENCY-01/02):**
1. Log in as super_admin. Go to `/tenants` — confirm a new "Agências" link appears in the header next to "Tenants".
2. Click it. Create a new agency ("Agência Teste"). Click "Gerenciar" on it.
3. Add a user (e.g. `agente-teste@example.com`) — copy the temp password shown.
4. In "Clientes vinculados", check ONE active tenant (e.g. `lukseg`). Refresh the page — confirm the checkbox stays checked.

**Script 2 — Agency post-login routing (AGENCY-03):**
1. Log out. Log in as the new agency user with the temp password from Script 1.
2. Expected: land on `/agencia` (NOT `/login?error=no_membership`).
3. Expected: page shows "Meus clientes" with exactly the ONE tenant granted in Script 1 (not all tenants).

**Script 3 — Scoped tenant access (AGENCY-04):**
1. Click "Entrar" on the granted tenant. Expected: dashboard loads normally (KPIs, campaigns).
2. Check the sidebar — expected: "AI Insights" and "Conta" (Configurações) are ABSENT; "Dashboard", "Campanhas", "Gestão de Leads", "Agente IA" ARE present.
3. Check the header switcher — expected: shows only the granted tenant; its "Gerenciar clientes…" option routes to `/agencia` (not `/tenants`).

**Script 4 — Lead status edit as agency (AGENCY-05):**
1. Go to "Gestão de Leads" for this tenant. Edit a lead's status via the dropdown.
2. Expected: saves successfully (no 403).

**Script 5 — Cross-tenant rejection (AGENCY-06/08):**
1. If a second tenant exists that was NOT granted to this agency, manually navigate to `/{other-tenant-slug}/dashboard` while logged in as the agency user.
2. Expected: redirected away — not shown that tenant's data.

**Script 6 — Cliente role collapse (AGENCY-07):**
1. Log in as an existing tenant user who was previously `viewer` (if one exists in `lukseg`/`beta-test`).
2. Expected: full access — can edit lead status, no longer restricted.

**Script 7 — Hook activation smoke-test:**
1. In Supabase Dashboard → Authentication → Hooks → Custom Access Token, confirm `public.custom_access_token_hook` is still selected.
2. Expected: still selected (migrations 0019/0020 use `CREATE OR REPLACE` on the same function name — should be unaffected, but confirm since a deselected hook fails silently).

When all 7 scripts pass, type: `uat-ok`
If any fail, type the failing script numbers (e.g., `fail: 3, 5`) with a description.
  </action>
  <verify>
    <automated>echo "Manual UAT — awaiting human confirmation"</automated>
  </verify>
  <acceptance_criteria>
    - User responds `uat-ok` confirming all 7 scripts pass
    - Script 5's cross-tenant navigation returns a redirect, not the other tenant's data
  </acceptance_criteria>
  <done>Manual UAT complete — Agência module verified end-to-end. Phase 5 ready for gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual UAT session → production-adjacent Supabase project | Scripts 1-2 create a real agency + a real auth user in the shared staging/prod Supabase project (per STATE.md, staging schema lives in the same project) — use a clearly-named test agency/email so it can be cleaned up afterward, consistent with the project's `test-` email prefix convention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Repudiation | Manual UAT steps not independently re-verifiable after the fact | accept | This is the project's established closure pattern (see Phase 1's and Phase 03.1's UAT checkpoints) — the human tester's `uat-ok` response, combined with the automated suite in Task 1, is the accepted level of rigor for a 1-3 tenant internal tool |
</threat_model>

<verification>
- Task 1's three commands all exit 0
- Task 2's 7-script manual UAT confirmed with `uat-ok`
</verification>

<success_criteria>
- [ ] Full automated suite green
- [ ] Agency user end-to-end flow confirmed working by a human
- [ ] Cross-tenant access confirmed blocked
- [ ] Cliente role collapse confirmed non-disruptive
- [ ] Custom Access Token Hook confirmed still active
</success_criteria>

<output>
After completion, create `.planning/phases/05-agencia-multi-cliente/05-09-SUMMARY.md`
</output>

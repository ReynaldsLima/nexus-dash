---
phase: 08-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "The live Supabase agencies table contains zero test-fixture rows (Agência Teste, rls-test-*, debug-agency)"
    - "The live auth.users table contains no agente-teste@example.com / cliente-teste@example.com test users"
    - "No production (non-fixture) agency, tenant, or user row was deleted"
  artifacts: []
  key_links: []
---

<objective>
Remove leftover test fixtures from the LIVE Supabase project (`rvkkvjitfddtbdpkupok`, sa-east-1) that were left behind by Phase 5 sessions and never cleaned up (no in-app user-deletion UI exists).

Purpose: The re-audit flagged a deactivated test agency ("Agência Teste"), two `@example.com` test users, and several `rls-test-*` / `debug-agency` agency rows still living in production. These are harmless but untidy and pollute any future real agency listing.
Output: Fixture rows removed from the live DB. No repo files change. Because this deletes LIVE data, the plan reads current state FIRST, pauses for human confirmation, and only then deletes — children before parents to respect FK constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate (read-only) the exact fixture rows and their FK dependents</name>
  <files>(none — read-only queries against live Supabase)</files>
  <read_first>
    - .planning/v1.0-MILESTONE-AUDIT.md (phase 05-agencia-multi-cliente tech_debt items — the authoritative list of fixture names: agency "Agência Teste"; users agente-teste@example.com / cliente-teste@example.com; agency slugs rls-test-agency-switcher, rls-test-agency-empty, rls-test-agency, debug-agency)
    - .planning/STATE.md (Phase 5 Plan 02 note: tables are `agencies`, `agency_users`, `agency_tenants` — the FK graph you must respect)
  </read_first>
  <action>
    Using the Supabase MCP tools (`mcp__supabase__list_tables` then `mcp__supabase__execute_sql`) against project `rvkkvjitfddtbdpkupok`, run READ-ONLY `SELECT` queries only. Perform NO writes/deletes in this task.

    Step 1 — Confirm the FK graph before assuming table/column names. Use `list_tables` (or query `information_schema.table_constraints` / `key_column_usage`) to find every foreign key that references `public.agencies(id)` — expected: `agency_users.agency_id`, `agency_tenants.agency_id`. Record the exact child table + column names.

    Step 2 — Enumerate the target agency rows and capture their `id`s:
      `SELECT id, name, slug, active FROM public.agencies WHERE name = 'Agência Teste' OR slug IN ('rls-test-agency-switcher','rls-test-agency-empty','rls-test-agency','debug-agency') OR slug LIKE 'rls-test-%';`

    Step 3 — For each matched agency id, enumerate dependent rows in the child tables found in Step 1 (e.g. `SELECT * FROM public.agency_users WHERE agency_id = ANY(<ids>);` and `SELECT * FROM public.agency_tenants WHERE agency_id = ANY(<ids>);`).

    Step 4 — Enumerate the test users in `auth.users`:
      `SELECT id, email FROM auth.users WHERE email IN ('agente-teste@example.com','cliente-teste@example.com');`
    Then check whether those user ids appear in `agency_users`, `tenant_users`, or `identities` (dependents that must be removed / will cascade) — query each with `WHERE user_id = ANY(<ids>)`.

    Step 5 — Produce a concise inventory: exact table → row count → primary keys for every row that Task 3 will delete, in FK-safe deletion order (children first, then `agencies`, then `auth.users`). This inventory is the payload handed to the Task 2 checkpoint.

    Do NOT delete anything in this task. If any query matches a row you are UNSURE is a fixture (e.g. an agency whose name is real), flag it explicitly for the checkpoint rather than including it in the delete set.
  </action>
  <verify>
    <automated>MISSING — read-only enumeration against live DB; correctness is confirmed by the Task 2 human checkpoint reviewing the row inventory. No automated command applies (no repo artifact produced).</automated>
  </verify>
  <acceptance_criteria>
    - A written inventory exists (in the execution transcript) listing, per table, the exact primary keys of rows matched by the fixture filters
    - The FK dependency order is stated: child tables (agency_users, agency_tenants, and any auth.users dependents) BEFORE agencies BEFORE auth.users
    - Zero `DELETE`/`UPDATE`/`INSERT` statements were executed in this task (only `SELECT`)
    - Any ambiguous (possibly-real) row is flagged separately, NOT placed in the delete set
  </acceptance_criteria>
  <done>The exact set of fixture rows to delete — with FK-safe ordering — is enumerated read-only and ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms the delete set before any destructive action</name>
  <files>(none — human confirmation gate, no file changes)</files>
  <what-built>Task 1 produced a read-only inventory of every test-fixture row proposed for deletion from the LIVE Supabase project (`rvkkvjitfddtbdpkupok`): the matched `agencies` rows, their `agency_users` / `agency_tenants` dependents, and the two `@example.com` `auth.users` and their dependents — in FK-safe deletion order.</what-built>
  <read_first>
    - The Task 1 inventory (row counts + primary keys per table)
  </read_first>
  <action>
    STOP and present the Task 1 inventory to the user for explicit approval before any DELETE runs. Print, per table, the exact primary keys / ids and human-readable identifiers (agency name/slug, user email) of every row proposed for deletion, in FK-safe order. Surface any ambiguous rows Task 1 flagged and ask the user to decide keep-or-delete for each. Do NOT proceed to Task 3 until the user replies with approval. This is a blocking gate on LIVE production data — no destructive action happens in this task.
  </action>
  <how-to-verify>
    1. Review the inventory printed by Task 1: confirm every listed agency is a fixture ("Agência Teste", `rls-test-*`, `debug-agency`) and NOT a real client agency.
    2. Confirm the only `auth.users` listed are `agente-teste@example.com` and `cliente-teste@example.com`.
    3. Confirm no row you recognize as production data is in the delete set.
    4. Pay attention to any rows Task 1 flagged as ambiguous — decide keep or delete for each.
  </how-to-verify>
  <verify>
    <automated>MISSING — human confirmation gate; verified by explicit user approval of the printed delete set. No automated command applies.</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly seen the full delete set (table + primary keys) before Task 3 runs
    - User has resolved every ambiguous-flagged row
  </acceptance_criteria>
  <resume-signal>Type "approved" to delete exactly the confirmed set, or reply with the specific rows to exclude / include.</resume-signal>
  <done>The user has reviewed and approved the exact set of rows to delete; the approved set (with any amendments) is frozen for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the confirmed fixture rows in FK-safe order and verify zero remain</name>
  <files>(none — writes against live Supabase)</files>
  <read_first>
    - The Task 1 inventory and any Task 2 amendments (the approved delete set + ordering)
  </read_first>
  <action>
    Delete ONLY the rows approved in Task 2, using `mcp__supabase__execute_sql` against `rvkkvjitfddtbdpkupok`, in strict FK-safe order (children first) to avoid FK violation errors:

    1. `DELETE FROM public.agency_tenants WHERE agency_id = ANY(<approved agency ids>);`
    2. `DELETE FROM public.agency_users  WHERE agency_id = ANY(<approved agency ids>);`
    3. Remove any `agency_users` / `tenant_users` rows keyed to the two test `auth.users` ids (if Task 1 found them and Task 2 approved).
    4. `DELETE FROM public.agencies WHERE id = ANY(<approved agency ids>);`
    5. Delete the two test users. Prefer the Supabase Auth admin path (`auth.admin.deleteUser(id)` — cascades identities/sessions cleanly); if using raw SQL `DELETE FROM auth.users WHERE id = ANY(<ids>)`, first confirm no remaining FK dependents block it. Only delete the exact ids approved in Task 2.

    Use the exact ids/keys from the approved set — do NOT re-run the broad `LIKE`/`name` filters as a live `DELETE` (a filter could match a newly-created real row between Task 1 and Task 3). Scope every DELETE by explicit primary key / id list.

    After deletion, re-run the Task 1 enumeration SELECTs to confirm 0 rows match the fixture filters and the two test emails return 0 rows.
  </action>
  <verify>
    <automated>MISSING — destructive live-DB operation; completion is proven by the post-delete re-SELECT returning 0 fixture rows (captured in the execution transcript). No repo-level automated command applies.</automated>
  </verify>
  <acceptance_criteria>
    - Post-delete `SELECT ... FROM public.agencies WHERE name = 'Agência Teste' OR slug LIKE 'rls-test-%' OR slug = 'debug-agency'` returns 0 rows
    - Post-delete `SELECT ... FROM auth.users WHERE email IN ('agente-teste@example.com','cliente-teste@example.com')` returns 0 rows
    - Post-delete `SELECT count(*) FROM public.agency_tenants/agency_users WHERE agency_id = ANY(<deleted ids>)` returns 0
    - Every DELETE was scoped by explicit id/primary-key list from the approved set (no broad filter DELETE)
    - No non-fixture row was deleted (spot-check: production agency/tenant/user counts unchanged aside from the approved fixtures)
  </acceptance_criteria>
  <done>All approved test fixtures are removed from the live Supabase project; a post-delete re-query confirms zero fixture rows remain and no production data was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → LIVE production Supabase | This plan issues DELETEs against production data. The risk is deleting real client data, not an external attacker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering / DoS (accidental prod data loss) | live `agencies`/`agency_users`/`agency_tenants`/`auth.users` DELETEs | mitigate | Read-only enumeration first (Task 1) → blocking human confirmation of the exact row set (Task 2) → DELETE scoped by explicit primary-key list, not broad filters (Task 3); children-first ordering avoids FK errors and orphan rows |
| T-08-06 | Tampering (TOCTOU) | broad LIKE/name filter matching a row created between enumerate and delete | mitigate | Task 3 deletes by the frozen id list captured in Task 1, never by re-running the discovery filter as a live DELETE |
| T-08-07 | Repudiation | which rows were removed | accept | Deleted ids are recorded in the execution transcript / SUMMARY; acceptable for internal fixture cleanup |
</threat_model>

<verification>
- Post-delete re-query returns 0 rows for every fixture filter and both test emails.
- Production agency/tenant/user counts are unchanged except for the approved fixtures.
- The human checkpoint approved the exact delete set before any DELETE ran.
</verification>

<success_criteria>
The live Supabase project is free of the Phase 5 test fixtures, with zero production data affected and an explicit human sign-off recorded before deletion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-03-SUMMARY.md`.
</output>

---
phase: 12-redesign-visual
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/[tenant-slug]/layout.tsx
  - components/layout/sidebar-nav.tsx
  - components/layout/header-actions.tsx
  - components/tenants/tenant-switcher.tsx
  - components/dashboard/date-range-picker.tsx
autonomous: true
requirements: [DESIGN-05]

must_haves:
  truths:
    - "The active sidebar nav item renders as a translucent lime wash with lime text at 500 weight, not a solid lime block with black text"
    - "The header logo renders in Syne 800 at 17px with the pulsing lime dot"
    - "The date-range trigger renders as a lime-text mono pill on the secondary surface"
    - "The tenant switcher renders as a rounded-full pill on the secondary surface with a Syne 700 label"
    - "Switching tenants, opening the date-range popover, and logging out all still work exactly as before"
  artifacts:
    - path: "components/layout/sidebar-nav.tsx"
      provides: "Sidebar with prototype .nav-item / .nav-item.active / .nav-lbl treatment"
      contains: "bg-primary/10 text-primary"
    - path: "app/[tenant-slug]/layout.tsx"
      provides: "Header chrome with prototype .header / .logo treatment"
      contains: "t-display"
    - path: "components/dashboard/date-range-picker.tsx"
      provides: "Date-range chip matching prototype .dchip"
      contains: "rounded-full"
  key_links:
    - from: "app/[tenant-slug]/layout.tsx"
      to: "components/layout/header-actions.tsx"
      via: "props role / tenants / activeSlug / manageHref / manageLabel — MUST remain unchanged"
      pattern: "<HeaderActions role=\\{role\\} tenants=\\{tenants\\} activeSlug=\\{urlSlug\\}"
    - from: "app/[tenant-slug]/layout.tsx"
      to: "components/layout/sidebar-nav.tsx"
      via: "props slug / role — MUST remain unchanged"
      pattern: "<SidebarNav slug=\\{urlSlug\\} role=\\{role\\}"
---

<objective>
Restyle the shared chrome — header bar, logo, sidebar navigation, tenant switcher, date-range chip — to match `prototipos/nexus-dash.html`, while leaving every prop signature, route, guard, and store binding untouched.

Purpose: DESIGN-05 requires visual consistency across all four target screens. The chrome is rendered once in `app/[tenant-slug]/layout.tsx` and inherited by every route under `[tenant-slug]`, so it is the single highest-leverage surface in the phase. It is also the surface where DESIGN-05's hard prop-contract constraint applies.
Output: Header, sidebar and header-action controls carry the prototype's tokens; `HeaderActions`/`SidebarNav` prop signatures are byte-identical to before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-redesign-visual/12-CONTEXT.md
@.planning/phases/12-redesign-visual/12-UI-SPEC.md
@.planning/phases/12-redesign-visual/12-01-SUMMARY.md

@prototipos/nexus-dash.html
@app/globals.css
</context>

<interfaces>
<!-- Utility classes created by Plan 01 in app/globals.css. APPLY these; do not redefine. -->

```
.t-label    — DM Mono 11px / 500 / .12em tracking / uppercase
.t-heading  — Syne 18px / 700
.t-display  — Syne 22px / 800 / -.02em tracking
.lift       — :hover translateY(-2px) + transition
.btn-accent — lime CTA button (Syne 700 13px, hover #d9ff33, active scale .97, disabled .45)
.kpi-glow   — radial glow driven by --kpi-glow-color
.input-accent — :focus border-color rgba(200,255,0,.3)
```

Tailwind colour utilities available from Plan 01: `text-viz-blue`, `text-viz-green`, `text-viz-orange`, `text-viz-red`, `text-viz-purple` (and `bg-viz-*`).

<!-- Prop contracts this plan MUST NOT change (DESIGN-05 success criterion 5, 12-UI-SPEC.md § Shared chrome, hard constraint) -->

```ts
// components/layout/header-actions.tsx
type HeaderActionsProps = {
  role: string | null
  tenants: TenantOption[]
  activeSlug: string
  manageHref?: string
  manageLabel?: string
}

// components/layout/sidebar-nav.tsx
export function SidebarNav({ slug, role }: { slug: string; role?: string | null })

// components/tenants/tenant-switcher.tsx
interface TenantSwitcherProps {
  role: 'super_admin' | 'tenant_admin' | 'agency' | string | null
  tenants: TenantOption[]
  activeSlug: string
  manageHref?: string
  manageLabel?: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the sidebar navigation to the prototype's nav treatment</name>

  <read_first>
    - `components/layout/sidebar-nav.tsx` (full file — 112 lines, contains `NavLink` and `SidebarNav`)
    - `prototipos/nexus-dash.html` lines 51-63 — the `.sidebar`, `.nav-section`, `.nav-lbl`, `.nav-item`, `.nav-item:hover`, `.nav-item.active` rules
    - `app/globals.css` (post Plan 01) — confirm `.t-label` exists before using it
    - `.planning/phases/12-redesign-visual/12-UI-SPEC.md` § "Shared chrome (header + sidebar) — DESIGN-05" and § "Interaction Contract" row "Active nav item"
  </read_first>

  <files>components/layout/sidebar-nav.tsx</files>

  <action>
Three edits. Do not change the `SidebarNav` or `NavLink` signatures, the `MARKETING_ITEMS`/`LEADS_ITEMS` arrays, the `useAnomalyAlertsStore` bindings, the `clearUnread` effect, the `isActive` computation, or the `role !== 'agency'` gate on the "Conta" section.

**1. `NavLink` active/inactive classes (currently lines 25-30).**

Replace the entire `className={cn(...)}` argument list with:

```tsx
      className={cn(
        'flex items-center gap-2.5 mx-2 px-3.5 py-2 rounded-md text-[13px] font-medium transition-all duration-150',
        isActive
          ? 'bg-primary/10 text-primary font-medium'
          : 'text-muted-foreground hover:text-foreground hover:bg-secondary',
      )}
```

Concretely, what changes and why:
- `bg-sidebar-primary text-sidebar-primary-foreground shadow-sm` -> `bg-primary/10 text-primary font-medium`. The current state is a SOLID lime block with black text; the prototype's `.nav-item.active` is `background:rgba(200,255,0,.08); color:var(--accent)` — a translucent wash with lime text. `bg-primary/10` is the closest Tailwind step to 8% opacity. `shadow-sm` is dropped because the prototype uses no shadows anywhere.
- **Weight is `font-medium` (500), NOT `font-semibold` (600).** The raw prototype CSS says `.nav-item.active{font-weight:600}`, but 600 is NOT one of D-04's locked (family, weight) pairs — Bricolage Grotesque is cleared for **400 and 500 only** (12-UI-SPEC.md § "Locked Exception": "Bricolage Grotesque: 400 (body default) + 500 (nav items, filter tabs only)"). 12-UI-SPEC.md § "Interaction Contract" already reconciles the prototype against D-04 for exactly this element: "Active nav item | translucent accent wash `rgba(200,255,0,.08)` bg + accent text + **500 weight**". Follow the UI-SPEC, not the raw prototype value. The active state is signalled by the lime wash + lime text, not by a weight step. `font-medium` is restated explicitly in the active branch (even though the shared base class string already carries it) so the intended weight is greppable and cannot silently drift back to 600.
- `hover:bg-accent` -> `hover:bg-secondary`. The prototype's `.nav-item:hover` uses `--s2` (`#16161c`), which in this codebase is `--secondary`/`--muted`, not `--accent` (`#1c1c26`).
- `text-sm` (14px) -> `text-[13px]` to match the prototype's `.nav-item{font-size:13px}`. This is a nav-chrome value, not one of the 4 capped content type sizes.
- `gap-3` -> `gap-2.5` (10px) and `px-3 py-2` -> `mx-2 px-3.5 py-2` to match `.nav-item{gap:10px;padding:9px 14px;margin:1px 8px}`.

**2. Icon opacity (currently line 32).** Replace:

```tsx
      <Icon className="size-4 shrink-0" aria-hidden="true" />
```

with:

```tsx
      <Icon className={cn('size-4 shrink-0', isActive ? 'opacity-100' : 'opacity-60')} aria-hidden="true" />
```

This implements `.nav-item svg{opacity:.6}` / `.nav-item.active svg{opacity:1}`.

**3. Section labels (three identical `<p>` elements, currently lines 60-62, 78-80, 97-99).** Replace each of the three occurrences of:

```tsx
        <p className="px-4 mb-2 text-[10px] font-semibold tracking-widest uppercase text-muted-foreground/60">
```

with:

```tsx
        <p className="t-label px-5 pb-2 text-muted-foreground">
```

`.t-label` supplies DM Mono / 11px / 500 / `.12em` tracking / uppercase, replacing the ad-hoc `text-[10px] font-semibold tracking-widest uppercase`. `px-5` (20px) matches `.nav-lbl{padding:0 20px 8px}`. Note this drops `mb-2` in favour of `pb-2` — same 8px result, matching the prototype's padding-based rule.

Because the nav `<p>` no longer sets `font-semibold`, and `.t-label` sets `font-weight:500`, this stays inside the D-04 locked weight set (DM Mono 400/500 only).

**4. Anomaly badge `<span>` weight (currently line 36).** Change ONLY the weight class on the badge:

```tsx
          className="ml-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full px-1 text-[10px] font-semibold"
```

becomes:

```tsx
          className="ml-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full px-1 text-[10px] font-medium"
```

Same D-04 reasoning as edit 1: this is a default-sans (Bricolage) element, and Bricolage has no 600 pair. After this task `components/layout/sidebar-nav.tsx` must contain ZERO occurrences of `font-semibold` — Plan 07's Check D asserts this phase-wide with no carve-out.

**Everything else about the badge stays byte-identical**: the `unreadCount` binding, the conditional render, the `style` block using `var(--chart-5)` (which Plan 01 deliberately did not change), and its `aria-*` attributes.

Since `NavLink` now calls `cn` in two places, verify `cn` is still imported from `@/lib/utils` (it already is, line 7).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "bg-primary/10 text-primary font-medium" components/layout/sidebar-nav.tsx && ! grep -q "font-semibold" components/layout/sidebar-nav.tsx && grep -q "hover:bg-secondary" components/layout/sidebar-nav.tsx && ! grep -q "bg-sidebar-primary" components/layout/sidebar-nav.tsx && ! grep -q "shadow-sm" components/layout/sidebar-nav.tsx && [ "$(grep -c 't-label px-5 pb-2' components/layout/sidebar-nav.tsx)" = "3" ] && grep -q "isActive ? 'opacity-100' : 'opacity-60'" components/layout/sidebar-nav.tsx && grep -q 'text-\[10px\] font-medium' components/layout/sidebar-nav.tsx && grep -q "export function SidebarNav({ slug, role }: { slug: string; role?: string | null })" components/layout/sidebar-nav.tsx && npx tsc --noEmit && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `components/layout/sidebar-nav.tsx` contains `bg-primary/10 text-primary font-medium`
    - `components/layout/sidebar-nav.tsx` contains ZERO occurrences of `font-semibold` — 600 is outside D-04's locked weight set; the active nav item is 500 per 12-UI-SPEC.md § Interaction Contract, and the anomaly badge is `text-[10px] font-medium`
    - `components/layout/sidebar-nav.tsx` contains ZERO occurrences of `bg-sidebar-primary`
    - `components/layout/sidebar-nav.tsx` contains ZERO occurrences of `shadow-sm`
    - `components/layout/sidebar-nav.tsx` contains `hover:bg-secondary` and ZERO occurrences of `hover:bg-accent`
    - `grep -c 't-label px-5 pb-2' components/layout/sidebar-nav.tsx` returns exactly `3`
    - `components/layout/sidebar-nav.tsx` contains ZERO occurrences of `text-[10px] font-semibold tracking-widest uppercase`
    - `components/layout/sidebar-nav.tsx` contains `isActive ? 'opacity-100' : 'opacity-60'`
    - `components/layout/sidebar-nav.tsx` still contains the exact line `export function SidebarNav({ slug, role }: { slug: string; role?: string | null }) {`
    - `components/layout/sidebar-nav.tsx` still contains `useAnomalyAlertsStore`, `clearUnread()`, and `var(--chart-5)`
    - The anomaly badge `<span>` still carries `ml-auto inline-flex h-5 min-w-5 items-center justify-center rounded-full px-1 text-[10px]` with only its weight class changed
    - `components/layout/sidebar-nav.tsx` still contains the three nav labels `Marketing`, `Leads`, `Conta` and the `Configurações` NavLink
    - `npx tsc --noEmit` exits 0 (baseline: the 2 known `tests/vault-rpc.test.ts` errors)
  </acceptance_criteria>

  <done>The active nav item is a translucent lime wash with lime text at 500 weight (`font-medium`, per D-04 and 12-UI-SPEC.md § Interaction Contract — never 600); section labels use `.t-label`; icons dim to 60% when inactive; the file carries zero `font-semibold`; the badge colour, role gate, and prop signature are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the header bar and logo in the tenant layout</name>

  <read_first>
    - `app/[tenant-slug]/layout.tsx` (full file — pay attention to the auth/`getClaims()` block, which must not be touched)
    - `prototipos/nexus-dash.html` lines 36-42 — the `.header`, `.logo`, `.logo-dot` rules
    - `app/globals.css` — the existing `.logo-dot` rule (7px dot, `pulse-glow` animation) and `@keyframes pulse-glow`
    - `.planning/phases/12-redesign-visual/12-UI-SPEC.md` § "Shared chrome" (header height 56px already correct; sidebar width must NOT change)
  </read_first>

  <files>app/[tenant-slug]/layout.tsx</files>

  <action>
Two edits, both inside the returned JSX (lines 73-107). **Do not touch anything above line 73** — the `getUser()`/`getClaims()` role resolution, the RLS-scoped tenant existence check, the `loadTenantsForSwitcher()` call, the `manageHref`/`manageLabel` derivation, and the `<Toaster>`/`<AnomalyListener>` blocks all stay byte-identical.

**1. `<header>` element (currently line 75).** Replace:

```tsx
      <header className="h-14 w-full bg-card border-b border-border flex-shrink-0 flex items-center justify-between px-6 z-40">
```

with:

```tsx
      <header className="h-14 w-full bg-background/95 backdrop-blur-md border-b border-border flex-shrink-0 flex items-center justify-between px-6 z-40">
```

Rationale: the prototype's `.header` is `background:rgba(6,6,8,.96); backdrop-filter:blur(12px)` — the app *background* colour at 96% with a blur, not the card surface. `#060608` is `--background`. `h-14` (56px) already matches `--header-h:56px` exactly and stays. `px-6` (24px) already matches `padding:0 24px` and stays.

**2. Logo `<Link>` (currently lines 76-78).** Replace the whole element:

```tsx
        <Link href="/" className="flex items-center gap-1.5 text-sm font-semibold tracking-tight" style={{ fontFamily: 'var(--font-syne)' }}>
          NEXUS<span className="logo-dot" />DASH
        </Link>
```

with:

```tsx
        <Link
          href="/"
          className="flex items-center gap-1.5 text-[17px] font-extrabold tracking-tight text-foreground"
          style={{ fontFamily: 'var(--font-syne)' }}
        >
          NEXUS<span className="logo-dot" />DASH
        </Link>
```

Rationale: the prototype's `.logo` is `font-family:var(--head); font-size:17px; font-weight:800`. `font-semibold` (600) is not in the D-04 permitted weight set for Syne — only 700 and 800 are — so it must become `font-extrabold` (800). The inline `fontFamily: 'var(--font-syne)'` stays as-is (it is already correct and is the established pattern here). `.logo-dot` is left untouched; it already renders the 7px lime dot with the `pulse-glow` animation defined in `globals.css`.

**Do NOT** change the `<aside className="w-56 ...">` width. The prototype is 228px vs the app's 224px; 12-UI-SPEC.md explicitly rules this a Tailwind-scale rounding, not a gap worth chasing.

**Do NOT** change `<main>`'s `px-8 py-8` — 12-UI-SPEC.md § "Spacing Scale" records 32px page padding as already correct.

**Do NOT** change the `<HeaderActions .../>` or `<SidebarNav .../>` call sites. Their five and two props respectively are a hard DESIGN-05 constraint.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'bg-background/95 backdrop-blur-md border-b border-border' "app/[tenant-slug]/layout.tsx" && grep -q 'text-\[17px\] font-extrabold tracking-tight text-foreground' "app/[tenant-slug]/layout.tsx" && ! grep -q 'h-14 w-full bg-card' "app/[tenant-slug]/layout.tsx" && grep -q 'className="w-56 flex-shrink-0 border-r border-border bg-sidebar overflow-y-auto"' "app/[tenant-slug]/layout.tsx" && grep -q 'className="px-8 py-8"' "app/[tenant-slug]/layout.tsx" && grep -q '<HeaderActions role={role} tenants={tenants} activeSlug={urlSlug} manageHref={manageHref} manageLabel={manageLabel} />' "app/[tenant-slug]/layout.tsx" && grep -q '<SidebarNav slug={urlSlug} role={role} />' "app/[tenant-slug]/layout.tsx" && grep -q 'supabase.auth.getClaims()' "app/[tenant-slug]/layout.tsx" && npx tsc --noEmit && echo TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `app/[tenant-slug]/layout.tsx` contains `bg-background/95 backdrop-blur-md`
    - `app/[tenant-slug]/layout.tsx` contains ZERO occurrences of `h-14 w-full bg-card`
    - `app/[tenant-slug]/layout.tsx` contains `text-[17px] font-extrabold`
    - `app/[tenant-slug]/layout.tsx` contains ZERO occurrences of `font-semibold` on the logo link
    - `app/[tenant-slug]/layout.tsx` still contains the literal `<span className="logo-dot" />`
    - `app/[tenant-slug]/layout.tsx` still contains `className="w-56 flex-shrink-0 border-r border-border bg-sidebar overflow-y-auto"` verbatim
    - `app/[tenant-slug]/layout.tsx` still contains `className="px-8 py-8"` verbatim
    - `app/[tenant-slug]/layout.tsx` still contains `<HeaderActions role={role} tenants={tenants} activeSlug={urlSlug} manageHref={manageHref} manageLabel={manageLabel} />` verbatim
    - `app/[tenant-slug]/layout.tsx` still contains `<SidebarNav slug={urlSlug} role={role} />` verbatim
    - `app/[tenant-slug]/layout.tsx` still contains `supabase.auth.getClaims()` and `redirect('/login')` and `redirect('/')`
    - `npx tsc --noEmit` exits 0 (known baseline errors only)
  </acceptance_criteria>

  <done>The header renders as a blurred translucent app-background bar with a Syne-800 17px logo; the auth guard, sidebar width, page padding and both prop contracts are provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the header action controls (tenant switcher + date-range chip)</name>

  <read_first>
    - `components/layout/header-actions.tsx` (23 lines)
    - `components/tenants/tenant-switcher.tsx` (48 lines — note the `role !== 'super_admin' && role !== 'agency'` early return and the `__manage__` sentinel option)
    - `components/dashboard/date-range-picker.tsx` (74 lines — note the base-ui `PopoverTrigger render={...}` pattern)
    - `components/ui/button.tsx` — the `outline` variant class string, so you know what you are overriding
    - `prototipos/nexus-dash.html` lines 43-49 — the `.hc`, `.tname`, `.hr`, `.dchip`, `.avatar` rules
    - `.planning/phases/12-redesign-visual/12-UI-SPEC.md` § "Shared chrome" bullet on `HeaderActions`/`TenantSwitcher`/`DateRangePicker`
  </read_first>

  <files>components/layout/header-actions.tsx, components/tenants/tenant-switcher.tsx, components/dashboard/date-range-picker.tsx</files>

  <action>
Three small edits. All logic — routing, store bindings, role gating, popover state — stays untouched.

**1. `components/layout/header-actions.tsx` (line 17).** Replace:

```tsx
    <div className="flex items-center gap-2">
```

with:

```tsx
    <div className="flex items-center gap-2.5">
```

The prototype's `.hr` is `gap:10px`. Nothing else in this file changes — the `HeaderActionsProps` type and all five forwarded props stay byte-identical.

**2. `components/tenants/tenant-switcher.tsx` (the `<select>` className, line 38).** Replace:

```tsx
      className="h-8 rounded-md border border-border bg-background px-2 text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-ring"
```

with:

```tsx
      className="h-8 rounded-full border border-border bg-card px-3.5 text-xs font-bold text-foreground transition-colors hover:border-primary/30 focus:outline-none focus:ring-2 focus:ring-ring"
      style={{ fontFamily: 'var(--font-syne)' }}
```

Rationale, mapped to the prototype's `.hc` + `.tname`: `.hc` is `background:var(--s1); border:1px solid var(--border); border-radius:100px; padding:6px 14px 6px 10px` with `hover{border-color:rgba(200,255,0,.3)}`; `.tname` is `font-family:var(--head); font-size:12px; font-weight:700`. So: `rounded-md` -> `rounded-full` (the 100px pill), `bg-background` -> `bg-card` (`--s1` = `#0e0e12`), `px-2` -> `px-3.5` (14px), `text-sm` -> `text-xs` (12px), `font-semibold` (600, not in the D-04 Syne set) -> `font-bold` (700, permitted), plus the accent border tint on hover and the Syne family.

Everything else in this file is untouched: the `TenantSwitcherProps` interface, the `role !== 'super_admin' && role !== 'agency'` early return, `useRouter`, the `__manage__` sentinel branch, `manageHref`/`manageLabel` defaults, and the `disabled={!t.active}` option attribute.

**3. `components/dashboard/date-range-picker.tsx` (the `PopoverTrigger`'s `render` prop, line 41).** Replace:

```tsx
          <Button variant="outline" className="gap-2 text-xs h-8" />
```

with:

```tsx
          <Button
            variant="outline"
            className="gap-1.5 h-8 rounded-full border-border bg-card px-3 font-mono text-[11px] font-normal text-primary hover:border-primary/30 hover:bg-card hover:text-primary"
          />
```

Rationale, mapped to the prototype's `.dchip`: `font-family:var(--mono); font-size:11px; color:var(--accent); background:var(--s1); border:1px solid var(--border); border-radius:100px; padding:5px 12px`. `font-mono` resolves to `--font-dm-mono` via the existing `@theme inline` mapping. `font-normal` pins DM Mono to 400, which is inside the D-04 set (the `Button` base sets `font-medium` = 500, also permitted, but 400 matches the prototype). The three `hover:` overrides neutralise the `outline` variant's default `hover:bg-muted hover:text-foreground` so the chip keeps its lime text and only tints its border.

Everything else in this file is untouched: `useDateRangeStore`, `PRESETS`, `handleSelect`, the `open` state, the `PopoverContent` layout, and the `<Calendar mode="range" numberOfMonths={2}>` block.

**Do NOT** touch `components/auth/logout-button.tsx` — it is not in this plan's `files_modified` and is not called out by 12-UI-SPEC.md.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'flex items-center gap-2.5' components/layout/header-actions.tsx && grep -q 'rounded-full border border-border bg-card px-3.5 text-xs font-bold' components/tenants/tenant-switcher.tsx && grep -q "fontFamily: 'var(--font-syne)'" components/tenants/tenant-switcher.tsx && grep -q "role !== 'super_admin' && role !== 'agency'" components/tenants/tenant-switcher.tsx && grep -q "__manage__" components/tenants/tenant-switcher.tsx && grep -q 'rounded-full border-border bg-card px-3 font-mono text-\[11px\] font-normal text-primary' components/dashboard/date-range-picker.tsx && grep -q 'useDateRangeStore' components/dashboard/date-range-picker.tsx && npx tsc --noEmit && npm run build && echo TASK3_OK</automated>
  </verify>

  <acceptance_criteria>
    - `components/layout/header-actions.tsx` contains `gap-2.5` and its `HeaderActionsProps` type block is unchanged (still lists `role`, `tenants`, `activeSlug`, `manageHref?`, `manageLabel?`)
    - `components/tenants/tenant-switcher.tsx` contains `rounded-full`, `bg-card`, `text-xs`, `font-bold`, `hover:border-primary/30`, and `fontFamily: 'var(--font-syne)'`
    - `components/tenants/tenant-switcher.tsx` contains ZERO occurrences of `rounded-md border border-border bg-background px-2 text-sm font-semibold`
    - `components/tenants/tenant-switcher.tsx` still contains `if (role !== 'super_admin' && role !== 'agency') return null`
    - `components/tenants/tenant-switcher.tsx` still contains `__manage__` and `disabled={!t.active}`
    - `components/dashboard/date-range-picker.tsx` contains `font-mono`, `text-[11px]`, `text-primary`, `rounded-full`
    - `components/dashboard/date-range-picker.tsx` contains ZERO occurrences of `className="gap-2 text-xs h-8"`
    - `components/dashboard/date-range-picker.tsx` still contains `useDateRangeStore`, `applyPreset(p.key)`, and `numberOfMonths={2}`
    - `npx tsc --noEmit` exits 0 (known baseline errors only)
    - `npm run build` completes without error
  </acceptance_criteria>

  <done>The tenant switcher and date-range trigger render as prototype-matching pills on the card surface; both retain 100% of their routing, store, and role-gating behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `app/[tenant-slug]/layout.tsx` (Server Component) | This file is the tenant-scope authorization gate: it resolves the caller's role from verified JWT claims and performs an RLS-scoped tenant existence check before rendering any child route. The boundary EXISTS but this plan does not cross or modify it — only the JSX below the guard is restyled. |
| client -> `TenantSwitcher` `<select>` | User-selected tenant slug is used in a client-side `router.push()`. Pre-existing boundary, unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Elevation of Privilege | `app/[tenant-slug]/layout.tsx` role guard | mitigate | The plan's action text explicitly forbids editing anything above line 73 (the `getUser()`/`getClaims()`/`tenants` existence-check block). Enforced by the acceptance criteria requiring `supabase.auth.getClaims()`, `redirect('/login')` and `redirect('/')` to still be present, and by the requirement that the `<SidebarNav>`/`<HeaderActions>` call sites match verbatim. STATE.md records this exact call site as the source of a prior live regression (`getUser().app_metadata` vs `getClaims()`), so it is treated as high-sensitivity. |
| T-12-06 | Elevation of Privilege | `SidebarNav` role gate (`role !== 'agency'` hides the "Conta"/Configurações section) and `TenantSwitcher` role gate | mitigate | Restyling must not alter which nav items a role sees. Acceptance criteria assert the `role !== 'super_admin' && role !== 'agency'` early return still exists in `tenant-switcher.tsx`, and that `Configurações` and the three section labels still render in `sidebar-nav.tsx`. Note these are UX-level gates only — the authoritative boundary is RLS plus the layout guard, neither of which this plan touches. |
| T-12-07 | Spoofing | `TenantSwitcher` `<select>` -> `router.push()` | accept | An attacker could already type any slug into the URL bar; the switcher offers no capability the address bar does not. Actual scoping is enforced server-side by the layout's RLS-scoped `tenants` query and by row-level policies. Purely visual changes here add no new capability. |
| T-12-08 | Information Disclosure | header/sidebar chrome | accept | No new value is rendered. `tenants` was already passed to and rendered by `TenantSwitcher`; `role` was already passed to `SidebarNav`. This plan changes only class strings and one `style` attribute. |
| T-12-09 | Tampering | `style={{ fontFamily: 'var(--font-syne)' }}` in `tenant-switcher.tsx` | accept | Static literal string authored in source, not derived from user input — no CSS-injection vector. This mirrors the identical pre-existing pattern already in `app/[tenant-slug]/layout.tsx`. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — exits 0 (baseline: 2 known `tests/vault-rpc.test.ts` errors)
2. `npm run build` — completes without error
3. `npx vitest run` — no new failures; in particular `tests/middleware.test.ts` and any role/nav tests still pass
4. `grep -rn "bg-sidebar-primary" components/layout/` returns no matches
5. `grep -c 't-label px-5 pb-2' components/layout/sidebar-nav.tsx` returns `3`
</verification>

<success_criteria>
- Active sidebar item = `bg-primary/10 text-primary font-medium` (translucent wash at D-04's 500 weight), never a solid `bg-sidebar-primary` block and never `font-semibold`
- `components/layout/sidebar-nav.tsx` contains zero `font-semibold` (600 is outside D-04's locked (family, weight) pairs)
- All three sidebar section labels use `.t-label`
- Header uses `bg-background/95 backdrop-blur-md`; logo is Syne `font-extrabold` at `text-[17px]` and still renders `.logo-dot`
- Tenant switcher is a `rounded-full` `bg-card` pill with Syne `font-bold` `text-xs`
- Date-range trigger is a `rounded-full` `bg-card` chip with `font-mono text-[11px] text-primary`
- `HeaderActions` and `SidebarNav` prop signatures and call sites are byte-identical to pre-change
- `w-56` sidebar width and `px-8 py-8` main padding are unchanged
- `npx tsc --noEmit` and `npm run build` pass; no new test failures
</success_criteria>

<output>
After completion, create `.planning/phases/12-redesign-visual/12-02-SUMMARY.md`
</output>

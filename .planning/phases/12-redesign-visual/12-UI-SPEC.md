---
phase: 12
slug: redesign-visual
status: draft
shadcn_initialized: true
preset: "style: base-nova · baseColor: neutral · cssVariables: true · iconLibrary: lucide"
created: 2026-08-01
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for a **reskin-only** phase (D-03, `12-CONTEXT.md`). No new screens, no new data, no new hooks — only design tokens (color, radius, typography emphasis, shadows/elevation, micro-interactions) change on the 4 target screens and the shared chrome. Source of truth: `prototipos/nexus-dash.html` (D-01). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present, no init gate needed) |
| Preset | `style: base-nova`, `baseColor: neutral`, `cssVariables: true`, `menuColor: default`, `menuAccent: subtle` |
| Component library | **base-ui** (`@base-ui/react/*` — confirmed via `components/ui/dialog.tsx` import; NOT Radix, despite the generic CLAUDE.md convention table) |
| Icon library | lucide-react |
| Font | 3-typeface system, already declared in `app/globals.css` and matching the prototype exactly (see note below) — **no font migration needed** |

### Important correction to `12-CONTEXT.md`'s "Claude's Discretion" note

`12-CONTEXT.md` flagged a possible font mismatch ("protótipo usa Plus Jakarta Sans/JetBrains Mono, app atual usa Bricolage/Syne/DM Mono") as something to resolve. Having read `prototipos/nexus-dash.html`'s actual `<style>` block (line 9), the prototype's Google Fonts `<link>` is:

```
family=Syne:wght@400;600;700;800&family=Bricolage+Grotesque:wght@300;400;500;600;700&family=DM+Mono:wght@400;500
```

This is **identical** to what's already wired in `app/globals.css` (`--font-syne`, `--font-bricolage`, `--font-dm-mono`). The CONTEXT.md note was based on an earlier/stale reading of the prototype family (possibly the abandoned `style.css`). **Decision: keep the current font stack as-is. Zero font changes in this phase.** This closes that open discretion item.

- **Bricolage Grotesque** (`--font-bricolage`, `--font-sans`) — body copy, table cells, form inputs, nav items
- **Syne** (`--font-syne`, `--font-heading`) — page titles, card titles, KPI/numeric display values, buttons, logo
- **DM Mono** (`--font-dm-mono`, `--font-mono`) — uppercase tracked labels, badges/chips, timestamps, mono data (IDs, dates, percentages)

---

## Spacing Scale

Project standard (8-point scale, already the convention in the codebase via Tailwind defaults):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal gaps |
| sm | 8px | Compact element spacing, nav item gaps |
| md | 16px | Default element spacing, card header padding (vertical) |
| lg | 24px | Card padding (horizontal), section gaps |
| xl | 32px | Page container padding (`px-8 py-8` in `layout.tsx` — already correct, no change) |
| 2xl | 48px | Major section breaks (not used at this density — dashboard is data-dense) |
| 3xl | 64px | Not used in this phase |

**Exceptions:** The prototype's raw CSS uses a few off-grid values (`18px 22px 14px` card header, `14px 22px 0` KPI padding, `9px 14px` nav item). Per D-03 (token swap, not pixel-perfect structural rewrite), **round these to the nearest 8-point value** rather than copying literally:
- Card header (`CardHeader`): `py-4 px-6` (16px/24px) — not 18px/22px
- Card body (`CardContent`): `py-4 px-6` (16px/24px) only — not 20px/22px. The prototype's raw `20px 22px` value is being **replaced**, not offered as an equally-valid alternative; standardize on 16px/24px across every `CardContent` instance, with no `py-5` variant.
- KPI card body: target is `pt-4 px-6 pb-0` (16px/24px), unambiguously. The current `pt-5 pb-5 px-5` (20px/20px) is **the value being replaced**, not an accepted alternative — both vertical padding (20px → 16px top, `pb-5` dropped entirely in favor of `pb-0`) and horizontal padding (20px → 24px) change.
- Sidebar nav item: keep current `px-3 py-2` (12px/8px, already close to prototype's 14px/9px and already implemented) — **no change needed**, this is a spacing detail, not a token gap

---

## Typography

The prototype deliberately uses 3 font families across weight tiers rather than a single-family 2-weight system. This is an intentional, already-partially-shipped identity choice (not a new decision for this phase). The size scale below is capped at exactly **4 distinct sizes**; the weight system is declared as a **Locked Exception, ratified by the user in `12-CONTEXT.md` decision D-04** (see below) rather than forced into the generic 2-weight default, because it is pre-existing and already shipped in production.

| Role | Size | Weight | Line Height | Family |
|------|------|--------|-------------|--------|
| Label (mono, uppercase, tracked) | 11px | 500 medium | 1.3 | DM Mono, `letter-spacing: 0.12em`, `text-transform: uppercase` |
| Body | 14px (`text-sm`, existing Tailwind scale) | 400 regular | 1.5 | Bricolage Grotesque |
| Heading | 18px | 700 bold | 1.3 | Syne |
| Display | 22px (page title **and** KPI numeric values — single merged value) | 800 extrabold | 1.1, `letter-spacing: -0.02em` | Syne |

**Body collapsed to a single value:** drop the 13px option entirely. Use `text-sm` (14px, Tailwind's existing scale value) everywhere Body typography applies — zero migration cost, since `text-sm` is already the value used across the current codebase.

**Display merged to a single value:** the prototype uses 22px for the page `<h1>` and 20px separately for `.kpi-val` numbers. To stay within the 4-distinct-size cap and because both roles serve the same "biggest, boldest number/word on the screen" function, this contract merges them onto one 22px Display value. KPI numeric values render at 22px/800/Syne, same as page titles — this is a 2px increase from the prototype's literal `.kpi-val` size, consistent with the "round, don't copy literally" principle already applied to spacing in this phase.

**Heading widened away from Body:** the previous draft placed Heading at 15px, only 1-2px from Body's 13-14px — too close to read as a distinct tier. Heading now sits at 18px, a clear 4px jump above the 14px Body size.

**Total distinct sizes in this contract: 4** (11, 14, 18, 22px). No other sizes are permitted in the 4 target screens without an explicit new exception.

### Locked Exception: 4-weight / 3-family system (ratified in `12-CONTEXT.md` D-04)

The generic project default caps weights at 2 (per `gsd-ui-checker` Dimension 4). During UI-SPEC verification, the checker flagged this phase's 4-weight system as exceeding that cap and required the exception to be an explicit, user-made decision recorded via `/gsd-discuss-phase` rather than a researcher default. **This has been done.** `12-CONTEXT.md` now records the following decision:

> **D-04 (`12-CONTEXT.md` → `<decisions>` → "Tipografia — sistema de pesos"):** O sistema de 4 pesos de fonte (400/500/700/800, distribuídos entre as 3 famílias Bricolage Grotesque/Syne/DM Mono) já está em produção em `app/globals.css` e não é exclusivo das 4 telas desta fase — é usado em outras rotas do app também. É uma exceção travada ao teto padrão de 2 pesos do UI checker: manter os 4 pesos como estão, documentados como decisão intencional, em vez de reduzir a hierarquia tipográfica já shipped. Reduzir para 2 pesos extrapolaria o escopo de reskin da Fase 12 (D-03 — reskin incremental, preservar o que já existe) para uma mudança de sistema tipográfico que afeta rotas fora do escopo (`/tenants`, `/agencies`, Leads, etc.).

This UI-SPEC locks a **4-weight, 3-family exception**, on the authority of `12-CONTEXT.md` D-04, because:
1. It is not a new decision introduced by this phase — `app/globals.css` already ships `--font-syne`, `--font-bricolage`, `--font-dm-mono` with this exact weight split, and the prototype (source of truth for DESIGN-01..05) uses the identical system.
2. Collapsing to 2 weights globally would erase the distinction between mono-label emphasis (labels vs. numeric emphasis inside mono contexts) and heading emphasis (card titles vs. page title/KPI values) — both of which are load-bearing parts of the editorial identity that this redesign phase exists to deliver.
3. The exception is scoped and finite: exactly these 4 weight values, exactly these 3 families, exactly these 6 (family, weight) pairs — not an open door to introduce arbitrary new weights.
4. Per D-04, reducing to 2 weights would extend beyond the reskin scope of Phase 12 into a typography-system change affecting routes outside this phase's boundary (`/tenants`, `/agencies`, Leads, etc.) — something D-03 (incremental reskin, preserve existing behavior) explicitly rules out.

Declared (family, weight) pairs — do not introduce 600, 800 outside Syne, or any weight not listed here:
- Bricolage Grotesque: 400 (body default) + 500 (nav items, filter tabs only)
- Syne: 700 (card/section titles, buttons) + 800 (page title, KPI/numeric display values, logo)
- DM Mono: 400 (default mono/data) + 500 (numeric emphasis inside mono contexts — e.g. filter counts)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#060608` (`--background`) | App background, main content area |
| Secondary (30%) | `#0e0e12` (`--card`) / `#16161c` (`--secondary`/`--muted`) | Cards, sidebar, header, nav item hover, table header row, input backgrounds |
| Accent (10%) | `#c8ff00` (`--primary`) | See "Accent reserved for" below |
| Destructive | `#ff4444` (`--destructive`) | Destructive actions and error states only |

**Accent (`#c8ff00`) reserved for:**
- Primary CTA buttons (`btn-accent` equivalent — e.g. "Conectar Google Ads", "Conectar Meta Ads", "Analisar agora")
- Active nav item background tint + text color (sidebar)
- Active filter tab / active toggle-group segment (Campanhas status tabs, Leads filter tabs)
- Logo dot + "Sincronizado agora" status pill pulse dot (already exists as `--chart-1`/pulse-glow animation, keep)
- KPI accent-category icon color + numeric value color for "Gasto Total" and "CTR" cards specifically (prototype's `k-accent` class) — NOT all KPI cards
- Focus rings (`--ring: #c8ff00`, already correct, no change)
- Lead status badges for "Fechado"/hot-lead states only

**Accent is NOT for:** secondary/ghost buttons, all KPI icons indiscriminately, table row hover, borders by default (borders stay `--border: #222230` except on hover, where a `rgba(200,255,0,.15-.3)` tint is used — see Interaction Contract).

### Semantic data-visualization palette (separate from the 60/30/10 UI-chrome accent — these are categorical/status colors for metrics and channels, not interactive-element accents)

| Semantic | Token | Target hex (prototype) | Current hex (`app/globals.css` `.dark`) | Gap |
|----------|-------|------------------------|------------------------------------------|-----|
| Positive trend / success / "Conectado" | green | `#4ade80` | not a dedicated token (KpiCard uses `oklch(0.75 0.18 155)` inline, Badge uses `emerald-500` Tailwind literal) | Minor — visually close, no forced change required, but prefer consolidating to one green value if touched |
| Negative trend / alert | `--chart-5` / `--destructive` | `#f87171` (prototype `--red`) | `#ff4757` (`--chart-5`) / `#ff4444` (`--destructive`) | Close enough — keep as-is, don't introduce a 3rd red |
| Google channel badge | `--chart-2` | `#60a5fa` (prototype `--blue`) | `#00d4ff` (cyan — **does not match**) | **Real gap.** Recommend updating `--chart-2` in `.dark` to `#60a5fa` so Google-channel badges/lines match the prototype exactly. Flag to executor: verify this token isn't relied on elsewhere for an unrelated series before changing (grep `--chart-2` usage across `components/dashboard/*`, `components/campanhas/*`) |
| Meta channel badge | `--chart-4` | `#a78bfa`/`#c4b0fd` (prototype `--purple`-ish, used in `.cm`) | `#a855f7` | Close enough — no change required |
| Warning / low balance | `--chart-3` / orange | `#fb923c` (prototype `--orange`) | `#ff6b00` (more red-orange — **noticeable gap**) | Recommend updating `--chart-3` in `.dark` to `#fb923c` to match prototype's CPA-KPI and "low balance" badge color |

---

## Component Token Gaps (radius & elevation — concrete executor targets)

The prototype's radius scale already maps 1:1 onto the existing `app/globals.css` `--radius-*` derived scale (both are built off a 10px base) — **no CSS variable changes needed for radius**, only fixing which Tailwind radius class each shadcn primitive uses:

| Prototype token | Value | Matching app scale token | Tailwind class | Used by | Current implementation | Gap |
|---|---|---|---|---|---|---|
| `--r-sm` | 8px | `--radius-md` | `rounded-md` | Buttons, nav items, inputs | `components/ui/button.tsx` uses `rounded-[min(var(--radius-md),10-12px)]` (close, minor) | Negligible |
| `--r` | 10px | `--radius`/`--radius-lg` | `rounded-lg` | Chips, avatar | mostly already correct | none |
| `--r-lg` | 14px | `--radius-xl` | `rounded-xl` | Chat bubbles, agent icon | n/a (chat UI is future scope) | n/a |
| `--r-xl` | 18px | `--radius-2xl` | `rounded-2xl` | **All cards, KPI cards, balance cards, insight cards** | `components/ui/card.tsx` line 15 hardcodes `rounded-xl` (14px) | **Real gap — Card primitive is one radius step too small.** Recommend changing `card.tsx`'s base class from `rounded-xl` to `rounded-2xl` (and the header/footer `rounded-t-xl`/`rounded-b-xl` to `-2xl`) so every screen using `<Card>` inherits the correct 18px radius in one place |
| pill (100px) | — | — | `rounded-full` | Status pills, filter tabs container, badges | `components/ui/badge.tsx` already uses `rounded-4xl` (pill) — correct | none |

**Elevation:** the prototype uses **no drop shadows** — only a 1px border (`--border`) that tints toward accent on hover (`rgba(200,255,0,.15-.3)`), plus a `translateY(-2px)` lift on hover for KPI/balance/insight cards. The current `Card` primitive uses `ring-1 ring-foreground/10` instead of a shadow — keep this, it's already correct and consistent with the prototype's "no shadow, border+lift" language. Do not add `shadow-md`/`shadow-lg` anywhere in this phase.

---

## Copywriting Contract

This is a **reskin-only phase** (Out of Scope: "Mudanças de comportamento/dados além de pequenos ajustes de UX"). No new user-facing copy is required by DESIGN-01..05. All existing button labels, empty states, and error messages are preserved verbatim. The table below documents the copy that already exists and must NOT be reworded during the reskin, plus the one visual-only micro-copy alignment identified during research.

| Element | Copy | Status |
|---------|------|--------|
| Primary CTA (Settings) | "Conectar Google Ads" / "Conectar Meta Ads" (existing, `GoogleAdsForm`/`MetaAdsForm`) | Preserve verbatim — restyle only |
| Primary CTA (Insights) | "Analisar agora" (existing, Phase 4) | Preserve verbatim — restyle only |
| Sync status pill (Dashboard) | "Dados sincronizados" (existing, `dashboard/page.tsx` line 249) | Preserve copy — this already IS the prototype's `.status-pill` concept (`"Sincronizado agora"` in the prototype is illustrative filler, not a copy requirement); only add the lime pulse-dot + mono font treatment, do not rename the label |
| Empty state (Insights history) | "Nenhuma análise ainda" (existing) | Preserve verbatim |
| Error state (Dashboard) | "Erro ao carregar dados do dashboard. Tente recarregar a página." (existing) | Preserve verbatim |
| Error state (Settings) | "Erro ao carregar configurações." / "Tenant não encontrado" (existing) | Preserve verbatim |
| Destructive confirmation | None introduced in this phase — no new destructive actions | N/A |

**If the executor finds a genuine copy gap while restyling** (e.g., a label that reads awkwardly once visually promoted to a Display-sized element), treat it as a "pequeno ajuste de UX" per the Out-of-Scope carve-out — keep changes minimal and do not introduce net-new user flows or messaging.

---

## Per-Screen Notes

### Dashboard (`app/[tenant-slug]/dashboard/page.tsx`) — DESIGN-01
- **Primary visual anchor:** KPI numeric values (`kpi-val`) are the primary visual anchor on this screen — Display typography (22px/800/Syne) plus the accent color on "Gasto Total"/"CTR" should draw the eye first, ahead of the page title.
- Page header (`.ph`/`.pt`/`.ps`/`.status-pill` prototype pattern) is already structurally present (`h1` + subtitle + sync pill) — apply Display typography (22px/800/Syne) to `h1`, Label typography (mono, uppercase, tracked) to the subtitle, and the accent pulse-dot + mono font to the sync pill.
- `KpiCard` (lines 66-~130): apply per-category accent color (`k-accent`/`k-blue`/`k-green`/`k-orange` from the prototype) to icon + value color instead of the current uniform `bg-muted text-muted-foreground` icon chip; replace the `isGood/isBad` gradient top-bar logic with the prototype's fixed per-category top-bar color; switch `Card` radius gap (auto-inherited once `card.tsx` is fixed, see above); add the radial-glow `::before` pseudo-element and `translateY(-2px)` hover lift.
- KPI numeric value (`kpi-val`): Display typography (22px/800/Syne, `letter-spacing:-0.02em` — merged with the page-title size, see Typography section), label (`kpi-lbl`): Label typography (11px mono uppercase).
- Account balance cards, charts row, stats row: same card/radius/hover treatment; no data or chart-library changes (Recharts stays, per D-03).

### Campanhas (`app/[tenant-slug]/campanhas/page.tsx`) — DESIGN-02
- Filter bar (`.fb`/`.tg`/`.tab`) already exists structurally (toggle group + search input, lines ~164-207) — apply accent-tinted active-tab state (`rgba(200,255,0,.1)` bg + accent text + 500 weight) and mono `.rc` result-count styling.
- KPI strip (`grid-cols-2 lg:grid-cols-4`, line 145): same KPI token treatment as Dashboard.
- Table (`thead`/`tbody`): apply mono uppercase tracked header labels (Label typography), `--s2` header row background, row hover background `--s2`, first/last cell padding asymmetry (22px left/right) already close to current — keep functional filters/drill-down (`ChannelSheet`) untouched, per D-03.

### Insights (`app/[tenant-slug]/insights/page.tsx`) — DESIGN-03
- Insight cards (`.ic`/`.ib`/`.itop`/`.ititle`/`.isum`) already structurally present (line 66, `rounded-xl` → fix to `rounded-2xl` via the Card primitive change above) — apply Heading typography (18px/700/Syne) to `ititle`, Body typography (14px/400/Bricolage) to `isum`, metric-chip (`.mc`) styling to any inline metric callouts, hover lift `translateY(-2px)`.
- `StreamingInsightCard`: preserve the streaming fetch/reader loop entirely (D-03) — only restyle the container/cursor/CTA button to match `.btn-accent` treatment (Syne 700, `rounded-md`/8px, `#000` text on `#c8ff00` bg, hover `#d9ff33` + `translateY(-1px)`).
- History list empty/error/loading states: apply Body/Label typography, no structural change.

### Configurações (`app/[tenant-slug]/settings/page.tsx`) — DESIGN-04 (no prototype coverage, D-02 — extrapolated)
- No `nexus-dash.html` screen exists for Settings. Apply the same tokens established above by extrapolation:
  - Page header: same Display/Label pattern as other 3 screens, with one adjustment — since this is a denser, form-heavy screen, use **Heading typography (18px/700/Syne)** for the `h1` "Configurações" rather than the full Display (22px/800), matching the same Heading tier already used for card titles. This is a locked choice (not a range), consistent with the capped 4-size scale above.
  - `Card` (Meta Ads / Google Ads connection cards, lines 161-219): once `card.tsx`'s radius is fixed, these inherit correctly automatically. `CardHeader` with `border-b` already matches the prototype's `.card-h` bottom-border pattern — keep.
  - `ChannelStatusBadge` (lines 80-92): map "Conectado" → green/success token, "Token inválido" → destructive, "Não configurado" → `--s2`/muted (`bdg-dim` prototype equivalent) — already close, verify green matches the semantic palette above rather than the current ad-hoc `emerald-500` Tailwind literal.
  - `BackfillWindowControl` (Phase 11 component, inside each connection card, `border-t pt-4`): apply Body typography to labels, mono typography to the numeric day count/slider value, keep the inline optimistic-update interaction entirely untouched (D-03 — behavior preserved).
  - `MetaAdsForm`/`GoogleAdsForm`: restyle inputs to `--s2` background + `--border` + focus-accent-tint (matching `.sinp:focus{border-color:rgba(200,255,0,.3)}`), CTA button to `.btn-accent` treatment — preserve all validation/submit logic.

### Shared chrome (header + sidebar) — DESIGN-05
- `app/[tenant-slug]/layout.tsx` header (`h-14`, 56px) already matches prototype's `--header-h:56px` exactly — no height change.
- Sidebar (`w-56`, 224px) is close to prototype's `--sidebar-w:228px` — **do not change the width**; this is a Tailwind-scale rounding, not a token gap worth chasing.
- `SidebarNav` (`components/layout/sidebar-nav.tsx`): active nav item already uses `bg-sidebar-primary text-sidebar-primary-foreground` (which resolves to accent bg / black text per `.dark` tokens) — this is BRIGHTER/more filled than the prototype's active state (`rgba(200,255,0,.08)` translucent tint + accent text, no solid fill). **Real gap**: prototype's active nav item is a subtle 8%-opacity accent wash, not a solid lime block. Recommend changing the active state class to something like `bg-primary/10 text-primary font-semibold` instead of `bg-sidebar-primary text-sidebar-primary-foreground`.
- Nav section labels ("Marketing", "Leads", "Conta") already use the correct mono-uppercase-tracked Label treatment (`text-[10px] font-semibold tracking-widest uppercase text-muted-foreground/60`) — keep, this already matches `.nav-lbl` closely.
- `HeaderActions`/`TenantSwitcher`/`DateRangePicker`/`LogoutButton`: apply `--s2` background + pill/rounded-full shape to the date-range chip (prototype `.dchip`) and tenant switcher trigger (prototype `.hc`), avatar-square treatment (30x30, `rounded-md`, accent-tinted bg) to any user-initials element — preserve the native `<select>`-based `TenantSwitcher` implementation and its role-gating logic entirely (D-03), restyle only.
- Contract preservation (explicit, per DESIGN-05 success criterion 5 and CONTEXT.md D-01): `HeaderActions` and `SidebarNav` prop signatures (`role`/`tenants`/`activeSlug`/`manageHref`/`manageLabel` and `slug`/`role`) must not change. This is a **hard constraint**, not a style preference — do not refactor these props while reskinning.
- `/tenants` and `/agencies` admin routes also render through `HeaderActions`/`SidebarNav`-adjacent chrome patterns but are outside DESIGN-01..05's formal scope (per CONTEXT.md's "Claude's Discretion" note) — they will inherit token changes made at the shared-component level incidentally; no dedicated work item required for them in this phase, and no regression check beyond "doesn't visually break" is expected.

---

## Interaction Contract

| Interaction | Spec |
|---|---|
| Card/KPI hover | `border-color` transition to `rgba(200,255,0,.15-.18)` + `transform: translateY(-2px)`, `transition: border-color .2s, transform .2s` |
| Button hover (accent) | bg `#c8ff00` → `#d9ff33`, `transform: translateY(-1px)` |
| Button active (accent) | `transform: scale(.97)` |
| Button disabled | `opacity: .45`, `cursor: not-allowed`, no transform |
| Input focus | `border-color: rgba(200,255,0,.3)` (no default browser outline ring beyond the existing `--ring` focus-visible pattern, keep both — accent border tint is the primary indicator) |
| Active nav item | translucent accent wash `rgba(200,255,0,.08)` bg + accent text + 500 weight — NOT a solid fill (see Shared chrome gap above) |
| Active filter tab | `rgba(200,255,0,.1)` bg + accent text + 500 weight, inside a `--s2` pill-track container |
| Table row hover | bg `--s2` (`#16161c`), no transform |
| Status/pulse dot (sync pill, logo, active tenant indicator) | existing `pulse-glow`/`pulse` keyframe animation, already implemented in `globals.css` — reuse as-is, do not duplicate |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | `card`, `badge`, `button`, `table`, `input`, `select`, `skeleton`, `sonner`, `checkbox`, `dialog`, `dropdown-menu`, `alert-dialog`, `calendar`, `popover`, `sheet`, `separator`, `form`, `label`, `chart` (all already installed, pre-existing) | not required |

No third-party registries declared or needed for this phase — it modifies token usage on existing installed primitives only. No new component installs are anticipated (D-03: reskin, not new components).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
</content>

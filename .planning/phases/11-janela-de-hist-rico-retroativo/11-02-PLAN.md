---
phase: 11-janela-de-hist-rico-retroativo
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/api/google-ads/connect/route.ts
  - app/api/google-ads/callback/route.ts
  - components/settings/google-ads-form.tsx
  - tests/unit/google-ads-connect-route.test.ts
  - tests/unit/google-ads-callback-route.test.ts
autonomous: true
requirements: [SET-03, SET-04]
must_haves:
  truths:
    - "Tenant Admin picks a backfill window (7–365, default 90) in the Google Ads connect form"
    - "The connect route signs backfillDays into the OAuth state"
    - "The callback route persists backfill_days into the ad_accounts row"
  artifacts:
    - path: "components/settings/google-ads-form.tsx"
      provides: "backfillDays number input feeding the connect query string"
      contains: "backfillDays"
    - path: "app/api/google-ads/connect/route.ts"
      provides: "parse + validate backfillDays, pass to signState"
      contains: "backfillDays"
    - path: "app/api/google-ads/callback/route.ts"
      provides: "read payload.backfillDays, upsert backfill_days"
      contains: "backfill_days"
  key_links:
    - from: "components/settings/google-ads-form.tsx onSubmit"
      to: "/api/google-ads/connect"
      via: "backfillDays query param"
      pattern: "backfillDays"
    - from: "app/api/google-ads/callback/route.ts"
      to: "ad_accounts.backfill_days"
      via: "upsert payload"
      pattern: "backfill_days: payload.backfillDays"
---

<objective>
Wire the Google Ads OAuth2 connect round-trip to carry and persist the backfill window. Add the number input to the form, parse+sign it in the connect route, and read+upsert it in the callback route.

Purpose: Delivers SET-03/SET-04 for the Google Ads channel — the window chosen at connect time survives the Google redirect (via the signed state from Plan 01) and lands in `ad_accounts.backfill_days`.
Output: Updated Google connect/callback routes + form, with their unit specs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md
@.planning/phases/11-janela-de-hist-rico-retroativo/11-01-PLAN.md

<interfaces>
After Plan 01, signState is:
```typescript
signState(tenantId: string, tenantSlug: string, customerId: string, backfillDays: number): string
```
StatePayload now includes `backfillDays: number`.

Connect route currently builds the state at step 6 with `signState(tenantId, tenantSlug, customerId)`.
Callback route currently upserts (step 7) `{ tenant_id, channel: 'google_ads', account_id: customerId, vault_secret_id, active: true }` with `onConflict: 'tenant_id,channel'`.
Form onSubmit currently builds `new URLSearchParams({ customerId, tenantId, tenantSlug })` and navigates via `window.location.href`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Google connect route parses + signs backfillDays</name>
  <read_first>
    - app/api/google-ads/connect/route.ts (full current handler — steps 1–6)
    - tests/unit/google-ads-connect-route.test.ts (existing spec — mock scaffold + makeRequest)
    - lib/google-ads/oauth-state.ts (Plan 01's 4-arg signState)
  </read_first>
  <files>app/api/google-ads/connect/route.ts, tests/unit/google-ads-connect-route.test.ts</files>
  <behavior>
    - request with backfillDays=30 → state signed with backfillDays=30 (round-trip verifiable via verifyState)
    - request omitting backfillDays → defaults to 90
    - request with backfillDays=5 or 400 (out of range) → coerced/rejected: falls back to default 90 (NOT an error redirect — keep connect resilient; DB CHECK is the hard gate)
  </behavior>
  <action>
In `app/api/google-ads/connect/route.ts`:

1. Add a Zod schema for the window near `CustomerIdSchema`:
   `const BackfillDaysSchema = z.coerce.number().int().min(7).max(365).catch(90)`
   (`.coerce` because the query param arrives as a string; `.catch(90)` makes any
   missing/malformed/out-of-range value fall back to the default 90 rather than blocking
   the whole connect flow — the DB CHECK + the callback re-validation are the hard gate).

2. In step 4 (after customerId parsing), read and parse the window:
   `const backfillDays = BackfillDaysSchema.parse(req.nextUrl.searchParams.get('backfillDays'))`

3. In step 6, change the signState call from
   `signState(tenantId, tenantSlug, customerId)` to
   `signState(tenantId, tenantSlug, customerId, backfillDays)`.

Do NOT change the auth/role/tenant-scope logic (steps 1–3, 5) — backfillDays is a
non-authorization value and must never influence tenant scoping.

In `tests/unit/google-ads-connect-route.test.ts`:
4. Add a test that a successful super_admin request with `&backfillDays=30` produces a
   redirect to `accounts.google.com` whose `state` param, when passed to the REAL
   `verifyState` (statically import from `@/lib/google-ads/oauth-state`), yields
   `payload.backfillDays === 30`.
5. Add a test that omitting `backfillDays` yields `payload.backfillDays === 90`.
6. Add a test that `&backfillDays=400` (out of range) yields `payload.backfillDays === 90`.
   (Extract the `state` query param from the Location header URL to run verifyState.)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/google-ads-connect-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `signState(tenantId, tenantSlug, customerId, backfillDays)` matches in connect/route.ts
    - grep `BackfillDaysSchema` matches in connect/route.ts
    - grep `.catch(90)` matches in connect/route.ts
    - New tests assert `payload.backfillDays` of 30 (explicit), 90 (default), 90 (out-of-range)
    - `npx vitest run tests/unit/google-ads-connect-route.test.ts` passes
  </acceptance_criteria>
  <done>Connect route signs the validated window into state; spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Google callback route persists backfill_days</name>
  <read_first>
    - app/api/google-ads/callback/route.ts (full current handler — steps 0–8)
    - tests/unit/google-ads-callback-route.test.ts (existing spec — upsert assertions in the happy-path test)
    - lib/google-ads/oauth-state.ts (StatePayload now has backfillDays)
  </read_first>
  <files>app/api/google-ads/callback/route.ts, tests/unit/google-ads-callback-route.test.ts</files>
  <behavior>
    - valid state (backfillDays=45) + successful token exchange → ad_accounts upsert payload includes `backfill_days: 45`
    - all existing callback behaviors (bad state, expired, no refresh_token, vault fail) unchanged
  </behavior>
  <action>
In `app/api/google-ads/callback/route.ts`:

1. Step 1 already destructures the payload: change
   `const { tenantId, tenantSlug, customerId } = payload`
   to include the window:
   `const { tenantId, tenantSlug, customerId, backfillDays } = payload`.

2. Step 7 upsert: add `backfill_days: backfillDays` to the upsert object, so it becomes:
   ```
   {
     tenant_id: tenantId,
     channel: 'google_ads',
     account_id: customerId,
     vault_secret_id: secretId as string,
     active: true,
     backfill_days: backfillDays,
   }
   ```
   Keep `onConflict: 'tenant_id,channel'` unchanged. No other step changes.

In `tests/unit/google-ads-callback-route.test.ts`:
3. In the existing happy-path test ("valid state + successful token exchange..."), the
   state is built with the REAL `signState`. Update that `signState('tenant-uuid-1','acme','1234567890')`
   call to a 4-arg call `signState('tenant-uuid-1','acme','1234567890', 45)` and add
   `expect(payload).toMatchObject({ backfill_days: 45 })` to the upsert-payload assertion
   block. Update the other `signState(...)` calls in the file to pass a 4th arg (any valid
   number, e.g. 90) so they compile against the new signature.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/google-ads-callback-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `backfill_days: backfillDays` matches in callback/route.ts
    - grep `customerId, backfillDays` matches (payload destructure) in callback/route.ts
    - No 3-arg `signState(` call remains in tests/unit/google-ads-callback-route.test.ts
    - Happy-path test asserts `backfill_days: 45` in the upsert payload
    - `npx vitest run tests/unit/google-ads-callback-route.test.ts` passes
  </acceptance_criteria>
  <done>Callback route upserts backfill_days from the signed state; spec green.</done>
</task>

<task type="auto">
  <name>Task 3: Add backfill window input to GoogleAdsForm</name>
  <read_first>
    - components/settings/google-ads-form.tsx (full current form — schema, onSubmit, JSX)
    - components/settings/meta-ads-form.tsx (sibling form, same field/help-text conventions)
    - .planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md D-01 (number input, min 7, max 365, default 90) and D-05 (help text about non-retroactive)
  </read_first>
  <files>components/settings/google-ads-form.tsx</files>
  <action>
In `components/settings/google-ads-form.tsx` (D-01 — number input, NOT select/slider):

1. Add `backfillDays` to `GoogleAdsSchema`:
   `backfillDays: z.coerce.number().int().min(7, 'Mínimo de 7 dias').max(365, 'Máximo de 365 dias')`
   (RHF registers number inputs as strings; `z.coerce.number()` matches the connect route.)

2. Add `defaultValues: { customerId: initialCustomerId ?? '', backfillDays: 90 }` (extend the
   existing defaultValues — keep the customerId default).

3. Add a new field block BEFORE the OAuth error block, mirroring the existing Customer ID
   block's markup (Label + Input + error `<p className="text-xs text-destructive">` + help
   `<p className="text-xs text-muted-foreground">`):
   ```tsx
   <div className="space-y-1.5">
     <Label htmlFor="google-backfill-days">
       Janela de histórico (dias)
       <span className="ml-1 text-muted-foreground font-normal">(7–365, padrão 90)</span>
     </Label>
     <Input
       id="google-backfill-days"
       type="number"
       min={7}
       max={365}
       aria-invalid={!!errors.backfillDays}
       {...register('backfillDays')}
     />
     {errors.backfillDays && (
       <p className="text-xs text-destructive">{errors.backfillDays.message}</p>
     )}
     <p className="text-xs text-muted-foreground">
       Define quantos dias de histórico serão puxados no primeiro sync desta conta. Mudanças aqui valem só para o próximo primeiro sync — não reprocessam dados já sincronizados.
     </p>
   </div>
   ```

4. In `onSubmit`, add `backfillDays` to the query string:
   `const params = new URLSearchParams({ customerId: values.customerId.replace(/-/g, ''), tenantId, tenantSlug, backfillDays: String(values.backfillDays) })`.

This is functional only — do NOT invest in final visual polish (Phase 12 restyles Settings).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "vault-rpc.test.ts" | grep -c "error TS" | grep -q "^0$" && grep -q "backfillDays" components/settings/google-ads-form.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep `backfillDays` matches in GoogleAdsSchema and in the URLSearchParams in google-ads-form.tsx
    - grep `type="number"` and `id="google-backfill-days"` match
    - grep for the help text `não reprocessam dados já sincronizados` matches (D-05)
    - `npx tsc --noEmit` shows no new errors beyond the 2 pre-existing vault-rpc.test.ts errors
  </acceptance_criteria>
  <done>Form has a number input for the window, defaulting to 90, wired into the connect query string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → connect route | backfillDays is untrusted user input |
| Google redirect → callback | backfillDays arrives inside the HMAC-verified state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | backfillDays query param on connect route | mitigate | `BackfillDaysSchema = z.coerce.number().int().min(7).max(365).catch(90)` clamps to a safe default; only the validated value is signed into state |
| T-11-04 | Elevation of Privilege | backfillDays influencing tenant scope | accept | backfillDays is explicitly NOT used for authorization — tenant scoping stays sourced from getClaims()/query per the existing route logic; a bad window can only affect that same caller's own account |
</threat_model>

<verification>
- `npx vitest run tests/unit/google-ads-connect-route.test.ts tests/unit/google-ads-callback-route.test.ts` green.
- `npx tsc --noEmit` no new errors.
- Grep confirms backfillDays flows form → connect → state → callback → upsert.
</verification>

<success_criteria>
- Google Ads connect form offers a 7–365 (default 90) window input.
- Connect route validates + signs it; callback route upserts `backfill_days`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-janela-de-hist-rico-retroativo/11-02-SUMMARY.md`
</output>

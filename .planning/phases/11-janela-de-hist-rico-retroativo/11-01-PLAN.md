---
phase: 11-janela-de-hist-rico-retroativo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql
  - types/database.types.ts
  - lib/google-ads/oauth-state.ts
  - tests/unit/oauth-state.test.ts
autonomous: true
requirements: [SET-03, SET-04]
must_haves:
  truths:
    - "The ad_accounts table has a backfill_days column (7–365, default 90) live in Supabase"
    - "signState carries backfillDays across the Google OAuth redirect boundary"
  artifacts:
    - path: "supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql"
      provides: "ALTER TABLE ad_accounts ADD COLUMN backfill_days with CHECK 7–365 DEFAULT 90"
      contains: "backfill_days"
    - path: "lib/google-ads/oauth-state.ts"
      provides: "StatePayload.backfillDays + 4-arg signState"
      contains: "backfillDays"
    - path: "types/database.types.ts"
      provides: "regenerated types including ad_accounts.backfill_days"
      contains: "backfill_days"
  key_links:
    - from: "lib/google-ads/oauth-state.ts signState"
      to: "StatePayload.backfillDays"
      via: "HMAC-signed payload"
      pattern: "backfillDays"
---

<objective>
Lay the schema + contract foundation for the configurable backfill window. Add the `ad_accounts.backfill_days` column (the single source of truth every downstream plan reads/writes), push it live to Supabase, regenerate TypeScript types, and extend the Google OAuth state helper so it can carry the chosen window across the redirect boundary.

Purpose: Every other Plan 11 slice (Google routes, Meta route, post-connect edit, N8N) depends on this column existing and on `signState` accepting `backfillDays`. This is the interface-first foundation.
Output: Migration `0024`, live column, regenerated types, and a 4-argument `signState`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md

<interfaces>
Current signature (lib/google-ads/oauth-state.ts) — the executor MUST preserve every
existing field and behavior, adding ONLY backfillDays:

```typescript
export interface StatePayload {
  tenantId: string
  tenantSlug: string
  customerId: string
  nonce: string
  iat: number
}
export function signState(tenantId: string, tenantSlug: string, customerId: string): string
export function verifyState(state: string): { payload: StatePayload; expired: boolean } | null
```

Existing ad_accounts schema (supabase/migrations/0006_create_ad_accounts.sql): columns
id, tenant_id, channel ('google_ads'|'meta_ads'), account_id, vault_secret_id,
refresh_token, token_expires_at, active, created_at. UNIQUE(tenant_id, channel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0024 adding ad_accounts.backfill_days</name>
  <read_first>
    - supabase/migrations/0006_create_ad_accounts.sql (current ad_accounts schema + comment style)
    - supabase/migrations/0023_revoke_user_sessions_function.sql (most recent migration, header-comment convention)
    - .planning/research/ARCHITECTURE.md lines ~163–207 (Feature 3 — the exact ALTER TABLE recommended)
  </read_first>
  <files>supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql</files>
  <action>
Create a new migration file `supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql` with EXACTLY this content:

```sql
-- Phase 11 — Janela de Histórico Retroativo (SET-03/SET-04/SET-05)
-- Adds a per-account/channel backfill window used by the N8N sync workflows on the
-- FIRST sync of each ad_accounts row. Default 90 preserves the current global
-- Set Constants BACKFILL_DAYS behavior for every existing row with zero migration risk.
-- CHECK (7–365) matches the Zod validation on the connect routes and the
-- updateBackfillWindow Server Action.
ALTER TABLE public.ad_accounts
  ADD COLUMN IF NOT EXISTS backfill_days INTEGER NOT NULL DEFAULT 90
  CHECK (backfill_days BETWEEN 7 AND 365);

COMMENT ON COLUMN public.ad_accounts.backfill_days IS
  'Phase 11 SET-03/04/05: dias de histórico retroativo (7-365, default 90) puxados pelo N8N no PRIMEIRO sync desta conta/canal. Editável pós-conexão via updateBackfillWindow; não é retroativo (afeta só futuros primeiros syncs).';
```

Do NOT touch any other column, policy, or grant on ad_accounts — RLS already covers this table (super_admin FOR ALL; tenant members SELECT). The new column inherits the existing row-level policies; no column-level grant change is needed because backfill_days is not sensitive (unlike the sheets_service_account case in migration 0016).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql && grep -q "backfill_days INTEGER NOT NULL DEFAULT 90" supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql && grep -q "BETWEEN 7 AND 365" supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql` exists
    - grep `backfill_days INTEGER NOT NULL DEFAULT 90` matches
    - grep `CHECK (backfill_days BETWEEN 7 AND 365)` matches (spanning the ADD COLUMN statement)
    - grep `COMMENT ON COLUMN public.ad_accounts.backfill_days` matches
  </acceptance_criteria>
  <done>Migration file created with the exact ALTER TABLE + CHECK + COMMENT above.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Push migration 0024 live and regenerate TypeScript types</name>
  <read_first>
    - supabase/migrations/0024_add_backfill_days_to_ad_accounts.sql (the migration just created)
    - types/database.types.ts (current generated types — confirm ad_accounts Row shape before regenerating)
  </read_first>
  <files>types/database.types.ts</files>
  <action>
This is a [BLOCKING] task — the column MUST exist live before any plan verifies against it.

1. Push the migration to the live Supabase project:
   `supabase db push`
   If the CLI prompts for interactive confirmation that cannot be suppressed in this
   environment, use the non-interactive form `supabase db push --yes` (ensure
   `SUPABASE_ACCESS_TOKEN` is set if auth is required). If `supabase db push` still
   cannot run non-interactively, fall back to applying the SAME SQL from Task 1 via the
   Supabase MCP tool `apply_migration` (name: `add_backfill_days_to_ad_accounts`) — the
   two are equivalent; the goal is the live `ad_accounts.backfill_days` column.

2. Regenerate the generated types so `ad_accounts.Row`/`Insert`/`Update` include
   `backfill_days: number`. Use the project's existing generation command — check
   `package.json` scripts for a `gen:types`/`types` script; otherwise:
   `supabase gen types typescript --linked > types/database.types.ts`
   Do NOT hand-edit the file — regenerate it so it is diff-clean (no stray CLI banner text).

3. Confirm the live column exists (via `supabase db query "SELECT column_name FROM information_schema.columns WHERE table_name='ad_accounts' AND column_name='backfill_days'"` or the Supabase MCP `list_tables`) — it must return one row.
  </action>
  <verify>
    <automated>grep -q "backfill_days" types/database.types.ts && npx tsc --noEmit 2>&1 | grep -v "vault-rpc.test.ts" | grep -c "error TS" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `types/database.types.ts` contains `backfill_days` in the ad_accounts type block
    - Live query confirms `ad_accounts.backfill_days` column exists in the database
    - `npx tsc --noEmit` reports no NEW errors beyond the 2 pre-existing unrelated `vault-rpc.test.ts` errors documented in STATE.md
  </acceptance_criteria>
  <done>Migration applied live, `ad_accounts.backfill_days` exists in the database, and regenerated types include the column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend signState + StatePayload with backfillDays</name>
  <read_first>
    - lib/google-ads/oauth-state.ts (full current implementation — preserve HMAC/TTL/safeTenantSlug behavior)
    - tests/unit/oauth-state.test.ts (existing RED-green spec — 6 tests calling signState with 3 args)
  </read_first>
  <files>lib/google-ads/oauth-state.ts, tests/unit/oauth-state.test.ts</files>
  <behavior>
    - round-trip: signState('t','acme','1234567890', 30) then verifyState returns expired:false and payload.backfillDays === 30
    - existing tampered-signature / tampered-payload / malformed / expired cases still pass (updated to the 4-arg call)
    - signState still produces `${payloadB64}.${signature}` (two base64url segments)
  </behavior>
  <action>
In `lib/google-ads/oauth-state.ts`:

1. Add `backfillDays: number` to the `StatePayload` interface (after `customerId`).
2. Change the `signState` signature to:
   `export function signState(tenantId: string, tenantSlug: string, customerId: string, backfillDays: number): string`
   and include `backfillDays` in the `payload` object it builds. Everything else (nonce,
   iat, HMAC-SHA256 over payloadB64, base64url) stays byte-for-byte identical.
3. `verifyState` needs NO logic change — it JSON-parses the whole payload, so
   `payload.backfillDays` flows through automatically. Do NOT add a numeric validation
   branch there (the connect/callback routes and the DB CHECK re-validate; keep verifyState
   a pure signature+TTL check as today).

In `tests/unit/oauth-state.test.ts`:
4. Update every `signState('tenant-uuid-1', 'acme', '1234567890')` call to pass a 4th arg,
   e.g. `signState('tenant-uuid-1', 'acme', '1234567890', 30)`.
5. In the round-trip test, add `expect(r!.payload.backfillDays).toBe(30)`.
6. In the expired-but-validly-signed test, add `expect(r!.payload.backfillDays).toBe(30)`
   (proves the window survives the redirect even when stale — D-04 recovery path).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/oauth-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `backfillDays: number` matches in lib/google-ads/oauth-state.ts (interface field)
    - grep `customerId: string, backfillDays: number` matches the signState signature
    - `tests/unit/oauth-state.test.ts` calls signState with 4 arguments (no 3-arg call remains: `grep -E "signState\([^)]*,[^)]*,[^)]*\)" ` shows 4 comma-separated args)
    - Test asserts `payload.backfillDays` in at least 2 cases
    - `npx vitest run tests/unit/oauth-state.test.ts` passes (all cases green)
  </acceptance_criteria>
  <done>signState carries backfillDays; oauth-state.test.ts is green with 4-arg calls and backfillDays assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google OAuth redirect → callback | backfillDays leaves and re-enters the app inside the signed state |
| any client → database | backfill_days written to ad_accounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | signed OAuth state carrying backfillDays | mitigate | backfillDays lives INSIDE the HMAC-SHA256-signed payload (signState); verifyState rejects any tampered payload/signature — a forged window cannot be injected across the redirect |
| T-11-02 | Tampering | ad_accounts.backfill_days value | mitigate | DB `CHECK (backfill_days BETWEEN 7 AND 365)` is the last line of defense — even a bypassed app-layer validation cannot persist an out-of-range window |
</threat_model>

<verification>
- Migration file exists with the exact CHECK constraint.
- Live column present in Supabase (`information_schema.columns`).
- Regenerated types include `backfill_days`.
- `npx vitest run tests/unit/oauth-state.test.ts` green.
- `npx tsc --noEmit` shows no new errors.
</verification>

<success_criteria>
- `ad_accounts.backfill_days` exists live (7–365, default 90).
- `signState` accepts and signs `backfillDays`; `verifyState` returns it.
- Types regenerated; oauth-state spec green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-janela-de-hist-rico-retroativo/11-01-SUMMARY.md`
</output>

---
phase: 11-janela-de-hist-rico-retroativo
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/api/meta-ads/connect/route.ts
  - components/settings/meta-ads-form.tsx
  - tests/unit/meta-ads-connect-route.test.ts
autonomous: true
requirements: [SET-03, SET-04]
must_haves:
  truths:
    - "Tenant Admin picks a backfill window (7–365, default 90) in the Meta Ads connect form"
    - "The Meta connect route validates and persists backfill_days into the ad_accounts row"
  artifacts:
    - path: "components/settings/meta-ads-form.tsx"
      provides: "backfillDays number input feeding the POST body"
      contains: "backfillDays"
    - path: "app/api/meta-ads/connect/route.ts"
      provides: "BodySchema.backfillDays + upsert backfill_days"
      contains: "backfill_days"
    - path: "tests/unit/meta-ads-connect-route.test.ts"
      provides: "spec asserting backfill_days reaches the upsert"
      contains: "backfill_days"
  key_links:
    - from: "components/settings/meta-ads-form.tsx onSubmit"
      to: "/api/meta-ads/connect"
      via: "backfillDays POST body field"
      pattern: "backfillDays"
    - from: "app/api/meta-ads/connect/route.ts"
      to: "ad_accounts.backfill_days"
      via: "upsert payload"
      pattern: "backfill_days: parsed.data.backfillDays"
---

<objective>
Wire the Meta Ads connect flow (a single POST, no OAuth redirect) to carry and persist the backfill window. Add the number input to the form, validate `backfillDays` in the route's BodySchema, and include it in the `ad_accounts` upsert.

Purpose: Delivers SET-03/SET-04 for the Meta Ads channel. Simpler than Google — no signed-state round trip; the value travels in the JSON body straight to the upsert.
Output: Updated Meta connect route + form, with a new focused unit spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md

<interfaces>
Meta connect route currently: BodySchema = z.object({ accountId, token, tenantId }).
Upsert (step 7): `{ tenant_id, channel: 'meta_ads', account_id: accountId, vault_secret_id, active: true }` with `onConflict: 'tenant_id,channel'`.
The route validates the token against Meta Graph API (steps 4–5) before the Vault write (step 6) and upsert (step 7).

Meta form onSubmit currently POSTs `{ accountId, token, tenantId }` to `/api/meta-ads/connect` and reads `{ success?, error? }`.

Existing test mock pattern to mirror (tests/unit/google-ads-callback-route.test.ts):
- `vi.mock('server-only', () => ({}))`
- `vi.mock('@/lib/supabase/server', ...)` returning `{ auth: { getUser } }` plus `rpc` for get_user_role
- `vi.mock('@/lib/supabase/service', ...)` capturing `rpc` (vault) + `from().upsert(payload, opts)`
- `vi.stubGlobal('fetch', vi.fn(...))` for the Graph API calls
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Meta connect route validates + upserts backfill_days (with new spec)</name>
  <read_first>
    - app/api/meta-ads/connect/route.ts (full current handler — BodySchema at step 0, upsert at step 7)
    - tests/unit/google-ads-callback-route.test.ts (mock scaffold to mirror — getUser, service rpc/upsert capture, fetch stub)
    - .planning/research/ARCHITECTURE.md lines ~183–189 (Meta path: add backfillDays to BodySchema + upsert)
  </read_first>
  <files>app/api/meta-ads/connect/route.ts, tests/unit/meta-ads-connect-route.test.ts</files>
  <behavior>
    - POST body with backfillDays=45 + valid token → ad_accounts upsert payload includes `backfill_days: 45`
    - POST body omitting backfillDays → upsert payload includes `backfill_days: 90` (default)
    - POST body with backfillDays=3 (below min) → 400, no upsert
  </behavior>
  <action>
In `app/api/meta-ads/connect/route.ts`:

1. Add to `BodySchema` (the z.object at step 0):
   `backfillDays: z.number().int().min(7).max(365).default(90),`
   (Meta posts JSON, so a real number is expected here — NOT z.coerce. The `.default(90)`
   handles an omitted field; an out-of-range value fails safeParse → the existing 400 path.)

2. In the step-7 upsert object, add `backfill_days: parsed.data.backfillDays`:
   ```
   {
     tenant_id: tenantId,
     channel: 'meta_ads',
     account_id: accountId,
     vault_secret_id: secretId as string,
     active: true,
     backfill_days: parsed.data.backfillDays,
   }
   ```
   Do NOT change the auth/role gate (step 2), the tenant-ownership derivation (step 3b),
   or the Meta Graph token validation (steps 4–5).

Create `tests/unit/meta-ads-connect-route.test.ts`, mirroring the mock scaffold of
`tests/unit/google-ads-callback-route.test.ts`:
3. Mock `server-only`, `@/lib/supabase/server` (getUser → `{ id: 'user-1' }`; `rpc` →
   role `'super_admin'`), and `@/lib/supabase/service` (capture the `rpc` vault call and
   the `from('ad_accounts').upsert(payload, opts)` call in a mockState object).
4. `vi.stubGlobal('fetch', ...)` returning `{ ok: true, json: async () => ({ id: '1', name: 'x' }) }`
   for BOTH Graph API calls (the /me check and the /act_ ads-permission check).
5. Tests:
   - valid body `{ accountId: '123456789', token: 'a'.repeat(20), tenantId: <uuid>, backfillDays: 45 }`
     → response `{ success: true }` AND captured upsert payload `toMatchObject({ backfill_days: 45, channel: 'meta_ads' })`.
   - body omitting `backfillDays` → captured upsert payload `toMatchObject({ backfill_days: 90 })`.
   - body with `backfillDays: 3` → `res.status === 400` AND upsert NOT called (mockState.upsertCalls.length === 0).
  Use a valid UUID string for tenantId (e.g. `'00000000-0000-0000-0000-000000000001'`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/meta-ads-connect-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `backfill_days: parsed.data.backfillDays` matches in meta-ads/connect/route.ts
    - grep `backfillDays: z.number().int().min(7).max(365).default(90)` matches in meta-ads/connect/route.ts
    - `tests/unit/meta-ads-connect-route.test.ts` exists and asserts `backfill_days: 45`, `backfill_days: 90`, and a 400 with no upsert
    - `npx vitest run tests/unit/meta-ads-connect-route.test.ts` passes
  </acceptance_criteria>
  <done>Meta route validates + upserts backfill_days; new spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Add backfill window input to MetaAdsForm</name>
  <read_first>
    - components/settings/meta-ads-form.tsx (full current form — MetaAdsSchema, onSubmit POST body, JSX)
    - components/settings/google-ads-form.tsx (sibling — same number-input + help-text block from Plan 02)
    - .planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md D-01 + D-05
  </read_first>
  <files>components/settings/meta-ads-form.tsx</files>
  <action>
In `components/settings/meta-ads-form.tsx` (D-01):

1. Add `backfillDays` to `MetaAdsSchema`:
   `backfillDays: z.coerce.number().int().min(7, 'Mínimo de 7 dias').max(365, 'Máximo de 365 dias')`.

2. Extend `defaultValues` to `{ accountId: '', token: '', backfillDays: 90 }`.

3. Add a new field block AFTER the System User Token block and BEFORE the serverError block,
   mirroring the Account ID block's markup (Label + Input `type="number"` + error `<p>` +
   help `<p className="text-xs text-muted-foreground">`):
   ```tsx
   <div className="space-y-1.5">
     <Label htmlFor="meta-backfill-days">
       Janela de histórico (dias)
       <span className="ml-1 text-muted-foreground font-normal">(7–365, padrão 90)</span>
     </Label>
     <Input
       id="meta-backfill-days"
       type="number"
       min={7}
       max={365}
       aria-invalid={!!errors.backfillDays}
       {...register('backfillDays')}
     />
     {errors.backfillDays && (
       <p className="text-xs text-destructive">{errors.backfillDays.message}</p>
     )}
     <p className="text-xs text-muted-foreground">
       Define quantos dias de histórico serão puxados no primeiro sync desta conta. Mudanças aqui valem só para o próximo primeiro sync — não reprocessam dados já sincronizados.
     </p>
   </div>
   ```
   (Import `Input` from `@/components/ui/input` — it is already imported in this file.)

4. In `onSubmit`, add `backfillDays: values.backfillDays` to the JSON body object sent to
   `/api/meta-ads/connect`.

5. In the success branch, keep the `reset({ accountId: values.accountId, token: '' })` call
   but add `backfillDays: values.backfillDays` so the chosen window stays visible after a
   successful connect: `reset({ accountId: values.accountId, token: '', backfillDays: values.backfillDays })`.

Functional only — no final visual polish (Phase 12 restyles Settings).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "vault-rpc.test.ts" | grep -c "error TS" | grep -q "^0$" && grep -q "backfillDays" components/settings/meta-ads-form.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep `backfillDays` matches in MetaAdsSchema, the POST body, and the success `reset(...)` call
    - grep `id="meta-backfill-days"` and `type="number"` match
    - grep for the help text `não reprocessam dados já sincronizados` matches (D-05)
    - `npx tsc --noEmit` shows no new errors beyond the 2 pre-existing vault-rpc.test.ts errors
  </acceptance_criteria>
  <done>Meta form has a number input for the window, defaulting to 90, wired into the POST body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Meta connect route | backfillDays is untrusted JSON body input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | backfillDays in POST body | mitigate | `z.number().int().min(7).max(365).default(90)` in BodySchema rejects out-of-range values via the existing 400 path; DB CHECK is the final guard |
| T-11-06 | Elevation of Privilege | backfillDays vs tenant scope | accept | tenant_id is still derived from getClaims() for tenant_admin (step 3b, unchanged); backfillDays cannot cross tenants |
</threat_model>

<verification>
- `npx vitest run tests/unit/meta-ads-connect-route.test.ts` green.
- `npx tsc --noEmit` no new errors.
- Grep confirms backfillDays flows form → POST body → BodySchema → upsert.
</verification>

<success_criteria>
- Meta Ads connect form offers a 7–365 (default 90) window input.
- Route validates + persists `backfill_days`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-janela-de-hist-rico-retroativo/11-03-SUMMARY.md`
</output>

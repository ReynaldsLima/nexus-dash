---
phase: 11-janela-de-hist-rico-retroativo
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - n8n-workflows/google-ads-sync.json
  - n8n-workflows/meta-ads-sync.json
autonomous: true
requirements: [SET-04]
must_haves:
  truths:
    - "Both sync workflows fetch backfill_days per account from ad_accounts"
    - "The first-sync date range uses the per-account backfill_days, falling back to the global constant"
  artifacts:
    - path: "n8n-workflows/google-ads-sync.json"
      provides: "backfill_days in the List active select + per-account use in Compute date range"
      contains: "backfill_days"
    - path: "n8n-workflows/meta-ads-sync.json"
      provides: "backfill_days in the List active select + per-account use in Compute date range"
      contains: "backfill_days"
  key_links:
    - from: "n8n-workflows/google-ads-sync.json Compute date range"
      to: "ad_accounts.backfill_days"
      via: "$('Loop tenants').item.json.backfill_days ?? BACKFILL_DAYS"
      pattern: "backfill_days"
---

<objective>
Make the two N8N sync workflows honor the per-account backfill window. Add `backfill_days` to the `List active … accounts` PostgREST select, and in `Compute date range` use the per-account value for the first-sync window with the global `Set Constants.BACKFILL_DAYS` as fallback.

Purpose: Closes the SET-04 loop — the window persisted in `ad_accounts.backfill_days` (Plans 01–03) becomes the value N8N actually uses on the first sync. Rows predating the migration (or any missing value) still work via the constant fallback. INCREMENTAL_DAYS stays global.
Output: Both workflow JSON files updated, still valid JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md

<interfaces>
Both workflows share identical node names: `Set Constants`, `List active {Google|Meta} Ads accounts`,
`Check first sync`, `Loop tenants`, `Compute date range`.

Current `List active` PostgREST URL ends with:
`select=id,tenant_id,account_id,vault_secret_id,tenants(slug)`

Current `Compute date range` jsCode (both files) contains:
`const days = isFirstSync ? $('Set Constants').first().json.BACKFILL_DAYS : $('Set Constants').first().json.INCREMENTAL_DAYS;`

The `Compute date range` node already reads other per-account fields via
`$('Loop tenants').item.json.<field>` (e.g. tenant_id, account_id), so `backfill_days`
becomes available on that same item once added to the select.

These are workflow-definition JSON files with jsCode stored as escaped JSON strings —
edit the string content carefully and keep the file valid JSON. `active: false` is expected;
the user imports/activates manually.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: google-ads-sync.json — select + use backfill_days</name>
  <read_first>
    - n8n-workflows/google-ads-sync.json (the `List active Google Ads accounts` node URL and the `Compute date range` node jsCode)
    - .planning/research/ARCHITECTURE.md lines ~187–189 (the exact N8N edit for both workflows)
  </read_first>
  <files>n8n-workflows/google-ads-sync.json</files>
  <action>
Make exactly two string edits in `n8n-workflows/google-ads-sync.json`:

1. In the `List active Google Ads accounts` node URL, change the PostgREST select from
   `select=id,tenant_id,account_id,vault_secret_id,tenants(slug)`
   to
   `select=id,tenant_id,account_id,vault_secret_id,backfill_days,tenants(slug)`.

2. In the `Compute date range` node's jsCode string, change the `days` assignment from
   `const days = isFirstSync ? $('Set Constants').first().json.BACKFILL_DAYS : $('Set Constants').first().json.INCREMENTAL_DAYS;`
   to
   `const days = isFirstSync ? ($('Loop tenants').item.json.backfill_days ?? $('Set Constants').first().json.BACKFILL_DAYS) : $('Set Constants').first().json.INCREMENTAL_DAYS;`

   Keep everything else in the jsCode byte-for-byte (the `\n` escapes, the rest of the
   node, INCREMENTAL_DAYS stays global). Do NOT touch INCREMENTAL_DAYS — only the
   first-sync (`isFirstSync === true`) branch becomes per-account.

After editing, confirm the file is still valid JSON.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('n8n-workflows/google-ads-sync.json','utf8')); console.log('valid json')" && grep -q "backfill_days,tenants(slug)" n8n-workflows/google-ads-sync.json && grep -q "Loop tenants').item.json.backfill_days ?? " n8n-workflows/google-ads-sync.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e JSON.parse(...)` succeeds (file is valid JSON)
    - grep `select=id,tenant_id,account_id,vault_secret_id,backfill_days,tenants(slug)` matches
    - grep `$('Loop tenants').item.json.backfill_days ?? $('Set Constants').first().json.BACKFILL_DAYS` matches inside the Compute date range jsCode
    - INCREMENTAL_DAYS branch unchanged (grep still shows `: $('Set Constants').first().json.INCREMENTAL_DAYS`)
  </acceptance_criteria>
  <done>Google workflow selects backfill_days and uses it (with fallback) for the first-sync window.</done>
</task>

<task type="auto">
  <name>Task 2: meta-ads-sync.json — select + use backfill_days</name>
  <read_first>
    - n8n-workflows/meta-ads-sync.json (the `List active Meta Ads accounts` node URL and the `Compute date range` node jsCode)
  </read_first>
  <files>n8n-workflows/meta-ads-sync.json</files>
  <action>
Apply the SAME two edits as Task 1, to `n8n-workflows/meta-ads-sync.json`:

1. `List active Meta Ads accounts` node URL select:
   `select=id,tenant_id,account_id,vault_secret_id,tenants(slug)` →
   `select=id,tenant_id,account_id,vault_secret_id,backfill_days,tenants(slug)`.

2. `Compute date range` node jsCode `days` assignment:
   `const days = isFirstSync ? $('Set Constants').first().json.BACKFILL_DAYS : $('Set Constants').first().json.INCREMENTAL_DAYS;`
   →
   `const days = isFirstSync ? ($('Loop tenants').item.json.backfill_days ?? $('Set Constants').first().json.BACKFILL_DAYS) : $('Set Constants').first().json.INCREMENTAL_DAYS;`

   The Meta jsCode has additional lines (the `act_` prefix handling for ad_account_id) —
   leave ALL of that untouched; change only the `days` line. Keep valid JSON.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('n8n-workflows/meta-ads-sync.json','utf8')); console.log('valid json')" && grep -q "backfill_days,tenants(slug)" n8n-workflows/meta-ads-sync.json && grep -q "Loop tenants').item.json.backfill_days ?? " n8n-workflows/meta-ads-sync.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e JSON.parse(...)` succeeds (file is valid JSON)
    - grep `select=id,tenant_id,account_id,vault_secret_id,backfill_days,tenants(slug)` matches
    - grep `$('Loop tenants').item.json.backfill_days ?? $('Set Constants').first().json.BACKFILL_DAYS` matches
    - The Meta-specific `act_` prefix logic in the same jsCode is unchanged (grep `ad_account_id` still present)
  </acceptance_criteria>
  <done>Meta workflow selects backfill_days and uses it (with fallback) for the first-sync window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ad_accounts (DB) → N8N sync | backfill_days is read from a trusted, CHECK-constrained column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | per-account backfill_days used to compute the sync window | accept | value originates from the CHECK-constrained ad_accounts column (7–365); the `?? BACKFILL_DAYS` fallback covers null/legacy rows — no untrusted input path reaches this Code node |
| T-11-11 | Denial of Service | an excessively large window causing a huge first-sync pull | accept | window is capped at 365 by the DB CHECK; only affects the first sync of a single account (INCREMENTAL_DAYS stays 2, global) — bounded, per the non-retroactive design |
</threat_model>

<verification>
- Both JSON files parse.
- Grep confirms `backfill_days` in both selects and both Compute date range nodes.
- INCREMENTAL_DAYS behavior and Meta `act_` handling unchanged.
</verification>

<success_criteria>
- Both workflows fetch and honor per-account `backfill_days` on the first sync, with the global constant as fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/11-janela-de-hist-rico-retroativo/11-05-SUMMARY.md`
</output>

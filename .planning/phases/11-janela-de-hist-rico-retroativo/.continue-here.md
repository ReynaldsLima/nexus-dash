---
context: phase
phase: 11-janela-de-hist-rico-retroativo
task: 0
total_tasks: 13
status: not_started
last_updated: 2026-07-14T12:06:09.546Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Supabase auth before execution — Plan 11-01 Task 2 is `[BLOCKING]` and runs `supabase db push` to push migration 0024 (`ad_accounts.backfill_days`) live. Confirm `supabase` CLI is authenticated (`SUPABASE_ACCESS_TOKEN` env var or a prior `supabase login`), or that the Supabase MCP server's account is authorized, before starting execution. All 4 wave-2 plans (11-02..11-05) depend on Plan 11-01 completing, so a failed/skipped push blocks the entire phase.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

_None discovered this session — planning passed verification on the first attempt (0 blockers, 0 warnings)._

<current_state>
Phase 11 ("Janela de Histórico Retroativo") is fully planned but **not yet executed**. `/gsd-plan-phase 11` ran end-to-end today (2026-07-14): CONTEXT.md already existed from an earlier session today; research was skipped (user's choice — ARCHITECTURE.md §Feature 3 already had the full technical design); UI-SPEC was skipped (user's choice — CONTEXT.md pins exact reused components, no new visual design needed, that's Phase 12); gsd-planner produced 5 PLAN.md files; gsd-plan-checker verified them with **0 blockers, 0 warnings** on the first pass — no revision loop was needed. STATE.md was updated to reflect "Ready to execute" for Phase 11 (5 plans, 0 completed).

No plan has been executed. No SUMMARY.md files exist yet for this phase.
</current_state>

<completed_work>

Planning-only session — no implementation work done yet:
- CONTEXT.md — Done (from earlier session same day)
- Research decision — Done (skipped, justified by existing ARCHITECTURE.md coverage)
- UI-SPEC decision — Done (skipped, justified by CONTEXT.md pinning exact patterns)
- 5 PLAN.md files (11-01 through 11-05) — Done, created by gsd-planner
- Plan verification (gsd-plan-checker) — Done, PASSED with 0 issues
- STATE.md updated to "Ready to execute" — Done
</completed_work>

<remaining_work>

Full phase execution — all 13 tasks across 5 plans, nothing started:

**Wave 1 (must complete first):**
- 11-01 (3 tasks): Migration `0024` adding `ad_accounts.backfill_days` (7–365, default 90) → **[BLOCKING]** `supabase db push` + regenerate TypeScript types → extend `signState`/`StatePayload` (`lib/google-ads/oauth-state.ts`) to carry `backfillDays`

**Wave 2 (parallel, all depend only on 11-01, zero file overlap):**
- 11-02 (3 tasks): Google Ads — number input in `google-ads-form.tsx` → `connect/route.ts` signs `backfillDays` into OAuth state → `callback/route.ts` upserts `backfill_days`
- 11-03 (2 tasks): Meta Ads — number input in `meta-ads-form.tsx` → `connect/route.ts` validates/upserts `backfill_days` (direct POST, no OAuth redirect)
- 11-04 (3 tasks): Post-connect edit (SET-05) — `updateBackfillWindow` Server Action (service-role client, tenant_admin + super_admin gate) → inline always-editable control next to `ChannelStatusBadge` in `app/[tenant-slug]/settings/page.tsx` with optimistic update + revert-on-failure
- 11-05 (2 tasks): N8N — `google-ads-sync.json` and `meta-ads-sync.json`, `List active accounts` node selects `backfill_days`, `Compute date range` node uses per-account value with global constant as fallback

Run: `/gsd-execute-phase 11`
</remaining_work>

<decisions_made>

- Skipped research (RESEARCH.md) — CONTEXT.md's canonical_refs already pointed to a complete technical design in `.planning/research/ARCHITECTURE.md` §"Feature 3 — Configurable backfill window per tenant" (schema, migration, full data flow Settings UI → connect routes → ad_accounts → N8N).
- Skipped UI-SPEC.md — phase has frontend keywords but CONTEXT.md already pins exact reused components (`google-ads-form.tsx`, `meta-ads-form.tsx`, `ChannelStatusBadge`) and explicitly says this phase must not invest in visual polish (Phase 12 handles the Settings redesign).
- `updateBackfillWindow` does NOT use `requireSuperAdmin()` — SET-05 needs tenant_admin access too, so the gate replicates the connect-route pattern (super_admin any tenant; tenant_admin scoped via `getClaims()`), writing through the service-role client because `ad_accounts` RLS (migration `0006`) blocks tenant_admin UPDATE directly.
</decisions_made>

<blockers>
None currently — see BLOCKING CONSTRAINTS above for a pre-execution check (Supabase auth), not a live blocker.
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-janela-de-hist-rico-retroativo/11-CONTEXT.md` — locked UX/mechanics decisions (D-01 through D-06)
2. `.planning/research/ARCHITECTURE.md` §"Feature 3 — Configurable backfill window per tenant" (~lines 150–207) — primary technical reference (schema, data flow, file list)
3. `.planning/phases/11-janela-de-hist-rico-retroativo/11-01-PLAN.md` through `11-05-PLAN.md` — the plans themselves, in order

## Critical Anti-Patterns (do NOT repeat these)
None recorded — this was a clean planning pass.

## Infrastructure State
- No dev server or background process was left running by this session.
- Git: HEAD is at commit `65d4295` ("docs(11): create phase plan"). `.planning/STATE.md` has an uncommitted update (from `gsd-tools state planned-phase`) reflecting "Ready to execute" — safe to commit alongside the next work, or will be picked up automatically by the next `/gsd-execute-phase` commit cycle.
- Pre-existing uncommitted changes **unrelated to Phase 11** (present before this session started, left untouched): `.planning/research/ARCHITECTURE.md` (modified), `next-env.d.ts` (modified), and untracked `.playwright-mcp/`, `d05-second-reset.png`, `prototipos/`. Investigate these separately if needed — do not assume they're part of Phase 11 scope.

<context>
This was a pure planning session — /gsd-plan-phase 11 ran clean end-to-end with no snags: research skipped by user choice (ARCHITECTURE.md already covers the technical design in full), UI-SPEC skipped by user choice (CONTEXT.md already pins the exact components to reuse), planner produced 5 well-scoped plans (2-3 tasks each) across 2 waves, and the checker passed everything on the first attempt with zero issues — no revision loop needed. The [BLOCKING] schema-push task (supabase db push for migration 0024) was correctly detected by the schema-push-detection gate and injected into Plan 11-01 Task 2, gating all of wave 2 behind it. Nothing has been executed. This phase is ready to run as soon as the user resumes tomorrow.
</context>

<next_action>
Start with: Run `/gsd-execute-phase 11` (after confirming Supabase CLI/MCP auth per the blocking constraint above). This will execute Wave 1 (11-01, including the blocking schema push) then Wave 2 (11-02 through 11-05 in parallel).
</next_action>

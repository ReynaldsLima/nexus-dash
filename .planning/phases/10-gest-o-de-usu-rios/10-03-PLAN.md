---
phase: 10-gest-o-de-usu-rios
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - lib/users.ts
  - components/users/user-scope.ts
  - components/users/edit-user-email-dialog.tsx
  - components/users/reset-user-password-dialog.tsx
  - components/users/remove-user-access-dialog.tsx
  - components/users/user-row-actions.tsx
  - components/users/users-table.tsx
  - app/tenants/[slug]/page.tsx
  - app/agencies/[id]/page.tsx
  - app/tenants/layout.tsx
  - app/agencies/layout.tsx
autonomous: true
requirements: [USER-01, USER-02]

must_haves:
  truths:
    - "Super Admin sees a table of users (email + actions column only) on /tenants/[slug], replacing the 'gerenciado via Supabase Dashboard' placeholder"
    - "Super Admin sees the same users table on /agencies/[id], replacing the same placeholder"
    - "Each row has a ⋮ dropdown exposing Editar e-mail, Resetar senha, Remover acesso"
    - "Selecting an action opens the correct Dialog/AlertDialog for that row without focus/portal conflicts"
    - "After removing access the row disappears and a toast 'Acesso removido e sessão encerrada' shows"
  artifacts:
    - path: "lib/users.ts"
      provides: "listTenantUsers / listAgencyUsers read path (join-row + admin.getUserById)"
      exports: ["listTenantUsers", "listAgencyUsers", "ManagedUser"]
    - path: "components/users/users-table.tsx"
      provides: "Email + actions table (email-only per D-03)"
      exports: ["UsersTable"]
    - path: "components/users/user-row-actions.tsx"
      provides: "⋮ dropdown + externally-controlled dialogs per row"
      exports: ["UserRowActions"]
    - path: "app/tenants/[slug]/page.tsx"
      provides: "Tenant users list (USER-01)"
      contains: "UsersTable"
    - path: "app/agencies/[id]/page.tsx"
      provides: "Agency users list (USER-02)"
      contains: "UsersTable"
  key_links:
    - from: "app/tenants/[slug]/page.tsx"
      to: "lib/users.ts listTenantUsers"
      via: "await listTenantUsers(tenant.id)"
      pattern: "listTenantUsers"
    - from: "components/users/user-row-actions.tsx"
      to: "lib/actions/tenants.ts + agencies.ts"
      via: "scope-dispatched Server Action call"
      pattern: "removeTenantUserAccess|removeAgencyUserAccess"
    - from: "app/tenants/layout.tsx + app/agencies/layout.tsx"
      to: "sonner Toaster"
      via: "<Toaster/> mounted so D-09 toast renders"
      pattern: "Toaster"
---

<objective>
Build the user-management UI and wire it into both admin detail pages, replacing the "gerenciado via Supabase Dashboard" placeholder with a real users table (email + ⋮ actions). One set of scope-parameterized shared components serves both the tenant (`/tenants/[slug]`) and agency (`/agencies/[id]`) contexts.

Purpose: Delivers USER-01/USER-02 (listing) and the front-end for USER-03/04/05 (the dialogs that call Plan 02's actions).
Output: read path (`lib/users.ts`), 5 UI files under `components/users/`, both pages wired, Toaster mounted in both admin layouts.

Note on structure: 10-RESEARCH.md sketched parallel `components/tenants/*` and `components/agencies/*` dialog sets. This plan consolidates them into scope-parameterized shared components (a `UserScope` discriminated union) to avoid ~10 near-duplicate files — the route already knows its context (CONTEXT.md "Diferenciação tenant vs agência"), so a single component set dispatching on `scope.type` delivers identical behavior with less duplication. All functionality is preserved at full fidelity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-gest-o-de-usu-rios/10-CONTEXT.md
@.planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md
@.planning/phases/10-gest-o-de-usu-rios/10-02-SUMMARY.md

<interfaces>
<!-- Server Actions available from Plan 02 (import into the client dialogs): -->
```typescript
// @/lib/actions/tenants
export async function editTenantUserEmail(input: { userId: string; email: string; tenantId: string }): Promise<{ ok: true } | { error: string }>
export async function resetTenantUserPassword(input: { userId: string; tenantId: string }): Promise<{ ok: true; tempPassword: string } | { error: string }>
export async function removeTenantUserAccess(input: { userId: string; tenantId: string }): Promise<{ ok: true } | { error: string }>
// @/lib/actions/agencies — same three with agencyId instead of tenantId:
//   editAgencyUserEmail, resetAgencyUserPassword, removeAgencyUserAccess
```

<!-- Scope discriminator this plan defines (components/users/user-scope.ts): -->
```typescript
export type UserScope =
  | { type: 'tenant'; tenantId: string; label: string }   // label = tenant name (for copy)
  | { type: 'agency'; agencyId: string; label: string }   // label = agency name
export interface ManagedUser { id: string; email: string }
```

<!-- UI primitives to reuse (already installed): -->
// @/components/ui/dropdown-menu → DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem
//   DropdownMenuTrigger uses `render={<Button .../>}` (Base UI render prop). DropdownMenuItem takes onClick + variant="destructive".
// @/components/ui/dialog → Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter (controlled via open/onOpenChange — do NOT use DialogTrigger here)
// @/components/ui/alert-dialog → AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel (controlled via open/onOpenChange)
// @/components/ui/table → Table, TableHeader, TableRow, TableHead, TableBody, TableCell
// @/components/ui/{button,input,label} ; lucide-react MoreVertical, Loader2 ; toast from 'sonner'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope type + the three controlled dialog components</name>
  <read_first>
    - components/tenants/add-user-modal.tsx (the Dialog shape + "show temp password once" success-state UX + copy button to mirror for reset-password)
    - components/tenants/deactivate-tenant-button.tsx (the AlertDialog shape + useTransition + router.refresh error handling to mirror for remove-access)
    - .planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md §"Pattern 3" (dialogs MUST be externally controlled via open/onOpenChange — NEVER nest a trigger inside a DropdownMenuItem)
    - lib/hooks/use-anomaly-alerts.tsx (the `import { toast } from 'sonner'` usage)
  </read_first>
  <action>
    Create `components/users/user-scope.ts` exporting the `UserScope` union and `ManagedUser` interface from the <interfaces> block.

    Create three `'use client'` components, each taking controlled props `{ open: boolean; onOpenChange: (v: boolean) => void; user: ManagedUser; scope: UserScope }` and dispatching to the right Server Action via `scope.type`:

    1. `components/users/edit-user-email-dialog.tsx` (`EditUserEmailDialog`, USER-03):
       - `<Dialog open={open} onOpenChange={onOpenChange}>` (NO DialogTrigger).
       - Form with an email `<Input defaultValue={user.email}>`; on submit inside `useTransition`, call `scope.type === 'tenant' ? editTenantUserEmail({ userId: user.id, email, tenantId: scope.tenantId }) : editAgencyUserEmail({ userId: user.id, email, agencyId: scope.agencyId })`.
       - On `{ ok: true }`: `router.refresh()`, `toast.success('E-mail atualizado')`, close. On `{ error }`: render inline `<p role="alert" className="text-xs text-destructive">`.
       - DialogDescription copy: "Alterar o e-mail deste usuário." (NOT "neste tenant" — Pitfall 4: email is a global auth.users value; must not imply tenant-scoping).

    2. `components/users/reset-user-password-dialog.tsx` (`ResetUserPasswordDialog`, USER-04):
       - Mirror AddUserModal's two-state UX: a "confirm reset" state, then a "show temp password once" success state with a code block + Copiar button (reuse AddUserModal's `handleCopy`/`copied` pattern verbatim).
       - On confirm, call `scope.type === 'tenant' ? resetTenantUserPassword({ userId: user.id, tenantId: scope.tenantId }) : resetAgencyUserPassword({ userId: user.id, agencyId: scope.agencyId })`; on `{ ok, tempPassword }` show the password.
       - DialogDescription: "Uma nova senha temporária será gerada e exibida uma única vez. Comunique-a ao usuário." Do NOT claim anything about whether the user stays logged in (D-05 is unverified — Plan 04 checks it live).

    3. `components/users/remove-user-access-dialog.tsx` (`RemoveUserAccessDialog`, USER-05):
       - `<AlertDialog open={open} onOpenChange={onOpenChange}>` with a simple "tem certeza?" body and a destructive `AlertDialogAction` (D-08: no type-to-confirm friction).
       - On confirm (inside useTransition), call `scope.type === 'tenant' ? removeTenantUserAccess({ userId: user.id, tenantId: scope.tenantId }) : removeAgencyUserAccess({ userId: user.id, agencyId: scope.agencyId })`.
       - On `{ ok: true }`: `router.refresh()` then `toast.success('Acesso removido e sessão encerrada')` (D-09 exact copy). On `{ error }`: `toast.error(result.error)` (or inline alert).
       - AlertDialogDescription: "O usuário perderá o acesso a {scope.label} e sua sessão será encerrada. A conta é preservada e o acesso pode ser concedido novamente."
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `components/users/user-scope.ts` exports `UserScope` and `ManagedUser` (`grep -c "export type UserScope\|export interface ManagedUser" components/users/user-scope.ts` == 2)
    - All three dialog files exist and each declares controlled props `open` and `onOpenChange` (`grep -c "onOpenChange" <each file>` >= 1)
    - No dialog file uses `DialogTrigger` or `AlertDialogTrigger` (`grep -c "DialogTrigger" components/users/*.tsx` returns 0) — Pattern 3 compliance
    - `grep -c "editTenantUserEmail\|editAgencyUserEmail" components/users/edit-user-email-dialog.tsx` == 2 (dispatches to both)
    - reset dialog reuses the "show once" success pattern: `grep -c "Copiar\|copied" components/users/reset-user-password-dialog.tsx` >= 1
    - remove dialog contains the exact toast copy `Acesso removido e sessão encerrada`
    - No dialog description implies email edit is tenant-scoped (`grep -ci "neste tenant\|deste tenant" components/users/edit-user-email-dialog.tsx` == 0)
    - `npx tsc --noEmit` clean apart from the 2 pre-existing `vault-rpc.test.ts` errors
  </acceptance_criteria>
  <done>Scope type + three externally-controlled dialogs exist, each dispatching to the correct tenant/agency Server Action.</done>
</task>

<task type="auto">
  <name>Task 2: Users table + row-actions dropdown (first real consumer of dropdown-menu)</name>
  <read_first>
    - components/tenants/tenants-table.tsx (the hand-rolled shadcn Table structure to mirror)
    - components/ui/dropdown-menu.tsx (exact exports + the `render={<Button/>}` trigger prop and `variant="destructive"` item — this is the FIRST real usage, Pitfall 5)
    - components/users/user-scope.ts, and the three dialogs from Task 1 (the components UserRowActions mounts)
    - .planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md §"Pattern 3" (the UserRowActions reference implementation with three useState open flags)
  </read_first>
  <action>
    Create `components/users/user-row-actions.tsx` (`'use client'`, `UserRowActions`), props `{ user: ManagedUser; scope: UserScope }`, following 10-RESEARCH.md Pattern 3 exactly:
    - Three `useState` booleans: `editOpen`, `resetOpen`, `removeOpen`.
    - `<DropdownMenu>` with `<DropdownMenuTrigger render={<Button variant="ghost" size="icon" aria-label="Ações do usuário" />}><MoreVertical className="h-4 w-4" /></DropdownMenuTrigger>` and `<DropdownMenuContent align="end">` containing three `<DropdownMenuItem onClick={() => setXOpen(true)}>` items: "Editar e-mail", "Resetar senha", and "Remover acesso" (`variant="destructive"`).
    - After the DropdownMenu, render the three dialogs with `open`/`onOpenChange` bound to the state setters and `user`/`scope` passed through.

    Create `components/users/users-table.tsx` (server-compatible; no client hooks needed at this level — can be a plain component), `UsersTable`, props `{ users: ManagedUser[]; scope: UserScope }`:
    - If `users.length === 0`, render an empty-state card ("Nenhum usuário com acesso ainda.").
    - Otherwise a `<Table>` with exactly TWO columns (D-03): `<TableHead>E-mail</TableHead>` and `<TableHead className="text-right">Ações</TableHead>`. Each row: email cell + a right-aligned cell rendering `<UserRowActions user={u} scope={scope} />`. No "data de vinculação", no "último login" columns.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `components/users/user-row-actions.tsx` exists, exports `UserRowActions`, and mounts all three dialogs (`grep -cE "EditUserEmailDialog|ResetUserPasswordDialog|RemoveUserAccessDialog" components/users/user-row-actions.tsx` == 3)
    - Row-actions uses three separate open-state flags (`grep -c "useState" components/users/user-row-actions.tsx` >= 3) and opens dialogs via `onClick` on DropdownMenuItem (`grep -c "DropdownMenuItem" components/users/user-row-actions.tsx` == 3)
    - `components/users/users-table.tsx` exports `UsersTable` and renders exactly two `<TableHead` elements (`grep -c "<TableHead" components/users/users-table.tsx` == 2) — email + actions only (D-03)
    - `grep -c "último login\|data de vinculação\|createdAt\|created_at" components/users/users-table.tsx` == 0
    - `npx tsc --noEmit` clean apart from the 2 pre-existing `vault-rpc.test.ts` errors (a NEW error inside dropdown-menu.tsx would indicate the Pitfall-5 API mismatch — must be zero)
  </acceptance_criteria>
  <done>UsersTable (email + ⋮ only) and UserRowActions (dropdown opening the three dialogs) exist and type-check as the first real consumer of dropdown-menu.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Read path + wire both pages + mount Toaster in both admin layouts</name>
  <read_first>
    - app/tenants/[slug]/page.tsx (lines 61-71 — the "Usuários" Card with the placeholder text to replace; keep the AddUserModal in the CardHeader)
    - app/agencies/[id]/page.tsx (lines 63-73 — same placeholder Card; keep AddAgencyUserModal)
    - app/tenants/layout.tsx and app/agencies/layout.tsx (the super_admin-guarded admin shells — add <Toaster> so D-09's toast can render; neither mounts one today, only app/[tenant-slug]/layout.tsx does)
    - lib/supabase/service.ts (createServiceClient — used for admin.getUserById) and .planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md §"Pattern 1" (read-path: join rows then per-row getUserById; N+1 acceptable at 1-3 tenant scale)
    - components/ui/sonner.tsx (the Toaster export + the props app/[tenant-slug]/layout.tsx passes)
  </read_first>
  <action>
    Create `lib/users.ts` (server-only module, no `'use server'`):
    ```typescript
    import { createServiceClient } from '@/lib/supabase/service'
    export interface ManagedUser { id: string; email: string }
    export async function listTenantUsers(tenantId: string): Promise<ManagedUser[]> { /* see below */ }
    export async function listAgencyUsers(agencyId: string): Promise<ManagedUser[]> { /* see below */ }
    ```
    Each function: `const service = createServiceClient()`, select join rows (`service.from('tenant_users').select('user_id').eq('tenant_id', tenantId)` / `agency_users` by `agency_id`), then `await Promise.all(rows.map(async (r) => { const { data } = await service.auth.admin.getUserById(r.user_id); return { id: r.user_id, email: data?.user?.email ?? '(desconhecido)' } }))`. Return `[]` on query error (don't throw — page should still render).
    Re-export `ManagedUser` type consistent with `components/users/user-scope.ts` (import from there OR keep one canonical definition and import it in both — pick one source of truth and reference it; grep target: both compile).

    Wire `app/tenants/[slug]/page.tsx`: import `listTenantUsers` and `UsersTable`; after fetching `tenant`, `const users = await listTenantUsers(tenant.id)`; replace the placeholder `<p>...gerenciada via Supabase Dashboard...</p>` in the Usuários CardContent with `<UsersTable users={users} scope={{ type: 'tenant', tenantId: tenant.id, label: tenant.name }} />`. Keep the `<AddUserModal>` in the CardHeader.

    Wire `app/agencies/[id]/page.tsx`: same pattern with `listAgencyUsers(agency.id)` and `scope={{ type: 'agency', agencyId: agency.id, label: agency.name }}`. Keep `<AddAgencyUserModal>`.

    Mount Toaster in BOTH `app/tenants/layout.tsx` and `app/agencies/layout.tsx`: import `{ Toaster } from '@/components/ui/sonner'` and render `<Toaster />` inside the returned root `<div>` (after `<main>`), matching how app/[tenant-slug]/layout.tsx mounts it (dark theme, position top-right).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `lib/users.ts` exports `listTenantUsers` and `listAgencyUsers` (`grep -cE "export async function (listTenantUsers|listAgencyUsers)" lib/users.ts` == 2) and uses `getUserById` (`grep -c "getUserById" lib/users.ts` == 2)
    - The placeholder string is GONE from both pages: `grep -rc "gerenciada via Supabase Dashboard\|gerenciado via Supabase Dashboard" app/tenants/[slug]/page.tsx app/agencies/[id]/page.tsx` returns 0 for both
    - `grep -c "UsersTable" app/tenants/[slug]/page.tsx` >= 1 and `grep -c "UsersTable" app/agencies/[id]/page.tsx` >= 1
    - `grep -c "listTenantUsers" app/tenants/[slug]/page.tsx` >= 1 and `grep -c "listAgencyUsers" app/agencies/[id]/page.tsx` >= 1
    - Both admin layouts mount Toaster: `grep -c "Toaster" app/tenants/layout.tsx` >= 1 and `grep -c "Toaster" app/agencies/layout.tsx` >= 1
    - AddUserModal / AddAgencyUserModal still present in their respective pages (not removed)
    - `npx tsc --noEmit` clean apart from the 2 pre-existing `vault-rpc.test.ts` errors; `npm run build` succeeds with `/tenants/[slug]` and `/agencies/[id]` in the route list
  </acceptance_criteria>
  <done>Read path exists; both pages render the users table in place of the placeholder; Toaster mounted in both admin layouts so the removal toast can appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin pages | `/tenants/*` and `/agencies/*` are guarded by super_admin checks in their layouts; the UI actions additionally hit Plan 02's gated Server Actions |
| Server Component → auth.users | Read path uses service-role admin.getUserById; runs server-side only, never shipped to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | UI action entry points | mitigate | Actual authorization is enforced in Plan 02's Server Actions (requireSuperAdmin); the UI is a convenience layer only. Route-level super_admin guard already exists in both admin layouts. |
| T-10-09 | Information Disclosure | UI copy for email edit | mitigate | Edit-email description does not imply tenant-scoping (Pitfall 4) — email is a global auth identity; acceptance grep enforces absence of "neste tenant". |
| T-10-10 | Denial of Service | N+1 admin.getUserById on list render | accept | Acceptable at v1.1's 1-3 tenants × few users each (D-03 scope); documented, no bulk-lookup over-engineering. |
</threat_model>

<verification>
- `npm run build` succeeds; both detail pages compile with the users table.
- Placeholder text removed from both pages; UsersTable + list functions wired.
- Toaster mounted in both admin layouts.
- Manual (Plan 04): dropdown opens correct dialog per row; removal refreshes + toasts.
</verification>

<success_criteria>
- USER-01: tenant users list renders (email + actions only) on /tenants/[slug].
- USER-02: agency users list renders on /agencies/[id].
- The three dialogs (edit email, reset password, remove access) are reachable from each row's ⋮ dropdown and call the correct scoped Server Actions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-gest-o-de-usu-rios/10-03-SUMMARY.md`
</output>

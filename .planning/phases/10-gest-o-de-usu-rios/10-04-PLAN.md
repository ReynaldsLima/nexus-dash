---
phase: 10-gest-o-de-usu-rios
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified: []
autonomous: false
requirements: [USER-01, USER-02, USER-03, USER-04, USER-05]

must_haves:
  truths:
    - "The ⋮ dropdown opens and keyboard-navigates in a real table row (first production use of dropdown-menu.tsx)"
    - "Each dropdown action opens the correct Dialog/AlertDialog for the correct row"
    - "Editing an email reflects the change in the listing; resetting a password shows the temp password once"
    - "Removing access makes the row disappear and shows the 'Acesso removido e sessão encerrada' toast"
    - "The actual session-survival behavior of password reset (D-05) is observed live, not assumed"
    - "A non-super_admin session cannot successfully invoke the edit/reset/remove actions"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the Phase 10 user-management feature end-to-end against production, covering the parts that automated tests cannot reach: the first-ever production rendering of `dropdown-menu.tsx`, the dialog/portal interaction, the toast + row refresh, the D-05 live behavior question, and the non-super_admin access gate.

Purpose: This project has no component-rendering test tooling (`@testing-library/react` is not installed), so USER-01/02 (list rendering) and the dropdown→dialog→toast interaction are manual-only per 10-VALIDATION.md. This plan also resolves the D-05 open question with a live observation instead of an assumption.
Output: a verified checkpoint (or a documented list of issues).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-usu-rios/10-CONTEXT.md
@.planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md
@.planning/phases/10-gest-o-de-usu-rios/10-VALIDATION.md
@.planning/phases/10-gest-o-de-usu-rios/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm full suite green + capture project JWT expiry for revocation-window context</name>
  <read_first>
    - .planning/phases/10-gest-o-de-usu-rios/10-VALIDATION.md §"Manual-Only Verifications" and §"Sampling Rate"
    - .planning/phases/10-gest-o-de-usu-rios/10-RESEARCH.md §"Pitfall 2" and §"Open Questions" #3 (JWT expiry bounds how "immediate" revocation is)
  </read_first>
  <action>
    Run the full suite `npm test` and confirm it is green (both new unit files pass, integration test skips or passes, zero regressions beyond the documented pre-existing `anomaly_alerts` realtime flake and the 2 `vault-rpc.test.ts` tsc errors).

    Then capture this project's configured access-token (JWT) expiry so USER-05's "revogação imediata" can be stated accurately: check via the Supabase MCP / Dashboard → Authentication → Sessions/JWT settings (or `supabase` CLI), and record the value (e.g. 3600s) in the SUMMARY. This bounds the residual window during which an already-issued access token remains valid after revocation (Pitfall 2 — the revocation stops NEW tokens, not one already in the browser).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (or only the documented pre-existing `anomaly_alerts` realtime flake fails on combined run — reconfirm via isolated re-run)
    - SUMMARY records the project's configured JWT/access-token expiry value (or explicitly notes it could not be retrieved and why)
  </acceptance_criteria>
  <done>Full suite green and the JWT-expiry figure captured for accurate USER-05 revocation-window framing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify user management end-to-end on production (Playwright MCP)</name>
  <action>Human verification checkpoint — no code changes. Execute the numbered steps in <how-to-verify> against Vercel production via Playwright MCP, using a test-prefixed throwaway user. Record the D-05 live observation (step 6) and any failures.</action>
  <what-built>
    A user-management table on `/tenants/[slug]` and `/agencies/[id]` (email + ⋮ actions), replacing the "gerenciado via Supabase Dashboard" placeholder. Each row's ⋮ dropdown (first production use of `dropdown-menu.tsx`) opens one of three modals: Editar e-mail, Resetar senha (shows a temp password once), Remover acesso (AlertDialog → soft-deletes the tenant/agency link, revokes the user's sessions via the new `revoke_user_sessions` RPC, refreshes the row out of the table, and shows a "Acesso removido e sessão encerrada" toast). All three Server Actions re-verify super_admin server-side.
  </what-built>
  <how-to-verify>
    Verify against Vercel production (https://nexusdash-chi.vercel.app) using Playwright MCP — this project's established preference (Phase 6 Plan 04 precedent). Use a `test-`-prefixed throwaway user so nothing real is affected.

    1. USER-01 — Sign in as super_admin, navigate to `/tenants/[slug]` for a tenant that has at least one user. Confirm the "Usuários" card shows a table with an E-mail column and an Ações column ONLY (no data-de-vinculação, no último-login).
    2. USER-02 — Navigate to `/agencies/[id]` for an agency with a user; confirm the same table appears in place of the old placeholder.
    3. Dropdown (Pitfall 5) — Click the ⋮ on a row. Confirm the menu opens, shows "Editar e-mail / Resetar senha / Remover acesso", is keyboard-navigable (arrow keys + Enter), and closes on Escape/outside-click — no focus-trap or portal glitch.
    4. USER-03 — Open "Editar e-mail", change to a new address, submit. Confirm the dialog closes, a success toast shows, and the listing reflects the new email after refresh.
    5. USER-04 — Open "Resetar senha", confirm. Confirm a temp password (>=16 chars) is shown once with a working Copiar button.
    6. D-05 LIVE CHECK (do NOT assume) — Keep the test user logged in in a SECOND browser/session BEFORE the reset. After resetting their password in step 5, in the second session make an authenticated navigation/request. Record whether the pre-existing session still works or was terminated by the platform. This answers 10-RESEARCH.md's Critical Finding empirically for THIS instance. Neither outcome is a bug — just record which happened.
    7. USER-05 — Open "Remover acesso", confirm in the AlertDialog. Confirm: (a) the row disappears from the table, (b) the toast "Acesso removido e sessão encerrada" appears, (c) the target user's link is gone (re-navigate to confirm), and (d) that user can no longer obtain a new session (their refresh no longer works — best observed with the second session from step 6 after its access token expires, or by attempting a fresh login-refresh cycle).
    8. Access gate (V4) — In a session that is NOT super_admin (e.g. a tenant_admin), confirm the `/tenants/*` and `/agencies/*` admin pages are not reachable (route guard) AND, if the actions can be invoked at all, that they return the "Apenas super_admin pode executar esta ação." error.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which steps failed / what the D-05 live behavior turned out to be.</resume-signal>
  <verify>Manual — all 8 steps in <how-to-verify> observed passing against production; D-05 live behavior recorded.</verify>
  <done>USER-01..05 confirmed live; dropdown/dialog/toast interaction works; D-05 behavior recorded; non-super_admin cannot use the actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-super_admin session → sensitive Server Actions | Verified live that the gate rejects non-super_admin callers |
| revoked user → app after removal | Verified the removed user cannot mint a new session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | Server Actions (live) | mitigate | Step 8 confirms a non-super_admin cannot successfully invoke edit/reset/remove. |
| T-10-05 | Repudiation / false sense of security | Session revocation (live) | mitigate | Step 7d confirms the removed user cannot obtain a new session; step 1 (Plan 04 Task 1) captures the JWT-expiry residual window. |
| T-10-07 | Information Disclosure | residual access-token window | accept | Bounded by the captured JWT expiry (Task 1); communicated, not code-fixable (stateless JWT). |
</threat_model>

<verification>
- Full suite green (Task 1).
- Human-verify checkpoint approved covering USER-01..05, the dropdown/dialog/toast interaction, the D-05 live observation, and the non-super_admin gate.
</verification>

<success_criteria>
- USER-01/02 list rendering confirmed live on both pages.
- USER-03/04/05 flows work end-to-end (edit reflected, temp password shown once, removal refreshes + toasts + revokes).
- D-05 live behavior recorded (assumption resolved, not baked in).
- Non-super_admin cannot use the actions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-gest-o-de-usu-rios/10-04-SUMMARY.md`
</output>

---
phase: 02-data-pipeline
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - supabase/migrations/0006_create_ad_accounts.sql
  - supabase/migrations/0007_create_campaign_metrics.sql
  - supabase/migrations/0008_create_sync_jobs.sql
  - supabase/migrations/0009_create_daily_rollups.sql
  - supabase/migrations/0010_create_pipeline_functions.sql
  - tests/unit/campaign-metrics-schema.test.ts
  - tests/unit/daily-rollups-schema.test.ts
  - tests/integration/sync-jobs-rls.test.ts
  - tests/integration/vault-rpc.test.ts
  - types/database.types.ts
autonomous: true
requirements:
  - SYNC-04
  - SYNC-05
user_setup:
  - service: supabase
    why: "Schema push em projeto remoto (rvkkvjitfddtbdpkupok) — requer SUPABASE_ACCESS_TOKEN para CLI não-TTY"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "Após push, recarregar PostgREST schema cache: Database → Functions → 'Reload schema' (ou NOTIFY pgrst, 'reload schema')"
        location: "Supabase Dashboard → rvkkvjitfddtbdpkupok"

must_haves:
  truths:
    - "4 novas tabelas existem em public: ad_accounts, campaign_metrics, sync_jobs, daily_rollups"
    - "campaign_metrics.attribution_window é NOT NULL com DEFAULT '7d_click' (SYNC-05)"
    - "campaign_metrics tem UNIQUE(tenant_id, campaign_id, channel, date) — verificado por it.todo preenchido"
    - "ad_accounts tem UNIQUE(tenant_id, channel) — um tenant tem 1 conta por canal em v1"
    - "Função public.read_vault_secret(TEXT) existe com SECURITY DEFINER + GRANT apenas a service_role"
    - "Função public.refresh_daily_rollups(UUID, DATE, DATE) existe com SECURITY DEFINER + GRANT apenas a service_role"
    - "RLS habilitado em todas as 4 tabelas; super_admin policy + tenant_select policy criadas seguindo o padrão da Fase 1"
    - "`supabase db push` executou com sucesso contra rvkkvjitfddtbdpkupok"
    - "types/database.types.ts regenerado contém Row types para ad_accounts, campaign_metrics, sync_jobs, daily_rollups"
    - "`npm test` passa — todos os it.todo de Plan 01 foram substituídos por testes reais e a suíte fica verde"
  artifacts:
    - path: "supabase/migrations/0006_create_ad_accounts.sql"
      provides: "Tabela ad_accounts + RLS"
      contains: "CREATE TABLE IF NOT EXISTS public.ad_accounts"
    - path: "supabase/migrations/0007_create_campaign_metrics.sql"
      provides: "Tabela campaign_metrics + RLS + indexes"
      contains: "CREATE TABLE IF NOT EXISTS public.campaign_metrics"
    - path: "supabase/migrations/0008_create_sync_jobs.sql"
      provides: "Tabela sync_jobs + RLS"
      contains: "CREATE TABLE IF NOT EXISTS public.sync_jobs"
    - path: "supabase/migrations/0009_create_daily_rollups.sql"
      provides: "Tabela daily_rollups + RLS"
      contains: "CREATE TABLE IF NOT EXISTS public.daily_rollups"
    - path: "supabase/migrations/0010_create_pipeline_functions.sql"
      provides: "Funções read_vault_secret e refresh_daily_rollups"
      contains: "CREATE OR REPLACE FUNCTION public.read_vault_secret"
    - path: "types/database.types.ts"
      provides: "TypeScript types regenerados pós-push"
      contains: "campaign_metrics:"
  key_links:
    - from: "supabase/migrations/0010_create_pipeline_functions.sql"
      to: "vault.decrypted_secrets (extensão Supabase Vault)"
      via: "função SECURITY DEFINER lê vault.decrypted_secrets via SELECT"
      pattern: "vault\\.decrypted_secrets"
    - from: "supabase/migrations/0010_create_pipeline_functions.sql"
      to: "public.campaign_metrics e public.daily_rollups"
      via: "refresh_daily_rollups INSERT ... SELECT ... FROM campaign_metrics ... ON CONFLICT"
      pattern: "FROM public\\.campaign_metrics"
    - from: "tests/integration/vault-rpc.test.ts"
      to: "supabase/migrations/0010_create_pipeline_functions.sql"
      via: "Tests validam que read_vault_secret retorna decrypted_secret apenas para service_role"
      pattern: "read_vault_secret"
    - from: "tests/unit/campaign-metrics-schema.test.ts"
      to: "supabase/migrations/0007_create_campaign_metrics.sql"
      via: "Tests validam attribution_window DEFAULT + UNIQUE constraint"
      pattern: "attribution_window"
---

<objective>
Criar as 4 tabelas do pipeline de dados (ad_accounts, campaign_metrics, sync_jobs, daily_rollups), as 2 funções Postgres (read_vault_secret, refresh_daily_rollups), todas as RLS policies, indexes, e a [BLOCKING] schema push no Supabase remoto. Preencher os it.todo() criados na Plan 01 com testes reais que rodam contra o staging schema.

Purpose: Esta é a fundação de schema da Fase 2 — sem isso, os workflows N8N (Plans 03 e 04) não têm onde escrever, e a UI de sync status (Plan 05) não tem o que ler. Cobre SYNC-04 (schema sync_jobs) e SYNC-05 (attribution_window day-one).
Output: 5 migrations aplicadas no projeto Supabase rvkkvjitfddtbdpkupok, types regenerados, suite Vitest com 18+ asserções reais passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@supabase/migrations/0002_create_tenants.sql
@supabase/migrations/0003_create_helper_functions.sql
@supabase/migrations/0004_create_rls_policies.sql

<interfaces>
<!-- Padrões obrigatórios herdados da Fase 1 que TODA migration nova DEVE seguir -->

From supabase/migrations/0003_create_helper_functions.sql:
```sql
-- Helpers disponíveis para reuso (NÃO recriar):
CREATE FUNCTION public.get_tenant_id() RETURNS UUID ...
CREATE FUNCTION public.get_user_role() RETURNS TEXT ...
-- USAR SEMPRE como (SELECT public.get_xxx()) — wrapper obrigatório (D-14)
```

From supabase/migrations/0004_create_rls_policies.sql:
```sql
-- Padrão estabelecido para super_admin + tenant_select (replicar para cada nova tabela):
CREATE POLICY <tbl>_super_admin_all ON public.<tbl>
  FOR ALL TO authenticated
  USING ((SELECT public.get_user_role()) = 'super_admin')
  WITH CHECK ((SELECT public.get_user_role()) = 'super_admin');

CREATE POLICY <tbl>_tenant_select ON public.<tbl>
  FOR SELECT TO authenticated
  USING (tenant_id = (SELECT public.get_tenant_id()));

REVOKE ALL ON public.<tbl> FROM anon;
```

From types/database.types.ts (formato pós-supabase gen types):
- Tabelas viram entradas em `Database['public']['Tables']` com `Row`, `Insert`, `Update`, `Relationships`.

Supabase project ref: `rvkkvjitfddtbdpkupok` (sa-east-1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0006 — tabela ad_accounts + RLS (D-04, D-05, D-06)</name>
  <files>supabase/migrations/0006_create_ad_accounts.sql</files>
  <read_first>
    - supabase/migrations/0002_create_tenants.sql (padrão de CREATE TABLE + COMMENT ON TABLE)
    - supabase/migrations/0004_create_rls_policies.sql (padrão exato de policies)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-04, §D-05, §D-06 (schema completo)
  </read_first>
  <action>
    Criar `supabase/migrations/0006_create_ad_accounts.sql` com o seguinte SQL exato:

    ```sql
    -- Phase 2 — Data Pipeline
    -- Decisions implemented: D-04 (Super Admin insere via Dashboard), D-05 (tokens no Vault), D-06 (schema)
    -- Per RESEARCH.md §Padrão 6, refresh_token e access_token nunca vivem em colunas em texto plano —
    -- vault_secret_id aponta para vault.decrypted_secrets. refresh_token coluna existe apenas para
    -- Google Ads (legacy migration path) e também deve ser movida para Vault em produção.

    CREATE TABLE IF NOT EXISTS public.ad_accounts (
      id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id         UUID NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
      channel           TEXT NOT NULL CHECK (channel IN ('google_ads', 'meta_ads')),
      account_id        TEXT NOT NULL,
      vault_secret_id   UUID NOT NULL,
      refresh_token     TEXT,
      token_expires_at  TIMESTAMPTZ,
      active            BOOLEAN NOT NULL DEFAULT TRUE,
      created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      UNIQUE(tenant_id, channel)
    );

    CREATE INDEX IF NOT EXISTS idx_ad_accounts_tenant_channel_active
      ON public.ad_accounts(tenant_id, channel) WHERE active = TRUE;

    ALTER TABLE public.ad_accounts ENABLE ROW LEVEL SECURITY;

    -- Super Admin: full access
    CREATE POLICY ad_accounts_super_admin_all ON public.ad_accounts
      FOR ALL TO authenticated
      USING ((SELECT public.get_user_role()) = 'super_admin')
      WITH CHECK ((SELECT public.get_user_role()) = 'super_admin');

    -- Tenant members: SELECT only on own tenant — INSERT/UPDATE/DELETE bloqueado em v1 (D-04)
    CREATE POLICY ad_accounts_tenant_select ON public.ad_accounts
      FOR SELECT TO authenticated
      USING (tenant_id = (SELECT public.get_tenant_id()));

    REVOKE ALL ON public.ad_accounts FROM anon;

    COMMENT ON TABLE public.ad_accounts IS 'Phase 2 D-04/D-05/D-06: credenciais de API por tenant. vault_secret_id referencia secret no Supabase Vault. UNIQUE(tenant_id, channel) = uma conta por canal em v1.';
    COMMENT ON POLICY ad_accounts_tenant_select ON public.ad_accounts IS 'D-04: tenant_admin/viewer somente leem; gestão de credenciais é do Super Admin via Dashboard em v1.';
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/0006_create_ad_accounts.sql && grep -q "CREATE TABLE IF NOT EXISTS public.ad_accounts" supabase/migrations/0006_create_ad_accounts.sql && grep -q "UNIQUE(tenant_id, channel)" supabase/migrations/0006_create_ad_accounts.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0006_create_ad_accounts.sql` existe
    - Contém literal `CREATE TABLE IF NOT EXISTS public.ad_accounts`
    - Contém literal `UNIQUE(tenant_id, channel)`
    - Contém literal `CHECK (channel IN ('google_ads', 'meta_ads'))`
    - Contém literal `vault_secret_id   UUID NOT NULL`
    - Contém literal `REFERENCES public.tenants(id) ON DELETE CASCADE`
    - Contém literal `ALTER TABLE public.ad_accounts ENABLE ROW LEVEL SECURITY`
    - Contém literal `CREATE POLICY ad_accounts_super_admin_all`
    - Contém literal `CREATE POLICY ad_accounts_tenant_select`
    - Contém literal `(SELECT public.get_user_role()) = 'super_admin'` (D-14 wrapper)
    - Contém literal `(SELECT public.get_tenant_id())` (D-14 wrapper)
    - Contém literal `REVOKE ALL ON public.ad_accounts FROM anon`
  </acceptance_criteria>
  <done>Migration 0006 criada com schema exato de D-06, RLS pattern da Fase 1, comentário documentando D-04/D-05/D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0007 — tabela campaign_metrics + RLS + indexes (D-01, D-02, D-03, SYNC-05)</name>
  <files>supabase/migrations/0007_create_campaign_metrics.sql</files>
  <read_first>
    - supabase/migrations/0006_create_ad_accounts.sql (Task 1 — replicar padrão de RLS)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-01, §D-02, §D-03 (schema exato com tipos)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 8" (RLS) e §Code Example "RLS policy para campaign_metrics"
  </read_first>
  <action>
    Criar `supabase/migrations/0007_create_campaign_metrics.sql` com o SQL exato:

    ```sql
    -- Phase 2 — Data Pipeline
    -- Decisions implemented: D-01 (day-level), D-02 (tabela única com coluna channel),
    -- D-03 (schema exato), SYNC-05 (attribution_window NOT NULL DEFAULT '7d_click' desde o dia 1).
    -- Per RESEARCH.md Pitfall 3, '7d_click' é o default correto após mudanças Meta de jan/2026.

    CREATE TABLE IF NOT EXISTS public.campaign_metrics (
      id                 UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id          UUID NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
      campaign_id        TEXT NOT NULL,
      campaign_name      TEXT NOT NULL,
      channel            TEXT NOT NULL CHECK (channel IN ('google_ads', 'meta_ads')),
      date               DATE NOT NULL,
      impressions        BIGINT NOT NULL DEFAULT 0,
      clicks             BIGINT NOT NULL DEFAULT 0,
      spend              NUMERIC(12,2) NOT NULL DEFAULT 0,
      conversions        NUMERIC(12,4) NOT NULL DEFAULT 0,
      conversion_value   NUMERIC(12,2) NOT NULL DEFAULT 0,
      status             TEXT,
      ad_group_id        TEXT,
      attribution_window TEXT NOT NULL DEFAULT '7d_click',
      synced_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      created_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      UNIQUE(tenant_id, campaign_id, channel, date)
    );

    -- Indexes for Phase 3 dashboard queries
    CREATE INDEX IF NOT EXISTS idx_campaign_metrics_tenant_date
      ON public.campaign_metrics(tenant_id, date DESC);
    CREATE INDEX IF NOT EXISTS idx_campaign_metrics_tenant_channel_date
      ON public.campaign_metrics(tenant_id, channel, date DESC);
    CREATE INDEX IF NOT EXISTS idx_campaign_metrics_campaign_date
      ON public.campaign_metrics(campaign_id, date DESC);

    ALTER TABLE public.campaign_metrics ENABLE ROW LEVEL SECURITY;

    CREATE POLICY campaign_metrics_super_admin_all ON public.campaign_metrics
      FOR ALL TO authenticated
      USING ((SELECT public.get_user_role()) = 'super_admin')
      WITH CHECK ((SELECT public.get_user_role()) = 'super_admin');

    CREATE POLICY campaign_metrics_tenant_select ON public.campaign_metrics
      FOR SELECT TO authenticated
      USING (tenant_id = (SELECT public.get_tenant_id()));

    REVOKE ALL ON public.campaign_metrics FROM anon;

    COMMENT ON TABLE public.campaign_metrics IS 'Phase 2 D-01/D-02/D-03 SYNC-05: day-level métricas unificadas. attribution_window NOT NULL DEFAULT 7d_click desde dia 1.';
    COMMENT ON COLUMN public.campaign_metrics.attribution_window IS 'SYNC-05: nunca NULL. Default 7d_click (Meta post jan/2026 changes; Google Ads default).';
    COMMENT ON COLUMN public.campaign_metrics.spend IS 'Em moeda real (já dividido por 1.000.000 quando vem de Google Ads cost_micros — ver Pitfall 5).';
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/0007_create_campaign_metrics.sql && grep -q "attribution_window TEXT NOT NULL DEFAULT '7d_click'" supabase/migrations/0007_create_campaign_metrics.sql && grep -q "UNIQUE(tenant_id, campaign_id, channel, date)" supabase/migrations/0007_create_campaign_metrics.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0007_create_campaign_metrics.sql` existe
    - Contém literal `CREATE TABLE IF NOT EXISTS public.campaign_metrics`
    - Contém literal `attribution_window TEXT NOT NULL DEFAULT '7d_click'`
    - Contém literal `UNIQUE(tenant_id, campaign_id, channel, date)`
    - Contém literal `CHECK (channel IN ('google_ads', 'meta_ads'))`
    - Contém os 3 indexes: `idx_campaign_metrics_tenant_date`, `idx_campaign_metrics_tenant_channel_date`, `idx_campaign_metrics_campaign_date`
    - Contém literal `ENABLE ROW LEVEL SECURITY`
    - Contém literal `CREATE POLICY campaign_metrics_super_admin_all`
    - Contém literal `CREATE POLICY campaign_metrics_tenant_select`
    - Contém literal `REVOKE ALL ON public.campaign_metrics FROM anon`
  </acceptance_criteria>
  <done>Migration 0007 criada com 16 colunas conforme D-03, 3 indexes, 2 policies, REVOKE anon.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 0008 — sync_jobs + 0009 — daily_rollups + RLS (D-07/D-08, D-09/D-11)</name>
  <files>supabase/migrations/0008_create_sync_jobs.sql, supabase/migrations/0009_create_daily_rollups.sql</files>
  <read_first>
    - supabase/migrations/0007_create_campaign_metrics.sql (Task 2 — replicar padrão)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-07, §D-08 (sync_jobs schema)
    - .planning/phases/02-data-pipeline/02-CONTEXT.md §D-09, §D-10, §D-11 (daily_rollups schema)
  </read_first>
  <action>
    Criar AMBOS os arquivos abaixo.

    **Arquivo 1 — `supabase/migrations/0008_create_sync_jobs.sql`:**
    ```sql
    -- Phase 2 — Data Pipeline
    -- Decisions implemented: D-07 (log de execuções), D-08 (schema exato).
    -- SYNC-04: status e erros gravados aqui, Super Admin inspeciona via UI (Plan 05) e SQL.

    CREATE TABLE IF NOT EXISTS public.sync_jobs (
      id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id       UUID NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
      channel         TEXT NOT NULL CHECK (channel IN ('google_ads', 'meta_ads')),
      status          TEXT NOT NULL CHECK (status IN ('running', 'success', 'failed')),
      started_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      completed_at    TIMESTAMPTZ,
      records_synced  INTEGER NOT NULL DEFAULT 0,
      date_from       DATE,
      date_to         DATE,
      error_message   TEXT,
      created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    -- Index for "last sync per tenant/channel" query (Plan 05 UI)
    CREATE INDEX IF NOT EXISTS idx_sync_jobs_tenant_channel_completed
      ON public.sync_jobs(tenant_id, channel, completed_at DESC NULLS LAST);
    -- Index for Pattern 10 "detectar primeira execução"
    CREATE INDEX IF NOT EXISTS idx_sync_jobs_tenant_channel_status
      ON public.sync_jobs(tenant_id, channel, status);

    ALTER TABLE public.sync_jobs ENABLE ROW LEVEL SECURITY;

    CREATE POLICY sync_jobs_super_admin_all ON public.sync_jobs
      FOR ALL TO authenticated
      USING ((SELECT public.get_user_role()) = 'super_admin')
      WITH CHECK ((SELECT public.get_user_role()) = 'super_admin');

    CREATE POLICY sync_jobs_tenant_select ON public.sync_jobs
      FOR SELECT TO authenticated
      USING (tenant_id = (SELECT public.get_tenant_id()));

    REVOKE ALL ON public.sync_jobs FROM anon;

    COMMENT ON TABLE public.sync_jobs IS 'Phase 2 D-07/D-08 SYNC-04: log de execuções de sync. service_role (N8N) bypassa RLS para INSERT/UPDATE.';
    ```

    **Arquivo 2 — `supabase/migrations/0009_create_daily_rollups.sql`:**
    ```sql
    -- Phase 2 — Data Pipeline
    -- Decisions implemented: D-09, D-10, D-11.
    -- Pré-requisito para Fase 3 (KPI cards e trend lines fazem queries diretas em daily_rollups).
    -- Populated by public.refresh_daily_rollups(...) (migration 0010), chamado pelo N8N via RPC após cada sync.
    -- 'all' channel = cross-channel rollup para queries que somam Google + Meta.

    CREATE TABLE IF NOT EXISTS public.daily_rollups (
      id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id           UUID NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE,
      channel             TEXT NOT NULL CHECK (channel IN ('google_ads', 'meta_ads', 'all')),
      date                DATE NOT NULL,
      total_spend         NUMERIC(12,2) NOT NULL DEFAULT 0,
      total_impressions   BIGINT NOT NULL DEFAULT 0,
      total_clicks        BIGINT NOT NULL DEFAULT 0,
      total_conversions   NUMERIC(12,4) NOT NULL DEFAULT 0,
      total_conv_value    NUMERIC(12,2) NOT NULL DEFAULT 0,
      campaign_count      INTEGER NOT NULL DEFAULT 0,
      updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      UNIQUE(tenant_id, channel, date)
    );

    CREATE INDEX IF NOT EXISTS idx_daily_rollups_tenant_date
      ON public.daily_rollups(tenant_id, date DESC);

    ALTER TABLE public.daily_rollups ENABLE ROW LEVEL SECURITY;

    CREATE POLICY daily_rollups_super_admin_all ON public.daily_rollups
      FOR ALL TO authenticated
      USING ((SELECT public.get_user_role()) = 'super_admin')
      WITH CHECK ((SELECT public.get_user_role()) = 'super_admin');

    CREATE POLICY daily_rollups_tenant_select ON public.daily_rollups
      FOR SELECT TO authenticated
      USING (tenant_id = (SELECT public.get_tenant_id()));

    REVOKE ALL ON public.daily_rollups FROM anon;

    COMMENT ON TABLE public.daily_rollups IS 'Phase 2 D-09/D-11: agregado day-level por tenant+channel. channel="all" = cross-channel. Populated by refresh_daily_rollups RPC (Plan 03/04).';
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/0008_create_sync_jobs.sql && test -f supabase/migrations/0009_create_daily_rollups.sql && grep -q "status TEXT NOT NULL CHECK (status IN ('running', 'success', 'failed'))" supabase/migrations/0008_create_sync_jobs.sql && grep -q "channel IN ('google_ads', 'meta_ads', 'all')" supabase/migrations/0009_create_daily_rollups.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0008_create_sync_jobs.sql` existe
    - Contém `CREATE TABLE IF NOT EXISTS public.sync_jobs`
    - Contém `status TEXT NOT NULL CHECK (status IN ('running', 'success', 'failed'))`
    - Contém `error_message   TEXT` (campo opcional)
    - Contém `idx_sync_jobs_tenant_channel_completed`
    - Contém `idx_sync_jobs_tenant_channel_status`
    - Contém `REVOKE ALL ON public.sync_jobs FROM anon`
    - Arquivo `supabase/migrations/0009_create_daily_rollups.sql` existe
    - Contém `CREATE TABLE IF NOT EXISTS public.daily_rollups`
    - Contém `CHECK (channel IN ('google_ads', 'meta_ads', 'all'))`
    - Contém `UNIQUE(tenant_id, channel, date)`
    - Contém `campaign_count      INTEGER NOT NULL DEFAULT 0`
    - Contém `REVOKE ALL ON public.daily_rollups FROM anon`
    - Ambas as tabelas têm policies super_admin_all + tenant_select com wrapper (SELECT public.get_xxx())
  </acceptance_criteria>
  <done>2 migrations criadas, ambas seguindo o padrão estabelecido.</done>
</task>

<task type="auto">
  <name>Task 4: Migration 0010 — funções read_vault_secret + refresh_daily_rollups (D-05, D-10)</name>
  <files>supabase/migrations/0010_create_pipeline_functions.sql</files>
  <read_first>
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 6" (read_vault_secret SECURITY DEFINER)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 7" (refresh_daily_rollups CTE com UNION ALL + ON CONFLICT)
    - supabase/migrations/0003_create_helper_functions.sql (padrão SECURITY DEFINER + GRANT)
    - supabase/migrations/0009_create_daily_rollups.sql (Task 3 — UNIQUE constraint usado no ON CONFLICT)
  </read_first>
  <action>
    Criar `supabase/migrations/0010_create_pipeline_functions.sql` com o SQL exato (NÃO modificar a estrutura SQL — copiado verbatim do RESEARCH.md §Padrão 6/7):

    ```sql
    -- Phase 2 — Data Pipeline
    -- Decisions implemented: D-05 (Vault read via SECURITY DEFINER), D-10 (refresh_daily_rollups via RPC).
    -- Per RESEARCH.md §Padrão 6 e §Padrão 7. Estas funções são chamadas pelo N8N via PostgREST /rpc/.
    -- Threat T-2-03: read_vault_secret SÓ pode ser executada por service_role.

    -- ------------------------------------------------------------
    -- read_vault_secret(p_secret_name TEXT) → TEXT
    -- Lê decrypted_secret do Supabase Vault. Encapsulamento necessário porque
    -- vault.decrypted_secrets não é acessível via PostgREST diretamente.
    -- ------------------------------------------------------------
    CREATE OR REPLACE FUNCTION public.read_vault_secret(p_secret_name TEXT)
    RETURNS TEXT
    LANGUAGE SQL
    SECURITY DEFINER
    SET search_path = public, vault
    AS $$
      SELECT decrypted_secret
      FROM vault.decrypted_secrets
      WHERE name = p_secret_name
      LIMIT 1;
    $$;

    -- Threat T-2-03 mitigation: SOMENTE service_role pode executar
    REVOKE EXECUTE ON FUNCTION public.read_vault_secret(TEXT) FROM PUBLIC, anon, authenticated;
    GRANT EXECUTE ON FUNCTION public.read_vault_secret(TEXT) TO service_role;

    COMMENT ON FUNCTION public.read_vault_secret(TEXT) IS 'Phase 2 D-05: lê secret do Supabase Vault. SECURITY DEFINER + GRANT apenas a service_role (Threat T-2-03).';

    -- ------------------------------------------------------------
    -- refresh_daily_rollups(p_tenant_id UUID, p_date_from DATE, p_date_to DATE) → VOID
    -- UPSERT agregado em daily_rollups a partir de campaign_metrics.
    -- Cria 1 linha por (channel, date) E 1 linha cross-channel ('all', date).
    -- Chamado pelo N8N via POST /rest/v1/rpc/refresh_daily_rollups após cada sync bem-sucedido.
    -- ------------------------------------------------------------
    CREATE OR REPLACE FUNCTION public.refresh_daily_rollups(
      p_tenant_id UUID,
      p_date_from DATE,
      p_date_to   DATE
    )
    RETURNS VOID
    LANGUAGE SQL
    SECURITY DEFINER
    SET search_path = public
    AS $$
      INSERT INTO public.daily_rollups (
        tenant_id, channel, date,
        total_spend, total_impressions, total_clicks,
        total_conversions, total_conv_value, campaign_count,
        updated_at
      )
      SELECT
        p_tenant_id,
        channel,
        date,
        COALESCE(SUM(spend), 0),
        COALESCE(SUM(impressions), 0),
        COALESCE(SUM(clicks), 0),
        COALESCE(SUM(conversions), 0),
        COALESCE(SUM(conversion_value), 0),
        COUNT(DISTINCT campaign_id),
        NOW()
      FROM public.campaign_metrics
      WHERE tenant_id = p_tenant_id
        AND date BETWEEN p_date_from AND p_date_to
      GROUP BY channel, date

      UNION ALL

      SELECT
        p_tenant_id,
        'all',
        date,
        COALESCE(SUM(spend), 0),
        COALESCE(SUM(impressions), 0),
        COALESCE(SUM(clicks), 0),
        COALESCE(SUM(conversions), 0),
        COALESCE(SUM(conversion_value), 0),
        COUNT(DISTINCT campaign_id),
        NOW()
      FROM public.campaign_metrics
      WHERE tenant_id = p_tenant_id
        AND date BETWEEN p_date_from AND p_date_to
      GROUP BY date

      ON CONFLICT (tenant_id, channel, date) DO UPDATE SET
        total_spend       = EXCLUDED.total_spend,
        total_impressions = EXCLUDED.total_impressions,
        total_clicks      = EXCLUDED.total_clicks,
        total_conversions = EXCLUDED.total_conversions,
        total_conv_value  = EXCLUDED.total_conv_value,
        campaign_count    = EXCLUDED.campaign_count,
        updated_at        = EXCLUDED.updated_at;
    $$;

    REVOKE EXECUTE ON FUNCTION public.refresh_daily_rollups(UUID, DATE, DATE) FROM PUBLIC, anon, authenticated;
    GRANT EXECUTE ON FUNCTION public.refresh_daily_rollups(UUID, DATE, DATE) TO service_role;

    COMMENT ON FUNCTION public.refresh_daily_rollups(UUID, DATE, DATE) IS 'Phase 2 D-09/D-10: UPSERT agregado em daily_rollups (per channel + cross-channel "all"). Chamado pelo N8N via PostgREST RPC após sync.';
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/0010_create_pipeline_functions.sql && grep -q "CREATE OR REPLACE FUNCTION public.read_vault_secret" supabase/migrations/0010_create_pipeline_functions.sql && grep -q "GRANT EXECUTE ON FUNCTION public.read_vault_secret(TEXT) TO service_role" supabase/migrations/0010_create_pipeline_functions.sql && grep -q "CREATE OR REPLACE FUNCTION public.refresh_daily_rollups" supabase/migrations/0010_create_pipeline_functions.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0010_create_pipeline_functions.sql` existe
    - Contém `CREATE OR REPLACE FUNCTION public.read_vault_secret(p_secret_name TEXT)`
    - Contém `SECURITY DEFINER` em ambas as funções
    - Contém `FROM vault.decrypted_secrets`
    - Contém `REVOKE EXECUTE ON FUNCTION public.read_vault_secret(TEXT) FROM PUBLIC, anon, authenticated`
    - Contém `GRANT EXECUTE ON FUNCTION public.read_vault_secret(TEXT) TO service_role`
    - Contém `CREATE OR REPLACE FUNCTION public.refresh_daily_rollups`
    - Contém `ON CONFLICT (tenant_id, channel, date) DO UPDATE SET`
    - Contém literal `'all'` (cross-channel rollup branch)
    - Contém `GRANT EXECUTE ON FUNCTION public.refresh_daily_rollups(UUID, DATE, DATE) TO service_role`
  </acceptance_criteria>
  <done>Migration 0010 com 2 funções SECURITY DEFINER, GRANTs corretos (service_role only), CTE UNION ALL + ON CONFLICT.</done>
</task>

<task type="auto">
  <name>Task 5 [BLOCKING]: supabase db push + reload PostgREST schema</name>
  <files>supabase/migrations/0006_create_ad_accounts.sql, supabase/migrations/0007_create_campaign_metrics.sql, supabase/migrations/0008_create_sync_jobs.sql, supabase/migrations/0009_create_daily_rollups.sql, supabase/migrations/0010_create_pipeline_functions.sql</files>
  <read_first>
    - .planning/STATE.md §"Infrastructure Provisioned" (project ref: rvkkvjitfddtbdpkupok)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md Pitfall 9 (PostgREST schema cache stale after migrations)
    - Todas as 5 migrations criadas em Tasks 1-4
  </read_first>
  <action>
    Aplicar todas as migrations criadas (0006–0010) ao projeto Supabase remoto `rvkkvjitfddtbdpkupok`.

    **Comando exato (Bash, non-TTY):**
    ```bash
    # SUPABASE_ACCESS_TOKEN deve estar no .env.local ou exportado. Se não estiver,
    # o executor deve PARAR e solicitar ao usuário via checkpoint.
    SUPABASE_ACCESS_TOKEN=${SUPABASE_ACCESS_TOKEN:?SUPABASE_ACCESS_TOKEN missing} \
      npx supabase db push --linked --include-all --debug
    ```

    Se `supabase` CLI não estiver linkado ao projeto remoto:
    ```bash
    npx supabase link --project-ref rvkkvjitfddtbdpkupok
    npx supabase db push --linked --include-all
    ```

    **Pós-push obrigatório (Pitfall 9):** Recarregar PostgREST schema cache via Supabase SQL Editor (https://supabase.com/dashboard/project/rvkkvjitfddtbdpkupok/sql):
    ```sql
    NOTIFY pgrst, 'reload schema';
    ```

    Ou aguardar até 60s pelo reload automático antes de prosseguir.

    **Verificação remota obrigatória (via supabase MCP ou SQL editor):**
    ```sql
    SELECT table_name FROM information_schema.tables
      WHERE table_schema = 'public'
        AND table_name IN ('ad_accounts','campaign_metrics','sync_jobs','daily_rollups')
      ORDER BY table_name;
    -- Esperado: 4 rows

    SELECT routine_name FROM information_schema.routines
      WHERE routine_schema = 'public'
        AND routine_name IN ('read_vault_secret','refresh_daily_rollups')
      ORDER BY routine_name;
    -- Esperado: 2 rows
    ```
  </action>
  <verify>
    <automated>npx supabase migration list --linked 2>&1 | grep -E "0006|0007|0008|0009|0010"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked --include-all` retorna exit code 0
    - `supabase migration list --linked` mostra 0006, 0007, 0008, 0009, 0010 como aplicadas (coluna "Remote")
    - Query SQL `SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('ad_accounts','campaign_metrics','sync_jobs','daily_rollups')` retorna `4`
    - Query SQL `SELECT count(*) FROM information_schema.routines WHERE routine_schema='public' AND routine_name IN ('read_vault_secret','refresh_daily_rollups')` retorna `2`
    - NOTIFY pgrst executado OU 60s de espera para reload automático
  </acceptance_criteria>
  <done>5 migrations aplicadas em produção (rvkkvjitfddtbdpkupok), 4 tabelas e 2 funções existem em information_schema, PostgREST schema cache recarregado.</done>
</task>

<task type="auto">
  <name>Task 6: Regenerar types/database.types.ts e preencher os it.todo() da Plan 01</name>
  <files>types/database.types.ts, tests/unit/campaign-metrics-schema.test.ts, tests/unit/daily-rollups-schema.test.ts, tests/integration/sync-jobs-rls.test.ts, tests/integration/vault-rpc.test.ts</files>
  <read_first>
    - types/database.types.ts (formato atual gerado pelo Supabase CLI)
    - tests/unit/campaign-metrics-schema.test.ts (Plan 01 Task 1 — it.todo a preencher)
    - tests/unit/daily-rollups-schema.test.ts (Plan 01 Task 2)
    - tests/integration/sync-jobs-rls.test.ts (Plan 01 Task 3)
    - tests/integration/vault-rpc.test.ts (Plan 01 Task 4)
    - tests/setup.ts (env loading)
  </read_first>
  <action>
    **Passo 1 — Regenerar types:**
    ```bash
    SUPABASE_ACCESS_TOKEN=${SUPABASE_ACCESS_TOKEN:?missing} \
      npx supabase gen types typescript --project-id rvkkvjitfddtbdpkupok --schema public > types/database.types.ts
    ```
    Verificar que `types/database.types.ts` agora contém entradas para `ad_accounts`, `campaign_metrics`, `sync_jobs`, `daily_rollups` em `Database['public']['Tables']`.

    **Passo 2 — Preencher it.todo() em `tests/unit/campaign-metrics-schema.test.ts`:**
    Substituir cada `it.todo(...)` por `it(...)` com lógica real usando `@supabase/supabase-js` createClient com `SUPABASE_TEST_URL` + `SUPABASE_TEST_SERVICE_KEY`. Exemplos exatos:

    - **Test 1 (default '7d_click'):**
      ```typescript
      it("attribution_window NOT NULL com DEFAULT '7d_click' — INSERT sem coluna grava '7d_click'", async () => {
        const { createClient } = await import('@supabase/supabase-js')
        const supa = createClient(process.env.SUPABASE_TEST_URL!, process.env.SUPABASE_TEST_SERVICE_KEY!)
        // criar tenant de teste primeiro
        const { data: tenant } = await supa.from('tenants').insert({ name: 'test-attr-window', slug: `test-attr-${Date.now()}` }).select().single()
        const { data, error } = await supa.from('campaign_metrics').insert({
          tenant_id: tenant!.id, campaign_id: 'c1', campaign_name: 'C1',
          channel: 'google_ads', date: '2026-01-01'
        }).select('attribution_window').single()
        expect(error).toBeNull()
        expect(data?.attribution_window).toBe('7d_click')
        await supa.from('tenants').delete().eq('id', tenant!.id) // cleanup via CASCADE
      })
      ```

    - **Test 2 (CHECK channel):**
      ```typescript
      it("channel CHECK rejeita valores fora de ('google_ads','meta_ads') com SQLSTATE 23514", async () => {
        const { createClient } = await import('@supabase/supabase-js')
        const supa = createClient(process.env.SUPABASE_TEST_URL!, process.env.SUPABASE_TEST_SERVICE_KEY!)
        const { data: tenant } = await supa.from('tenants').insert({ name: 'test-check', slug: `test-check-${Date.now()}` }).select().single()
        const { error } = await supa.from('campaign_metrics').insert({
          tenant_id: tenant!.id, campaign_id: 'c2', campaign_name: 'C2',
          channel: 'tiktok_ads' as any, date: '2026-01-01'
        })
        expect(error).not.toBeNull()
        expect(error?.code).toBe('23514')
        await supa.from('tenants').delete().eq('id', tenant!.id)
      })
      ```

    - **Test 3 (UNIQUE):**
      ```typescript
      it('UNIQUE(tenant_id, campaign_id, channel, date) — inserir duplicata retorna SQLSTATE 23505', async () => {
        const { createClient } = await import('@supabase/supabase-js')
        const supa = createClient(process.env.SUPABASE_TEST_URL!, process.env.SUPABASE_TEST_SERVICE_KEY!)
        const { data: tenant } = await supa.from('tenants').insert({ name: 'test-uniq', slug: `test-uniq-${Date.now()}` }).select().single()
        const row = { tenant_id: tenant!.id, campaign_id: 'c3', campaign_name: 'C3', channel: 'google_ads', date: '2026-01-01' }
        await supa.from('campaign_metrics').insert(row)
        const { error } = await supa.from('campaign_metrics').insert(row)
        expect(error?.code).toBe('23505')
        await supa.from('tenants').delete().eq('id', tenant!.id)
      })
      ```

    - **Test 4 (defaults to 0):** SELECT após INSERT mínimo e assert `impressions === 0`, `clicks === 0`, `spend === '0.00'`, `conversions === '0.0000'`, `conversion_value === '0.00'`.

    - **Test 5 (CASCADE):** INSERT row, DELETE tenant, SELECT campaign_metrics WHERE tenant_id deve retornar 0 rows.

    **Passo 3 — Preencher it.todo() em `tests/unit/daily-rollups-schema.test.ts`:** mesmo padrão (testar UNIQUE, channel CHECK incluindo 'all', defaults, CASCADE).

    **Passo 4 — Preencher it.todo() em `tests/integration/sync-jobs-rls.test.ts`:** criar 2 tenants A e B, criar 2 usuários (tenant_admin de A e B), gerar JWTs via `supa.auth.admin.generateLink` ou usar `signInWithPassword`, criar sync_job para tenant A via service_role, e:
    - assert tenant_admin de B vê 0 rows ao SELECT
    - assert service_role (super_admin equivalente) vê todos
    - assert anon retorna 401 ou 0 rows
    - INSERT com status='pending' deve falhar com 23514

    **Passo 5 — Preencher it.todo() em `tests/integration/vault-rpc.test.ts`:**
    - Inserir um secret via `vault.create_secret('test_value', 'test_vault_rpc_secret', 'integration test')` (chamando via service_role)
    - Chamar `rpc('read_vault_secret', { p_secret_name: 'test_vault_rpc_secret' })` com service_role — assert retorna 'test_value'
    - Chamar mesmo RPC com `createClient(URL, ANON_KEY)` — assert error.code = '42883' (function does not exist no schema cache do anon) OU error.message contém 'permission denied'
    - Chamar com tenant_admin authenticated — assert mesma rejeição

    **Passo 6 — Confirmar suite verde:**
    ```bash
    # Garantir que .env.test.local existe com SUPABASE_TEST_URL e SUPABASE_TEST_SERVICE_KEY
    # apontando para rvkkvjitfddtbdpkupok ou staging schema. Se não existir, o executor cria
    # baseado em .env.local com prefixos test-.
    npm test
    ```
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `types/database.types.ts` regenerado e contém `campaign_metrics:` em `Tables`
    - `types/database.types.ts` contém `ad_accounts:`, `sync_jobs:`, `daily_rollups:` em `Tables`
    - `types/database.types.ts` contém `read_vault_secret:` ou `refresh_daily_rollups:` em `Functions` (verificável via grep)
    - Nenhum arquivo de teste em `tests/unit/` ou `tests/integration/` ainda contém `it.todo(` em describes referentes a campaign_metrics/daily_rollups/sync_jobs/vault — todos foram substituídos por `it(...)` com lógica real
    - `grep -rE "it\\.todo\\(" tests/unit/campaign-metrics-schema.test.ts tests/unit/daily-rollups-schema.test.ts tests/integration/sync-jobs-rls.test.ts tests/integration/vault-rpc.test.ts` retorna ZERO matches (todos preenchidos)
    - `npm test` sai com código 0 (com SUPABASE_TEST_URL configurado)
    - Mínimo de 18 testes reais (não-todos) somando os 4 arquivos
  </acceptance_criteria>
  <done>types regenerados, 4 arquivos de teste preenchidos, `npm test` verde com staging schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Dashboard (Super Admin) → ad_accounts table | Super Admin manualmente insere tokens via Dashboard; vault_secret_id criado via SQL `vault.create_secret()` |
| N8N (service_role) → public.read_vault_secret | service_role bypassa RLS; única role autorizada a chamar a função |
| N8N (service_role) → vault.decrypted_secrets (indireto) | Acesso encapsulado via SECURITY DEFINER function; vault não exposto via PostgREST |
| authenticated/anon → tabelas data-pipeline | RLS bloqueia escritas; SELECT apenas no próprio tenant via (SELECT public.get_tenant_id()) |

## STRIDE Threat Register (ASVS L1: V2, V4, V6)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | campaign_metrics: cross-tenant insert via authenticated | mitigate | RLS WITH CHECK em policy super_admin_all bloqueia INSERT; tenant_select é FOR SELECT only — authenticated não pode INSERT (sem policy FOR INSERT criada) |
| T-2-02 | Information Disclosure | sync_jobs.error_message exposto a tenant errado | mitigate | RLS tenant_select restringe a `tenant_id = (SELECT public.get_tenant_id())`; cross-tenant SELECT retorna 0 rows. Validado por tests/integration/sync-jobs-rls.test.ts |
| T-2-03 | Elevation of Privilege | read_vault_secret callable via authenticated/anon (vault leak) | mitigate | `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated; GRANT EXECUTE ... TO service_role;` (V4 Access Control). Validado por tests/integration/vault-rpc.test.ts |
| T-2-04 | Information Disclosure | vault.decrypted_secrets exposto via PostgREST se schema "vault" estiver published | accept | Per Supabase managed, schema `vault` NÃO está exposto a PostgREST por padrão. SET search_path inclui vault apenas dentro do SECURITY DEFINER context, não no role do caller. |
| T-2-05 | Tampering | refresh_daily_rollups callable por authenticated → spam/DoS | mitigate | REVOKE EXECUTE FROM PUBLIC, anon, authenticated; GRANT to service_role only |
| T-2-06 | Repudiation | sync_jobs sem trace de quem causou o sync | accept | service_role identifica origem N8N; created_at + started_at + completed_at + status fornecem trail suficiente para v1 (1-3 tenants internos) |

</threat_model>

<verification>
- `supabase migration list --linked` mostra 0006, 0007, 0008, 0009, 0010 aplicadas em Remote
- `npm test` passa com exit code 0 (todos os 18+ testes reais verdes contra staging)
- `grep -r "it\\.todo(" tests/unit/campaign-metrics-schema.test.ts tests/integration/sync-jobs-rls.test.ts` retorna 0 matches
- `grep "campaign_metrics:" types/database.types.ts` retorna pelo menos 1 match
- Query manual `SELECT has_function_privilege('service_role', 'public.read_vault_secret(text)', 'execute')` retorna `true`
- Query manual `SELECT has_function_privilege('authenticated', 'public.read_vault_secret(text)', 'execute')` retorna `false`
</verification>

<success_criteria>
- 4 tabelas existem em `rvkkvjitfddtbdpkupok` schema public: ad_accounts, campaign_metrics, sync_jobs, daily_rollups
- 2 funções existem: read_vault_secret, refresh_daily_rollups
- Todas têm RLS habilitado + policies super_admin_all + tenant_select (FOR SELECT)
- Funções têm GRANT EXECUTE TO service_role e REVOKE FROM PUBLIC/anon/authenticated
- types/database.types.ts regenerado e commitado
- Wave 0 scaffolds preenchidos: 18+ testes reais passando
- PostgREST schema cache recarregado (manualmente via NOTIFY pgrst OU automaticamente após 60s)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-02-SUMMARY.md` documenting:
- Confirmação de que `supabase migration list --linked` mostra todas as 5 migrations aplicadas
- Resultado de `npm test` (count de testes passing)
- Snippet do diff de `types/database.types.ts` mostrando as 4 novas tabelas
- Qualquer ajuste feito ao SQL durante o push (ex: extensão pgsodium não habilitada — passos manuais necessários)
- Próximo passo: Plans 03 e 04 (workflows N8N) podem rodar em paralelo (Wave 2)
</output>

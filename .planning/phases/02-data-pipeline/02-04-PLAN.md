---
phase: 02-data-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - n8n-workflows/meta-ads-sync.json
autonomous: false
requirements:
  - SYNC-02
  - SYNC-04
  - SYNC-05
  - SYNC-06
user_setup:
  - service: meta-ads
    why: "Sync workflow precisa de System User access_token (long-lived, sem expiry por tempo) por tenant"
    env_vars:
      - name: META_SYSTEM_USER_TOKEN
        source: "Meta Business Manager → Business Settings → System Users → Generate Token (com permissão ads_read no Ad Account de cada tenant)"
    dashboard_config:
      - task: "Inserir System User token de cada tenant no Supabase Vault: `SELECT vault.create_secret('<token>', 'tenant_<slug>_meta_system_token', 'Meta System User token');` — copiar UUID retornado para ad_accounts.vault_secret_id"
        location: "Supabase Dashboard → SQL Editor"
      - task: "Confirmar que System User tem permissão 'ads_read' (mínimo) em cada Ad Account dos tenants v1"
        location: "https://business.facebook.com → Business Settings → System Users → Assigned Assets"
      - task: "Importar n8n-workflows/meta-ads-sync.json via N8N UI → atribuir credentials → Activate (após confirmar tokens)"
        location: "https://evo.wrdigitalgroup.com.br → Workflows"

must_haves:
  truths:
    - "Workflow JSON 'Meta Ads Sync' existe em n8n-workflows/meta-ads-sync.json e pode ser importado via N8N UI"
    - "Workflow tem Schedule Trigger configurado com cron '0 */6 * * *' (a cada 6 horas)"
    - "Workflow tem uma constante META_ADS_API_VERSION = 'v24.0' em UM ÚNICO Set node — TODA chamada à Meta Graph API referencia essa variável (SYNC-06)"
    - "Workflow não contém credenciais em texto plano — todos os tokens vêm de Supabase Vault via read_vault_secret RPC"
    - "Workflow itera sobre ad_accounts WHERE channel='meta_ads' AND active=true (loop multi-tenant — D-12)"
    - "Para cada tenant: ler System User token via /rpc/read_vault_secret → GET /v24.0/{ad_account_id}/insights (level=campaign, time_increment=1) → seguir paginação cursor-based → upsert em campaign_metrics → chamar /rpc/refresh_daily_rollups → gravar em sync_jobs"
    - "Backfill: workflow detecta primeira execução (ausência de sync_jobs status=success) e usa date_from = today - 90d; incremental usa today - 2d (D-16, D-17)"
    - "actions/action_values são parseados para extrair action_type='purchase' (conversions e conversion_value)"
    - "attribution_window é '7d_click' literal em todo upsert (SYNC-05, Pitfall 3: 7d_view foi removido por Meta em jan/2026)"
    - "Falha de um tenant grava sync_jobs.status='failed' + error_message e continua para o próximo (D-13 item 4)"
    - "Paginação Meta segue paging.next até retornar sem next (Pitfall 10)"
  artifacts:
    - path: "n8n-workflows/meta-ads-sync.json"
      provides: "Workflow N8N exportável (JSON) — Meta Ads multi-tenant sync"
      contains: "\"META_ADS_API_VERSION\""
      min_lines: 200
  key_links:
    - from: "n8n-workflows/meta-ads-sync.json"
      to: "graph.facebook.com/{{META_ADS_API_VERSION}}/{ad_account_id}/insights"
      via: "HTTP Request GET com fields, time_range, time_increment=1"
      pattern: "graph\\.facebook\\.com"
    - from: "n8n-workflows/meta-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/campaign_metrics?on_conflict=tenant_id,campaign_id,channel,date"
      via: "HTTP Request POST com Prefer: resolution=merge-duplicates"
      pattern: "on_conflict=tenant_id,campaign_id,channel,date"
    - from: "n8n-workflows/meta-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/rpc/refresh_daily_rollups"
      via: "HTTP Request POST após upsert"
      pattern: "rpc/refresh_daily_rollups"
    - from: "n8n-workflows/meta-ads-sync.json"
      to: "{SUPABASE_URL}/rest/v1/sync_jobs"
      via: "POST status=running no início, PATCH status=success/failed no fim"
      pattern: "sync_jobs"
---

<objective>
Criar o arquivo JSON do workflow N8N "Meta Ads Sync" — multi-tenant, agendado a cada 6 horas, com System User token lookup via Supabase Vault, chamadas à Meta Marketing API v24.0 Insights endpoint, paginação cursor-based, upserts em campaign_metrics via PostgREST, e logging em sync_jobs. Cobre SYNC-02, SYNC-04 (writes), SYNC-05 (attribution_window), SYNC-06 (API version como constante).

Purpose: Esta é a pipeline real que popula campaign_metrics com dados do Meta Ads. NÃO há blocker como o Google Ads — System User token já é gerável imediatamente via Business Manager (per STATE.md, Meta Business Manager + System User estão configurados, falta confirmar permissões `ads_read` por tenant).
Output: Um arquivo JSON em `n8n-workflows/meta-ads-sync.json` que pode ser importado via N8N UI e ativado assim que os tokens de cada tenant estiverem no Vault.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@supabase/migrations/0007_create_campaign_metrics.sql
@supabase/migrations/0010_create_pipeline_functions.sql

<interfaces>
<!-- Endpoints e contratos exatos que o workflow chama -->

Supabase project: `rvkkvjitfddtbdpkupok` (sa-east-1)
Supabase URL pattern: `https://rvkkvjitfddtbdpkupok.supabase.co`
Auth header (N8N Credential, NÃO literal):
```
apikey: {{SUPABASE_SERVICE_ROLE_KEY}}
Authorization: Bearer {{SUPABASE_SERVICE_ROLE_KEY}}
```

ad_accounts schema (read-only, channel='meta_ads'):
```
GET /rest/v1/ad_accounts?channel=eq.meta_ads&active=eq.true&select=id,tenant_id,account_id,vault_secret_id,tenants(slug)
```

read_vault_secret RPC (criado em Plan 02 Task 4):
```
POST /rest/v1/rpc/read_vault_secret
Body: { "p_secret_name": "tenant_<slug>_meta_system_token" }
Returns: text (decrypted System User token) OR null
```

Meta Marketing API Insights endpoint (level=campaign):
```
GET https://graph.facebook.com/{{META_ADS_API_VERSION}}/{ad_account_id}/insights
Query params:
  level=campaign
  fields=campaign_id,campaign_name,impressions,clicks,spend,actions,action_values,adset_id,date_start,date_stop
  time_range={"since":"YYYY-MM-DD","until":"YYYY-MM-DD"}
  time_increment=1
  action_attribution_windows=["7d_click","1d_view"]   ← Pitfall 3: 7d_view removido em jan/2026
  limit=100
  access_token={{token}}

Response (data array):
{
  "data": [
    {
      "campaign_id": "123",
      "campaign_name": "C1",
      "impressions": "15000",
      "clicks": "450",
      "spend": "125.50",
      "actions": [{"action_type":"purchase","value":"45"}, ...],
      "action_values": [{"action_type":"purchase","value":"12500.00"}, ...],
      "adset_id": "987",
      "date_start": "2026-05-15",
      "date_stop": "2026-05-15"
    }
  ],
  "paging": {
    "cursors": { "before": "...", "after": "..." },
    "next": "https://graph.facebook.com/...&after=..."  ← only when more pages exist
  }
}
```

Nota crítica: `ad_account_id` em ad_accounts já deve incluir prefixo `act_` (ex: `act_1234567890`). Se não tiver, o workflow PREPENDS `act_`.

campaign_metrics upsert: idêntico ao google-ads-sync (channel='meta_ads').

refresh_daily_rollups: idêntico.

sync_jobs lifecycle: idêntico.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar n8n-workflows/meta-ads-sync.json (estrutura completa do workflow)</name>
  <files>n8n-workflows/meta-ads-sync.json</files>
  <read_first>
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 3" (Meta Insights GET com fields exatos)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 9" (cron '0 */6 * * *')
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Padrão 10" (detecção first sync)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md §"Code Examples — Meta Ads" (parser actions array)
    - .planning/phases/02-data-pipeline/02-RESEARCH.md Pitfalls 2, 3, 10 (v24 mínimo, attribution window, paginação cursor)
    - n8n-workflows/google-ads-sync.json (Plan 03 — pattern de Set Constants, sync_jobs lifecycle, error handling)
  </read_first>
  <action>
    Criar `n8n-workflows/meta-ads-sync.json` seguindo o MESMO padrão estrutural do `google-ads-sync.json` (Plan 03), substituindo os nodes específicos da API.

    **Estrutura mínima obrigatória de nodes (em ordem):**

    1. **Schedule Trigger** node — cron `0 */6 * * *` (a cada 6 horas)
    2. **Set (Constants)** node — `name="Set Constants"`:
       - `META_ADS_API_VERSION` = `'v24.0'` (string literal — SYNC-06)
       - `BACKFILL_DAYS` = 90
       - `INCREMENTAL_DAYS` = 2
       - `SUPABASE_URL` = `https://rvkkvjitfddtbdpkupok.supabase.co`
       - `ATTRIBUTION_WINDOWS_JSON` = `'["7d_click","1d_view"]'` (Pitfall 3)
    3. **HTTP Request** node — `name="List active Meta Ads accounts"`:
       - Method: GET
       - URL: `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/ad_accounts?channel=eq.meta_ads&active=eq.true&select=id,tenant_id,account_id,vault_secret_id,tenants(slug)`
       - Auth: `Supabase Service Role` credential
    4. **Split In Batches** node — `name="Loop tenants"` — batchSize: 1
    5. **HTTP Request** node — `name="Check first sync"`:
       - GET `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs?tenant_id=eq.{{$json.tenant_id}}&channel=eq.meta_ads&status=eq.success&select=id&limit=1`
    6. **Code** node — `name="Compute date range"`:
       ```javascript
       const today = new Date();
       const prevJobs = $('Check first sync').item.json;
       const isFirstSync = !Array.isArray(prevJobs) || prevJobs.length === 0;
       const days = isFirstSync ? $('Set Constants').first().json.BACKFILL_DAYS : $('Set Constants').first().json.INCREMENTAL_DAYS;
       const from = new Date(today); from.setDate(today.getDate() - days);
       // Meta requer 'act_' prefix em ad_account_id
       const rawAcc = $('Loop tenants').item.json.account_id;
       const ad_account_id = String(rawAcc).startsWith('act_') ? rawAcc : `act_${rawAcc}`;
       return [{ json: {
         tenant_id: $('Loop tenants').item.json.tenant_id,
         ad_account_id,
         tenant_slug: $('Loop tenants').item.json.tenants?.slug,
         date_from: from.toISOString().split('T')[0],
         date_to: today.toISOString().split('T')[0],
         is_first_sync: isFirstSync
       }}];
       ```
    7. **HTTP Request** node — `name="Open sync_job"`:
       - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs`
       - Header `Prefer: return=representation`
       - Body: `{ "tenant_id": "{{$json.tenant_id}}", "channel": "meta_ads", "status": "running", "date_from": "{{$json.date_from}}", "date_to": "{{$json.date_to}}" }`
    8. **HTTP Request** node — `name="Read vault secret"`:
       - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/rpc/read_vault_secret`
       - Body: `{ "p_secret_name": "tenant_{{$('Compute date range').item.json.tenant_slug}}_meta_system_token" }`
       - Response: string (System User access_token)
    9. **Set** node — `name="Init pagination cursor"`:
       - `next_url` = `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/...` ← inicializar como URL da primeira chamada Meta (computada via expressão):
       ```
       https://graph.facebook.com/{{$('Set Constants').item.json.META_ADS_API_VERSION}}/{{$('Compute date range').item.json.ad_account_id}}/insights?level=campaign&fields=campaign_id,campaign_name,impressions,clicks,spend,actions,action_values,adset_id,date_start,date_stop&time_range={"since":"{{$('Compute date range').item.json.date_from}}","until":"{{$('Compute date range').item.json.date_to}}"}&time_increment=1&action_attribution_windows={{$('Set Constants').item.json.ATTRIBUTION_WINDOWS_JSON}}&limit=100&access_token={{$('Read vault secret').item.json}}
       ```
       - `all_rows` = `[]` (array vazio)
    10. **Loop (IF + HTTP)** — paginar enquanto `next_url` existe (Pitfall 10):
        a. **HTTP Request** `name="Meta Insights paged"` — Method: GET, URL: `={{$('Init pagination cursor').item.json.next_url}}`
        b. **Code** `name="Accumulate page + extract next"`:
        ```javascript
        const resp = $input.first().json;
        const rows = $('Init pagination cursor').first().json.all_rows;
        for (const d of (resp.data || [])) {
          rows.push(d);
        }
        const next = resp.paging?.next || null;
        return [{ json: { all_rows: rows, next_url: next, has_more: !!next } }];
        ```
        c. **IF** `name="More pages?"` — true: loop back to (a); false: continue
    11. **Code** node — `name="Map Meta results"` (verbatim de RESEARCH.md §Code Examples, parser actions):
        ```javascript
        function extractActionValue(actionsArray, actionType) {
          if (!Array.isArray(actionsArray)) return 0;
          const action = actionsArray.find(a => a.action_type === actionType);
          return action ? parseFloat(action.value) : 0;
        }
        const tenantId = $('Compute date range').item.json.tenant_id;
        const all = $('Accumulate page + extract next').first().json.all_rows;
        return all.map(d => ({ json: {
          tenant_id: tenantId,
          campaign_id: d.campaign_id,
          campaign_name: d.campaign_name,
          channel: 'meta_ads',
          date: d.date_start,
          impressions: parseInt(d.impressions || '0'),
          clicks: parseInt(d.clicks || '0'),
          spend: parseFloat(d.spend || '0'),
          conversions: extractActionValue(d.actions, 'purchase'),
          conversion_value: extractActionValue(d.action_values, 'purchase'),
          status: null,
          ad_group_id: d.adset_id || null,
          attribution_window: '7d_click',
          synced_at: new Date().toISOString()
        }}));
        ```
    12. **HTTP Request** node — `name="Upsert campaign_metrics"`:
        - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/campaign_metrics?on_conflict=tenant_id,campaign_id,channel,date`
        - Headers: service_role + `Prefer: resolution=merge-duplicates`
        - Body: `={{ $('Map Meta results').all().map(i => i.json) }}`
    13. **HTTP Request** node — `name="Refresh daily_rollups"`:
        - POST `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/rpc/refresh_daily_rollups`
        - Body: `{ "p_tenant_id": "{{$('Compute date range').item.json.tenant_id}}", "p_date_from": "{{$('Compute date range').item.json.date_from}}", "p_date_to": "{{$('Compute date range').item.json.date_to}}" }`
    14. **HTTP Request** node — `name="Close sync_job (success)"`:
        - PATCH `={{$('Set Constants').item.json.SUPABASE_URL}}/rest/v1/sync_jobs?id=eq.{{$('Open sync_job').item.json[0].id}}`
        - Body: `{ "status": "success", "completed_at": "{{$now.toISO()}}", "records_synced": {{$('Map Meta results').all().length}} }`
    15. **Error path** — em caso de erro nos nodes 8-13:
        - PATCH sync_jobs com `{ "status": "failed", "completed_at": "{{$now.toISO()}}", "error_message": "{{$json.error.message}}" }`
        - Continue → próximo tenant via Split In Batches

    **Conventions obrigatórias:**
    - TODA URL de Meta API DEVE usar `={{$('Set Constants').item.json.META_ADS_API_VERSION}}` — NUNCA `v24.0` hardcoded em outro lugar (SYNC-06)
    - `ATTRIBUTION_WINDOWS_JSON` definido UMA vez em Set Constants — referenciado por expressão
    - Credentials referenciadas via `$credentials` por nome — NUNCA literais
    - `"active": false` no JSON exportado — operador ativa após importar e atribuir credentials
    - Topo do arquivo: `"name": "Meta Ads Sync"`
    - Sanitizar error.message em PATCH sync_jobs (regex remove `access_token=[A-Za-z0-9._-]+`) — não vazar System User token em error_message

    **Estrutura mínima do arquivo:**
    ```json
    {
      "name": "Meta Ads Sync",
      "meta": { "templateCredsSetupCompleted": false },
      "active": false,
      "nodes": [ /* 14+ nodes */ ],
      "connections": { /* edges em ordem topológica */ },
      "settings": { "executionOrder": "v1" },
      "versionId": "1"
    }
    ```
  </action>
  <verify>
    <automated>test -f n8n-workflows/meta-ads-sync.json && node -e "const w=require('./n8n-workflows/meta-ads-sync.json'); if(!w.nodes||w.nodes.length<13)throw new Error('expected >=13 nodes, got '+(w.nodes?.length||0)); const json=JSON.stringify(w); if(!json.includes('META_ADS_API_VERSION'))throw new Error('missing API version constant'); if(json.match(/['\"]v24\\.0['\"]/g)?.length!==1)throw new Error('v24.0 must appear exactly once (in Set Constants), not hardcoded elsewhere — SYNC-06'); if(!json.includes('on_conflict=tenant_id,campaign_id,channel,date'))throw new Error('missing PostgREST upsert URL'); if(!json.includes('graph.facebook.com'))throw new Error('missing Meta Graph endpoint'); if(!json.includes('refresh_daily_rollups'))throw new Error('missing rpc call'); if(!json.includes('7d_click'))throw new Error('missing attribution_window default'); if(!json.includes('action_attribution_windows'))throw new Error('missing action_attribution_windows param'); console.log('OK: '+w.nodes.length+' nodes');"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `n8n-workflows/meta-ads-sync.json` existe e é JSON válido
    - JSON tem chave `"name": "Meta Ads Sync"`
    - JSON tem chave `"active": false`
    - `nodes` array tem ≥ 13 entradas
    - Existe exatamente UMA ocorrência da string literal `"v24.0"` no arquivo inteiro — dentro do node "Set Constants" — TODAS as outras chamadas referenciam via expressão `{{$('Set Constants')...META_ADS_API_VERSION}}` (SYNC-06)
    - Existe `"0 */6 * * *"` no Schedule Trigger
    - Existe node "Set Constants" com `META_ADS_API_VERSION`, `BACKFILL_DAYS=90`, `INCREMENTAL_DAYS=2`, `ATTRIBUTION_WINDOWS_JSON`
    - Existe URL contendo `graph.facebook.com` (Meta Graph API)
    - Existe URL contendo `on_conflict=tenant_id,campaign_id,channel,date`
    - Existe URL contendo `rpc/refresh_daily_rollups`
    - Existe URL contendo `rpc/read_vault_secret`
    - Existe expressão literal `extractActionValue(d.actions, 'purchase')` no Code node "Map Meta results"
    - Existe string literal `'7d_click'` no Code node "Map Meta results" (NÃO `7d_view` — Pitfall 3)
    - Existe expressão `String(rawAcc).startsWith('act_')` (prefix `act_` handling)
    - Existe lógica de paginação cursor: nó IF "More pages?" + Code "Accumulate page + extract next"
    - JSON inclui PATCH sync_jobs com `status: success` E `status: failed` (sucesso e erro paths)
    - NENHUMA ocorrência de `access_token=EAA` ou similar literal (todos os tokens via expressão de Vault)
    - NENHUMA ocorrência da string `7d_view` (Pitfall 3: removido por Meta em jan/2026)
  </acceptance_criteria>
  <done>Workflow JSON completo (≥13 nodes), API version v24.0 como constante única, paginação cursor implementada, parsers de actions/action_values corretos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — importar workflow Meta no N8N e teste estrutural</name>
  <files>n8n-workflows/meta-ads-sync.json</files>
  <action>
    Importar manualmente o workflow Meta no N8N (https://evo.wrdigitalgroup.com.br) seguindo os passos em <how-to-verify>. Verificação HUMANA — Claude não tem acesso à UI do N8N nem aos System User tokens de cada tenant.
  </action>
  <verify>
    <automated>test -f n8n-workflows/meta-ads-sync.json</automated>
  </verify>
  <done>Usuário digitou "aprovado" confirmando que o JSON importou no N8N, schedule trigger mostra "0 */6 * * *", constante META_ADS_API_VERSION está em Set Constants, paginação cursor aparece como IF + Code accumulator, e nenhum literal "7d_view" foi detectado.</done>
  <what-built>
    Workflow JSON `n8n-workflows/meta-ads-sync.json` criado na Task 1.
  </what-built>
  <how-to-verify>
    1. Abrir N8N: https://evo.wrdigitalgroup.com.br
    2. Workflows → Import from File → selecionar `n8n-workflows/meta-ads-sync.json`
    3. Verificar que TODOS os 13+ nodes foram importados sem erro
    4. Verificar Schedule Trigger mostra `0 */6 * * *` (a cada 6 horas)
    5. Verificar node "Set Constants" mostra `META_ADS_API_VERSION = v24.0`
    6. NÃO ativar o workflow ainda — manter `active: false`
    7. Atribuir N8N Credential `Supabase Service Role` aos nodes HTTP que falam com Supabase (reusar da Plan 03)
    8. **Pré-requisitos antes de ativar (NÃO bloqueia esta task):**
       a. Gerar System User token em https://business.facebook.com com permissão `ads_read` em cada Ad Account
       b. Inserir token no Supabase Vault:
          ```sql
          SELECT vault.create_secret('<TOKEN>', 'tenant_<slug>_meta_system_token', 'Meta System User token');
          ```
          Anotar UUID retornado.
       c. Inserir linha em ad_accounts:
          ```sql
          INSERT INTO public.ad_accounts (tenant_id, channel, account_id, vault_secret_id, active)
          VALUES ('<tenant_uuid>', 'meta_ads', 'act_<ad_account_id>', '<vault_secret_uuid>', true);
          ```
    9. **Teste estrutural (mock):** Clicar em "Execute Workflow":
       - Se nenhum ad_account inserido → "List active Meta Ads accounts" retorna [] → loop não executa → ok
       - Se ad_account inserido mas token inválido → falha esperada em "Meta Insights paged" com error.message útil → confirma estrutura correta

    **Aprovar se:**
    - JSON importou sem corromper
    - Schedule trigger `0 */6 * * *` correto
    - Constante META_ADS_API_VERSION visível e referenciada
    - Loop de paginação aparece corretamente (IF + accumulator)
    - Erros (se ocorrerem) são em pontos esperados (token missing/invalid)

    **Rejeitar se:**
    - Erros de parsing de JSON
    - `v24.0` aparece hardcoded fora de Set Constants
    - `7d_view` aparece em qualquer lugar (Pitfall 3 violado)
    - Credentials estão como literal no JSON
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o workflow Meta está estruturalmente correto, OU descreva o problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N8N (service_role) → Supabase PostgREST | service_role bypassa RLS — chave em N8N Credential criptografada |
| N8N → Meta Graph API | System User token (long-lived) via Authorization Bearer / access_token query param — TLS obrigatório |
| Supabase Vault → N8N (via rpc) | System User token decriptado retornado; N8N não persiste, usa apenas durante execução |

## STRIDE Threat Register (ASVS L1: V2 Auth, V4 Access, V6 Crypto)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04-01 | Information Disclosure | System User token (long-lived, sem expiry) em logs N8N ou error_message | mitigate | `error_message` sanitizado por regex `replace(/access_token=[A-Za-z0-9._-]+/g, 'access_token=***')` antes de PATCH sync_jobs. N8N_LOG_LEVEL=info na VPS. |
| T-2-04-02 | Information Disclosure | System User token em URL (query param) interceptado em proxy | accept | Comunicação via HTTPS apenas (TLS 1.3 default em graph.facebook.com). Meta recomenda Authorization header como alternativa — pode ser ajustado pós-v1 se logs de proxy forem preocupação. |
| T-2-04-03 | Tampering | Workflow grava métricas no tenant_id errado | mitigate | tenant_id lido de ad_accounts.tenant_id (por linha do loop), não inferido. Coluna passada explicitamente em cada upsert row. |
| T-2-04-04 | Elevation of Privilege | Token tem `ads_read` mas atacante consegue obter token e usar fora do workflow | mitigate | Tokens armazenados no Supabase Vault (AES-256). Acesso apenas via SECURITY DEFINER + service_role. Atacante precisaria de service_role para extrair — mesma superfície de qualquer outra credencial. |
| T-2-04-05 | Denial of Service | Workflow excede rate limit Meta (5 req síncronas/min por ad account) | mitigate | Schedule a cada 6h × 1-3 tenants × 1 endpoint chamado = 4-12 req/dia por ad_account. Bem abaixo do limite. Paginação adiciona ~5 páginas por backfill — ainda dentro do limite. |
| T-2-04-06 | Information Disclosure | Conversion value de tenant A vazado para tenant B via shared response cache | accept | N8N não cacheia HTTP responses entre executions; cada loop iteration é isolado. |

</threat_model>

<verification>
- Arquivo `n8n-workflows/meta-ads-sync.json` é JSON válido
- Comando `node -e "const w=require('./n8n-workflows/meta-ads-sync.json'); console.log(w.nodes.length+' nodes, active='+w.active);"` retorna pelo menos 13 nodes e `active=false`
- `grep -c '"v24.0"' n8n-workflows/meta-ads-sync.json` retorna `1`
- `grep "0 \*/6 \* \* \*" n8n-workflows/meta-ads-sync.json` retorna ≥1 match
- `grep -c "7d_view" n8n-workflows/meta-ads-sync.json` retorna `0` (Pitfall 3)
- Human verification (Task 2) aprovada
- Sem blocker explícito — apenas pendente provisionar System User token por tenant (manual, não-crítico para entregar o JSON)
</verification>

<success_criteria>
- Workflow JSON importável via N8N UI
- Schedule Trigger cron `0 */6 * * *` configurado
- META_ADS_API_VERSION = 'v24.0' em Set Constants — referenciada por expressão em TODAS as URLs Meta
- Workflow itera sobre ad_accounts WHERE channel='meta_ads' AND active=true
- Para cada tenant: read_vault_secret → Meta Insights GET → paginar → upsert campaign_metrics → refresh_daily_rollups → PATCH sync_jobs
- Erro de um tenant: sync_jobs.status='failed' + continua para próximo
- Backfill 90d primeira execução, incremental 2d
- attribution_window = '7d_click' (nunca '7d_view' — Pitfall 3)
- Paginação cursor-based implementada (Pitfall 10)
- Prefix `act_` adicionado se ad_account_id não tiver
- Nenhum token em texto plano no JSON exportado
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-04-SUMMARY.md` documentando:
- Path do arquivo JSON criado
- Contagem de nodes
- Screenshot da N8N UI mostrando o workflow importado (Task 2 checkpoint)
- Lista de N8N Credentials utilizadas
- Status dos System User tokens por tenant (provisionados / pendentes)
- Próximo passo: Plan 05 (UI sync status) — Wave 3
</output>

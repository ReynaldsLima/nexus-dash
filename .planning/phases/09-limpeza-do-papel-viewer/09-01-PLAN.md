---
phase: 09-limpeza-do-papel-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/stores/tenant-store.tsx
  - proxy.ts
  - components/tenants/tenant-switcher.tsx
  - app/api/meta-ads/connect/route.ts
  - tests/middleware.test.ts
  - tests/unit/google-ads-connect-route.test.ts
  - tests/unit/insights-generate-route.test.ts
  - tests/unit/leads-status-route.test.ts
  - tests/unit/leads-get-route.test.ts
  - tests/unit/leads-chat-route.test.ts
autonomous: true
requirements: [AUTH-07]

must_haves:
  truths:
    - "O Role type em lib/stores/tenant-store.tsx não inclui 'viewer'"
    - "proxy.ts não contém nenhum branch condicional para 'viewer'"
    - "A prop role de tenant-switcher.tsx não lista 'viewer' na sua union type"
    - "O comentário em app/api/meta-ads/connect/route.ts não menciona 'viewer'"
    - "A suíte de testes passa com o sentinel 'invalid_role' no lugar de 'viewer' (nenhum teste assume 'viewer' como papel válido do sistema)"
    - "npx tsc --noEmit compila limpo (exceto os 2 erros pré-existentes não relacionados em tests/vault-rpc.test.ts)"
  artifacts:
    - path: "lib/stores/tenant-store.tsx"
      provides: "Role type sem 'viewer'"
      contains: "export type Role = 'super_admin' | 'tenant_admin' | 'agency' | 'none' | null"
    - path: "proxy.ts"
      provides: "AppMetadata.role sem 'viewer' e branch de redirect sem 'viewer'"
    - path: "components/tenants/tenant-switcher.tsx"
      provides: "prop role sem 'viewer'"
    - path: "app/api/meta-ads/connect/route.ts"
      provides: "comentário do RPC corrigido"
  key_links:
    - from: "proxy.ts branch de redirect por papel"
      to: "redirect para /${tenantSlug}/dashboard"
      via: "role === 'tenant_admin' (após remoção de || role === 'viewer')"
      pattern: "role === 'tenant_admin'.*tenantSlug"
---

<objective>
Remover toda referência ao papel morto `'viewer'` da camada de aplicação (tipos TypeScript, middleware, componente e testes), fechando AUTH-07. O papel `'viewer'` já é impossível de existir no banco desde a migration `0020` (Phase 5) e o valor foi mantido apenas como fallback de rollout-safety, cuja janela de deploy já fechou (ver STATE.md, decisão da Phase 05 Plan 04).

Purpose: Eliminar código morto e simplificar o `Role` type ANTES de a UI de gestão de usuários (Phase 10) ser construída sobre ele — evita construir a nova UI sobre um union type com um valor que nunca pode ocorrer.
Output: 10 arquivos existentes editados. Nenhuma rota, componente ou tabela nova. Nenhuma mudança de comportamento observável (uma stale JWT com `role: 'viewer'` — impossível de emitir hoje — passa a cair no branch else de `proxy.ts` e é redirecionada para `/login?error=no_membership` em vez de para o dashboard: postura estritamente mais restritiva, fail-closed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-limpeza-do-papel-viewer/09-CONTEXT.md

<decisoes_de_discricao_do_claude>
Resolvidas no planejamento (documentar na SUMMARY se algo mudar):

1. **`tests/integration/tenant-role-migration.test.ts` fica FORA do escopo.** É um teste de camada de banco cujo propósito é PROVAR que a CHECK constraint da migration 0020 rejeita `'viewer'`. Remover o literal `'viewer'` desse arquivo destruiria o próprio teste. NÃO tocar. (Confirma o critério de "camada de aplicação" do CONTEXT.md.)

2. **Duplicação do `Role` type: remoção mínima, NÃO consolidação.** `proxy.ts:5` (`AppMetadata.role`) e `tenant-switcher.tsx:13` (`TenantSwitcherProps.role`) mantêm cópias locais da union. A decisão é apenas remover `'viewer'` de cada cópia (diff mínimo), NÃO consolidar num import único de `lib/stores/tenant-store.tsx`. Rationale: a duplicação é um padrão pré-existente (não introduzido por esta phase), as três unions não são idênticas (`tenant-switcher` tem escape hatch `| string`), e consolidar expandiria o escopo de uma phase de limpeza de código morto sem necessidade. A remoção mínima já satisfaz os 4 success criteria do ROADMAP.

3. **`| string` em `tenant-switcher.tsx` fica.** É um escape hatch pré-existente; removê-lo quebraria callers e é fora de escopo. Só o literal `'viewer'` sai.

4. **Comentário obsoleto em `app/api/meta-ads/connect/route.ts:43` é corrigido** — parte natural da limpeza.

5. **`CLAUDE.md` NÃO é tocado.** A menção a "viewer" ali está na tabela de documentação do padrão RLS (project instructions auto-geradas), não é código de aplicação e não está na lista enumerada de AUTH-07 (tipos/middleware/componentes/testes).
</decisoes_de_discricao_do_claude>

<sentinel_para_testes>
Sentinel de "papel inválido/desconhecido" a ser usado em TODAS as substituições de teste: **`'invalid_role'`** (string literal exata, snake_case, entre aspas simples). Preserva a cobertura de "papel não reconhecido é rejeitado" sem reintroduzir a semântica de um papel morto do sistema.
</sentinel_para_testes>

<nota_sobre_tsc>
STATE.md documenta que `npx tsc --noEmit` tem 2 erros pré-existentes NÃO relacionados em `tests/vault-rpc.test.ts`. Esses 2 erros são esperados e NÃO devem ser corrigidos nesta phase — não são regressão. Qualquer erro NOVO de tsc é regressão desta phase e deve ser corrigido.
</nota_sobre_tsc>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover 'viewer' da camada de aplicação (tipos, middleware, componente, comentário)</name>
  <files>lib/stores/tenant-store.tsx, proxy.ts, components/tenants/tenant-switcher.tsx, app/api/meta-ads/connect/route.ts</files>
  <read_first>
    - lib/stores/tenant-store.tsx (fonte canônica do Role type)
    - proxy.ts (AppMetadata local + branch de redirect por papel)
    - components/tenants/tenant-switcher.tsx (prop role local)
    - app/api/meta-ads/connect/route.ts (comentário obsoleto na linha ~43)
  </read_first>
  <action>
    Fazer exatamente 4 edições (5 substituições), cada uma removendo o literal `'viewer'`:

    1. **`lib/stores/tenant-store.tsx`, linha 13** — trocar:
       DE:   `export type Role = 'super_admin' | 'tenant_admin' | 'viewer' | 'agency' | 'none' | null`
       PARA: `export type Role = 'super_admin' | 'tenant_admin' | 'agency' | 'none' | null`

    2. **`proxy.ts`** — duas substituições:
       (a) linha 5, dentro de `type AppMetadata`, trocar:
           DE:   `  role?: 'super_admin' | 'tenant_admin' | 'viewer' | 'agency' | 'none' | null`
           PARA: `  role?: 'super_admin' | 'tenant_admin' | 'agency' | 'none' | null`
       (b) linha 68, o branch de redirect por papel, trocar:
           DE:   `    } else if ((role === 'tenant_admin' || role === 'viewer') && tenantSlug) {`
           PARA: `    } else if (role === 'tenant_admin' && tenantSlug) {`
       Manter o corpo do branch (`url.pathname = \`/${tenantSlug}/dashboard\``) e todos os outros branches (`super_admin`, `agency`, `else`) inalterados.

    3. **`components/tenants/tenant-switcher.tsx`, linha 13** — dentro de `interface TenantSwitcherProps`, trocar:
       DE:   `  role: 'super_admin' | 'tenant_admin' | 'viewer' | 'agency' | string | null`
       PARA: `  role: 'super_admin' | 'tenant_admin' | 'agency' | string | null`
       (Manter o `| string`. A lógica do componente — `if (role !== 'super_admin' && role !== 'agency') return null` — já ignora viewer e não muda.)

    4. **`app/api/meta-ads/connect/route.ts`, linha ~43** — o comentário, trocar:
       DE:   `  // Uses get_user_role() RPC (returns 'super_admin' | 'tenant_admin' | 'viewer')`
       PARA: `  // Uses get_user_role() RPC (returns 'super_admin' | 'tenant_admin')`
       (Somente o comentário; a linha `const { data: role, error: roleErr } = await supabase.rpc('get_user_role')` abaixo não muda.)

    NÃO tocar em nenhum outro arquivo nesta task. NÃO consolidar as unions duplicadas num import único.
  </action>
  <verify>
    <automated>cd "c:/Users/ReinaldoLima/Desktop/Cowork/NEXUS-DASH" && ! grep -rn "viewer" lib/stores/tenant-store.tsx proxy.ts components/tenants/tenant-switcher.tsx app/api/meta-ads/connect/route.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "viewer" lib/stores/tenant-store.tsx proxy.ts components/tenants/tenant-switcher.tsx app/api/meta-ads/connect/route.ts` retorna ZERO linhas (exit code 1).
    - `grep -n "export type Role" lib/stores/tenant-store.tsx` mostra exatamente `export type Role = 'super_admin' | 'tenant_admin' | 'agency' | 'none' | null`.
    - `grep -n "role === 'tenant_admin' && tenantSlug" proxy.ts` retorna 1 linha (branch preservado, viewer removido).
    - `grep -n "role === 'tenant_admin'" proxy.ts` NÃO aparece junto com `role === 'viewer'` na mesma linha.
    - `npx tsc --noEmit` não introduz nenhum erro novo (os 2 erros pré-existentes em `tests/vault-rpc.test.ts` são aceitáveis; nenhum outro).
  </acceptance_criteria>
  <done>
    Os 4 arquivos de aplicação não contêm mais o literal `'viewer'`; o `Role` type e as duas cópias locais da union foram simplificados; o branch de redirect de `proxy.ts` trata `tenant_admin` sozinho; o comentário do RPC está corrigido; tsc sem erros novos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir 'viewer' por sentinel 'invalid_role' nos 6 arquivos de teste</name>
  <files>tests/middleware.test.ts, tests/unit/google-ads-connect-route.test.ts, tests/unit/insights-generate-route.test.ts, tests/unit/leads-status-route.test.ts, tests/unit/leads-get-route.test.ts, tests/unit/leads-chat-route.test.ts</files>
  <read_first>
    - tests/middleware.test.ts (linhas 43-47 e 56-64)
    - tests/unit/google-ads-connect-route.test.ts (linhas 59-60)
    - tests/unit/insights-generate-route.test.ts (linhas 81-82)
    - tests/unit/leads-status-route.test.ts (linhas 113-114)
    - tests/unit/leads-get-route.test.ts (linhas 67-68)
    - tests/unit/leads-chat-route.test.ts (linhas 71-72)
    - .planning/phases/09-limpeza-do-papel-viewer/09-CONTEXT.md (§decisions D-01/D-02 — regra do sentinel)
  </read_first>
  <action>
    Aplicar D-01/D-02: substituir toda ocorrência do literal `'viewer'` (incluindo em nomes de teste `it(...)`) pelo sentinel `'invalid_role'`. Isso preserva a cobertura de "papel não reconhecido é rejeitado (403/redirect)" sem reintroduzir a semântica de um papel morto do sistema. Edições exatas:

    1. **`tests/middleware.test.ts`** — quatro substituições:
       (a) linha 43, nome do teste, trocar:
           DE:   `  it('blocks viewer from /tenants (returns 307 redirect to /)', () => {`
           PARA: `  it('blocks invalid_role from /tenants (returns 307 redirect to /)', () => {`
       (b) linha 44, o token, trocar:
           DE:   `    const token = makeToken({ role: 'viewer', tenant_id: 't-1', tenant_slug: 'acme' })`
           PARA: `    const token = makeToken({ role: 'invalid_role', tenant_id: 't-1', tenant_slug: 'acme' })`
       (c) linha 59, o payload de exemplo do teste de decodificação de JWT, trocar:
           DE:   `      JSON.stringify({ app_metadata: { role: 'viewer', tenant_id: 't1', tenant_slug: 'acme' } })`
           PARA: `      JSON.stringify({ app_metadata: { role: 'invalid_role', tenant_id: 't1', tenant_slug: 'acme' } })`
       (d) linha 62, a asserção correspondente, trocar:
           DE:   `    expect(decoded.app_metadata.role).toBe('viewer')`
           PARA: `    expect(decoded.app_metadata.role).toBe('invalid_role')`

    2. **`tests/unit/google-ads-connect-route.test.ts`** — duas substituições:
       (a) linha 59, nome do teste, trocar:
           DE:   `  it("role 'viewer' → redirect ?google_error=forbidden", async () => {`
           PARA: `  it("role 'invalid_role' → redirect ?google_error=forbidden", async () => {`
       (b) linha 60, trocar:
           DE:   `    mockState.role = 'viewer'`
           PARA: `    mockState.role = 'invalid_role'`

    3. **`tests/unit/insights-generate-route.test.ts`** — duas substituições:
       (a) linha 81, trocar:  `  it("role 'viewer' → 403", async () => {`  →  `  it("role 'invalid_role' → 403", async () => {`
       (b) linha 82, trocar:  `    mockState.role = 'viewer'`  →  `    mockState.role = 'invalid_role'`

    4. **`tests/unit/leads-status-route.test.ts`** — duas substituições:
       (a) linha 113, trocar:  `  it("role 'viewer' → 403", async () => {`  →  `  it("role 'invalid_role' → 403", async () => {`
       (b) linha 114, trocar:  `    mockState.role = 'viewer'`  →  `    mockState.role = 'invalid_role'`

    5. **`tests/unit/leads-get-route.test.ts`** — duas substituições:
       (a) linha 67, trocar:  `  it("role 'viewer' → 403", async () => {`  →  `  it("role 'invalid_role' → 403", async () => {`
       (b) linha 68, trocar:  `    mockState.role = 'viewer'`  →  `    mockState.role = 'invalid_role'`

    6. **`tests/unit/leads-chat-route.test.ts`** — duas substituições:
       (a) linha 71, trocar:  `  it("role 'viewer' → 403", async () => {`  →  `  it("role 'invalid_role' → 403", async () => {`
       (b) linha 72, trocar:  `    mockState.role = 'viewer'`  →  `    mockState.role = 'invalid_role'`

    NÃO tocar em `tests/integration/tenant-role-migration.test.ts` (fora de escopo — ver decisão de discrição 1 no <context>). Todas as asserções de status/redirect (403, `?google_error=forbidden`, etc.) permanecem idênticas — só o valor do papel e o nome do teste mudam. Os testes devem continuar verdes porque um papel desconhecido é rejeitado exatamente como `'viewer'` era.
  </action>
  <verify>
    <automated>cd "c:/Users/ReinaldoLima/Desktop/Cowork/NEXUS-DASH" && ! grep -rn "viewer" tests/middleware.test.ts tests/unit/google-ads-connect-route.test.ts tests/unit/insights-generate-route.test.ts tests/unit/leads-status-route.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts && npx vitest run tests/middleware.test.ts tests/unit/google-ads-connect-route.test.ts tests/unit/insights-generate-route.test.ts tests/unit/leads-status-route.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "viewer" tests/middleware.test.ts tests/unit/google-ads-connect-route.test.ts tests/unit/insights-generate-route.test.ts tests/unit/leads-status-route.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts` retorna ZERO linhas (exit code 1) — nem em nomes de teste, nem em valores.
    - `grep -rn "invalid_role" tests/middleware.test.ts tests/unit/google-ads-connect-route.test.ts tests/unit/insights-generate-route.test.ts tests/unit/leads-status-route.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts` retorna 8 linhas (4 em middleware.test.ts + 2 no par nome/valor de cada um dos... na verdade: middleware=4, e cada uma das 5 rotas=2 → 14 no total). Confirmar que cada arquivo de rota tem exatamente 2 (o `it(...)` e o `mockState.role`).
    - `npx vitest run` nos 6 arquivos: todos os testes passam, zero falhas, zero regressões (mesmo total de testes de antes, só com nomes/valores trocados).
    - `tests/integration/tenant-role-migration.test.ts` permanece com o literal `'viewer'` intacto (NÃO tocado): `grep -c "viewer" tests/integration/tenant-role-migration.test.ts` retorna valor > 0.
  </acceptance_criteria>
  <done>
    Nenhum dos 6 arquivos de teste da camada de aplicação contém `'viewer'`; o sentinel `'invalid_role'` está no lugar tanto nos nomes de teste quanto nos valores; a suíte roda verde com a mesma contagem de testes; o teste de migração de banco (fora de escopo) permanece intocado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JWT → middleware (proxy.ts) | O cliente apresenta um access token cujo `app_metadata.role` é decodificado e usado para roteamento por papel. Fronteira única afetada por esta phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | `proxy.ts` branch de redirect por papel (remoção de `|| role === 'viewer'`) | accept (no new surface) | A mudança é estritamente restritiva: uma JWT com `role: 'viewer'` — impossível de ser emitida pelo banco desde a migration `0020` e pelo Custom Access Token Hook atual — deixa de casar com o branch de `tenant_admin` e cai no `else`, sendo redirecionada para `/login?error=no_membership` (fail-closed) em vez de para o dashboard. Nenhum novo caminho de acesso é criado; um caminho legado é fechado. Nenhuma autorização é afrouxada. |
| T-09-02 | Tampering | Remoção do valor `'viewer'` das union types TypeScript | accept | Tipos TypeScript são apagados em build-time e não constituem controle de segurança em runtime. A autorização real é feita por RLS no Supabase e pelas RPCs `get_user_role()` nas rotas — nenhuma delas é alterada por esta phase. |
| T-09-03 | Spoofing / auth bypass | Testes com sentinel `'invalid_role'` | accept | O sentinel apenas renomeia o valor de exemplo de "papel não reconhecido"; a asserção continua sendo "papel desconhecido → 403/redirect". A cobertura de rejeição de papel inválido é preservada, não reduzida. |

**Conclusão de segurança:** Esta é uma phase de remoção de código morto. Nenhuma rota, tabela, política RLS, RPC ou fluxo de autenticação novo é introduzido. A única mudança comportamental (T-09-01) é uma restrição adicional (fail-closed) sobre um papel que já não pode ser emitido. Sem nova superfície de ataque; nenhum achado de severidade alta (gate ASVS L1 satisfeito).
</threat_model>

<verification>
Checagens de fim de phase (rodar da raiz do projeto):

1. **Zero `'viewer'` na camada de aplicação** (tipos, middleware, componente, comentário, testes de aplicação):
   `grep -rn "viewer" lib/stores/tenant-store.tsx proxy.ts components/tenants/tenant-switcher.tsx app/api/meta-ads/connect/route.ts tests/middleware.test.ts tests/unit/google-ads-connect-route.test.ts tests/unit/insights-generate-route.test.ts tests/unit/leads-status-route.test.ts tests/unit/leads-get-route.test.ts tests/unit/leads-chat-route.test.ts` → ZERO linhas.
2. **Teste de banco fora de escopo intocado:** `grep -c "viewer" tests/integration/tenant-role-migration.test.ts` → > 0.
3. **Compilação:** `npx tsc --noEmit` → nenhum erro novo (só os 2 pré-existentes de `tests/vault-rpc.test.ts`).
4. **Suíte completa:** `npm test` → verde, zero regressões.
</verification>

<success_criteria>
AUTH-07 satisfeito quando (mapeando 1:1 os 4 success criteria do ROADMAP §Phase 9):
1. `lib/stores/tenant-store.tsx` — `Role` type não inclui `'viewer'`.
2. `proxy.ts` — nenhum branch condicional para `'viewer'`; apenas `tenant_admin`/`agency`/`super_admin` tratados.
3. `components/tenants/tenant-switcher.tsx` — prop `role` não lista `'viewer'`.
4. Suíte de testes passa sem nenhum caso que assuma `'viewer'` como papel válido do sistema (sentinel `'invalid_role'` aplicado).
Extra: comentário obsoleto em `app/api/meta-ads/connect/route.ts` corrigido; `tsc` sem erros novos.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/09-limpeza-do-papel-viewer/09-01-SUMMARY.md`.
</output>

---
phase: 03.1-leads-management-via-google-sheets-integration
plan: 01
subsystem: database
tags: [supabase, migrations, google-auth-library, jsonb, service-account]

# Dependency graph
requires: []
provides:
  - "tenants.sheets_service_account JSONB column live on Supabase (rvkkvjitfddtbdpkupok)"
  - "database.types.ts recognizes sheets_service_account in tenants Row/Insert/Update"
  - "google-auth-library (^10.9.0) installed for Service Account OAuth2 signing"
affects: [03.1-02-write-status-route, 03.1-03-leads-status-ui]

# Tech tracking
tech-stack:
  added: [google-auth-library@^10.9.0]
  patterns:
    - "Per-tenant write credential stored as JSONB (sheets_service_account), separate column from read-only sheets_api_key (0012)"
    - "Idempotent ALTER TABLE ADD COLUMN IF NOT EXISTS + explicit COMMENT ON COLUMN for security-sensitive columns"

key-files:
  created:
    - supabase/migrations/0015_add_sheets_service_account_to_tenants.sql
  modified:
    - package.json
    - package-lock.json
    - types/database.types.ts

key-decisions:
  - "Installed google-auth-library (lightweight, ~600KB) instead of full googleapis package (~207MB) per RESEARCH Standard Stack guidance"
  - "sheets_service_account kept as a separate column from sheets_api_key rather than replacing it — read path (existing production feature) stays untouched"

patterns-established:
  - "Write credentials for external APIs (Service Account JSON) stored as JSONB per-tenant column with explicit server-side-only COMMENT annotation"

requirements-completed: [LEADS-02]

# Metrics
duration: 6min
completed: 2026-07-05
---

# Phase 03.1 Plan 01: Data Layer for Google Sheets Write-Back Summary

**Added `tenants.sheets_service_account` JSONB column (live on Supabase) plus `google-auth-library` dependency, laying the data-layer groundwork for Service Account-authenticated Sheets writes without touching the existing read path.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-07-05T03:38:00Z (approx, based on session/STATE.md timestamps)
- **Completed:** 2026-07-05T03:44:04Z
- **Tasks:** 2 completed
- **Files modified:** 4

## Accomplishments
- `google-auth-library` (^10.9.0) installed — lightweight OAuth2 server-to-server client for Service Account JWT signing, avoiding the 207MB `googleapis` bundle
- Migration `0015_add_sheets_service_account_to_tenants.sql` created (idempotent, matches 0012's style) and applied to the LIVE Supabase database (project `rvkkvjitfddtbdpkupok`)
- `types/database.types.ts` `tenants` block extended in all 3 locations (Row, Insert, Update) with `sheets_service_account: Json | null`
- Verified live column via PostgREST REST query (`GET /rest/v1/tenants?select=id,sheet_id,sheets_service_account`) — returned `sheets_service_account: null` with no schema error, confirming the column exists on the production database, not just in the migration file

## Task Commits

Each task was committed atomically:

1. **Task 1: Instalar google-auth-library, criar migration 0015 e atualizar tipos** - `033b83b` (feat)
2. **Task 2: [BLOCKING] Aplicar migration ao banco live (supabase db push)** - no new commit (database-only operation; migration file already committed in Task 1). Also applied two pre-existing pending migrations (`0013_create_vault_write_function.sql`, `0014_revoke_authenticator_vault_write.sql`) that were unpushed from a prior phase — see Deviations.

**Plan metadata:** (this commit) `docs(03.1-01): complete data layer plan`

## Files Created/Modified
- `supabase/migrations/0015_add_sheets_service_account_to_tenants.sql` - Idempotent ALTER TABLE adding the `sheets_service_account JSONB` column with a security-annotated COMMENT
- `types/database.types.ts` - `tenants` block (Row/Insert/Update) extended with `sheets_service_account: Json | null`
- `package.json` / `package-lock.json` - `google-auth-library` added as a production dependency

## Decisions Made
- Followed RESEARCH guidance: installed `google-auth-library` only, not the full `googleapis` package, to keep bundle size minimal (Vercel Hobby constraint)
- Kept `sheets_service_account` as an additive column, not a replacement for `sheets_api_key` — the existing read path (`app/api/leads/route.ts`, `lib/leads.ts`) is untouched, satisfying the plan's isolation requirement

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Applied two pre-existing unpushed migrations alongside 0015**
- **Found during:** Task 2 (`supabase migration list` before push)
- **Issue:** `supabase migration list` showed migrations `0013_create_vault_write_function.sql` and `0014_revoke_authenticator_vault_write.sql` were present locally but not yet applied to the remote database (left over from a prior phase's execution). `supabase db push` applies all pending migrations in order — it cannot selectively push only 0015 while skipping earlier unapplied ones, since 0015 depends on the migration history being contiguous.
- **Fix:** Reviewed both files' content (Vault write RPC + Vault RPC security-hardening REVOKE for `create_or_update_vault_secret`) — confirmed both are already-reviewed, previously-planned Phase 3 migrations with no relation to the current tenant read path. Applied all three (0013, 0014, 0015) via a single `supabase db push`.
- **Files modified:** None (database-only; migration files were already present in the repo from prior work, not created by this plan)
- **Verification:** `supabase migration list` now shows `0013`, `0014`, `0015` all present in both Local and Remote columns; live PostgREST query confirms `sheets_service_account` column exists and is queryable
- **Committed in:** N/A (no code change — this was a live-database state sync, not a file change)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to unblock `supabase db push` for migration 0015 — the CLI does not support pushing a single migration out of sequence. No scope creep: the two additional migrations were pre-existing, already-written code from a prior phase, not new work created in this plan.

## Issues Encountered
- `npx tsc --noEmit` reports 4 pre-existing errors in `tests/integration/vault-rpc.test.ts` and `tests/tenants.test.ts`, unrelated to this plan's changes. Confirmed via `git stash` that these errors exist identically before and after this plan's edits — out of scope per deviation rules (pre-existing test-file issues, not caused by this plan). Not fixed; left as-is.
- `requirements mark-complete LEADS-02` returned `not_found` — `.planning/REQUIREMENTS.md` does not yet contain LEADS-01..LEADS-05 (referenced by ROADMAP.md and all three 03.1 plans, but never added to REQUIREMENTS.md by the planner). Logged to [deferred-items.md](./deferred-items.md) — out of scope to fix from Plan 01 since it involves requirement text for Plans 02/03 not yet executed.

## User Setup Required

None for this plan. The `user_setup` block in the plan frontmatter (creating a Google Cloud Service Account, sharing the Sheets file, inserting the JSON into `tenants.sheets_service_account`) is required before Plan 02's write route can function end-to-end, but is not a blocking prerequisite for completing this plan's automated tasks (schema + dependency only). This will need to be surfaced to the user before Plan 02 verification.

## Next Phase Readiness
- `tenants.sheets_service_account` column is live and type-safe — Plan 02 (write route) can now `.select('sheet_id, sheets_service_account')` without compilation errors
- `google-auth-library` is installed and ready for JWT-based Service Account authentication in Plan 02
- Existing read path (`app/api/leads/route.ts`, `lib/leads.ts`, leads pages) was not touched — no regression risk
- Blocker for Plan 02 end-to-end testing: the actual Service Account JSON credential still needs to be created in Google Cloud Console and inserted into the database per the `user_setup` steps in this plan's frontmatter — this is a manual step for the user, not something this plan could automate

---
*Phase: 03.1-leads-management-via-google-sheets-integration*
*Completed: 2026-07-05*

## Self-Check: PASSED

- FOUND: supabase/migrations/0015_add_sheets_service_account_to_tenants.sql
- FOUND: .planning/phases/03.1-leads-management-via-google-sheets-integration/03.1-01-SUMMARY.md
- FOUND: commit 033b83b (Task 1)

# Phase 03.1: Leads Management via Google Sheets integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 03.1-leads-management-via-google-sheets-integration
**Areas discussed:** Estratégia de sincronização, Configuração de credenciais por tenant, Completude do escopo / Edição de status, Comportamento em falha de escrita, Auditoria de mudanças de status

---

## Estratégia de sincronização

| Option | Description | Selected |
|--------|-------------|----------|
| Manter fetch direto na Sheets API | Já funciona, zero infra adicional, aceitável pro volume atual | ✓ |
| Migrar para N8N -> Supabase | Alinha com padrão arquitetural, mais trabalho agora | |
| Híbrido: manter agora, migrar depois | Formaliza v1, registra migração como backlog | |

**User's choice:** Manter fetch direto na Sheets API (Recomendado)
**Notes:** Segunda pergunta — limite de 500 linhas e revalidate de 60s — usuário confirmou manter como está.

---

## Configuração de credenciais por tenant

| Option | Description | Selected |
|--------|-------------|----------|
| Update manual via Supabase Studio | Aceitável com 1-3 tenants gerenciados manualmente | ✓ |
| Criar tela de configuração nesta fase | Form em tenant settings | |
| Registrar como item futuro (backlog) | Aceitar manual agora, anotar necessidade futura | |

**User's choice:** Update manual via Supabase Studio (Recomendado)
**Notes:** Nenhuma.

---

## Completude do escopo

| Option | Description | Selected |
|--------|-------------|----------|
| Já fecha o escopo | Suficiente, resto vira ideia adiada | |
| Falta editar/atualizar status do lead | Página só lê da planilha hoje | ✓ |
| Falta paginar/além de 500 linhas | Suportar planilhas maiores | |

**User's choice:** Falta editar/atualizar status do lead
**Notes:** Abriu uma nova sub-discussão sobre como a edição deve funcionar (ver abaixo).

### Sub-discussão: mecanismo de edição de status

| Option | Description | Selected |
|--------|-------------|----------|
| Escrever de volta na planilha | Exige Service Account com escrita; planilha continua fonte única | ✓ |
| Guardar override no Supabase | Tabela `lead_status_overrides`, não escreve na planilha | |
| Adiar para outra fase | Formalizar 03.1 só com leitura | |

**User's choice:** Escrever de volta na planilha (Recomendado p/ paridade)

### Sub-discussão: armazenamento da credencial de escrita

| Option | Description | Selected |
|--------|-------------|----------|
| Nova coluna `sheets_service_account` (JSON) | Complementa `sheets_api_key` existente | ✓ |
| Reaproveitar `sheets_api_key` para tudo | Simplifica schema, obsoleta configuração atual | |
| Você decide | Deixa pro planner/executor | |

**User's choice:** Nova coluna `sheets_service_account` (JSON) no tenants (Recomendado)

### Sub-discussão: interação de edição na UI

| Option | Description | Selected |
|--------|-------------|----------|
| Dropdown inline na tabela | Select direto na coluna Status | ✓ |
| Modal/drawer de detalhes do lead | Painel lateral com todos os campos | |

**User's choice:** Dropdown inline na tabela (Recomendado)

---

## Comportamento em falha de escrita

| Option | Description | Selected |
|--------|-------------|----------|
| Revert + toast de erro | Volta ao status anterior, mostra erro | ✓ |
| Manter valor otimista, avisar discretamente | Mantém seleção, aviso sutil | |
| Retry automático (1x) antes de reverter | Tenta de novo antes de reverter | |

**User's choice:** Revert + toast de erro (Recomendado)

---

## Auditoria de mudanças de status

| Option | Description | Selected |
|--------|-------------|----------|
| Sem auditoria por ora | Valor atual na planilha é suficiente | ✓ |
| Registrar em tabela de log no Supabase | `lead_status_log` com tenant_id, lead_id, status anterior/novo, user_id, changed_at | |

**User's choice:** Sem auditoria por ora (Recomendado)

---

## Claude's Discretion

- Padrão de chamada à Claude API em `app/api/leads/chat/route.ts` (raw fetch vs `@anthropic-ai/sdk`) — usuário não selecionou esta área para discussão nas duas rodadas de "mais áreas"; permanece como decisão de implementação.
- Mapeamento de `id` (índice de linha) para o range exato da planilha na escrita (ex. `Leads!F{id+2}`) — detalhe de implementação, não levado ao usuário.

## Deferred Ideas

- Migração da sincronização de leads para N8N → Supabase — adiada explicitamente (ver CONTEXT.md D-01).
- Tela de configuração de credenciais por tenant — adiada explicitamente (ver CONTEXT.md D-03).
- Auditoria/histórico de mudanças de status — adiada explicitamente (ver CONTEXT.md D-08).
- Paginação além de 500 linhas — não solicitada, sem ação.

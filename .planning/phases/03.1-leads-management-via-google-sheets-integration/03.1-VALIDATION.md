---
phase: 03.1
slug: leads-management-via-google-sheets-integration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-05
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ^2.1.9 |
| **Config file** | `vitest.config.mts` — `environment: 'node'`, `setupFiles: ['./tests/setup.ts']`, `include: ['tests/**/*.test.ts']` |
| **Quick run command** | `npm test -- tests/unit/<arquivo>.test.ts` |
| **Full suite command** | `npm test` (script `"test": "vitest run"`) |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- tests/unit/<arquivo-tocado>.test.ts`
- **After every plan wave:** Run `npm test` (full suite — integration tests self-skip without env, don't block local CI)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD-01 | TBD | 0 | D-05/Pattern 3 | — | `id → row` mapping (`row = id + 2`) builds `Leads!F{row}` range correctly | unit | `npm test -- tests/unit/sheets-row-mapping.test.ts` | ❌ W0 | ⬜ pending |
| TBD-02 | TBD | 0 | D-07/Pitfall 3 | T-03.1-01 (Access Control) | Write route maps 429→rate-limit message, 403→permission message, other→generic error | unit (mocking `client.fetch`/gaxios) | `npm test -- tests/unit/leads-status-route.test.ts` | ❌ W0 | ⬜ pending |
| TBD-03 | TBD | 0 | Pitfall 2 | T-03.1-02 (Info Disclosure) | Real write to a test spreadsheet with a test Service Account | integration, self-skip without env | `npm test -- tests/integration/sheets-write.test.ts` | ❌ W0 | ⬜ pending |
| TBD-04 | TBD | — | D-06 (UI) | — | Dropdown reverts to previous status on API error | manual-only | — | — | ⬜ pending |

*Task IDs are placeholders (TBD) — the planner assigns real plan/task IDs; this map records what MUST be covered regardless of numbering.*

---

## Wave 0 Requirements

- [ ] `tests/unit/sheets-row-mapping.test.ts` — tests the pure `id → A1 range` mapping function (extract this logic into a testable function in `lib/sheets.ts`, not inline in the route)
- [ ] `tests/unit/leads-status-route.test.ts` — tests Gaxios error → HTTP response mapping (mock `JWT.prototype.fetch`)
- [ ] `tests/integration/sheets-write.test.ts` — optional, requires a real test spreadsheet + test Service Account; self-skips if env absent (same pattern as `tests/integration/vault-rpc.test.ts`)
- [ ] No framework installation needed — Vitest already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Status dropdown reverts to previous value + shows error toast on write failure | D-06/D-07 | UI interaction (local state revert + toast) has low ROI to automate for a 1-3 tenant phase; trivially checkable visually | Open `/{tenant-slug}/leads`, change a lead's status dropdown, simulate a write failure (e.g. temporarily break `sheet_id` or revoke Service Account Editor access), confirm dropdown reverts and a toast/error message appears |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

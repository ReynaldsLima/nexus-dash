---
phase: quick-260805-0jj
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260805-0jj]
files_modified:
  - components/layout/sidebar-nav.tsx
  - app/[tenant-slug]/leads/page.tsx
  - app/api/leads/route.ts
  - app/[tenant-slug]/leads/agente/page.tsx
  - app/api/leads/chat/route.ts
  - lib/rate-limit.ts
  - tests/unit/leads-chat-route.test.ts
  - tests/unit/rate-limit.test.ts

must_haves:
  truths:
    - "A rota /{slug}/leads/agente retorna 404 — a página não existe mais"
    - "O endpoint POST /api/leads/chat não existe mais no bundle de produção"
    - "A sidebar da seção Leads mostra apenas 'Gestão de Leads' — sem item 'Agente IA'"
    - "A página Gestão de Leads não tem mais o botão CTA 'Agente IA' no header"
    - "Nenhum import pendente para os arquivos deletados (tsc --noEmit limpo)"
    - "A suíte de testes passa com exatamente 14 testes a menos e zero falhas novas"
    - "AI Insights continua funcionando — lib/ai/anthropic.ts e app/api/insights/* intactos"
    - "A lista de leads continua funcionando — lib/leads.ts e GET/PATCH /api/leads* intactos"
  artifacts:
    - path: "components/layout/sidebar-nav.tsx"
      provides: "LEADS_ITEMS com um único item ('leads'), sem import Bot"
      contains: "Gestão de Leads"
    - path: "app/[tenant-slug]/leads/page.tsx"
      provides: "Header da página sem CTA Agente IA, sem imports Bot e Link"
      contains: "Gestão de Leads"
  key_links:
    - from: "components/layout/sidebar-nav.tsx"
      to: "lucide-react"
      via: "import sem Bot"
      pattern: "^import \\{ LayoutDashboard.*Settings \\} from 'lucide-react'$"
    - from: "app/[tenant-slug]/leads/page.tsx"
      to: "lucide-react"
      via: "import sem Bot"
      pattern: "from 'lucide-react'"
---

<objective>
Remover completamente o módulo "Agente IA" (chat de leads) do NEXUS-DASH — a feature foi descontinuada e não será mantida.

Purpose: Eliminar código morto e reduzir superfície de ataque (o endpoint de chat era um proxy autenticado para a chave Anthropic compartilhada). Menos código para manter no redesign visual da v1.1.

Output: 5 arquivos deletados, 3 arquivos editados (remoção de referências), suíte verde com 14 testes a menos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@components/layout/sidebar-nav.tsx
@app/[tenant-slug]/leads/page.tsx
@app/[tenant-slug]/leads/agente/page.tsx
@app/api/leads/chat/route.ts
@lib/rate-limit.ts

## Inventário verificado (grep executado durante o planejamento — não precisa re-investigar)

**Consumidores de `checkRateLimit` / `@/lib/rate-limit` em código-fonte:**
- `app/api/leads/chat/route.ts:6,75` (será deletado)
- `tests/unit/rate-limit.test.ts` (será deletado)
- Nenhum outro. `lib/rate-limit.ts` fica órfão → deletar.

**Referências a `leads/agente` / `Agente IA` / `leads/chat` em código-fonte:**
- `components/layout/sidebar-nav.tsx:18,84,85` → editar
- `app/[tenant-slug]/leads/page.tsx:229,233` → editar
- `app/api/leads/route.ts:88` → comentário obsoleto, editar (só comentário, zero mudança de comportamento)
- `app/[tenant-slug]/leads/agente/page.tsx` + `app/api/leads/chat/route.ts` → deletar
- `tests/unit/leads-chat-route.test.ts` → deletar

**Diretório `app/[tenant-slug]/leads/agente/` contém APENAS `page.tsx`** — nenhum sub-componente, layout ou loading. Deletar o diretório inteiro.

**Uso de imports que ficam órfãos:**
- `sidebar-nav.tsx`: `Bot` usado APENAS na linha 18 → remover do import. `Link` continua usado por `NavLink` → MANTER.
- `leads/page.tsx`: `Bot` usado APENAS na linha 232 E `Link` (next/link) usado APENAS nas linhas 228-234 → remover AMBOS os imports.

## NÃO TOCAR (compartilhado com outras features — confirmado)

- `lib/ai/anthropic.ts` (`insightModel`) — compartilhado com AI Insights (`app/api/insights/*`)
- `lib/leads.ts` (`Lead`, `cat()`, `CATEGORY_LABELS`) — compartilhado com a lista de leads que permanece
- `ANTHROPIC_API_KEY` — compartilhado com AI Insights
- Nenhuma migration existe para esta feature (é stateless — sem tabelas de chat/message/conversation). Nada para migrar ou dropar.
- `.planning/milestones/v1.0-*` (docs arquivados) — registro histórico, NÃO editar
- `.planning/REQUIREMENTS.md` — grep confirmou ZERO referência a esta feature (o único match de "rate limit" é sobre sync de API, requisito SET-05, não relacionado). **Não editar.**

## Baseline da suíte de testes (medido durante o planejamento, 2026-08-05)

```
Test Files  37 passed (37)
     Tests  322 passed | 1 skipped | 5 todo (328)
```

Arquivos a deletar contribuem exatamente 14 testes:
```
tests/unit/rate-limit.test.ts        (4 tests)
tests/unit/leads-chat-route.test.ts  (10 tests)
```

**Baseline pós-remoção esperado:**
```
Test Files  35 passed (35)
     Tests  308 passed | 1 skipped | 5 todo (314)
```

Nota: o flake conhecido de realtime em `tests/unit/anomaly-alerts-schema.test.ts` NÃO ocorreu na medição do baseline (37/37 verdes). Se ele aparecer durante a execução, é anomalia pré-existente documentada — não é regressão desta tarefa.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover as referências de UI ao Agente IA</name>
  <files>components/layout/sidebar-nav.tsx, app/[tenant-slug]/leads/page.tsx, app/api/leads/route.ts</files>
  <action>
Remover as referências ANTES de deletar os arquivos, para que em nenhum momento intermediário exista um link vivo apontando para uma rota removida.

**1. `components/layout/sidebar-nav.tsx`**

a) Linha 6 — remover `Bot` do import do lucide-react (verificado: `Bot` só é usado na linha 18):
```tsx
import { LayoutDashboard, Megaphone, Sparkles, Users2, Settings } from 'lucide-react'
```
NÃO remover `Link` daqui — `NavLink` ainda usa.

b) Linhas 16-19 — `LEADS_ITEMS` fica com um único item:
```tsx
const LEADS_ITEMS = [
  { icon: Users2, label: 'Gestão de Leads', key: 'leads' },
]
```

c) Linhas 82-91 — remover o caso especial de active-state para `'leads/agente'`. O comportamento preservado para `'leads'` é match exato (`pathname === href`), idêntico ao branch `else` atual:
```tsx
{LEADS_ITEMS.map(({ icon, label, key }) => {
  const href = `/${slug}/${key}`
  return (
    <NavLink key={key} href={href} icon={icon} label={label} isActive={pathname === href} />
  )
})}
```
Remover também o comentário da linha 84 que descreve o caso especial.

**2. `app/[tenant-slug]/leads/page.tsx`**

a) Linha 5 — remover `Bot` do import do lucide-react (verificado: só usado na linha 232):
```tsx
import { Search, Users, Flame, Handshake, PhoneOff, TrendingUp, CheckCircle2, MapPinOff, Users2, IdCard, AlertTriangle, Download } from 'lucide-react'
```

b) Linha 6 — remover inteiramente `import Link from 'next/link'` (verificado: `Link` só era usado no CTA das linhas 228-234; após a remoção fica órfão e o `npm run lint` falha com `@typescript-eslint/no-unused-vars`).

c) Linhas 221-235 — remover o bloco `<Link href={`/${slug}/leads/agente`}>...</Link>` e desembrulhar o wrapper flex, que passa a ter um único filho. O header vira:
```tsx
      {/* Page header */}
      <div>
        <h1 className="text-2xl font-bold tracking-tight">Gestão de Leads</h1>
        <p className="text-sm text-muted-foreground mt-1">
          {loading ? 'Carregando…' : `${leads.length} leads · últimas 500 entradas da planilha`}
        </p>
      </div>
```
Visualmente idêntico (`justify-between` com um filho é no-op). NÃO alterar mais nada nesta página — a tabela, KPIs, funil, filtros, dropdown de status e o botão Exportar CSV permanecem exatamente como estão.

**3. `app/api/leads/route.ts` linha 88** — comentário obsoleto que cita o Agente IA. Trocar apenas o texto do comentário (zero mudança de comportamento; a lógica de ordenação permanece intacta), por exemplo: `// para que todo consumidor da rota receba a mesma ordem.`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
`grep -rn "Bot" components/layout/sidebar-nav.tsx "app/[tenant-slug]/leads/page.tsx"` retorna zero linhas.
`grep -rn "leads/agente\|Agente IA" app components lib` retorna apenas os arquivos que serão deletados na Task 2 (`app/[tenant-slug]/leads/agente/page.tsx`, `app/api/leads/chat/route.ts`).
`npx tsc --noEmit` e `npm run lint` passam sem erros novos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deletar os arquivos do módulo Agente IA</name>
  <files>app/[tenant-slug]/leads/agente/page.tsx, app/api/leads/chat/route.ts, lib/rate-limit.ts, tests/unit/leads-chat-route.test.ts, tests/unit/rate-limit.test.ts</files>
  <action>
Deletar os 5 arquivos (e o diretório `agente/`, que fica vazio):

```
app/[tenant-slug]/leads/agente/page.tsx   → deletar o arquivo E o diretório agente/ (contém só este arquivo)
app/api/leads/chat/route.ts               → deletar o arquivo E o diretório chat/ (contém só este arquivo)
lib/rate-limit.ts                         → deletar (único consumidor era o chat route, confirmado por grep)
tests/unit/leads-chat-route.test.ts       → deletar
tests/unit/rate-limit.test.ts             → deletar
```

Antes de deletar `app/api/leads/chat/`, confirme com `ls` que ela contém apenas `route.ts`. Se houver qualquer outro arquivo, PARE e reporte — o inventário estaria incompleto.

Use `git rm` (ou `rm` + `git add -A`) para que a remoção fique registrada no índice.

NÃO deletar, NÃO editar: `lib/ai/anthropic.ts`, `lib/leads.ts`, `app/api/leads/route.ts` (já editado na Task 1), `app/api/leads/[id]/status/route.ts`, `app/api/insights/*`, nem qualquer teste além dos dois listados.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
Os 5 arquivos não existem mais; os diretórios `app/[tenant-slug]/leads/agente/` e `app/api/leads/chat/` não existem mais.
`grep -rn "leads/agente\|Agente IA\|leads/chat\|checkRateLimit\|@/lib/rate-limit" app lib components tests` retorna zero linhas.
`npx tsc --noEmit` passa (zero imports pendentes) e `npm run lint` passa.
`lib/ai/anthropic.ts` e `lib/leads.ts` continuam presentes e inalterados (`git status` não os lista).
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate de verificação completo</name>
  <files>(nenhum — apenas verificação)</files>
  <action>
Rodar o gate completo e comparar com o baseline documentado no `<context>`.

```bash
npx tsc --noEmit
npm run lint
npm run build
npx vitest run
```

**Contagens esperadas em `npx vitest run`:**
```
Test Files  35 passed (35)
     Tests  308 passed | 1 skipped | 5 todo (314)
```
(baseline pré-remoção: 37 arquivos / 322 passed | 1 skipped | 5 todo = 328)

Critérios de aceitação da contagem:
- Arquivos de teste: exatamente 37 → 35 (queda de 2)
- Testes passando: exatamente 322 → 308 (queda de 14)
- `skipped` e `todo` inalterados (1 e 5)

Se a queda for diferente de 14, PARE e reporte — significa que algo além do escopo foi removido.

**Única falha tolerada:** o flake conhecido de realtime em `tests/unit/anomaly-alerts-schema.test.ts` (anomalia pré-existente documentada, não relacionada a esta remoção). Ele não ocorreu na medição do baseline. Qualquer OUTRA falha é regressão desta tarefa → investigar antes de fechar.

Verificar também que `npm run build` compila sem warning de rota/módulo faltando, e que as rotas `/[tenant-slug]/leads` e `/api/leads` continuam aparecendo no output de rotas do build, enquanto `/[tenant-slug]/leads/agente` e `/api/leads/chat` NÃO aparecem.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npx vitest run</automated>
  </verify>
  <done>
`npx tsc --noEmit`: zero erros.
`npm run lint`: zero erros.
`npm run build`: sucesso; output de rotas contém `/[tenant-slug]/leads` e `/api/leads`, e NÃO contém `/[tenant-slug]/leads/agente` nem `/api/leads/chat`.
`npx vitest run`: 35 arquivos, 308 passed | 1 skipped | 5 todo, zero falhas novas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/leads/chat | Fronteira removida por esta tarefa — era o único ponto onde input de usuário chegava ao proxy da chave Anthropic compartilhada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0jj-01 | Information Disclosure | `app/api/leads/chat/route.ts` | mitigate | Endpoint deletado por completo (Task 2) — a superfície que expunha o modelo Claude via `insightModel` a input de usuário deixa de existir. Verificado por `npm run build` não listar mais a rota. |
| T-0jj-02 | Denial of Service | `lib/rate-limit.ts` removido | accept | O rate limiter existia exclusivamente para proteger a cota Anthropic no endpoint de chat. Com o endpoint removido, o vetor de abuso desaparece junto. `app/api/insights/*` (o outro consumidor da chave Anthropic) nunca usou este limiter e mantém seu próprio gate de auth — comportamento inalterado. |
| T-0jj-03 | Tampering | `components/layout/sidebar-nav.tsx`, `app/[tenant-slug]/leads/page.tsx` | mitigate | Edições estritamente cirúrgicas (remoção de item de nav, CTA e imports órfãos). Nenhuma lógica de auth, RLS ou escopo de tenant é tocada. Verificado por `npx tsc --noEmit` + suíte completa com queda de exatamente 14 testes. |
| T-0jj-04 | Elevation of Privilege | `lib/ai/anthropic.ts`, `lib/leads.ts` | accept | Explicitamente fora de escopo (marcados NÃO TOCAR). São compartilhados com AI Insights e a lista de leads; `git status` na Task 2 confirma que permanecem intactos. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero imports pendentes para os módulos deletados
2. `npm run lint` — zero imports órfãos (`Bot`, `Link`)
3. `npm run build` — build limpo; rotas `agente`/`chat` ausentes do output
4. `npx vitest run` — 35 arquivos / 308 passed | 1 skipped | 5 todo; queda de exatamente 14 testes; zero falhas novas
5. `grep -rn "leads/agente\|Agente IA\|leads/chat\|checkRateLimit" app lib components tests` — zero linhas
6. `git status` — `lib/ai/anthropic.ts` e `lib/leads.ts` NÃO aparecem como modificados
</verification>

<success_criteria>
- 5 arquivos deletados: `app/[tenant-slug]/leads/agente/page.tsx`, `app/api/leads/chat/route.ts`, `lib/rate-limit.ts`, `tests/unit/leads-chat-route.test.ts`, `tests/unit/rate-limit.test.ts`
- 2 diretórios vazios removidos: `app/[tenant-slug]/leads/agente/`, `app/api/leads/chat/`
- 3 arquivos editados cirurgicamente: `components/layout/sidebar-nav.tsx`, `app/[tenant-slug]/leads/page.tsx`, `app/api/leads/route.ts` (só comentário)
- Zero referências remanescentes ao módulo em `app/`, `lib/`, `components/`, `tests/`
- Gate completo verde: tsc + lint + build + vitest (308 passed, queda de exatamente 14)
- AI Insights e a lista de leads inalterados e funcionando
- Docs arquivados `.planning/milestones/v1.0-*` NÃO editados; `.planning/REQUIREMENTS.md` NÃO editado (sem referência à feature)
</success_criteria>

<output>
Ao concluir, criar `.planning/quick/260805-0jj-remover-completamente-o-modulo-agente-ia/260805-0jj-SUMMARY.md`
</output>

---
phase: quick-260805-17a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/leads.ts
  - tests/unit/leads-date-range.test.ts
  - app/[tenant-slug]/leads/page.tsx
autonomous: true
requirements: [QUICK-260805-17a]

must_haves:
  truths:
    - "Com o range 01/08–04/08 selecionado no header, a tabela de leads mostra apenas leads criados entre 01/08 e 04/08 (inclusive nas duas pontas)"
    - "Os KPI cards (Total, Novos, Quentes, Negociando, Fechados, etc.) contam apenas os leads dentro do período selecionado"
    - "O Funil de Conversão e a lista de Distribuição na sidebar refletem apenas os leads do período selecionado"
    - "O contador 'N result.' e o CSV exportado refletem período + categoria + busca combinados"
    - "Trocar o preset/range no header re-renderiza a tela de leads imediatamente, sem novo fetch"
    - "Filtro de categoria, busca, ordenação por coluna e export CSV continuam funcionando como antes, agora compostos com o filtro de data"
    - "Um lead criado hoje às 14:30 aparece quando o range termina em 'hoje' (limite superior é fim do dia, não meia-noite)"
  artifacts:
    - path: "lib/leads.ts"
      provides: "filterLeadsByDateRange — filtro puro de leads por intervalo de datas, reutilizando parseLeadDate"
      contains: "export function filterLeadsByDateRange"
    - path: "tests/unit/leads-date-range.test.ts"
      provides: "Cobertura unitária das bordas do filtro de data (inclusividade, fim do dia, datas nulas, imutabilidade)"
      contains: "filterLeadsByDateRange"
    - path: "app/[tenant-slug]/leads/page.tsx"
      provides: "Página de leads consumindo useDateRangeStore como fonte única de verdade para KPIs, tabela, funil e export"
      contains: "useDateRangeStore"
  key_links:
    - from: "app/[tenant-slug]/leads/page.tsx"
      to: "lib/stores/date-range.ts"
      via: "useDateRangeStore() lendo { from, to }"
      pattern: "useDateRangeStore\\(\\)"
    - from: "app/[tenant-slug]/leads/page.tsx"
      to: "lib/leads.ts"
      via: "filterLeadsByDateRange(leads, from, to) alimentando stats e filtered"
      pattern: "filterLeadsByDateRange\\("
    - from: "stats useMemo + Distribuição sidebar"
      to: "dateFiltered"
      via: "derivação a partir do array já filtrado por data (não mais de `leads`)"
      pattern: "dateFiltered"
---

<objective>
A tela de Gestão de Leads ignora completamente o filtro de período do header. O chip de date range aparece na tela (herdado do layout compartilhado, `components/layout/header-actions.tsx` → `DateRangePicker`) e sugere ao usuário que filtra a página inteira — como já acontece em Dashboard e Campanhas — mas na prática não tem efeito nenhum sobre a lista, os KPIs ou o funil.

Causa raiz (já confirmada, NÃO reinvestigar): `app/[tenant-slug]/leads/page.tsx` busca `/api/leads?tenant=${slug}` (até 500 linhas) e o `useMemo` `filtered` filtra apenas por categoria + texto de busca. A página nunca lê `useDateRangeStore`.

Purpose: fazer a UI cumprir o que ela promete visualmente — o período selecionado no header passa a valer para tudo na tela de leads.
Output: uma função pura de filtro em `lib/leads.ts` com testes unitários, e a página de leads reescrita para ter um único array-fonte-de-verdade já filtrado por data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@lib/leads.ts
@lib/stores/date-range.ts
@app/[tenant-slug]/leads/page.tsx
@tests/unit/leads-sort.test.ts
@tests/unit/leads-csv.test.ts
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o repo. -->

De `lib/stores/date-range.ts` (Zustand, global, já usado por Dashboard/Campanhas):
```typescript
export type DateRange = { from: Date; to: Date }
export const useDateRangeStore: UseBoundStore<...>  // { from: Date, to: Date, setRange, applyPreset }
```
IMPORTANTE — formato dos valores no store: `getPresetRange()` produz datas **à meia-noite local**
(`new Date(y, m, d)`), e o `<Calendar mode="range">` do `DateRangePicker` também entrega dias à
meia-noite. Ou seja, `to` é sempre 00:00 do último dia do intervalo, NUNCA o fim do dia.

De `lib/leads.ts` (já existente — REUTILIZAR, não reimplementar):
```typescript
export interface Lead {
  id: number; nome: string; telefone: string; email: string
  empresa: string; criado_em: string; status: string
  hora_resposta: string; tipo_seguro: string
}
export function parseLeadDate(s: string): number | null  // pt-BR DD/MM/YYYY [HH:MM[:SS]] ou ISO; null se inválida
export function compareByCriadoEm(a: Lead, b: Lead, asc: boolean): number
export function sortLeadsByCriadoEmDesc(leads: Lead[]): Lead[]
```

De `lib/leads-csv.ts` (consumidor a jusante, NÃO alterar):
```typescript
export function leadsToCsv(leads: Lead[]): string
export function buildLeadsCsvFilename(slug: string, date: Date): string
```

Padrão de teste já estabelecido nesta página (`tests/unit/leads-sort.test.ts`):
helper local `makeLead(overrides: Partial<Lead>): Lead` + import via alias `@/lib/leads`.
</interfaces>

<constraints>
- **NÃO** alterar `app/api/leads/route.ts` — nem a rota, nem o cap de 500 linhas, nem adicionar query params `from`/`to`. O filtro é 100% client-side: os dados já vêm inteiros e 500 objetos são baratíssimos de filtrar em memória. Filtrar no servidor exigiria filtro de data via Google Sheets API, que não existe nessa fonte REST.
- **NÃO** escrever um segundo parser de data. `parseLeadDate` de `lib/leads.ts` já existe e é testado — usar ele.
- **NÃO** quebrar filtro de categoria, busca, ordenação por coluna nem export CSV. Um lead visível deve passar por TODOS os filtros ativos: período **E** categoria **E** busca.
- **NÃO** instalar dependência nova (sem date-fns, sem dayjs) — aritmética nativa de `Date` basta.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar filterLeadsByDateRange puro em lib/leads.ts com testes unitários</name>
  <files>lib/leads.ts, tests/unit/leads-date-range.test.ts</files>

  <behavior>
  Escrever `tests/unit/leads-date-range.test.ts` PRIMEIRO (deve falhar por import inexistente), cobrindo:
  - Inclui lead cuja data cai no meio do intervalo.
  - **Inclusivo no limite inferior:** lead datado exatamente em `from` (ex.: `01/08/2026`, range `from = new Date(2026,7,1)`) é incluído.
  - **Inclusivo no limite superior — sem hora:** lead datado exatamente em `to` (ex.: `04/08/2026`, range `to = new Date(2026,7,4)` = 00:00) é incluído.
  - **Inclusivo no limite superior — COM hora (caso crítico):** lead `'04/08/2026 14:30'` com `to = new Date(2026,7,4)` (00:00) é incluído. Sem normalizar `to` para o fim do dia, esse lead seria descartado — é exatamente o cenário "leads de hoje somem" que o usuário veria com o preset "Últimos 7 dias".
  - Exclui lead anterior a `from` (ex.: `31/07/2026` num range que começa em `01/08/2026`).
  - Exclui lead posterior a `to` (ex.: `05/08/2026` num range que termina em `04/08/2026`).
  - **Exclui leads com `criado_em` não parseável** — `''`, `'   '`, `'lixo'`, `'99/99/2026'` (parseLeadDate retorna null → sem data para comparar → fora de qualquer visão filtrada por data).
  - Aceita data em formato ISO (`'2026-08-02'`) dentro do intervalo, já que `parseLeadDate` suporta ambos.
  - Preserva a ordem relativa do array de entrada (é filtro, não sort).
  - Não muta o array de entrada e retorna um array novo (`expect(result).not.toBe(input)`).
  - Array vazio de entrada → array vazio de saída.
  Usar o mesmo helper local `makeLead(overrides: Partial<Lead>): Lead` de `tests/unit/leads-sort.test.ts` (copiar o helper — é o padrão já usado nos arquivos irmãos, não extrair para fixture compartilhada).
  </behavior>

  <action>
  Depois que os testes estiverem em RED, adicionar em `lib/leads.ts` (ao final do arquivo, logo abaixo de `sortLeadsByCriadoEmDesc`):

  ```typescript
  export function filterLeadsByDateRange(leads: Lead[], from: Date, to: Date): Lead[] {
    // O store de date range (lib/stores/date-range.ts) sempre entrega dias à MEIA-NOITE
    // local — tanto os presets (`new Date(y, m, d)`) quanto o Calendar do picker. Se
    // comparássemos direto contra `to`, um lead criado hoje às 14:30 cairia FORA de um
    // range que termina "hoje". Por isso normalizamos: `from` para o início do dia e
    // `to` para o último milissegundo do dia — filtro inclusivo nas duas pontas.
    const inicio = new Date(from.getFullYear(), from.getMonth(), from.getDate()).getTime()
    const fim = new Date(to.getFullYear(), to.getMonth(), to.getDate(), 23, 59, 59, 999).getTime()

    return leads.filter(l => {
      const t = parseLeadDate(l.criado_em)
      // Sem data parseável não há como comparar com o intervalo. Numa visão filtrada por
      // período, esses leads ficam de fora (coerente com o sort, que já os joga para o fim).
      if (t === null) return false
      return t >= inicio && t <= fim
    })
  }
  ```

  Rodar os testes até GREEN. Não refatorar `parseLeadDate`, `compareByCriadoEm` nem `sortLeadsByCriadoEmDesc`.
  </action>

  <verify>
    <automated>cd "C:/Users/ReinaldoLima/Desktop/Cowork/NEXUS-DASH" && npx vitest run tests/unit/leads-date-range.test.ts tests/unit/leads-sort.test.ts tests/unit/leads-csv.test.ts</automated>
  </verify>

  <done>`tests/unit/leads-date-range.test.ts` existe e passa 100%; `leads-sort` e `leads-csv` continuam verdes (zero regressão em `lib/leads.ts`); `filterLeadsByDateRange` exportado de `lib/leads.ts` sem novo parser de data e sem dependência nova.</done>
</task>

<task type="auto">
  <name>Task 2: Ligar o date range do header à página de leads (KPIs, tabela, funil, export)</name>
  <files>app/[tenant-slug]/leads/page.tsx</files>

  <action>
  Editar `app/[tenant-slug]/leads/page.tsx` para que o período selecionado no header valha para TUDO na tela. Estabelecer `dateFiltered` como o único array-fonte-de-verdade derivado de `leads`:

  1. **Imports:** adicionar `filterLeadsByDateRange` ao import existente de `@/lib/leads` e `import { useDateRangeStore } from '@/lib/stores/date-range'`.

  2. **Ler o store** dentro de `LeadsPage`, junto dos outros hooks (a página já é `'use client'`, então não há mudança de boundary):
     ```typescript
     const { from, to } = useDateRangeStore()
     ```

  3. **Criar o array-fonte-de-verdade**, logo antes do `useMemo` de `stats` (linha ~104):
     ```typescript
     // Fonte única de verdade da tela: tudo abaixo (KPIs, funil, distribuição, tabela,
     // contador e export CSV) deriva daqui, para que o range do header valha para a
     // página inteira — e não só para a tabela.
     const dateFiltered = useMemo(() => filterLeadsByDateRange(leads, from, to), [leads, from, to])
     ```

  4. **`stats` (useMemo, linha ~104-127):** trocar TODAS as ocorrências de `leads` por `dateFiltered` no corpo (`const total = dateFiltered.length`, todos os `.filter(l => cat(l.status) === ...)`) e trocar a dep array de `[leads]` para `[dateFiltered]`. Isso conserta os 10 KPI cards e, por tabela, os `FunnelBar` (que já leem de `stats`).

  5. **`filtered` (useMemo, linha ~129-142):** trocar a base `leads` por `dateFiltered` (`return dateFiltered.filter(...).sort(...)`) e a dep array de `[leads, search, filter, sortKey, sortAsc]` para `[dateFiltered, search, filter, sortKey, sortAsc]`. Composição resultante: data → categoria → busca → sort. Não mexer na lógica de sort nem no `compareByCriadoEm`.

  6. **Distribuição na sidebar (linha ~379):** trocar `const count = leads.filter(l => cat(l.status) === c).length` por `const count = dateFiltered.filter(l => cat(l.status) === c).length`. Esse é o último lugar que ainda lê `leads` cru para exibição — é fácil esquecer.

  7. **Subtítulo do header da página (linha ~223):** hoje diz `` `${leads.length} leads · últimas 500 entradas da planilha` ``. Trocar por `` `${dateFiltered.length} leads no período · últimas 500 entradas da planilha` `` — com o filtro ativo, mostrar a contagem crua contradiz a tela.

  8. **Verificar (não presumir) os consumidores a jusante:** o contador `{filtered.length} result.` (linha ~297), o `disabled={filtered.length === 0}` do botão de export (linha ~301) e o `leadsToCsv(filtered)` em `handleExport` (linha ~151) já leem `filtered`. Como `filtered` agora deriva de `dateFiltered`, eles passam a honrar o período automaticamente — confirmar lendo o código depois da edição, sem alterá-los.

  9. **NÃO alterar:** o `useEffect` de fetch (continua buscando tudo uma vez, `[slug]` como dep — o filtro de data é puramente de renderização, trocar o range NÃO deve disparar novo fetch), `changeStatus` (o update otimista continua operando sobre o `leads` state cru, que é o correto — `dateFiltered` é derivado e reflete a mudança sozinho), `StatusDropdown`, `toggleSort`, `TABS`, `COLS`.
  </action>

  <verify>
    <automated>cd "C:/Users/ReinaldoLima/Desktop/Cowork/NEXUS-DASH" && npx tsc --noEmit && npm run lint && npm run build && npx vitest run</automated>
  </verify>

  <done>`npx tsc --noEmit` limpo; `npm run lint` sem novos erros; `npm run build` verde; `npx vitest run` sem falhas novas (a única falha tolerada é a flake pré-existente e documentada de realtime em `anomaly_alerts`). Grep em `app/[tenant-slug]/leads/page.tsx` não retorna nenhum uso de `leads.filter(` nem `leads.length` para exibição — todos os consumidores de exibição passam por `dateFiltered` ou `filtered`. `app/api/leads/route.ts` permanece byte-idêntico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Sheets → `/api/leads` → browser | `criado_em` é string crua controlada por quem edita a planilha; chega ao filtro sem sanitização |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17a-01 | Denial of Service | `filterLeadsByDateRange` em `lib/leads.ts` | mitigate | `parseLeadDate` usa regex ancorada (`^`) com quantificadores limitados (`\d{1,2}`, `\d{4}`) — sem backtracking catastrófico. Entrada limitada a 500 linhas pelo cap da rota; O(n) sobre 500 itens é irrelevante. |
| T-17a-02 | Information Disclosure | Export CSV pós-filtro | accept | O filtro só REDUZ o conjunto exportado; nenhum lead fora do escopo do tenant se torna visível. O gate de tenant/role continua no servidor em `app/api/leads/route.ts` (inalterado). Escape de fórmula CSV já coberto por T-wnx-01 em `lib/leads-csv.ts`. |
| T-17a-03 | Tampering | `criado_em` malformado vindo da planilha | mitigate | `parseLeadDate` retorna `null` para qualquer entrada inválida e valida round-trip de data (rejeita 31/02); `filterLeadsByDateRange` descarta `null` explicitamente em vez de deixar `NaN` propagar para a comparação. |
</threat_model>

<verification>
1. `npx vitest run tests/unit/leads-date-range.test.ts` — novo arquivo verde, incluindo o caso de lead com hora no limite superior.
2. `npx tsc --noEmit` — zero erros.
3. `npm run lint` — zero erros novos.
4. `npm run build` — build de produção verde.
5. `npx vitest run` — suíte completa, zero falhas novas além da flake pré-existente de realtime em `anomaly_alerts`.
6. `git diff --stat app/api/leads/route.ts` — vazio (rota intocada, cap de 500 preservado).
</verification>

<success_criteria>
- Selecionar "01/08 – 04/08" no chip do header faz a tabela mostrar apenas leads de 01/08 a 04/08 — leads de julho somem.
- Os 10 KPI cards, o Funil de Conversão e a Distribuição contam apenas o período selecionado.
- `{N} result.` e o CSV exportado batem com período + categoria + busca combinados.
- Trocar o preset re-renderiza na hora, sem novo request a `/api/leads`.
- Categoria, busca, ordenação por coluna e export CSV continuam funcionando exatamente como antes.
- `app/api/leads/route.ts` inalterado.
</success_criteria>

<output>
Ao concluir, criar `.planning/quick/260805-17a-fazer-a-tela-de-gestao-de-leads-respeita/260805-17a-SUMMARY.md`.
</output>

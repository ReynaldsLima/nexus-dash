---
phase: quick-260805-17a
plan: 01
subsystem: ui
tags: [leads, date-range, zustand, filtering, react]

# Dependency graph
requires:
  - phase: n/a (quick task)
    provides: pre-existing lib/leads.ts (parseLeadDate, compareByCriadoEm) and lib/stores/date-range.ts (useDateRangeStore), both already used by Dashboard/Campanhas
provides:
  - "filterLeadsByDateRange pure function in lib/leads.ts (inclusive date-range filter, end-of-day normalized)"
  - "Leads page (app/[tenant-slug]/leads/page.tsx) now honors the header date range for KPIs, table, funnel and sidebar distribution"
affects: [leads, dashboard-date-range, future-leads-features]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "dateFiltered as single source-of-truth useMemo: leads -> dateFiltered -> stats/filtered/export, composed client-side filters (date -> category -> search -> sort)"

key-files:
  created:
    - tests/unit/leads-date-range.test.ts
  modified:
    - lib/leads.ts
    - app/[tenant-slug]/leads/page.tsx

key-decisions:
  - "filterLeadsByDateRange normalizes `to` to 23:59:59.999 of that day because useDateRangeStore always delivers midnight-local Date objects — without this, leads created today with a time component would be wrongly excluded from a range ending 'today'"
  - "Leads with unparseable criado_em are excluded from any date-filtered view (consistent with sortLeadsByCriadoEmDesc pushing them to the end)"
  - "Filtering stays 100% client-side in useMemo — no change to app/api/leads/route.ts, no new fetch on date-range change"

patterns-established:
  - "Pure filter functions in lib/leads.ts reuse parseLeadDate rather than re-parsing dates — single source of date-parsing truth"

requirements-completed: [QUICK-260805-17a]

# Metrics
duration: 5min
completed: 2026-08-05
---

# Quick Task 260805-17a: Leads page respects header date range Summary

**Leads page now filters KPIs, table, funnel and sidebar distribution by the header's selected date range via a new `filterLeadsByDateRange` pure function, composed with existing category/search/sort filters, entirely client-side with no extra fetch.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-05T03:56:44Z
- **Completed:** 2026-08-05T04:01:35Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Added `filterLeadsByDateRange(leads, from, to)` to `lib/leads.ts` — inclusive on both ends, normalizes `to` to end-of-day to avoid dropping today's leads that have a time component, reuses `parseLeadDate`, excludes unparseable dates, immutable/pure.
- 11 new unit tests covering all documented edge cases (inclusivity at both bounds, critical "today with time" case, ISO format, unparseable dates, order preservation, immutability, empty input).
- Rewired `app/[tenant-slug]/leads/page.tsx` so `dateFiltered` (leads filtered by `useDateRangeStore`) is the single source of truth: KPI `stats`, table `filtered`, Funil de Conversão, sidebar Distribuição, the `{N} result.` counter, and CSV export all now derive from it — composed with category filter, search, and column sort exactly as before.
- Page subtitle now reads "`{N} leads no período · últimas 500 entradas da planilha`" instead of showing the raw unfiltered total, so it no longer contradicts the visible filtered data.

## Task Commits

Each task was committed atomically (TDD for Task 1):

1. **Task 1: Add filterLeadsByDateRange (RED)** - `c07c696` (test)
2. **Task 1: Add filterLeadsByDateRange (GREEN)** - `088d32f` (feat)
3. **Task 2: Wire header date range into leads page** - `665d0a7` (feat)

**Plan metadata:** commit pending (docs, made by orchestrator)

## Files Created/Modified
- `tests/unit/leads-date-range.test.ts` - 11 unit tests for `filterLeadsByDateRange`, following the `makeLead` helper pattern from `tests/unit/leads-sort.test.ts`
- `lib/leads.ts` - added `filterLeadsByDateRange` export, reusing `parseLeadDate`
- `app/[tenant-slug]/leads/page.tsx` - added `useDateRangeStore` + `dateFiltered` useMemo as the page's single source of truth; `stats`, `filtered`, and sidebar Distribuição now derive from `dateFiltered` instead of raw `leads`; subtitle updated to show period-scoped count

## Decisions Made
- Normalize `to` to end-of-day inside `filterLeadsByDateRange` rather than changing the date-range store's contract — keeps the store's midnight-local convention (already shared by Dashboard/Campanhas) untouched and localizes the fix to the leads-specific consumer.
- Leave `app/api/leads/route.ts` and the fetch `useEffect` completely untouched — filtering by date is purely a render-time concern over already-fetched data (max 500 rows), so no new request is triggered when the header range changes.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- No blockers. `app/api/leads/route.ts` verified byte-identical (`git diff --stat` empty).
- `npx tsc --noEmit`, `npm run lint`, `npm run build`, and the full `npx vitest run` (319 tests) all pass with zero new errors; the only pre-existing `tsc` errors are in `tests/integration/vault-rpc.test.ts`, unrelated to this task and untouched by it.

---
*Phase: quick-260805-17a*
*Completed: 2026-08-05*

## Self-Check: PASSED

- FOUND: tests/unit/leads-date-range.test.ts
- FOUND: lib/leads.ts
- FOUND: app/[tenant-slug]/leads/page.tsx
- FOUND commit: c07c696
- FOUND commit: 088d32f
- FOUND commit: 665d0a7
